import { PanelCard } from '@/components/domain/PanelCard'

/**
 * RotasTab — STUB (Wave 2). Preenchido integralmente no 08-07
 * (tabela read de route_scores). Nao adicionar logica aqui:
 * 08-07 substitui este arquivo inteiro.
 */
export function RotasTab() {
  return (
    <PanelCard title="Rotas">
      <p className="text-sm text-muted-foreground">Aba em construção.</p>
    </PanelCard>
  )
}
