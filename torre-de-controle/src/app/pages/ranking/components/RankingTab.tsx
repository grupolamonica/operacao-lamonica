import { PanelCard } from '@/components/domain/PanelCard'

/**
 * RankingTab — STUB (Wave 2). Preenchido integralmente no 08-04
 * (DataTable ordenavel por pontuacao + rank). Nao adicionar logica aqui:
 * 08-04 substitui este arquivo inteiro. Props ficam opcionais para nao
 * quebrar a montagem na RankingPage quando o conteudo real chegar.
 */
export function RankingTab() {
  return (
    <PanelCard title="Ranking">
      <p className="text-sm text-muted-foreground">Aba em construção.</p>
    </PanelCard>
  )
}
