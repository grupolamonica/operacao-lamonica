import { PanelCard } from '@/components/domain/PanelCard'

/**
 * LogsTab — STUB (Wave 2). Preenchido integralmente no 08-08
 * (DataTable de logs + diff JSON). Nao adicionar logica aqui:
 * 08-08 substitui este arquivo inteiro.
 */
export function LogsTab() {
  return (
    <PanelCard title="Logs">
      <p className="text-sm text-muted-foreground">Aba em construção.</p>
    </PanelCard>
  )
}
