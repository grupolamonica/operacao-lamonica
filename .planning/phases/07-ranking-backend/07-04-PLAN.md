---
phase: 07-ranking-backend
plan: 04
type: execute
wave: 3
depends_on: ["07-02", "07-03"]
files_modified:
  - api/src/modules/ranking/ranking.service.ts
  - api/src/modules/ranking/ranking.plugin.ts
  - api/src/modules/ranking/ranking.service.test.ts
  - api/src/index.ts
autonomous: false
requirements:
  - PHASE7-ENDPOINTS-READ
  - PHASE7-EDEN-TYPES
  - PHASE7-SCORING-PARITY

must_haves:
  truths:
    - "GET /api/ranking/drivers retorna o ARRAY COMPLETO de Driver (cada um com campo `status` ATIVO|BLOQUEADO e `rank`), ordenado por pontuacao desc; `rank` é a posição 1..N calculada APENAS sobre os ATIVOS (bloqueados recebem rank null). A UI (Phase 8) filtra por status."
    - "GET /api/ranking/trips retorna SOMENTE trips FECHADA (transformTrips + ajuste_manual), aceitando filtro opcional from/to; viagens NO SHOW NÃO são concatenadas aqui (ficam em campo/endpoint separado se necessário na Phase 8/9)."
    - "GET /api/ranking/blocks retorna driver_blocks ativos; GET /api/ranking/route-scores retorna route_scores; GET /api/ranking/stats retorna { activeDrivers, top3Avg, totalTrips, activeBlocks }."
    - "Todos os endpoints /api/ranking/* exigem o auth-cookie do Torre (authGuard) — 401 sem sessão."
    - "Os tipos do ranking são alcançáveis pelo Eden Treaty via export type App em api/src/index.ts."
    - "PHASE7-SCORING-PARITY é fechado por: (a) golden-sample test (Plan 02) + composição test (este plano) — OBRIGATÓRIOS, rodam sem credencial; (b) smoke 401-sem-auth. O teste de paridade com DADOS REAIS via RANK_SUPABASE_SERVICE_KEY é checkpoint pós-setup (não bloqueia o build; exigido para marcar a fase verificada)."
  artifacts:
    - path: "api/src/modules/ranking/ranking.service.ts"
      provides: "Pipeline de composição que replica o DataContext do ride-rank (transform → ajuste_manual → deriveDrivers → status bloqueio → rank sobre ativos + stats)"
      exports: ["getRankingDrivers", "getRankingTrips", "getRankingBlocks", "getRankingRouteScores", "getRankingStats", "composeRanking"]
    - path: "api/src/modules/ranking/ranking.service.test.ts"
      provides: "Teste da composição (composeRanking) — ajuste_manual, bloqueio/override, rank sobre ativos, stats"
      contains: "composeRanking"
    - path: "api/src/modules/ranking/ranking.plugin.ts"
      provides: "Plugin Elysia com 5 GET atrás de authGuard"
      exports: ["rankingPlugin"]
    - path: "api/src/index.ts"
      provides: "Registro do rankingPlugin (antes do wsPlugin) + tag swagger + export App"
      contains: "rankingPlugin"
  key_links:
    - from: "api/src/modules/ranking/ranking.service.ts"
      to: "ranking.scoring.ts + ranking.reads.ts + ranking.sheets.ts (getRouteBasePoints vem via scoring/routes)"
      via: "compõe o pipeline de ranking"
      pattern: "transformTrips|deriveDrivers"
    - from: "api/src/modules/ranking/ranking.plugin.ts"
      to: "authGuard"
      via: ".use(authGuard).group('/api/ranking')"
      pattern: "\\.use\\(authGuard\\)"
    - from: "api/src/index.ts"
      to: "rankingPlugin"
      via: ".use(rankingPlugin) antes de .use(wsPlugin)"
      pattern: "\\.use\\(rankingPlugin\\)"
---

<objective>
Compõe o ranking server-side e o expõe: `ranking.service.ts` replica o pipeline do `DataContext` do ride-rank (transform → aplica `ajuste_manual` das evaluations → `deriveDrivers` → marca `BLOQUEADO` → calcula `rank` sobre os ativos + stats); `ranking.plugin.ts` expõe 5 GET atrás do `authGuard`; registra o plugin em `index.ts` (antes do `wsPlugin`), adiciona a tag swagger e mantém o `export type App` para o Eden Treaty da Phase 8. Inclui o teste de composição e um checkpoint de paridade com dados reais.

Purpose: Fecha PHASE7-ENDPOINTS-READ, PHASE7-EDEN-TYPES e PHASE7-SCORING-PARITY. É o "blueprint→casa": junta as camadas pura (Plan 02) e de I/O (Plan 03) num contrato HTTP consumível pelo front. A paridade do `/drivers` depende de replicar a cadeia COMPLETA do DataContext, não só deriveDrivers. O CONTRATO de resposta é FIXADO aqui (consumido pelo Eden Treaty da Phase 8).
Output: ranking acessível em `/api/ranking/*` atrás de auth; tipos no `App`; contrato fixo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-ranking-backend/07-CONTEXT.md

<interfaces>
<!-- VERIFY: `bun test <path>` e `bun --bun tsc --noEmit` sinalizam por EXIT CODE (0 = verde). CONFIE no exit code — NÃO faça grep de "pass"/"fail"/"error" na saída (falso-positivo). grep só para checar EXISTÊNCIA de tokens no código (-q). -->

<!-- Do Plan 02 (já existe) — ranking.scoring.ts + ranking.routes.ts + ranking.types.ts: -->
```typescript
// ranking.scoring.ts (importa getRouteBasePoints de ./ranking.routes)
export function transformTrips(sheetTrips: SheetTrip[], ignoredOccurrences: string[], routeScores: RouteScoreRecord[]): Trip[]
export function transformSheetNoShowTrips(sheetTrips: SheetTrip[]): Trip[]
export function deriveDrivers(trips: Trip[]): Driver[]   // ordena pontuacao DESC
export function extractUniqueOccurrences(sheetTrips: SheetTrip[]): string[]
export function parseDateBR(dateStr: string): Date | null
// ranking.types.ts: Trip, Driver, Block, StatusMetrics, SheetTrip, RouteScoreRecord, EvaluationRecord, DriverBlockRecord, EvaluationLogRecord, DriverRecord
```

<!-- Do Plan 03 (já existe) — reads + sheets: -->
```typescript
// ranking.reads.ts
export async function fetchEvaluations(): Promise<EvaluationRecord[]>
export async function fetchDriverBlocks(): Promise<DriverBlockRecord[]>
export async function fetchEvaluationLogs(): Promise<EvaluationLogRecord[]>
export async function fetchRouteScores(): Promise<RouteScoreRecord[]>
export async function fetchDrivers(): Promise<DriverRecord[]>
// ranking.sheets.ts
export async function getSheetTrips(): Promise<SheetTrip[]>
```

<!-- Pipeline de PARIDADE a replicar — de ride-rank DataContext.tsx (useMemo, ~L127-218). Ordem EXATA: -->
<!-- 1. t = transformTrips(sheetTrips, ignoredOccurrences, routeScores) -->
<!-- 2. (opcional) enriquecer driverName via DriverRecord map por driver_id normalizado (remove pontos) -->
<!-- 3. Aplicar evaluations: para cada trip com eval, score_final = clamp(0..100, score_final + ajuste_manual); evaluated=true -->
<!-- 4. activelyBlockedIds = Set(driver_blocks onde ativo===true && manual_override===false → driver_id) -->
<!-- 5. d = deriveDrivers(t)  (já ordenado por pontuacao desc) -->
<!-- 6. status = activelyBlockedIds.has(d.id) ? 'BLOQUEADO' : 'ATIVO' -->
<!-- 7. activeDrivers = d.filter(status !== 'BLOQUEADO') -->

<!-- CONTRATO DE RESPOSTA FIXADO (consumido pelo Eden Treaty da Phase 8): -->
<!-- GET /drivers → Array<Driver & { status: 'ATIVO'|'BLOQUEADO'; rank: number | null }>, ordenado por pontuacao desc.
       `rank` = posição sequencial 1..N atribuída percorrendo a lista ordenada e contando SÓ ativos (bloqueados → rank: null). A UI filtra por status; o array inclui TODOS (ativos + bloqueados). -->
<!-- GET /trips → Trip[] (SOMENTE FECHADA, já com ajuste_manual aplicado), filtro opcional from/to. NÃO concatenar NO SHOW. -->
<!-- GET /blocks → DriverBlockRecord[] com ativo===true. -->
<!-- GET /route-scores → RouteScoreRecord[]. -->
<!-- GET /stats → { activeDrivers: number; top3Avg: number; totalTrips: number; activeBlocks: number }. -->

<!-- PADRÃO de plugin Elysia + authGuard — api/src/modules/dashboard/dashboard.plugin.ts + trips/trips.plugin.ts: -->
```typescript
import { Elysia } from 'elysia'
import { authGuard } from '../../lib/rbac'
export const xPlugin = new Elysia({ name: 'x' })
  .use(authGuard)
  .group('/api/x', (app) => app.get('/y', () => svc(), { detail: { tags: ['x'], summary: '...' } }))
```

<!-- index.ts: importar rankingPlugin, .use(rankingPlugin) ANTES de .use(wsPlugin) (regra: wsPlugin fica por último), adicionar tag 'ranking' no array de tags do swagger, manter `export type App = typeof app` no final. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ranking.service.ts — pipeline de composição (paridade DataContext) + teste</name>
  <read_first>
    - C:\Users\antonio.magalhaes\Documents\Projetos\produção\ride-rank-buddy\src\contexts\DataContext.tsx (FONTE da ORQUESTRAÇÃO — useMemo ~L127-218: ordem transform→ajuste_manual clamp→activelyBlockedIds→deriveDrivers→status→activeDrivers; DEFAULT_IGNORED_OCCURRENCES ~L11-17)
    - api/src/modules/ranking/ranking.scoring.ts (transformTrips/transformSheetNoShowTrips/deriveDrivers/extractUniqueOccurrences/parseDateBR — Plan 02)
    - api/src/modules/ranking/ranking.reads.ts + ranking.sheets.ts (Plan 03)
    - api/src/modules/dashboard/dashboard.service.ts (PADRÃO de service: funções async exportadas + Promise.all dos reads)
  </read_first>
  <files>api/src/modules/ranking/ranking.service.ts, api/src/modules/ranking/ranking.service.test.ts</files>
  <behavior>
    - composeRanking (PURA) retorna { trips, drivers, activeDrivers, stats } onde `drivers` é o array COMPLETO ordenado por pontuacao desc com `status` e `rank` (rank 1..N só sobre ativos; bloqueados rank null).
    - Test 1 (composição/ajuste_manual): sheetTrips fixos + 1 evaluation ajuste_manual=+5 num trip do motorista X → pontuacao de X reflete o clamp(0..100) aplicado ao score do trip avaliado.
    - Test 2 (bloqueio): driver com block ativo (ativo=true, manual_override=false) → status 'BLOQUEADO', rank null, fora de activeDrivers; com manual_override=true → 'ATIVO' e entra no ranking.
    - Test 3 (rank): rank é sequencial 1..N sobre os ativos na ordem de pontuacao desc; bloqueados não consomem número de rank.
    - Test 4 (stats): getRankingStats deriva { activeDrivers, top3Avg, totalTrips, activeBlocks } coerentes (top3Avg = média da pontuacao dos 3 primeiros ATIVOS).
  </behavior>
  <action>
    Criar `ranking.service.ts` com a função interna PURA `composeRanking({ sheetTrips, evaluations, driverBlocks, routeScores, drivers, ignoredOccurrences })` que replica o useMemo do DataContext na ORDEM exata: (1) `transformTrips(sheetTrips, ignoredOccurrences, routeScores)`; (2) enriquecer `driverName` por `DriverRecord` (map por driver_id com pontos removidos); (3) aplicar evaluations: para trip com eval, `score_final = Math.max(0, Math.min(100, score_final + (ajuste_manual||0)))`, `evaluated=true`; (4) `activelyBlockedIds = new Set(driverBlocks.filter(b => b.ativo && !b.manual_override).map(b => b.driver_id))`; (5) `deriveDrivers(t)`; (6) mapear status BLOQUEADO/ATIVO; (7) atribuir `rank`: percorrer a lista ordenada, contador incrementa só para ATIVOS (1..N), BLOQUEADO recebe `rank: null`; (8) `activeDrivers = filter(status !== 'BLOQUEADO')`. Definir `DEFAULT_IGNORED_OCCURRENCES` (copiar a lista do DataContext). `composeRanking` é named export (para teste). Exportar as 5 funções de service:
    - `getRankingDrivers()`: Promise.all([getSheetTrips, fetchEvaluations, fetchDriverBlocks, fetchRouteScores, fetchDrivers]) → composeRanking → retorna `drivers` (array COMPLETO com status + rank, ordenado desc) — CONTRATO FIXADO.
    - `getRankingTrips(filter?)`: retorna `trips` (SOMENTE FECHADA, com ajuste) aplicando filtro opcional `from`/`to` via parseDateBR (espelha o DataContext). NÃO concatenar transformSheetNoShowTrips.
    - `getRankingBlocks()`: `fetchDriverBlocks()` filtrando `ativo===true`.
    - `getRankingRouteScores()`: `fetchRouteScores()`.
    - `getRankingStats()`: { activeDrivers: activeDrivers.length, top3Avg: média da pontuacao dos top 3 ATIVOS, totalTrips: trips.length, activeBlocks: count(driverBlocks ativos) }.
    Criar `ranking.service.test.ts` (Tests 1-4) usando sheetTrips/evaluations/driverBlocks fixos in-memory (testa `composeRanking` puro — sem I/O). NÃO logar PII.
  </action>
  <verify>
    <automated>cd api && bun test src/modules/ranking/ranking.service.test.ts && bun --bun tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Exporta getRankingDrivers, getRankingTrips, getRankingBlocks, getRankingRouteScores, getRankingStats + composeRanking.
    - `composeRanking` aplica a ordem EXATA do DataContext (transform → ajuste_manual clamp 0..100 → activelyBlocked → deriveDrivers → status → rank-sobre-ativos → activeDrivers).
    - `/drivers` retorna array COMPLETO com `status` + `rank` (null para bloqueados); `/trips` só FECHADA, sem NO SHOW concatenado.
    - `cd api && bun test src/modules/ranking/ranking.service.test.ts` → exit 0 (Tests 1-4 passam).
    - `cd api && bun --bun tsc --noEmit` → exit 0.
  </acceptance_criteria>
  <done>Pipeline de paridade + contrato fixado, testado (exit 0); 5 funções de service + composeRanking exportadas.</done>
</task>

<task type="auto">
  <name>Task 2: ranking.plugin.ts — 5 GET atrás do authGuard</name>
  <read_first>
    - api/src/modules/dashboard/dashboard.plugin.ts (PADRÃO mínimo: new Elysia({name}).use(authGuard).group('/api/...', app => app.get(...)))
    - api/src/modules/trips/trips.plugin.ts (PADRÃO com query params via t.Object + detail tags)
    - api/src/modules/ranking/ranking.service.ts (as 5 funções da Task 1)
  </read_first>
  <files>api/src/modules/ranking/ranking.plugin.ts</files>
  <action>
    Criar `ranking.plugin.ts`: `new Elysia({ name: 'ranking' }).use(authGuard).group('/api/ranking', app => app...)` com 5 GET, cada um com `detail: { tags: ['ranking'], summary: '...' }`:
    - `GET /drivers` → getRankingDrivers().
    - `GET /trips` → getRankingTrips({ from: query.from, to: query.to }) — query opcional `from`/`to` (t.Optional(t.String())) para o dateRange, repassada ao service.
    - `GET /blocks` → getRankingBlocks().
    - `GET /route-scores` → getRankingRouteScores().
    - `GET /stats` → getRankingStats().
    Exportar `rankingPlugin`. NÃO duplicar `.use(authGuard)` em cada rota (uma vez no plugin basta — segue o padrão dashboard). Erros do service propagam pro `onError` global do index.ts (que já trata 401/403/422/500).
  </action>
  <verify>
    <automated>cd api && grep -q "new Elysia({ name: 'ranking' })" src/modules/ranking/ranking.plugin.ts && grep -q ".use(authGuard)" src/modules/ranking/ranking.plugin.ts && grep -q "'/drivers'" src/modules/ranking/ranking.plugin.ts && grep -q "'/stats'" src/modules/ranking/ranking.plugin.ts && bun --bun tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Plugin nomeado `ranking`, com `.use(authGuard)` e `.group('/api/ranking', ...)`.
    - 5 rotas GET: /drivers, /trips, /blocks, /route-scores, /stats (todas presentes — grep -q de cada path).
    - Cada rota com `detail.tags: ['ranking']`.
    - `cd api && bun --bun tsc --noEmit` → exit 0.
  </acceptance_criteria>
  <done>rankingPlugin com 5 GET protegidos por authGuard; tsc limpo (exit 0).</done>
</task>

<task type="auto">
  <name>Task 3: Registrar rankingPlugin no index.ts + tag swagger + smoke 401</name>
  <read_first>
    - api/src/index.ts (onde os plugins são registrados — importar e `.use(rankingPlugin)` ANTES de `.use(wsPlugin)` na L165; adicionar tag 'ranking' no array de tags do swagger ~L100-116; manter `export type App = typeof app` na última linha)
    - api/src/modules/ranking/ranking.plugin.ts (rankingPlugin — Task 2)
  </read_first>
  <files>api/src/index.ts</files>
  <action>
    Editar `api/src/index.ts`: (1) adicionar `import { rankingPlugin } from './modules/ranking/ranking.plugin'` junto aos outros imports de módulo; (2) inserir `.use(rankingPlugin)` na cadeia de plugins ANTES de `.use(wsPlugin)` (wsPlugin permanece o último plugin — regra de ordem de composição do Elysia 1.4); (3) adicionar `{ name: 'ranking', description: 'Ranking de motoristas (proxy Supabase ride-rank + Sheets, Redis cache 60s)' }` ao array `tags` da config do swagger; (4) NÃO alterar `export type App = typeof app` (já existe — garante que os tipos do ranking entram no Eden Treaty). Depois rodar o smoke 401: subir o server com envs RANK_* de teste (valores fake bastam — o authGuard barra antes de tocar o Supabase) e validar que `GET /api/ranking/drivers` SEM cookie retorna 401, provando o authGuard. Documentar no SUMMARY o resultado do smoke.
  </action>
  <verify>
    <automated>cd api && grep -q "import { rankingPlugin }" src/index.ts && grep -q ".use(rankingPlugin)" src/index.ts && grep -q "name: 'ranking'" src/index.ts && bun --bun tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `index.ts` importa e usa `rankingPlugin`; `.use(rankingPlugin)` aparece ANTES de `.use(wsPlugin)`.
    - Tag `ranking` presente na config do swagger.
    - `export type App = typeof app` intacto (Eden Treaty).
    - `cd api && bun --bun tsc --noEmit` → exit 0 (projeto inteiro).
    - Smoke: `GET /api/ranking/drivers` sem cookie retorna 401 (authGuard ativo). Resultado documentado no SUMMARY.
  </acceptance_criteria>
  <done>rankingPlugin registrado antes do wsPlugin; tag swagger; tsc limpo (exit 0); smoke 401-sem-auth confirmado.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Checkpoint — paridade do ranking com DADOS REAIS (pós-setup)</name>
  <action>
    Gate de verificação manual (NÃO bloqueia o build — os testes golden-sample do Plan 02 + composição da Task 1 já cobrem a paridade sem credencial; este gate é exigido para marcar a fase como VERIFICADA). Após o RANK_SUPABASE_SERVICE_KEY estar configurado no .env do api/, subir a API e comparar a saída de GET /api/ranking/drivers (autenticado) com o app ride-rank original numa amostra, confirmando paridade de pontuacao/rank. Apresentar ao usuário os passos de how-to-verify abaixo e aguardar o resume-signal.
  </action>
  <what-built>
    Endpoints `/api/ranking/*` funcionando atrás do authGuard, com o pipeline de paridade (golden-sample test + composição test já verdes, rodando sem credencial). Falta o gate de paridade com DADOS REAIS do ride-rank, que exige o `RANK_SUPABASE_SERVICE_KEY` (user-setup, segredo) e não roda no build autônomo.
  </what-built>
  <how-to-verify>
    Pré-requisito: `RANK_SUPABASE_SERVICE_KEY` (service_role do ride-rank) preenchido no `.env` do `api/` + um cookie de sessão válido do Torre (login).
    1. Subir a API: `cd api && bun run dev` (com as envs RANK_* reais no .env).
    2. Autenticar no Torre e capturar o cookie `access_token` (ou usar o login do front).
    3. `GET /api/ranking/drivers` autenticado → conferir que retorna um array de Driver ordenado por `pontuacao` desc, com `status` e `rank` (1..N sobre ativos).
    4. Escolher 1-2 motoristas e COMPARAR `pontuacao`/`rank` com o app ride-rank original (mesma janela de dados) — devem BATER (paridade).
    5. `GET /api/ranking/stats` → conferir activeDrivers/top3Avg/totalTrips/activeBlocks plausíveis.
  </how-to-verify>
  <verify>
    <automated>MANUAL — checkpoint pós-setup; requer RANK_SUPABASE_SERVICE_KEY (segredo) + cookie de sessão. Não roda no build autônomo. A paridade automatizada (sem credencial) já é coberta por: `cd api && bun test src/modules/ranking/`.</automated>
  </verify>
  <done>Usuário confirma "aprovado" após validar a paridade de pontuacao/rank com o app original numa amostra de dados reais.</done>
  <resume-signal>Digite "aprovado" se a paridade com dados reais bater, ou descreva as divergências (motorista, valor esperado vs retornado) para ajuste.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser (front Torre) → /api/ranking/* | Único ponto de entrada do front aos dados do ride-rank. Deve exigir o auth-cookie do Torre (sem 2º login — D-V2-01). |
| /api/ranking/* → camadas internas | O plugin é a fronteira de autorização; service/reads/sheets são internos (já tratados nos Plans 01/03). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-09 | Spoofing / Elevation of Privilege | endpoints /api/ranking/* | mitigate | `.use(authGuard)` no plugin exige cookie JWT válido + checa blacklist no Redis (mesmo dos outros módulos); smoke confirma 401 sem cookie. |
| T-07-10 | Information Disclosure | respostas dos endpoints (PII de motorista) | mitigate | Acesso só autenticado (authGuard). Nesta fase, leitura por qualquer papel logado é aceitável; o RBAC fino (admin|supervisor|analyst) aplica-se só a ESCRITA (Phase 9, D-V2-06). |
| T-07-11 | Information Disclosure | onError global | accept | `onError` do index.ts já mascara 500 ("Internal server error") e não vaza stack/credencial; erros do service propagam por ele. |
| T-07-12 | Tampering | ordem de plugins (wsPlugin deve ser o último) | mitigate | rankingPlugin inserido ANTES de wsPlugin, preservando a regra de composição do Elysia 1.4 (evita o bug de derive leak do authGuard documentado no index.ts). |
</threat_model>

<verification>
- `cd api && bun --bun tsc --noEmit` → exit 0 no projeto inteiro.
- `cd api && bun test src/modules/ranking/` → exit 0 (golden-sample do Plan 02 + composição deste plano).
- Smoke (autônomo, sem credencial real): `GET /api/ranking/drivers` sem cookie → 401; `/swagger/json` lista `/api/ranking/{drivers,trips,blocks,route-scores,stats}`.
- Contrato fixo conferível: `/drivers` array completo com status+rank; `/trips` só FECHADA.
- `export type App` intacto → tipos do ranking disponíveis ao Eden Treaty da Phase 8.
- GATE DE PARIDADE (PHASE7-SCORING-PARITY): fechado por (a) golden-sample + composição tests (obrigatórios, build) + (b) smoke 401; o checkpoint de DADOS REAIS (RANK_SUPABASE_SERVICE_KEY) é pós-setup e exigido para marcar a fase verificada (não bloqueia o build).
</verification>

<success_criteria>
- `/api/ranking/*` (5 GET) funcionando atrás do authGuard, compondo ride-rank + Sheets com paridade ao app original; contrato de resposta fixado.
- Tipos exportados via App (Eden Treaty). tsc + bun test + smoke 401 OK. Checkpoint de paridade-real aprovado.
</success_criteria>

<output>
After completion, create `.planning/phases/07-ranking-backend/07-04-SUMMARY.md`
</output>
