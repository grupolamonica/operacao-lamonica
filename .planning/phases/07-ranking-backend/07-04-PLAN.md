---
phase: 07-ranking-backend
plan: 04
type: execute
wave: 3
depends_on: ["07-02", "07-03"]
files_modified:
  - api/src/modules/ranking/ranking.service.ts
  - api/src/modules/ranking/ranking.plugin.ts
  - api/src/index.ts
autonomous: true
requirements:
  - PHASE7-ENDPOINTS-READ
  - PHASE7-EDEN-TYPES
  - PHASE7-SCORING-PARITY

must_haves:
  truths:
    - "GET /api/ranking/drivers retorna o ranking computado (ordenado desc, bloqueados marcados/excluídos) com paridade ao app original"
    - "GET /api/ranking/{trips,blocks,route-scores,stats} retornam dados reais do ride-rank+Sheets"
    - "Todos os endpoints /api/ranking/* exigem o auth-cookie do Torre (authGuard) — 401 sem sessão"
    - "Os tipos do ranking são alcançáveis pelo Eden Treaty via export type App em api/src/index.ts"
  artifacts:
    - path: "api/src/modules/ranking/ranking.service.ts"
      provides: "Pipeline de composição que replica o DataContext do ride-rank (transform → ajuste_manual → deriveDrivers → status bloqueio → activeDrivers + stats)"
      exports: ["getRankingDrivers", "getRankingTrips", "getRankingBlocks", "getRankingRouteScores", "getRankingStats"]
    - path: "api/src/modules/ranking/ranking.plugin.ts"
      provides: "Plugin Elysia com 5 GET atrás de authGuard"
      exports: ["rankingPlugin"]
    - path: "api/src/index.ts"
      provides: "Registro do rankingPlugin (antes do wsPlugin) + tag swagger + export App"
      contains: "rankingPlugin"
  key_links:
    - from: "api/src/modules/ranking/ranking.service.ts"
      to: "ranking.scoring.ts + ranking.reads.ts + ranking.sheets.ts + ranking.routes.ts"
      via: "compõe o pipeline de ranking"
      pattern: "transformTrips|deriveDrivers"
    - from: "api/src/modules/ranking/ranking.plugin.ts"
      to: "authGuard"
      via: ".use(authGuard).group('/api/ranking')"
      pattern: "\\.use\\(authGuard\\)"
    - from: "api/src/index.ts"
      to: "rankingPlugin"
      via: ".use(rankingPlugin) antes de .use(wsPlugin)"
      pattern: "\\.use\\(rankingPlugin\\)"
---

<objective>
Compõe o ranking server-side e o expõe: `ranking.service.ts` replica o pipeline do `DataContext` do ride-rank (transform → aplica `ajuste_manual` das evaluations → `deriveDrivers` → marca `BLOQUEADO` → `activeDrivers` + stats); `ranking.plugin.ts` expõe 5 GET atrás do `authGuard`; registra o plugin em `index.ts` (antes do `wsPlugin`), adiciona a tag swagger e mantém o `export type App` para o Eden Treaty da Phase 8.

Purpose: Fecha PHASE7-ENDPOINTS-READ, PHASE7-EDEN-TYPES e PHASE7-SCORING-PARITY (smoke). É o "blueprint→casa": junta as camadas pura (Plan 02) e de I/O (Plan 03) num contrato HTTP consumível pelo front. A paridade do `/drivers` depende de replicar a cadeia COMPLETA do DataContext, não só deriveDrivers.
Output: ranking acessível em `/api/ranking/*` atrás de auth; tipos no `App`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-ranking-backend/07-CONTEXT.md

<interfaces>
<!-- Do Plan 02 (já existe) — ranking.scoring.ts + ranking.types.ts: -->
```typescript
// ranking.scoring.ts
export function transformTrips(sheetTrips: SheetTrip[], ignoredOccurrences: string[], routeScores: RouteScoreRecord[]): Trip[]
export function transformSheetNoShowTrips(sheetTrips: SheetTrip[]): Trip[]
export function deriveDrivers(trips: Trip[]): Driver[]   // ordena pontuacao DESC
export function extractUniqueOccurrences(sheetTrips: SheetTrip[]): string[]
// ranking.types.ts: Trip, Driver, Block, StatusMetrics, SheetTrip, RouteScoreRecord, EvaluationRecord, DriverBlockRecord, EvaluationLogRecord, DriverRecord
```

<!-- Do Plan 03 (já existe) — reads + sheets + routes: -->
```typescript
// ranking.reads.ts
export async function fetchEvaluations(): Promise<EvaluationRecord[]>
export async function fetchDriverBlocks(): Promise<DriverBlockRecord[]>
export async function fetchEvaluationLogs(): Promise<EvaluationLogRecord[]>
export async function fetchRouteScores(): Promise<RouteScoreRecord[]>
export async function fetchDrivers(): Promise<DriverRecord[]>
// ranking.sheets.ts
export async function getSheetTrips(): Promise<SheetTrip[]>
// ranking.routes.ts
export function getRouteBasePoints(routeScores, originCode, destinationCode, tripDate?): number
```

<!-- Pipeline de PARIDADE a replicar — de ride-rank DataContext.tsx (useMemo, ~L127-218). Ordem EXATA: -->
<!-- 1. t = transformTrips(sheetTrips, ignoredOccurrences, routeScores) -->
<!-- 2. (opcional) enriquecer driverName via DriverRecord map por driver_id normalizado (remove pontos) -->
<!-- 3. Aplicar evaluations: para cada trip com eval, score_final = clamp(0..100, score_final + ajuste_manual); evaluated=true -->
<!-- 4. activelyBlockedIds = Set(driver_blocks onde ativo===true && manual_override===false → driver_id) -->
<!-- 5. d = deriveDrivers(t) -->
<!-- 6. status = activelyBlockedIds.has(d.id) ? 'BLOQUEADO' : 'ATIVO' -->
<!-- 7. activeDrivers = d.filter(status !== 'BLOQUEADO') -->

<!-- PADRÃO de plugin Elysia + authGuard — api/src/modules/dashboard/dashboard.plugin.ts + trips/trips.plugin.ts: -->
```typescript
import { Elysia } from 'elysia'
import { authGuard } from '../../lib/rbac'
export const xPlugin = new Elysia({ name: 'x' })
  .use(authGuard)
  .group('/api/x', (app) => app.get('/y', () => svc(), { detail: { tags: ['x'], summary: '...' } }))
```

<!-- index.ts: importar rankingPlugin, .use(rankingPlugin) ANTES de .use(wsPlugin) (regra: wsPlugin fica por último), adicionar tag 'ranking' no array de tags do swagger, manter `export type App = typeof app` no final. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ranking.service.ts — pipeline de composição (paridade DataContext)</name>
  <read_first>
    - C:\Users\antonio.magalhaes\Documents\Projetos\produção\ride-rank-buddy\src\contexts\DataContext.tsx (FONTE da ORQUESTRAÇÃO — useMemo ~L127-218: ordem transform→ajuste_manual clamp→activelyBlockedIds→deriveDrivers→status→activeDrivers; DEFAULT_IGNORED_OCCURRENCES ~L11-17)
    - api/src/modules/ranking/ranking.scoring.ts (transformTrips/transformSheetNoShowTrips/deriveDrivers/extractUniqueOccurrences — Plan 02)
    - api/src/modules/ranking/ranking.reads.ts + ranking.sheets.ts + ranking.routes.ts (Plan 03)
    - api/src/modules/dashboard/dashboard.service.ts (PADRÃO de service: funções async exportadas + Promise.all dos reads)
  </read_first>
  <files>api/src/modules/ranking/ranking.service.ts</files>
  <behavior>
    - getRankingDrivers() retorna Driver[] ordenado por pontuacao DESC (de deriveDrivers) com status calculado; activelyBlocked → BLOQUEADO.
    - Test 1 (paridade composição): dado um conjunto fixo de sheetTrips + 1 evaluation com ajuste_manual=+5 num trip do motorista X, a pontuacao de X = (soma dos score_final) refletindo o clamp(0..100) do score do trip avaliado. (Testar a função de composição PURA — ver implementation.)
    - Test 2: um driver com block ativo (ativo=true, manual_override=false) recebe status 'BLOQUEADO' e é excluído de activeDrivers; com manual_override=true permanece 'ATIVO'.
    - Test 3: getRankingStats() retorna { activeDrivers, top3Avg, totalTrips, activeBlocks } coerentes com os arrays derivados.
  </behavior>
  <action>
    Criar `ranking.service.ts` com uma função interna PURA `composeRanking({ sheetTrips, evaluations, driverBlocks, routeScores, drivers, ignoredOccurrences })` que replica o useMemo do DataContext na ORDEM exata: (1) `transformTrips(sheetTrips, ignoredOccurrences, routeScores)`; (2) enriquecer `driverName` por `DriverRecord` (map por driver_id com pontos removidos) — opcional mas preservar; (3) aplicar evaluations: para trip com eval, `score_final = Math.max(0, Math.min(100, score_final + (ajuste_manual||0)))`, `evaluated=true`; (4) `activelyBlockedIds = new Set(driverBlocks.filter(b => b.ativo && !b.manual_override).map(b => b.driver_id))`; (5) `deriveDrivers(t)`; (6) mapear status BLOQUEADO/ATIVO; (7) `activeDrivers = filter(status !== 'BLOQUEADO')`. Definir `DEFAULT_IGNORED_OCCURRENCES` (copiar a lista do DataContext). Exportar as 5 funções de service que buscam I/O e chamam composeRanking:
    - `getRankingDrivers()`: Promise.all([getSheetTrips, fetchEvaluations, fetchDriverBlocks, fetchRouteScores, fetchDrivers]) → composeRanking → retorna `drivers` (todos, com status) — ou expor `activeDrivers` conforme o contrato; retornar o array ordenado de Driver com rank implícito pela ordem.
    - `getRankingTrips()`: retorna `t` (trips FECHADA transformados + ajuste) — aceitar filtro de dateRange opcional (parseDateBR) espelhando o DataContext; incluir/concatenar no-show via transformSheetNoShowTrips se o contrato pedir (documentar a escolha no SUMMARY).
    - `getRankingBlocks()`: `fetchDriverBlocks()` filtrando `ativo===true` (blocks ativos).
    - `getRankingRouteScores()`: `fetchRouteScores()`.
    - `getRankingStats()`: deriva { activeDrivers: activeDrivers.length, top3Avg: média da pontuacao dos top 3, totalTrips: t.length, activeBlocks: blocks ativos.length }.
    Tornar `composeRanking` EXPORTÁVEL (named export) para o teste unitário de paridade. NÃO logar PII.
  </action>
  <verify>
    <automated>cd api && bun test src/modules/ranking/ 2>&1 | grep -qiE "pass|0 fail" && echo TESTS_OK</automated>
  </verify>
  <acceptance_criteria>
    - Exporta getRankingDrivers, getRankingTrips, getRankingBlocks, getRankingRouteScores, getRankingStats + composeRanking (para teste).
    - `composeRanking` aplica a ordem EXATA do DataContext (transform → ajuste_manual clamp 0..100 → activelyBlocked → deriveDrivers → status → activeDrivers).
    - Teste de composição passa (`bun test src/modules/ranking/`): ajuste_manual reflete no score; block ativo+!override → BLOQUEADO e fora de activeDrivers; override → ATIVO.
    - `bun --bun tsc --noEmit` limpo.
  </acceptance_criteria>
  <done>Pipeline de paridade implementado e testado; 5 funções de service exportadas.</done>
</task>

<task type="auto">
  <name>Task 2: ranking.plugin.ts — 5 GET atrás do authGuard</name>
  <read_first>
    - api/src/modules/dashboard/dashboard.plugin.ts (PADRÃO mínimo: new Elysia({name}).use(authGuard).group('/api/...', app => app.get(...)))
    - api/src/modules/trips/trips.plugin.ts (PADRÃO com query params via t.Object + detail tags)
    - api/src/modules/ranking/ranking.service.ts (as 5 funções da Task 1)
  </read_first>
  <files>api/src/modules/ranking/ranking.plugin.ts</files>
  <action>
    Criar `ranking.plugin.ts`: `new Elysia({ name: 'ranking' }).use(authGuard).group('/api/ranking', app => app...)` com 5 GET, cada um com `detail: { tags: ['ranking'], summary: '...' }`:
    - `GET /drivers` → getRankingDrivers().
    - `GET /trips` → getRankingTrips(query) — aceitar query opcional `from`/`to` (t.Optional(t.String())) para o dateRange, repassada ao service.
    - `GET /blocks` → getRankingBlocks().
    - `GET /route-scores` → getRankingRouteScores().
    - `GET /stats` → getRankingStats().
    Exportar `rankingPlugin`. NÃO duplicar `.use(authGuard)` em cada rota (uma vez no plugin basta — segue o padrão dashboard). Erros do service propagam pro `onError` global do index.ts (que já trata 401/403/422/500).
  </action>
  <verify>
    <automated>cd api && grep -q "new Elysia({ name: 'ranking' })" src/modules/ranking/ranking.plugin.ts && grep -q ".use(authGuard)" src/modules/ranking/ranking.plugin.ts && grep -cE "\.get\('/(drivers|trips|blocks|route-scores|stats)'" src/modules/ranking/ranking.plugin.ts | grep -qx 5 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Plugin nomeado `ranking`, com `.use(authGuard)` e `.group('/api/ranking', ...)`.
    - Exatamente 5 rotas GET: /drivers, /trips, /blocks, /route-scores, /stats.
    - Cada rota com `detail.tags: ['ranking']`.
    - `bun --bun tsc --noEmit` limpo.
  </acceptance_criteria>
  <done>rankingPlugin com 5 GET protegidos por authGuard.</done>
</task>

<task type="auto">
  <name>Task 3: Registrar rankingPlugin no index.ts + tag swagger + smoke</name>
  <read_first>
    - api/src/index.ts (onde os plugins são registrados — importar e `.use(rankingPlugin)` ANTES de `.use(wsPlugin)` na L165; adicionar tag 'ranking' no array de tags do swagger ~L100-116; manter `export type App = typeof app` na última linha)
    - api/src/modules/ranking/ranking.plugin.ts (rankingPlugin — Task 2)
  </read_first>
  <files>api/src/index.ts</files>
  <action>
    Editar `api/src/index.ts`: (1) adicionar `import { rankingPlugin } from './modules/ranking/ranking.plugin'` junto aos outros imports de módulo; (2) inserir `.use(rankingPlugin)` na cadeia de plugins ANTES de `.use(wsPlugin)` (wsPlugin permanece o último plugin — regra de ordem de composição do Elysia 1.4); (3) adicionar `{ name: 'ranking', description: 'Ranking de motoristas (proxy Supabase ride-rank + Sheets, Redis cache 60s)' }` ao array `tags` da config do swagger; (4) NÃO alterar `export type App = typeof app` (já existe — garante que os tipos do ranking entram no Eden Treaty). Depois rodar o smoke: subir o server com envs de teste e `curl` os endpoints sem cookie (deve dar 401, provando o authGuard) e `curl` `/swagger/json` deve listar as rotas `/api/ranking/*`.
  </action>
  <verify>
    <automated>cd api && grep -q "import { rankingPlugin }" src/index.ts && grep -q ".use(rankingPlugin)" src/index.ts && bun --bun tsc --noEmit 2>&1 | (grep -v '^#' | grep -ciE "error" || true) | grep -qx 0 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `index.ts` importa e usa `rankingPlugin`; `.use(rankingPlugin)` aparece ANTES de `.use(wsPlugin)`.
    - Tag `ranking` presente na config do swagger.
    - `export type App = typeof app` intacto (Eden Treaty).
    - `bun --bun tsc --noEmit` no projeto inteiro: 0 erros.
    - Smoke: `GET /api/ranking/drivers` sem cookie retorna 401 (authGuard ativo). Documentar no SUMMARY o resultado do smoke (idealmente com envs reais: `/drivers` retorna ranking ordenado).
  </acceptance_criteria>
  <done>rankingPlugin registrado antes do wsPlugin; tag swagger; tsc limpo no projeto; smoke 401-sem-auth confirmado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser (front Torre) → /api/ranking/* | Único ponto de entrada do front aos dados do ride-rank. Deve exigir o auth-cookie do Torre (sem 2º login — D-V2-01). |
| /api/ranking/* → camadas internas | O plugin é a fronteira de autorização; service/reads/sheets são internos (já tratados nos Plans 01/03). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-09 | Spoofing / Elevation of Privilege | endpoints /api/ranking/* | mitigate | `.use(authGuard)` no plugin exige cookie JWT válido + checa blacklist no Redis (mesmo dos outros módulos); smoke confirma 401 sem cookie. |
| T-07-10 | Information Disclosure | respostas dos endpoints (PII de motorista) | mitigate | Acesso só autenticado (authGuard). Nesta fase, leitura por qualquer papel logado é aceitável; o RBAC fino (admin|supervisor|analyst) aplica-se só a ESCRITA (Phase 9, D-V2-06). |
| T-07-11 | Information Disclosure | onError global | accept | `onError` do index.ts já mascara 500 ("Internal server error") e não vaza stack/credencial; erros do service propagam por ele. |
| T-07-12 | Tampering | ordem de plugins (wsPlugin deve ser o último) | mitigate | rankingPlugin inserido ANTES de wsPlugin, preservando a regra de composição do Elysia 1.4 (evita o bug de derive leak do authGuard documentado no index.ts). |
</threat_model>

<verification>
- `cd api && bun --bun tsc --noEmit` → 0 erros no projeto inteiro.
- `cd api && bun test src/modules/ranking/` → paridade do scoring (Plan 02) + composição (Plan 04) passam.
- Smoke: `GET /api/ranking/drivers` sem cookie → 401; `/swagger/json` lista `/api/ranking/{drivers,trips,blocks,route-scores,stats}`.
- Com envs reais (RANK_*), `GET /api/ranking/drivers` (autenticado) retorna array de Driver ordenado por pontuacao desc — paridade com o app original numa amostra.
- `export type App` intacto → tipos do ranking disponíveis ao Eden Treaty da Phase 8.
</verification>

<success_criteria>
- `/api/ranking/*` (5 GET) funcionando atrás do authGuard, compondo ride-rank + Sheets com paridade ao app original.
- Tipos exportados via App (Eden Treaty). tsc + bun test + smoke OK.
</success_criteria>

<output>
After completion, create `.planning/phases/07-ranking-backend/07-04-SUMMARY.md`
</output>
