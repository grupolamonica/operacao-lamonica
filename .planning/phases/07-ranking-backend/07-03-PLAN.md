---
phase: 07-ranking-backend
plan: 03
type: execute
wave: 2
depends_on: ["07-01", "07-02"]
files_modified:
  - api/src/modules/ranking/ranking.reads.ts
  - api/src/modules/ranking/ranking.sheets.ts
autonomous: true
requirements:
  - PHASE7-PORT-SUPABASE-READS
  - PHASE7-PORT-SHEETS

must_haves:
  truths:
    - "O backend lê as 5 tabelas do Supabase ride-rank (evaluations, driver_blocks, evaluation_logs, route_scores, drivers) via service_role"
    - "O backend busca o CSV público do Google Sheets (gviz) e o parseia para SheetTrip[], com cache Redis de ~60s"
  artifacts:
    - path: "api/src/modules/ranking/ranking.reads.ts"
      provides: "Reads do Supabase ride-rank via rankSupabase (5 tabelas), com fetchDrivers paginado"
      exports: ["fetchEvaluations", "fetchDriverBlocks", "fetchEvaluationLogs", "fetchRouteScores", "fetchDrivers"]
    - path: "api/src/modules/ranking/ranking.sheets.ts"
      provides: "Fetch CSV gviz + parse para SheetTrip[] + cache Redis 60s"
      exports: ["getSheetTrips"]
  key_links:
    - from: "api/src/modules/ranking/ranking.reads.ts"
      to: "api/src/modules/ranking/ranking.supabase.ts (rankSupabase)"
      via: "import { rankSupabase } + rankSupabase.from('<tabela>').select(...)"
      pattern: "rankSupabase\\.from\\("
    - from: "api/src/modules/ranking/ranking.sheets.ts"
      to: "Redis (ranking:sheets:trips)"
      via: "redis.get / redis.set EX 60"
      pattern: "redis\\.(get|set)\\("
    - from: "api/src/modules/ranking/ranking.sheets.ts"
      to: "docs.google.com gviz CSV"
      via: "fetch do CSV_URL montado de RANK_SHEET_ID/RANK_SHEET_TAB"
      pattern: "gviz/tq"
---

<objective>
Porta a CAMADA DE I/O do ranking: leituras do Supabase do ride-rank (`ranking.reads.ts`) e fetch+parse do CSV público do Sheets com cache Redis (`ranking.sheets.ts`).

Purpose: Implementa PHASE7-PORT-SUPABASE-READS e PHASE7-PORT-SHEETS. Estes módulos alimentam o pipeline de composição do Plan 04. Reads usam o `rankSupabase` (service_role, Plan 01); Sheets reusa o Redis do Torre para cache curto (D-V2 cache). A função pura `getRouteBasePoints` NÃO está aqui — é do Plan 02 (fonte única); este plano só faz o READ da tabela `route_scores` via `fetchRouteScores`.
Output: 2 módulos de I/O prontos para o service de composição consumir.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-ranking-backend/07-CONTEXT.md

<interfaces>
<!-- VERIFY: `bun --bun tsc --noEmit` sinaliza por EXIT CODE (0 = limpo). Confie no exit code; grep só para checar EXISTÊNCIA (-q), não para contar ocorrências exatas. -->

<!-- Do Plan 01 (já existe quando este plano roda): -->
From api/src/modules/ranking/ranking.supabase.ts:
```typescript
export const rankSupabase: SupabaseClient   // service_role, bypassa RLS
export type RankSupabase = typeof rankSupabase
```

<!-- Do Plan 02 (já existe): tipos em ranking.types.ts -->
From api/src/modules/ranking/ranking.types.ts:
```typescript
export interface SheetTrip { /* sta_origin_date, trip_number, status_agrupado, driver_id, driver_name, origin_station_code, destination_station_code, eta_*, status_eta, status_eta_destino, ocorrencia_*, ... [key:string]:string */ }
export interface RouteScoreRecord { origin_code: string; destination_code: string; pontuacao: number; data_inicio: string; data_fim: string | null; observacao: string | null; }
export interface EvaluationRecord { trip_id: string; driver_id: string; driver_name: string; ajuste_manual: number; comunicacao: string; atendeu: boolean; desvio_rota: string; postura: string; observacao: string; operador: string; }
export interface DriverBlockRecord { driver_id: string; driver_name: string; tipo: string; motivo: string; ativo: boolean; manual_override: boolean; data_inicio?: string; data_fim?: string | null; created_by: string; }
export interface EvaluationLogRecord { trip_id?: string; driver_id?: string; driver_name?: string; operador: string; acao: string; dados_antes?; dados_depois?; created_at?: string; }
export interface DriverRecord { driver_id: string; driver_name: string; }
```

<!-- Redis do Torre (PADRÃO de cache — ioredis) — api/src/redis/client.ts + dashboard.service.ts: -->
```typescript
import { redis } from '../../redis/client'
const cached = await redis.get(KEY); if (cached) return JSON.parse(cached)
// ... computa ...
await redis.set(KEY, JSON.stringify(value), 'EX', TTL_SECONDS)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reads do Supabase ride-rank (ranking.reads.ts)</name>
  <read_first>
    - C:\Users\antonio.magalhaes\Documents\Projetos\produção\ride-rank-buddy\src\services\supabaseService.ts (FONTE: fetchEvaluations, fetchDriverBlocks, fetchEvaluationLogs, fetchDrivers paginado — portar SÓ as funções de LEITURA, não os upsert/block/unblock que são da Phase 9)
    - C:\Users\antonio.magalhaes\Documents\Projetos\produção\ride-rank-buddy\src\services\routeScoreService.ts (FONTE: fetchRouteScores — só o READ; a função pura getRouteBasePoints já está no Plan 02/ranking.routes.ts)
    - api/src/modules/ranking/ranking.supabase.ts (rankSupabase — criado no Plan 01)
    - api/src/modules/ranking/ranking.types.ts (os *Record — criados no Plan 02)
  </read_first>
  <files>api/src/modules/ranking/ranking.reads.ts</files>
  <action>
    Criar `ranking.reads.ts` portando APENAS as funções de leitura de supabaseService.ts + fetchRouteScores de routeScoreService.ts, trocando o client `supabase` por `rankSupabase` (import de `./ranking.supabase`) e os tipos por imports de `./ranking.types`:
    - `fetchEvaluations()` → `rankSupabase.from('evaluations').select('*').order('created_at', {ascending:false})`.
    - `fetchDriverBlocks()` → `.from('driver_blocks').select('*').order('data_inicio', {ascending:false})`.
    - `fetchEvaluationLogs()` → `.from('evaluation_logs').select('*').order('created_at',{ascending:false}).limit(200)`.
    - `fetchRouteScores()` → `.from('route_scores').select('*').order('origin_code').order('data_inicio',{ascending:false})`.
    - `fetchDrivers()` → paginado em páginas de 1000 via `.range(from, from+999)` até retornar < 1000 (preservar o loop do original — pode passar de 1000 motoristas).
    Cada função: `if (error) throw error; return (data ?? []) as Tipo[]`. NÃO portar upsertEvaluation/blockDriver/unblockDriver/createEvaluationLog/upsertDrivers/resetManualOverride (são writes da Phase 9). NÃO portar getRouteBasePoints (é a função pura do Plan 02). Os reads são server-side; o RLS é bypassado pelo service_role.
  </action>
  <verify>
    <automated>cd api && grep -q "rankSupabase.from('evaluations')" src/modules/ranking/ranking.reads.ts && grep -q "rankSupabase.from('route_scores')" src/modules/ranking/ranking.reads.ts && bun --bun tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Exporta exatamente: fetchEvaluations, fetchDriverBlocks, fetchEvaluationLogs, fetchRouteScores, fetchDrivers.
    - Usa `rankSupabase` (não o Drizzle/db do Torre) — `grep -q "from '../../db/client'" ranking.reads.ts` NÃO casa (sem o client do Torre).
    - `fetchDrivers` é paginado (loop com `.range`).
    - Nenhuma função de escrita presente (`grep -nE "\\.(insert|upsert|update|delete)\\(" ranking.reads.ts` retorna vazio).
    - `cd api && bun --bun tsc --noEmit` → exit 0.
  </acceptance_criteria>
  <done>5 reads portados via rankSupabase, sem writes, tsc limpo (exit 0).</done>
</task>

<task type="auto">
  <name>Task 2: Fetch+parse CSV Sheets com cache Redis (ranking.sheets.ts)</name>
  <read_first>
    - C:\Users\antonio.magalhaes\Documents\Projetos\produção\ride-rank-buddy\src\services\sheetsService.ts (FONTE: CSV_URL gviz, parseCSVLine, parseCSV, SheetTrip — portar fetch+parse; substituir o cache in-memory do original por Redis)
    - api/src/redis/client.ts + api/src/modules/dashboard/dashboard.service.ts (PADRÃO de cache Redis: redis.get → JSON.parse → senão computa → redis.set EX TTL)
    - api/src/modules/ranking/ranking.types.ts (SheetTrip)
  </read_first>
  <files>api/src/modules/ranking/ranking.sheets.ts</files>
  <action>
    Criar `ranking.sheets.ts` portando o fetch+parse de sheetsService.ts e trocando o cache in-memory (cachedTrips/fetchPromise) pelo Redis do Torre:
    - Montar o `CSV_URL` a partir de `process.env.RANK_SHEET_ID` e `process.env.RANK_SHEET_TAB` (fallback para os valores públicos conhecidos `1MWTiaXU3HXW_iVn-n70WSk3o8rcHTRrQP2ac07W9cCU`/`DBLHHISTORICO`): `https://docs.google.com/spreadsheets/d/${id}/gviz/tq?tqx=out:csv&sheet=${encodeURIComponent(tab)}`.
    - Portar `parseCSVLine` e `parseCSV` 1:1 (lidam com aspas, vírgulas, linhas vazias; só incluem linhas com `trip_number`).
    - `getSheetTrips()`: tenta `redis.get('ranking:sheets:trips')` → se hit, `JSON.parse` e retorna; senão `fetch(CSV_URL)`, valida `response.ok` (senão throw), `parseCSV(text)`, grava `redis.set('ranking:sheets:trips', JSON.stringify(trips), 'EX', 60)` e retorna. Em erro de fetch, propagar o throw (NÃO retornar [] silenciosamente como o original fazia — o endpoint do Plan 04 trata o erro). Usar `import { redis } from '../../redis/client'`.
    Exportar `getSheetTrips` e (opcional) a const da cache key. NÃO logar o CSV inteiro.
  </action>
  <verify>
    <automated>cd api && grep -q "getSheetTrips" src/modules/ranking/ranking.sheets.ts && grep -q "'EX', 60" src/modules/ranking/ranking.sheets.ts && grep -q "gviz/tq" src/modules/ranking/ranking.sheets.ts && bun --bun tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Exporta `getSheetTrips(): Promise<SheetTrip[]>`.
    - Cache: usa `redis.get`/`redis.set('ranking:sheets:trips', ..., 'EX', 60)`.
    - CSV_URL montado de `RANK_SHEET_ID`/`RANK_SHEET_TAB` (com fallback público) — sem credencial no fetch.
    - `parseCSV` só inclui linhas com `trip_number`.
    - `cd api && bun --bun tsc --noEmit` → exit 0.
  </acceptance_criteria>
  <done>getSheetTrips busca CSV público, parseia e cacheia em Redis por 60s; tsc limpo (exit 0).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Torre backend → Supabase ride-rank | Reads via service_role (Plan 01). Dados retornados incluem PII de motoristas (driver_name) e avaliações. |
| Torre backend → docs.google.com (gviz) | Fetch de CSV PÚBLICO de terceiro (Google). Conteúdo é não-confiável (planilha editável por terceiros) e entra no parser. |
| Redis (cache) | CSV parseado fica em Redis (`ranking:sheets:trips`). Contém dados operacionais de viagens. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | Tampering | ranking.sheets.ts parser sobre CSV de terceiro | mitigate | Parser trata aspas/células ausentes e só aceita linhas com `trip_number`; downstream (Plan 02 transformTrips) filtra driver_id inválido e status != FECHADA. Sem `eval`/exec do conteúdo. |
| T-07-06 | Information Disclosure | logs do fetch/reads | mitigate | NÃO logar o CSV inteiro nem linhas de avaliação/PII; em erro logar só `response.status`/`error.message`. Service_role nunca aparece em log (vem do Plan 01). |
| T-07-07 | Denial of Service | re-fetch do CSV a cada request | mitigate | Cache Redis `EX 60` evita rebuscar o Sheets a cada chamada (D-V2 cache). |
| T-07-08 | Information Disclosure | reads expõem dados de outros tenants | accept | service_role bypassa RLS por design (D-V2-01); o gating de acesso fica no `authGuard` dos endpoints (Plan 04). Esta camada é interna ao server. |
</threat_model>

<verification>
- `cd api && bun --bun tsc --noEmit` → exit 0 nos 2 arquivos.
- `ranking.reads.ts` usa `rankSupabase` (5 tabelas), sem writes, sem Drizzle.
- `ranking.sheets.ts` usa Redis `EX 60` e monta CSV_URL gviz sem credencial.
- Nenhum arquivo loga PII/CSV completo/service_role.
</verification>

<success_criteria>
- 2 módulos de I/O portados com paridade e prontos para composição (Plan 04).
- Cache Redis curto no Sheets implementado.
</success_criteria>

<output>
After completion, create `.planning/phases/07-ranking-backend/07-03-SUMMARY.md`
</output>
