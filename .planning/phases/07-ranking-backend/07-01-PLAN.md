---
phase: 07-ranking-backend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/package.json
  - api/.env.example
  - api/src/modules/ranking/ranking.supabase.ts
autonomous: true
requirements:
  - PHASE7-RANKING-MODULE-SETUP
user_setup:
  - service: supabase-ride-rank
    why: "Projeto Supabase externo do ride-rank (vrlhfgfyjvkzfnafibnc). O service_role key é necessário porque o RLS bloqueia o anon em evaluations/driver_blocks/route_scores/evaluation_logs — só drivers é lido pelo anon."
    env_vars:
      - name: RANK_SUPABASE_URL
        source: "https://vrlhfgfyjvkzfnafibnc.supabase.co (já conhecido — Dashboard ride-rank → Settings → API → Project URL)"
      - name: RANK_SUPABASE_SERVICE_KEY
        source: "Dashboard ride-rank → Settings → API → service_role (secret). SERVER-SIDE ONLY — nunca expor no browser/VITE_*/logs."
      - name: RANK_SHEET_ID
        source: "1MWTiaXU3HXW_iVn-n70WSk3o8rcHTRrQP2ac07W9cCU (planilha pública gviz — sem credencial)"
      - name: RANK_SHEET_TAB
        source: "DBLHHISTORICO (aba da planilha)"

must_haves:
  truths:
    - "O backend tem a dependência @supabase/supabase-js instalada e bun tsc passa"
    - "Existe um client Supabase server-side apontando para o projeto ride-rank usando o service_role lido de env"
    - "As 4 envs RANK_* estão documentadas no api/.env.example com aviso de que o service_role é server-side only"
  artifacts:
    - path: "api/src/modules/ranking/ranking.supabase.ts"
      provides: "Client @supabase/supabase-js singleton para o Supabase do ride-rank (service_role)"
      exports: ["rankSupabase"]
    - path: "api/package.json"
      provides: "Dependência @supabase/supabase-js"
      contains: "@supabase/supabase-js"
    - path: "api/.env.example"
      provides: "Documentação das 4 envs RANK_*"
      contains: "RANK_SUPABASE_SERVICE_KEY"
  key_links:
    - from: "api/src/modules/ranking/ranking.supabase.ts"
      to: "process.env.RANK_SUPABASE_URL / RANK_SUPABASE_SERVICE_KEY"
      via: "createClient lê as envs no module load"
      pattern: "createClient\\("
---

<objective>
Cria a base do módulo `ranking`: adiciona a dependência `@supabase/supabase-js` ao backend, cria o client Supabase server-side que aponta para o projeto EXTERNO do ride-rank (`vrlhfgfyjvkzfnafibnc`) usando o `service_role` lido de env, e documenta as 4 envs `RANK_*` no `.env.example`.

Purpose: Implementa D-V2-01 (PROXY) — a credencial sensível do ride-rank fica server-side. Este client é a porta de entrada de TODAS as leituras do Supabase do ride-rank nos planos seguintes (03 e 04). Nenhum acesso direto do browser ao ride-rank.
Output: `api/src/modules/ranking/ranking.supabase.ts` (export `rankSupabase`), `@supabase/supabase-js` em `package.json`, envs em `.env.example`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-ranking-backend/07-CONTEXT.md
@.planning/MILESTONE-v2-ROADMAP.md

<interfaces>
<!-- Padrões do Torre que o executor deve seguir. Não explorar o codebase além disto. -->

Client de infra do Torre (PADRÃO de singleton lendo env no module-load) — api/src/redis/client.ts:
```typescript
import Redis from 'ioredis'
if (!process.env.REDIS_URL) { throw new Error('REDIS_URL is not defined') }
export const redis = new Redis(process.env.REDIS_URL, { maxRetriesPerRequest: 3, ... })
redis.on('error', (err) => { console.error('[redis] error:', err.message) })
export type RedisClient = typeof redis
```

NOTA: o Torre usa Drizzle/postgres.js (api/src/db/client.ts) APENAS para o DB próprio (torre-controle-prod). O `ranking` NÃO usa esse client — usa um client `@supabase/supabase-js` SEPARADO apontando para o Supabase EXTERNO do ride-rank. São DBs diferentes.

Runtime: Bun 1.3.13. `@supabase/supabase-js` funciona em Bun. tsconfig tem `noEmit:true`, `strict:true`, paths `@/* -> ./src/*`.
NOTA DE VERIFY: o ambiente tem bash; `bun --bun tsc --noEmit` retorna exit code (0 = limpo). CONFIE no exit code — não conte erros via grep do nome do módulo (tsc não prefixa o nome do arquivo de forma confiável).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar dependência @supabase/supabase-js</name>
  <read_first>
    - api/package.json (o arquivo sendo modificado — note que NÃO há @supabase/supabase-js hoje, só drizzle/postgres/ioredis)
  </read_first>
  <files>api/package.json</files>
  <action>
    Rodar `bun add @supabase/supabase-js` dentro de `api/` para instalar o SDK e gravá-lo em `dependencies` do `api/package.json`. Use a versão estável mais recente que o Bun resolver. NÃO adicionar nenhuma outra dependência. Confirmar que o lockfile do Bun (`bun.lock`/`bun.lockb`) foi atualizado.
  </action>
  <verify>
    <automated>cd api && grep -q "@supabase/supabase-js" package.json && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `api/package.json` lista `@supabase/supabase-js` em `dependencies`.
    - `cd api && bun install` roda sem erro (deps resolvidas).
  </acceptance_criteria>
  <done>@supabase/supabase-js presente em api/package.json e instalado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Client Supabase ride-rank server-side (rankSupabase)</name>
  <read_first>
    - api/src/redis/client.ts (PADRÃO de client singleton que valida env no module-load e exporta a instância + type)
    - C:\Users\antonio.magalhaes\Documents\Projetos\produção\ride-rank-buddy\src\services\supabaseService.ts (mostra que os reads chamam `supabase.from('<tabela>').select(...)` — o client precisa expor essa mesma API; este client substitui o `@/integrations/supabase/client` do ride-rank)
    - .planning/phases/07-ranking-backend/07-CONTEXT.md (D-V2-01: service_role server-side; tabelas: evaluations, driver_blocks, evaluation_logs, route_scores, drivers)
  </read_first>
  <files>api/src/modules/ranking/ranking.supabase.ts</files>
  <behavior>
    - Importa `createClient` de `@supabase/supabase-js`.
    - No module-load, lê `process.env.RANK_SUPABASE_URL` e `process.env.RANK_SUPABASE_SERVICE_KEY`; se qualquer uma faltar, lança `Error` com mensagem clara (mesmo padrão de fail-fast do redis/client.ts) — NUNCA logar o valor da key.
    - Cria o client com `auth: { persistSession: false, autoRefreshToken: false }` (server-side, sem sessão de browser) e o exporta como `rankSupabase`.
    - Exporta também `type RankSupabase = typeof rankSupabase`.
  </behavior>
  <action>
    Criar `api/src/modules/ranking/ranking.supabase.ts` seguindo o padrão de singleton do `redis/client.ts`: valida as 2 envs no topo do módulo (fail-fast com `throw new Error`), chama `createClient(RANK_SUPABASE_URL, RANK_SUPABASE_SERVICE_KEY, { auth: { persistSession: false, autoRefreshToken: false } })` e exporta `rankSupabase` + `type RankSupabase`. O service_role usa o `apikey`/`Authorization` internamente — bypassa o RLS, por isso lê todas as tabelas. NÃO inicializar nenhuma sessão de auth, NÃO logar a key. Não tipar contra um schema gerado (os reads do Plan 03 fazem o cast dos tipos).
  </action>
  <verify>
    <automated>cd api && RANK_SUPABASE_URL=https://x.supabase.co RANK_SUPABASE_SERVICE_KEY=test bun --bun tsc --noEmit && echo TSC_OK</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo exporta `rankSupabase` (instância do SupabaseClient) e `type RankSupabase`.
    - `cd api && bun --bun tsc --noEmit` sai com exit 0 (sem erros).
    - Importar o módulo SEM as envs lança erro (fail-fast) — verificável: `cd api && bun -e "process.env.RANK_SUPABASE_URL=''; process.env.RANK_SUPABASE_SERVICE_KEY=''; import('./src/modules/ranking/ranking.supabase.ts').then(()=>process.exit(1)).catch(()=>{console.log('THREW'); process.exit(0)})"` imprime `THREW` e sai 0.
  </acceptance_criteria>
  <done>rankSupabase exportado, valida envs no load, tsc limpo (exit 0).</done>
</task>

<task type="auto">
  <name>Task 3: Documentar envs RANK_* no .env.example</name>
  <read_first>
    - api/.env.example (o arquivo sendo modificado — note o comentário-padrão usado em outras seções sensíveis, ex. VAPID "private fica só no backend")
    - .planning/MILESTONE-v2-ROADMAP.md (seção "Pré-requisitos" — service_role server-side; Sheets é CSV público sem credencial)
  </read_first>
  <files>api/.env.example</files>
  <action>
    Adicionar uma seção `# Ranking (Phase 7 — Supabase EXTERNO do ride-rank)` ao final do `api/.env.example` com as 4 chaves: `RANK_SUPABASE_URL=https://vrlhfgfyjvkzfnafibnc.supabase.co`, `RANK_SUPABASE_SERVICE_KEY=` (vazio, com comentário "service_role do ride-rank — SERVER-SIDE ONLY, nunca expor no browser/VITE_*"), `RANK_SHEET_ID=1MWTiaXU3HXW_iVn-n70WSk3o8rcHTRrQP2ac07W9cCU` e `RANK_SHEET_TAB=DBLHHISTORICO` (com comentário "CSV público gviz — sem credencial"). Não preencher o service_role (é segredo do user-setup).
  </action>
  <verify>
    <automated>cd api && grep -q "RANK_SUPABASE_SERVICE_KEY" .env.example && grep -q "RANK_SHEET_ID=1MWTiaXU3HXW" .env.example && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `.env.example` contém as 4 chaves `RANK_*`.
    - `RANK_SUPABASE_SERVICE_KEY` aparece vazio com comentário de "server-side only".
    - `RANK_SHEET_ID` e `RANK_SHEET_TAB` preenchidos com os valores públicos conhecidos.
  </acceptance_criteria>
  <done>4 envs RANK_* documentadas com aviso de segurança no service_role.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Torre backend → Supabase ride-rank | A API Elysia (server) usa o `service_role` (bypassa RLS) para ler o projeto externo. Esta key é o segredo mais sensível desta fase. |
| env / código-fonte → repositório | `.env.example` NÃO deve conter o valor real do service_role; o valor real vive só no `.env` do servidor (não versionado). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information Disclosure | ranking.supabase.ts (service_role) | mitigate | Key lida só de `process.env.RANK_SUPABASE_SERVICE_KEY`; NUNCA logada (sem console.log do valor); nunca prefixada com `VITE_` (não chega ao bundle do front). |
| T-07-02 | Information Disclosure | api/.env.example | mitigate | `RANK_SUPABASE_SERVICE_KEY=` fica VAZIO no exemplo + comentário "server-side only". Valor real só no `.env` do servidor (gitignored). |
| T-07-03 | Elevation of Privilege | service_role bypassa RLS do ride-rank | accept | Inerente ao proxy (D-V2-01): o anon não lê 4 das 5 tabelas. Mitigado a jusante: endpoints atrás do `authGuard` (Plan 04); nesta fase só leitura. |
</threat_model>

<verification>
- `cd api && bun install` resolve `@supabase/supabase-js`.
- `cd api && RANK_SUPABASE_URL=https://x.supabase.co RANK_SUPABASE_SERVICE_KEY=test bun --bun tsc --noEmit` → exit 0.
- `ranking.supabase.ts` exporta `rankSupabase`; falha de env é fail-fast no load (teste do `bun -e` imprime THREW).
- `.env.example` tem as 4 envs RANK_* (service_role vazio + comentário server-side).
- Nenhum `console.log`/`logger` imprime o valor do service_role.
</verification>

<success_criteria>
- Módulo `api/src/modules/ranking/` criado com o client Supabase ride-rank server-side.
- Dependência instalada, tsc limpo, envs documentadas.
- Base pronta para os reads (Plan 03) consumirem `rankSupabase`.
</success_criteria>

<output>
After completion, create `.planning/phases/07-ranking-backend/07-01-SUMMARY.md`
</output>
