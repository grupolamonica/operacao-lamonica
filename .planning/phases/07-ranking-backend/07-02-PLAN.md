---
phase: 07-ranking-backend
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - api/src/modules/ranking/ranking.types.ts
  - api/src/modules/ranking/ranking.scoring.ts
  - api/src/modules/ranking/ranking.scoring.test.ts
autonomous: true
requirements:
  - PHASE7-PORT-SCORING
  - PHASE7-SCORING-PARITY
  - PHASE7-EDEN-TYPES

must_haves:
  truths:
    - "O scoring do ride-rank (calculateTripScore, transformTrips, transformSheetNoShowTrips, deriveDrivers) roda server-side com paridade numérica ao original"
    - "Os tipos do ranking (Trip, Driver, Block, SheetTrip, RouteScoreRecord, StatusMetrics) existem no backend e são exportáveis"
    - "Um teste de paridade prova que calculateTripScore e deriveDrivers produzem os mesmos números do app original em casos conhecidos"
  artifacts:
    - path: "api/src/modules/ranking/ranking.types.ts"
      provides: "Tipos portados do ride-rank (mockData.ts + SheetTrip + RouteScoreRecord + os *Record de leitura)"
      contains: "export interface Driver"
    - path: "api/src/modules/ranking/ranking.scoring.ts"
      provides: "Funções puras de scoring portadas de dataAdapter.ts (sem I/O)"
      exports: ["calculateTripScore", "transformTrips", "transformSheetNoShowTrips", "deriveDrivers", "extractUniqueOccurrences", "normalizeStatusAgrupado", "parseDateBR"]
    - path: "api/src/modules/ranking/ranking.scoring.test.ts"
      provides: "Teste de paridade do scoring (bun test)"
      contains: "calculateTripScore"
  key_links:
    - from: "api/src/modules/ranking/ranking.scoring.ts"
      to: "api/src/modules/ranking/ranking.types.ts"
      via: "import type { Trip, Driver, StatusMetrics }"
      pattern: "from '\\./ranking\\.types'"
    - from: "api/src/modules/ranking/ranking.scoring.test.ts"
      to: "api/src/modules/ranking/ranking.scoring.ts"
      via: "import das funções sob teste"
      pattern: "from '\\./ranking\\.scoring'"
---

<objective>
Porta a CAMADA PURA do ranking (sem I/O) do ride-rank para o backend: os tipos (`ranking.types.ts`) e o algoritmo de scoring (`ranking.scoring.ts`) de `dataAdapter.ts`, mais um teste de paridade (`bun test`) que prova que os números batem com o app original.

Purpose: Implementa D-V2-04 (scoring reusado SEM reescrever o algoritmo) e PHASE7-SCORING-PARITY. Esta é a base testável e independente do ranking — não toca rede, Supabase nem Redis, então roda em paralelo ao Plan 01. Plan 03 (reads) e Plan 04 (service+endpoints) consomem esses tipos e funções.
Output: tipos + scoring puro + teste de paridade verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-ranking-backend/07-CONTEXT.md

<interfaces>
<!-- Runtime: Bun 1.3.13 (test runner nativo `bun test`; sem vitest/jest). tsconfig strict, noEmit, paths @/*. -->
<!-- O ride-rank usa alias `@/...` (= src/). No backend o equivalente é `@/...` (= api/src/). Porém, dentro do módulo ranking, USE imports relativos `./ranking.types` (mais robusto p/ bun test). -->
<!-- ATENÇÃO: dataAdapter.ts tem caracteres mojibake ("â€”", "Nao atribuido") herdados de encoding. Ao portar, NORMALIZE para os caracteres corretos: o em-dash deve ser "—" e o fallback de nome "Não atribuído". Isso NÃO altera o scoring (são só strings de display). -->
</interfaces>
</context>

<feature>
  <name>Scoring puro do ranking (paridade com ride-rank)</name>
  <files>api/src/modules/ranking/ranking.types.ts, api/src/modules/ranking/ranking.scoring.ts, api/src/modules/ranking/ranking.scoring.test.ts</files>
  <behavior>
    Regras EXATAS a preservar (de dataAdapter.ts — não reescrever, portar):
    - statusPointOrigem: 'ON TIME'|'EARLY' => +1; 'DELAY' => -3; senão 0.
    - statusPointDestino: 'ON TIME' => +1; 'EARLY' => -1; 'DELAY' => -3; senão 0.
    - calculateTripScore(trip, basePoints=1) = basePoints + statusPointOrigem(status_eta) + statusPointDestino(status_eta_destino).
    - transformTrips: filtra driver_id válido (não vazio, != '0') E normalizeStatusAgrupado(status_agrupado) === 'FECHADA'; resolve status (usa valor existente se ∈ {ON TIME,EARLY,DELAY}, senão calcula de datas), conta ocorrências (ignora vazias/'-'/ignoredOccurrences), calcula base via getRouteBasePoints e score_final.
    - transformSheetNoShowTrips: filtra driver_id válido E isSheetNoShowStatus(status_agrupado); score_final=0.
    - calcStatusMetrics: %, arredondado a 1 casa (Math.round(x*1000)/10), sobre trips com status válido.
    - deriveDrivers: agrupa por driver_id, nome = `${driverName} (${driver_id})`, pontuacao = soma dos score_final, ocorrencias = count(ocorrencia), status default 'ATIVO', vinculo default '—', ordena por pontuacao DESC.

    Casos de teste de PARIDADE (mínimos):
    - Test 1: calculateTripScore({status_eta:'ON TIME', status_eta_destino:'ON TIME'}, 1) === 3.
    - Test 2: calculateTripScore({status_eta:'DELAY', status_eta_destino:'EARLY'}, 1) === -3 (1 -3 -1).
    - Test 3: calculateTripScore({status_eta:'EARLY', status_eta_destino:'DELAY'}, 2) === 0 (2 +1 -3).
    - Test 4: transformTrips ignora linhas com status_agrupado != FECHADA e driver_id '0'/vazio.
    - Test 5: deriveDrivers soma scores por motorista e ordena desc; nome formatado como "Nome (id)".
    - Test 6: calcStatusMetrics retorna onTime/early/delay corretos em 1 casa decimal para uma amostra fixa.
  </behavior>
  <implementation>
    RED: criar `ranking.scoring.test.ts` com os 6 casos acima ANTES da implementação; rodar `bun test` no módulo → deve FALHAR (funções inexistentes).
    GREEN:
      1. Portar `ranking.types.ts` de `mockData.ts`: copiar APENAS as definições de tipo (DriverStatus, Comunicacao, DesvioRota, Postura, BlockType, StatusMetrics, Driver, Trip, OperatorEvaluation, Block). NÃO portar mockDrivers/mockTrips/mockBlocks nem os helpers de cor/label de UI. Adicionar `SheetTrip` (de sheetsService.ts) e `RouteScoreRecord` (de routeScoreService.ts) e os *Record de leitura (EvaluationRecord, DriverBlockRecord, EvaluationLogRecord, DriverRecord de supabaseService.ts) aqui — serão usados pelos Plans 03/04. Corrigir mojibake ("—", "Não atribuído").
      2. Portar `ranking.scoring.ts` de `dataAdapter.ts`: copiar 1:1 as funções puras — normalizeStatusAgrupado, isSheetNoShowStatus, parseDateBR, parseFlexibleDate, calculateDateDiffMinutes, calculateStatusFromDates, resolveStatus, statusPointOrigem, statusPointDestino, calculateTripScore, isOcorrenciaValida, extractUniqueOccurrences, transformTrips, transformSheetNoShowTrips, calcStatusMetrics, deriveDrivers. INCLUIR getRouteBasePoints aqui OU importá-lo (será definido no Plan 03 em ranking.routes.ts) — para manter este plano independente, COPIE `getRouteBasePoints` para `ranking.scoring.ts` como helper interno (o Plan 03 reexporta de ranking.routes.ts; combine na composição). Trocar imports `@/services/*` e `@/data/mockData` por `./ranking.types`. NÃO portar deriveBlocks (retorna []), NÃO portar nada que toque supabase/fetch.
      Rodar `bun test` → deve PASSAR.
    REFACTOR (se necessário): remover imports não usados (tsconfig tem noUnusedLocals).
    Commits: `test(07-02): paridade scoring ranking` (RED), `feat(07-02): portar tipos + scoring puro do ride-rank` (GREEN).
  </implementation>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (nenhuma nova) | Este plano é 100% puro (sem I/O, sem rede, sem credencial). Não cruza trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Tampering | scoring portado diverge do original | mitigate | Teste de paridade `bun test` com casos numéricos fixos (calculateTripScore, deriveDrivers, métricas) garante que o port preserva o algoritmo (D-V2-04). |
</threat_model>

<verification>
- `cd api && bun test src/modules/ranking/ranking.scoring.test.ts` → todos os casos passam.
- `cd api && bun --bun tsc --noEmit` sem erros nos arquivos do módulo.
- `ranking.scoring.ts` NÃO importa de `@supabase/supabase-js`, nem usa `fetch`/`redis` (é puro). Verificável: `grep -E "supabase|fetch\\(|redis" api/src/modules/ranking/ranking.scoring.ts` retorna vazio.
- `ranking.types.ts` exporta `Driver`, `Trip`, `Block`, `SheetTrip`, `RouteScoreRecord`.
</verification>

<success_criteria>
- Tipos e scoring portados com paridade comprovada por teste.
- Camada pura independente (sem I/O), pronta para o service de composição (Plan 04).
</success_criteria>

<output>
After completion, create `.planning/phases/07-ranking-backend/07-02-SUMMARY.md`
</output>
