---
phase: 07-ranking-backend
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - api/src/modules/ranking/ranking.types.ts
  - api/src/modules/ranking/ranking.routes.ts
  - api/src/modules/ranking/ranking.scoring.ts
  - api/src/modules/ranking/ranking.scoring.test.ts
autonomous: true
requirements:
  - PHASE7-PORT-SCORING
  - PHASE7-PORT-ROUTESCORES
  - PHASE7-SCORING-PARITY
  - PHASE7-EDEN-TYPES

must_haves:
  truths:
    - "O scoring do ride-rank (calculateTripScore, transformTrips, transformSheetNoShowTrips, deriveDrivers) roda server-side com paridade numérica ao original"
    - "getRouteBasePoints (função pura) é a FONTE ÚNICA da pontuação-base de rota e é importada por ranking.scoring.ts (sem cópia duplicada)"
    - "Os tipos do ranking (Trip, Driver, Block, SheetTrip, RouteScoreRecord, StatusMetrics + os *Record de leitura) existem no backend e são exportáveis"
    - "Um teste de paridade — incluindo ≥1 golden-sample de SheetTrip REAL com score_final/pontuacao travados — prova paridade ponta-a-ponta com o app original"
  artifacts:
    - path: "api/src/modules/ranking/ranking.types.ts"
      provides: "Tipos portados do ride-rank (mockData.ts + SheetTrip + RouteScoreRecord + os *Record de leitura)"
      contains: "export interface Driver"
    - path: "api/src/modules/ranking/ranking.routes.ts"
      provides: "getRouteBasePoints (função pura, FONTE ÚNICA) portada de routeScoreService.ts"
      exports: ["getRouteBasePoints"]
    - path: "api/src/modules/ranking/ranking.scoring.ts"
      provides: "Funções puras de scoring portadas de dataAdapter.ts (sem I/O); importa getRouteBasePoints de ranking.routes.ts"
      exports: ["calculateTripScore", "transformTrips", "transformSheetNoShowTrips", "deriveDrivers", "extractUniqueOccurrences", "normalizeStatusAgrupado", "parseDateBR"]
    - path: "api/src/modules/ranking/ranking.scoring.test.ts"
      provides: "Teste de paridade do scoring (bun test) — casos sintéticos + golden-sample real"
      contains: "calculateTripScore"
  key_links:
    - from: "api/src/modules/ranking/ranking.scoring.ts"
      to: "api/src/modules/ranking/ranking.routes.ts"
      via: "import { getRouteBasePoints } — fonte única, sem cópia"
      pattern: "from '\\./ranking\\.routes'"
    - from: "api/src/modules/ranking/ranking.scoring.ts"
      to: "api/src/modules/ranking/ranking.types.ts"
      via: "import type { Trip, Driver, StatusMetrics, SheetTrip, RouteScoreRecord }"
      pattern: "from '\\./ranking\\.types'"
    - from: "api/src/modules/ranking/ranking.scoring.test.ts"
      to: "api/src/modules/ranking/ranking.scoring.ts"
      via: "import das funções sob teste"
      pattern: "from '\\./ranking\\.scoring'"
---

<objective>
Porta a CAMADA PURA do ranking (sem I/O): os tipos (`ranking.types.ts`), o resolvedor de pontuação-base de rota (`ranking.routes.ts` — função pura `getRouteBasePoints`, FONTE ÚNICA) e o algoritmo de scoring (`ranking.scoring.ts`) de `dataAdapter.ts`, mais um teste de paridade (`bun test`) que inclui ≥1 golden-sample de dados REAIS.

Purpose: Implementa D-V2-04 (scoring reusado SEM reescrever o algoritmo), PHASE7-PORT-ROUTESCORES (a função pura `getRouteBasePoints` — o READ da tabela route_scores fica no Plan 03) e PHASE7-SCORING-PARITY. Camada independente: não toca rede/Supabase/Redis, então roda em paralelo ao Plan 01. Plan 03 (reads) e Plan 04 (service+endpoints) consomem esses tipos e funções.
Output: tipos + getRouteBasePoints (fonte única) + scoring puro + teste de paridade verde (inclui golden-sample).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-ranking-backend/07-CONTEXT.md

<interfaces>
<!-- Runtime: Bun 1.3.13 (test runner nativo `bun test`; sem vitest/jest). tsconfig strict, noEmit, paths @/*. -->
<!-- O ride-rank usa alias `@/...` (= src/). Dentro do módulo ranking USE imports relativos `./ranking.types`, `./ranking.routes` (mais robusto p/ bun test). -->
<!-- VERIFY: `bun test <arquivo>` e `bun --bun tsc --noEmit` sinalizam por EXIT CODE (0 = verde). Confie no exit code; não faça grep de "pass"/"fail" na saída. -->
<!-- PARIDADE ESTRITA (fase read-only): o dataAdapter.ts emite literais com mojibake como VALORES DE DADOS, não só display:
       - fallback de status_eta / status_eta_destino  (transformTrips ~L185-187)
       - nome fallback 'Nao atribuido' (~L172) ; vinculo default (deriveDrivers ~L287)
     NÃO normalizar/"corrigir" esses literais — portá-los BYTE-A-BYTE (copiar exatamente os bytes do arquivo fonte) para preservar paridade exata da saída do app original. A correção de encoding fica para a Phase 8 (UI). -->
</interfaces>
</context>

<feature>
  <name>Scoring puro do ranking + getRouteBasePoints (paridade com ride-rank)</name>
  <files>api/src/modules/ranking/ranking.types.ts, api/src/modules/ranking/ranking.routes.ts, api/src/modules/ranking/ranking.scoring.ts, api/src/modules/ranking/ranking.scoring.test.ts</files>
  <read_first>
    - C:\Users\antonio.magalhaes\Documents\Projetos\produção\ride-rank-buddy\src\services\dataAdapter.ts (FONTE do scoring — portar 1:1; COPIAR os literais de fallback byte-a-byte, sem normalizar encoding)
    - C:\Users\antonio.magalhaes\Documents\Projetos\produção\ride-rank-buddy\src\services\routeScoreService.ts (FONTE de getRouteBasePoints ~L44-69, função pura)
    - C:\Users\antonio.magalhaes\Documents\Projetos\produção\ride-rank-buddy\src\data\mockData.ts (FONTE dos tipos — portar SÓ as interfaces, não os mock* nem helpers de UI)
    - C:\Users\antonio.magalhaes\Documents\Projetos\produção\ride-rank-buddy\src\services\sheetsService.ts (FONTE do tipo SheetTrip + colunas reais — usar como base do golden-sample)
    - C:\Users\antonio.magalhaes\Documents\Projetos\produção\ride-rank-buddy\src\services\supabaseService.ts (FONTE dos *Record: EvaluationRecord, DriverBlockRecord, EvaluationLogRecord, DriverRecord)
  </read_first>
  <behavior>
    Regras EXATAS a preservar (de dataAdapter.ts — não reescrever, portar):
    - statusPointOrigem: 'ON TIME'|'EARLY' => +1; 'DELAY' => -3; senão 0.
    - statusPointDestino: 'ON TIME' => +1; 'EARLY' => -1; 'DELAY' => -3; senão 0.
    - calculateTripScore(trip, basePoints=1) = basePoints + statusPointOrigem(status_eta) + statusPointDestino(status_eta_destino).
    - transformTrips: filtra driver_id válido (não vazio, != '0') E normalizeStatusAgrupado(status_agrupado) === 'FECHADA'; resolve status (usa valor existente se ∈ {ON TIME,EARLY,DELAY}, senão calcula de datas; fallback = literal byte-a-byte do original), conta ocorrências (ignora vazias/'-'/ignoredOccurrences), base via getRouteBasePoints, score_final.
    - transformSheetNoShowTrips: filtra driver_id válido E isSheetNoShowStatus(status_agrupado); score_final=0.
    - calcStatusMetrics: %, arredondado a 1 casa (Math.round(x*1000)/10), sobre trips com status válido.
    - deriveDrivers: agrupa por driver_id, nome = `${driverName} (${driver_id})`, pontuacao = soma score_final, ocorrencias = count(ocorrencia), status default 'ATIVO', vinculo default (literal byte-a-byte do original), ordena por pontuacao DESC.
    - getRouteBasePoints: filtra por origin+destination; sem match → 1; senão acha ativo na data (refDate ∈ [data_inicio, data_fim]) → pontuacao; fallback matching[0].pontuacao ?? 1.

    Casos de teste (mínimos):
    - Test 1 (sintético): calculateTripScore({status_eta:'ON TIME', status_eta_destino:'ON TIME'}, 1) === 3.
    - Test 2 (sintético): calculateTripScore({status_eta:'DELAY', status_eta_destino:'EARLY'}, 1) === -3.
    - Test 3 (sintético): calculateTripScore({status_eta:'EARLY', status_eta_destino:'DELAY'}, 2) === 0.
    - Test 4 (sintético): transformTrips ignora linhas com status_agrupado != FECHADA e driver_id '0'/vazio.
    - Test 5 (sintético): deriveDrivers soma scores por motorista e ordena desc; nome "Nome (id)".
    - Test 6 (sintético): calcStatusMetrics retorna onTime/early/delay em 1 casa decimal numa amostra fixa.
    - Test 7 (sintético): getRouteBasePoints retorna 1 sem rota; retorna a pontuacao do registro ativo na data quando há match.
    - Test 8 (GOLDEN-SAMPLE — paridade ponta-a-ponta, OBRIGATÓRIO): montar ≥1 objeto `SheetTrip` REAL representativo de FECHADA (datas em formato BR "DD/MM/AAAA HH:MM:SS", status_eta/status_eta_destino preenchidos, driver_id válido) com TODOS os campos que transformTrips lê. Passar por `transformTrips([sample], [], [])` → `deriveDrivers(...)` e travar com asserts EXATOS: `score_final` do trip e `pontuacao` do driver com os valores numéricos esperados (calculados à mão a partir das regras acima). Isto cobre resolveStatus + parseDateBR (timezone) + base default 1 ponta-a-ponta. Incluir também 1 linha NO SHOW (status_agrupado='NO SHOW') e assertar que `transformSheetNoShowTrips` produz `score_final===0`.
  </behavior>
  <implementation>
    RED: criar `ranking.scoring.test.ts` com os 8 casos (incl. golden-sample) ANTES da implementação; `cd api && bun test src/modules/ranking/ranking.scoring.test.ts` → exit != 0 (FALHA, funções inexistentes).
    GREEN:
      1. `ranking.types.ts` — portar de mockData.ts SÓ as interfaces (DriverStatus, Comunicacao, DesvioRota, Postura, BlockType, StatusMetrics, Driver, Trip, OperatorEvaluation, Block). Adicionar `SheetTrip` (de sheetsService.ts), `RouteScoreRecord` (de routeScoreService.ts) e os *Record de leitura (EvaluationRecord, DriverBlockRecord, EvaluationLogRecord, DriverRecord de supabaseService.ts). NÃO portar mock*/helpers de UI.
      2. `ranking.routes.ts` — portar `getRouteBasePoints` 1:1 de routeScoreService.ts (FONTE ÚNICA). Importar `RouteScoreRecord` de `./ranking.types`. NÃO portar createRouteScore/updateRouteScore/deleteRouteScore (writes, Phase 9).
      3. `ranking.scoring.ts` — portar de dataAdapter.ts 1:1 as funções puras: normalizeStatusAgrupado, isSheetNoShowStatus, parseDateBR, parseFlexibleDate, calculateDateDiffMinutes, calculateStatusFromDates, resolveStatus, statusPointOrigem, statusPointDestino, calculateTripScore, isOcorrenciaValida, extractUniqueOccurrences, transformTrips, transformSheetNoShowTrips, calcStatusMetrics, deriveDrivers. IMPORTAR `getRouteBasePoints` de `./ranking.routes` (NÃO copiar — fonte única, evita drift de paridade e conflito com noUnusedLocals). Trocar imports `@/services/*` e `@/data/mockData` por `./ranking.types`. PRESERVAR literais de fallback byte-a-byte (paridade estrita — copiar exatamente do fonte). NÃO portar deriveBlocks (retorna []) nem nada com I/O.
      Rodar `cd api && bun test src/modules/ranking/ranking.scoring.test.ts` → exit 0 (PASSA, incl. golden-sample).
    REFACTOR (se necessário): remover imports não usados (noUnusedLocals).
    Commits: `test(07-02): paridade scoring ranking + golden-sample` (RED), `feat(07-02): portar tipos + getRouteBasePoints + scoring puro do ride-rank` (GREEN).
  </implementation>
  <acceptance_criteria>
    - `cd api && bun test src/modules/ranking/ranking.scoring.test.ts` sai com exit 0 (8 casos passam, incl. golden-sample com asserts numéricos exatos).
    - `cd api && bun --bun tsc --noEmit` sai com exit 0.
    - `ranking.scoring.ts` IMPORTA getRouteBasePoints de `./ranking.routes` (não há definição local) — verificável: `grep -q "from './ranking.routes'" api/src/modules/ranking/ranking.scoring.ts` E `grep -Lq "function getRouteBasePoints" api/src/modules/ranking/ranking.scoring.ts` (não define a função localmente).
    - `ranking.scoring.ts` é puro: `grep -nE "supabase|fetch\\(|redis" api/src/modules/ranking/ranking.scoring.ts` retorna vazio.
    - `ranking.types.ts` exporta Driver, Trip, Block, SheetTrip, RouteScoreRecord.
    - Literais de fallback preservados byte-a-byte (nenhuma normalização aplicada).
  </acceptance_criteria>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (nenhuma nova) | Este plano é 100% puro (sem I/O, sem rede, sem credencial). Não cruza trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Tampering | scoring portado diverge do original | mitigate | Teste de paridade `bun test` com casos numéricos fixos + golden-sample de dados REAIS (calculateTripScore, deriveDrivers, métricas, getRouteBasePoints) garante que o port preserva o algoritmo (D-V2-04). |
| T-07-13 | Tampering | drift de paridade por cópia duplicada de getRouteBasePoints | mitigate | Fonte ÚNICA em ranking.routes.ts; ranking.scoring.ts importa (não copia). Acceptance criteria proíbe definição local. |
</threat_model>

<verification>
- `cd api && bun test src/modules/ranking/ranking.scoring.test.ts` → exit 0 (8 casos, incl. golden-sample).
- `cd api && bun --bun tsc --noEmit` → exit 0.
- `ranking.scoring.ts` importa getRouteBasePoints de `./ranking.routes` (fonte única; sem definição local).
- `ranking.scoring.ts` é puro (sem supabase/fetch/redis).
- `ranking.types.ts` exporta os tipos consumidos pelos Plans 03/04.
- Literais de fallback portados byte-a-byte (sem normalização — paridade estrita).
</verification>

<success_criteria>
- Tipos, getRouteBasePoints (fonte única) e scoring portados com paridade comprovada por teste (sintético + golden-sample com asserts numéricos).
- Camada pura independente (sem I/O), pronta para os reads (Plan 03) e o service de composição (Plan 04).
</success_criteria>

<output>
After completion, create `.planning/phases/07-ranking-backend/07-02-SUMMARY.md`
</output>
