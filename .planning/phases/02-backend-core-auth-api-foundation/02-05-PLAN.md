---
phase: 02-backend-core-auth-api-foundation
plan: 05
type: execute
wave: 3
depends_on: ["02-03"]
files_modified:
  - api/src/modules/trips/trips.service.ts
  - api/src/modules/trips/trips.plugin.ts
  - api/src/modules/drivers/drivers.service.ts
  - api/src/modules/drivers/drivers.plugin.ts
  - api/src/modules/alerts/alerts.service.ts
  - api/src/modules/alerts/alerts.plugin.ts
  - api/src/modules/vehicles/vehicles.service.ts
  - api/src/modules/vehicles/vehicles.plugin.ts
  - api/src/modules/dashboard/dashboard.service.ts
  - api/src/modules/dashboard/dashboard.plugin.ts
autonomous: true
requirements:
  - PHASE2-TRIPS-CRUD
  - PHASE2-TRIPS-FILTERS
  - PHASE2-TRIPS-STATS
  - PHASE2-DRIVERS-CRUD
  - PHASE2-DRIVERS-STATS
  - PHASE2-ALERTS-CRUD
  - PHASE2-ALERTS-ASSIGN
  - PHASE2-ALERTS-TREATMENTS
  - PHASE2-VEHICLES-READ
  - PHASE2-DASHBOARD-KPIS
user_setup: []
must_haves:
  truths:
    - "GET /api/trips returns array of trips matching seed; supports query params status, slaStatus, clientName, driverName, priority, routeCode, search, page, limit"
    - "GET /api/trips/:id returns one trip joined with driver/vehicle/client/route"
    - "GET /api/trips/stats returns KPIViagens-shape object (total, noPrazo, emRisco, atrasadas, progressoMedio)"
    - "GET /api/drivers supports status, base, search filters; returns Driver shape with documents joined"
    - "GET /api/drivers/stats returns KPIMotoristas shape"
    - "GET /api/alerts supports severity, status, type, clientName, routeCode, assignedTo, period, search filters"
    - "PATCH /api/alerts/:id/assign assigns alert to a user; updates status to em_tratativa"
    - "POST /api/alerts/:id/treatments inserts a treatment row"
    - "PATCH /api/alerts/:id/resolve sets status='resolvido' + resolved_at=now"
    - "GET /api/alerts/stats returns KPIAlertas shape"
    - "GET /api/vehicles returns vehicles list"
    - "GET /api/dashboard/kpis returns KPIDashboard shape with Redis 30s cache"
    - "All endpoints protected by authGuard (401 without cookie)"
  artifacts:
    - path: "api/src/modules/trips/trips.plugin.ts"
      provides: "/api/trips, /api/trips/:id, /api/trips/stats"
      exports: ["tripsPlugin"]
    - path: "api/src/modules/drivers/drivers.plugin.ts"
      provides: "/api/drivers, /api/drivers/:id, /api/drivers/stats"
      exports: ["driversPlugin"]
    - path: "api/src/modules/alerts/alerts.plugin.ts"
      provides: "/api/alerts, /api/alerts/:id, /api/alerts/:id/assign, /api/alerts/:id/treatments, /api/alerts/:id/resolve, /api/alerts/stats"
      exports: ["alertsPlugin"]
    - path: "api/src/modules/vehicles/vehicles.plugin.ts"
      provides: "/api/vehicles"
      exports: ["vehiclesPlugin"]
    - path: "api/src/modules/dashboard/dashboard.plugin.ts"
      provides: "/api/dashboard/kpis"
      exports: ["dashboardPlugin"]
  key_links:
    - from: "every plugin"
      to: "api/src/lib/rbac.ts (authGuard)"
      via: ".use(authGuard)"
      pattern: "\\.use\\(authGuard\\)"
    - from: "alerts.plugin.ts treatments POST"
      to: "treatments table"
      via: "db.insert(treatments)"
      pattern: "db\\.insert\\(treatments\\)"
    - from: "dashboard.plugin.ts"
      to: "Redis kpi cache"
      via: "redis.get/set with EX 30"
      pattern: "kpi:dashboard"
---

<objective>
Implement the four CRUD modules (trips, drivers, alerts, vehicles) plus the dashboard module. Filter contracts MUST exactly match the frontend hook query params (Phase 3 will swap the mock hooks for Eden Treaty calls — same param names = zero churn). Output shapes match `torre-de-controle/src/data/types.ts` so Eden Treaty types align with frontend's existing type expectations.

Purpose: Roadmap success criterion: "GET /api/trips retorna dados reais. Auth funcional. Swagger acessível." This plan delivers the data API; auth was Plan 04; swagger and bootstrap are Plan 06.
Output: Five Elysia plugins ready to be composed by Plan 06.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ARCHITECTURE.md
@.planning/phases/02-backend-core-auth-api-foundation/02-CONTEXT.md
@.planning/phases/02-backend-core-auth-api-foundation/02-RESEARCH.md
@.planning/phases/02-backend-core-auth-api-foundation/02-PATTERNS.md
@.planning/phases/02-backend-core-auth-api-foundation/02-04-PLAN.md
@torre-de-controle/src/data/types.ts
@torre-de-controle/src/hooks/useTrips.ts
@torre-de-controle/src/hooks/useDrivers.ts
@torre-de-controle/src/hooks/useAlerts.ts

<interfaces>
<!-- Filter param names MUST match frontend hooks exactly (Phase 3 transition is param-clean). -->

From useTrips.ts (lines 13-30):
```typescript
filters: { status?, slaStatus?, clientName?, driverName?, priority?, routeCode?, search? }
```

From useDrivers.ts:
```typescript
filters: { status?, base?, search? }
```

From useAlerts.ts:
```typescript
filters: { severity?, status?, type?, clientName?, routeCode?, assignedTo?, period?, search? }
// period: 'today' | '7d' | '30d'
```

KPI shapes (from types.ts):
```typescript
KPIViagens     = { total:{count}, noPrazo:{count,pct}, emRisco:{count,pct}, atrasadas:{count,pct}, progressoMedio:{pct} }
KPIMotoristas  = { ativos:{count,total}, disponiveis:{count}, emRota:{count}, comAtraso:{count}, documentosVencendo:{count} }
KPIAlertas     = { criticos:{count}, abertos:{count}, resolvidosHoje:{count}, slaTratativas:{pct} }
KPIDashboard   = { entregas:{onTime,total,pct}, sla:{pct,meta}, motoristasEmRisco:{count,total,sparkline}, atrasosCriticos:{count,total,sparkline}, paradasNaoPlanejadas:{count,total,sparkline} }
```

From auth.guard (Plan 04):
```typescript
.use(authGuard)  // adds context.user: { id, role, jti }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trips module — service + plugin (CRUD + filters + stats)</name>
  <read_first>
    - api/src/db/schema/trips.ts, api/src/db/schema/relations.ts (Plan 02)
    - api/src/db/client.ts (Plan 03)
    - api/src/lib/rbac.ts (Plan 04 — authGuard)
    - torre-de-controle/src/hooks/useTrips.ts (filter contract — exact param names)
    - torre-de-controle/src/data/types.ts (Trip + KPIViagens shapes)
    - .planning/phases/02-backend-core-auth-api-foundation/02-PATTERNS.md (section "Trips module")
    - .planning/phases/02-backend-core-auth-api-foundation/02-RESEARCH.md (section "Drizzle Query com filtros e join")
  </read_first>
  <files>api/src/modules/trips/trips.service.ts, api/src/modules/trips/trips.plugin.ts</files>
  <action>
**api/src/modules/trips/trips.service.ts**:
```typescript
import { and, eq, ilike, or, sql, count } from 'drizzle-orm'
import { db } from '../../db/client'
import { trips } from '../../db/schema/trips'
import { drivers } from '../../db/schema/drivers'
import { vehicles } from '../../db/schema/vehicles'
import { clients } from '../../db/schema/clients'
import { routes } from '../../db/schema/routes'

export type TripFilters = {
  status?:     'planned'|'in_progress'|'completed'|'delayed'|'cancelled'
  slaStatus?:  'no_prazo'|'em_risco'|'atrasado'|'sem_sinal'
  clientName?: string
  driverName?: string
  priority?:   'alta'|'media'|'baixa'
  routeCode?:  string
  search?:     string
}

export async function listTrips(filters: TripFilters, page = 0, limit = 100) {
  const conditions = []
  if (filters.status)     conditions.push(eq(trips.status, filters.status))
  if (filters.slaStatus)  conditions.push(eq(trips.slaStatus, filters.slaStatus))
  if (filters.priority)   conditions.push(eq(trips.priority, filters.priority))

  // Subqueries / joined filters
  if (filters.clientName) {
    const [c] = await db.select({ id: clients.id }).from(clients).where(eq(clients.name, filters.clientName)).limit(1)
    conditions.push(c ? eq(trips.clientId, c.id) : sql`false`)
  }
  if (filters.routeCode) {
    const [r] = await db.select({ id: routes.id }).from(routes).where(eq(routes.code, filters.routeCode)).limit(1)
    conditions.push(r ? eq(trips.routeId, r.id) : sql`false`)
  }
  if (filters.driverName) {
    // sub-select drivers matching name
    const matchingDrivers = await db.select({ id: drivers.id }).from(drivers).where(ilike(drivers.name, `%${filters.driverName}%`))
    if (matchingDrivers.length === 0) {
      conditions.push(sql`false`)
    } else {
      conditions.push(sql`${trips.driverId} IN (${sql.join(matchingDrivers.map(d => sql`${d.id}`), sql`, `)})`)
    }
  }
  if (filters.search) {
    conditions.push(or(
      ilike(trips.code, `%${filters.search}%`),
      ilike(trips.origin, `%${filters.search}%`),
      ilike(trips.destination, `%${filters.search}%`),
    )!)
  }

  const where = conditions.length ? and(...conditions) : undefined

  const rows = await db.query.trips.findMany({
    where,
    with: {
      driver:  { columns: { id: true, name: true, photoUrl: true, code: true } },
      vehicle: { columns: { plate: true, type: true } },
      client:  { columns: { name: true } },
      route:   { columns: { code: true, name: true } },
    },
    orderBy: (t, { desc }) => [desc(t.windowStart)],
    limit,
    offset: page * limit,
  })

  return rows.map(toTripDto)
}

export async function getTripById(id: string) {
  const trip = await db.query.trips.findFirst({
    where: eq(trips.id, id),
    with: {
      driver:  { columns: { id: true, name: true, photoUrl: true, code: true } },
      vehicle: { columns: { plate: true, type: true } },
      client:  { columns: { name: true } },
      route:   { columns: { code: true, name: true } },
    },
  })
  return trip ? toTripDto(trip) : null
}

export async function getTripStats() {
  const allActive = await db.select().from(trips).where(or(eq(trips.status, 'in_progress'), eq(trips.status, 'planned'), eq(trips.status, 'delayed')))
  const total = allActive.length
  const noPrazo  = allActive.filter(t => t.slaStatus === 'no_prazo').length
  const emRisco  = allActive.filter(t => t.slaStatus === 'em_risco').length
  const atrasadas = allActive.filter(t => t.slaStatus === 'atrasado' || t.status === 'delayed').length
  const avgProgress = total > 0 ? Math.round(allActive.reduce((s, t) => s + (t.progressPct ?? 0), 0) / total) : 0

  return {
    total:          { count: total },
    noPrazo:        { count: noPrazo,   pct: total ? Math.round((noPrazo  / total) * 100) : 0 },
    emRisco:        { count: emRisco,   pct: total ? Math.round((emRisco  / total) * 100) : 0 },
    atrasadas:      { count: atrasadas, pct: total ? Math.round((atrasadas/ total) * 100) : 0 },
    progressoMedio: { pct: avgProgress },
  }
}

// Map joined row → frontend Trip shape (denormalized fields)
function toTripDto(row: any) {
  return {
    id:            row.id,
    code:          row.code,
    driverId:      row.driverId,
    driverName:    row.driver?.name ?? '',
    driverPhoto:   row.driver?.photoUrl ?? null,
    plate:         row.vehicle?.plate ?? '',
    clientName:    row.client?.name ?? '',
    operationName: row.route?.name ?? '',
    routeCode:     row.route?.code ?? '',
    priority:      row.priority,
    origin:        row.origin,
    destination:   row.destination,
    originLat:     row.originLat ? Number(row.originLat) : 0,
    originLng:     row.originLng ? Number(row.originLng) : 0,
    destLat:       row.destLat ? Number(row.destLat) : 0,
    destLng:       row.destLng ? Number(row.destLng) : 0,
    windowStart:   row.windowStart,
    windowEnd:     row.windowEnd,
    eta:           row.eta,
    departedAt:    row.departedAt,
    arrivedAt:     row.arrivedAt,
    status:        row.status,
    slaStatus:     row.slaStatus,
    progressPct:   row.progressPct,
    distanceTotal: row.distanceTotal ? Number(row.distanceTotal) : 0,
    distanceDone:  row.distanceDone  ? Number(row.distanceDone)  : 0,
  }
}
```

**api/src/modules/trips/trips.plugin.ts**:
```typescript
import { Elysia, t } from 'elysia'
import { authGuard } from '../../lib/rbac'
import { listTrips, getTripById, getTripStats } from './trips.service'

const tripStatus = t.Union([t.Literal('planned'), t.Literal('in_progress'), t.Literal('completed'), t.Literal('delayed'), t.Literal('cancelled')])
const slaStatus  = t.Union([t.Literal('no_prazo'), t.Literal('em_risco'), t.Literal('atrasado'), t.Literal('sem_sinal')])
const priority   = t.Union([t.Literal('alta'), t.Literal('media'), t.Literal('baixa')])

export const tripsPlugin = new Elysia({ name: 'trips' })
  .use(authGuard)
  .group('/api/trips', (app) =>
    app
      .get('/stats', () => getTripStats(), { detail: { tags: ['trips'], summary: 'KPIViagens aggregate' } })
      .get('/', ({ query }) => listTrips(
        {
          status:     query.status,
          slaStatus:  query.slaStatus,
          clientName: query.clientName,
          driverName: query.driverName,
          priority:   query.priority,
          routeCode:  query.routeCode,
          search:     query.search,
        },
        query.page  ?? 0,
        query.limit ?? 100,
      ), {
        query: t.Object({
          status:     t.Optional(tripStatus),
          slaStatus:  t.Optional(slaStatus),
          clientName: t.Optional(t.String()),
          driverName: t.Optional(t.String()),
          priority:   t.Optional(priority),
          routeCode:  t.Optional(t.String()),
          search:     t.Optional(t.String()),
          page:       t.Optional(t.Numeric({ minimum: 0 })),
          limit:      t.Optional(t.Numeric({ minimum: 1, maximum: 500 })),
        }),
        detail: { tags: ['trips'], summary: 'List trips with filters' },
      })
      .get('/:id', async ({ params, set }) => {
        const trip = await getTripById(params.id)
        if (!trip) { set.status = 404; return { error: 'Trip not found' } }
        return trip
      }, {
        params: t.Object({ id: t.String({ format: 'uuid' }) }),
        detail: { tags: ['trips'], summary: 'Get trip by id' },
      })
  )
```
  </action>
  <verify>
    <automated>cd api && test -f src/modules/trips/trips.service.ts && test -f src/modules/trips/trips.plugin.ts && grep -q "listTrips" src/modules/trips/trips.service.ts && grep -q "authGuard" src/modules/trips/trips.plugin.ts && grep -q "/stats" src/modules/trips/trips.plugin.ts && bun --bun tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `trips.service.ts` exports `listTrips`, `getTripById`, `getTripStats`
    - All 7 filter keys (status, slaStatus, clientName, driverName, priority, routeCode, search) accepted as query params with TypeBox validation
    - Response shape matches `Trip` interface from `torre-de-controle/src/data/types.ts` (denormalized fields driverName, plate, clientName, etc.)
    - `/api/trips/stats` returns object with keys: total, noPrazo, emRisco, atrasadas, progressoMedio (matches KPIViagens)
    - `/api/trips/:id` returns 404 when not found, else trip with joined driver/vehicle/client/route
    - tripsPlugin uses `.use(authGuard)` (auth required)
    - `bun --bun tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Trips module ready. Will be smoke-tested in Plan 06.</done>
</task>

<task type="auto">
  <name>Task 2: Drivers + Vehicles modules</name>
  <read_first>
    - api/src/db/schema/drivers.ts, api/src/db/schema/driver-documents.ts, api/src/db/schema/vehicles.ts (Plan 02)
    - api/src/lib/rbac.ts (Plan 04)
    - torre-de-controle/src/hooks/useDrivers.ts (filter contract)
    - torre-de-controle/src/data/types.ts (Driver, KPIMotoristas)
  </read_first>
  <files>api/src/modules/drivers/drivers.service.ts, api/src/modules/drivers/drivers.plugin.ts, api/src/modules/vehicles/vehicles.service.ts, api/src/modules/vehicles/vehicles.plugin.ts</files>
  <action>
**api/src/modules/drivers/drivers.service.ts**:
```typescript
import { and, eq, ilike, or, gt, lte } from 'drizzle-orm'
import { db } from '../../db/client'
import { drivers } from '../../db/schema/drivers'
import { vehicles } from '../../db/schema/vehicles'

export type DriverFilters = {
  status?: 'available'|'on_route'|'unavailable'
  base?:   string
  search?: string
}

export async function listDrivers(f: DriverFilters) {
  const conditions = []
  if (f.status) conditions.push(eq(drivers.status, f.status))
  if (f.base)   conditions.push(eq(drivers.base, f.base))
  if (f.search) conditions.push(or(
    ilike(drivers.name, `%${f.search}%`),
    ilike(drivers.code, `%${f.search}%`),
  )!)

  const where = conditions.length ? and(...conditions) : undefined

  const rows = await db.query.drivers.findMany({
    where,
    with: {
      documents: true,
      vehicles:  { columns: { plate: true, type: true } },
    },
    orderBy: (d, { asc }) => [asc(d.name)],
  })

  return rows.map(d => ({
    id:               d.id,
    code:             d.code,
    name:             d.name,
    phone:            d.phone ?? '',
    email:            d.email ?? undefined,
    photoUrl:         d.photoUrl ?? undefined,
    status:           d.status,
    operationalScore: d.operationalScore,
    plate:            d.vehicles?.[0]?.plate ?? '',
    vehicleType:      d.vehicles?.[0]?.type ?? '',
    base:             d.base ?? '',
    documents:        d.documents.map((doc: any) => ({
      type:      doc.type,
      status:    doc.status,
      expiresAt: doc.expiresAt,
      issuedAt:  doc.issuedAt ?? undefined,
    })),
    deliveriesToday:  d.deliveriesToday,
    avgDelayMinutes:  d.avgDelayMinutes,
    lat:              d.lat ? Number(d.lat) : 0,
    lng:              d.lng ? Number(d.lng) : 0,
    address:          d.address ?? '',
  }))
}

export async function getDriverById(id: string) {
  const list = await listDrivers({})
  return list.find(d => d.id === id) ?? null
}

export async function getDriverStats() {
  const allDrivers = await db.select().from(drivers)
  const ativos = allDrivers.filter(d => d.status !== 'unavailable').length
  const disponiveis = allDrivers.filter(d => d.status === 'available').length
  const emRota = allDrivers.filter(d => d.status === 'on_route').length
  const comAtraso = allDrivers.filter(d => d.avgDelayMinutes > 10).length

  // documents expiring in 30 days
  const docsExp = await db.execute<{ count: string }>({
    sql:    `SELECT COUNT(*) AS count FROM driver_documents WHERE status = 'vence_em_breve' OR (expires_at IS NOT NULL AND expires_at <= CURRENT_DATE + INTERVAL '30 days')`,
    params: [],
  } as any)
  const documentosVencendo = Number((docsExp as any).rows?.[0]?.count ?? (docsExp as any)[0]?.count ?? 0)

  return {
    ativos:             { count: ativos, total: allDrivers.length },
    disponiveis:        { count: disponiveis },
    emRota:             { count: emRota },
    comAtraso:          { count: comAtraso },
    documentosVencendo: { count: documentosVencendo },
  }
}
```

**api/src/modules/drivers/drivers.plugin.ts**:
```typescript
import { Elysia, t } from 'elysia'
import { authGuard } from '../../lib/rbac'
import { listDrivers, getDriverById, getDriverStats } from './drivers.service'

const driverStatus = t.Union([t.Literal('available'), t.Literal('on_route'), t.Literal('unavailable')])

export const driversPlugin = new Elysia({ name: 'drivers' })
  .use(authGuard)
  .group('/api/drivers', (app) =>
    app
      .get('/stats', () => getDriverStats(), { detail: { tags: ['drivers'], summary: 'KPIMotoristas' } })
      .get('/', ({ query }) => listDrivers({ status: query.status, base: query.base, search: query.search }), {
        query: t.Object({
          status: t.Optional(driverStatus),
          base:   t.Optional(t.String()),
          search: t.Optional(t.String()),
        }),
      })
      .get('/:id', async ({ params, set }) => {
        const d = await getDriverById(params.id)
        if (!d) { set.status = 404; return { error: 'Driver not found' } }
        return d
      }, { params: t.Object({ id: t.String({ format: 'uuid' }) }) })
  )
```

**api/src/modules/vehicles/vehicles.service.ts**:
```typescript
import { db } from '../../db/client'
import { vehicles } from '../../db/schema/vehicles'

export async function listVehicles() {
  return db.select().from(vehicles)
}
```

**api/src/modules/vehicles/vehicles.plugin.ts**:
```typescript
import { Elysia } from 'elysia'
import { authGuard } from '../../lib/rbac'
import { listVehicles } from './vehicles.service'

export const vehiclesPlugin = new Elysia({ name: 'vehicles' })
  .use(authGuard)
  .group('/api/vehicles', (app) =>
    app.get('/', () => listVehicles(), { detail: { tags: ['vehicles'], summary: 'List vehicles' } })
  )
```
  </action>
  <verify>
    <automated>cd api && for f in src/modules/drivers/drivers.service.ts src/modules/drivers/drivers.plugin.ts src/modules/vehicles/vehicles.service.ts src/modules/vehicles/vehicles.plugin.ts; do test -f "$f" || { echo "MISSING $f"; exit 1; }; done && grep -q "listDrivers" src/modules/drivers/drivers.service.ts && grep -q "/stats" src/modules/drivers/drivers.plugin.ts && grep -q "documentosVencendo" src/modules/drivers/drivers.service.ts && bun --bun tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - drivers/* and vehicles/* files exist
    - drivers query supports `status`, `base`, `search` (matches useDrivers.ts contract)
    - drivers list response includes `documents` array per driver
    - drivers/stats returns ativos, disponiveis, emRota, comAtraso, documentosVencendo (matches KPIMotoristas)
    - vehiclesPlugin uses `.use(authGuard)`
    - `bun --bun tsc --noEmit` passes
  </acceptance_criteria>
  <done>Drivers + vehicles modules ready.</done>
</task>

<task type="auto">
  <name>Task 3: Alerts module — list + filters + assign + treatments + resolve + stats</name>
  <read_first>
    - api/src/db/schema/alerts.ts, api/src/db/schema/treatments.ts, api/src/db/schema/relations.ts (Plan 02)
    - api/src/lib/rbac.ts (Plan 04)
    - torre-de-controle/src/hooks/useAlerts.ts (filter contract — period in particular)
    - torre-de-controle/src/data/types.ts (Alert, KPIAlertas)
  </read_first>
  <files>api/src/modules/alerts/alerts.service.ts, api/src/modules/alerts/alerts.plugin.ts</files>
  <action>
**api/src/modules/alerts/alerts.service.ts**:
```typescript
import { and, eq, gte, ilike, or, sql, inArray } from 'drizzle-orm'
import { db } from '../../db/client'
import { alerts } from '../../db/schema/alerts'
import { treatments } from '../../db/schema/treatments'
import { trips } from '../../db/schema/trips'
import { drivers } from '../../db/schema/drivers'
import { vehicles } from '../../db/schema/vehicles'
import { clients } from '../../db/schema/clients'
import { routes } from '../../db/schema/routes'

export type AlertFilters = {
  severity?:   'critico'|'medio'|'baixo'
  status?:     'aberto'|'em_tratativa'|'resolvido'
  type?:       string
  clientName?: string
  routeCode?:  string
  assignedTo?: string
  period?:     'today'|'7d'|'30d'
  search?:     string
}

function periodCutoff(p?: string): Date | null {
  if (!p) return null
  const d = new Date()
  if (p === 'today') { d.setHours(0,0,0,0); return d }
  if (p === '7d')    { d.setDate(d.getDate() - 7);  return d }
  if (p === '30d')   { d.setDate(d.getDate() - 30); return d }
  return null
}

export async function listAlerts(f: AlertFilters) {
  const conditions = []
  if (f.severity) conditions.push(eq(alerts.severity, f.severity))
  if (f.status)   conditions.push(eq(alerts.status, f.status))
  if (f.type)     conditions.push(eq(alerts.type, f.type))
  if (f.assignedTo) conditions.push(eq(alerts.assignedTo, f.assignedTo))
  const cutoff = periodCutoff(f.period)
  if (cutoff) conditions.push(gte(alerts.occurredAt, cutoff))

  // join-filtered (clientName, routeCode) — resolve via trip
  if (f.clientName || f.routeCode) {
    const tripConditions = []
    if (f.clientName) {
      const [c] = await db.select({ id: clients.id }).from(clients).where(eq(clients.name, f.clientName)).limit(1)
      tripConditions.push(c ? eq(trips.clientId, c.id) : sql`false`)
    }
    if (f.routeCode) {
      const [r] = await db.select({ id: routes.id }).from(routes).where(eq(routes.code, f.routeCode)).limit(1)
      tripConditions.push(r ? eq(trips.routeId, r.id) : sql`false`)
    }
    const matchingTrips = await db.select({ id: trips.id }).from(trips).where(and(...tripConditions))
    if (matchingTrips.length === 0) {
      conditions.push(sql`false`)
    } else {
      conditions.push(inArray(alerts.tripId, matchingTrips.map(t => t.id)))
    }
  }

  if (f.search) {
    conditions.push(or(
      ilike(alerts.title, `%${f.search}%`),
      ilike(alerts.description, `%${f.search}%`),
    )!)
  }

  const where = conditions.length ? and(...conditions) : undefined

  const rows = await db.query.alerts.findMany({
    where,
    with: {
      trip:    { columns: { code: true } },
      driver:  { columns: { id: true, name: true, photoUrl: true } },
      vehicle: { columns: { plate: true } },
    },
    orderBy: (a, { desc }) => [desc(a.occurredAt)],
    limit: 500,
  })

  // we still need clientName/routeCode in DTO — fetch trips bulk
  const tripIds = [...new Set(rows.map(r => r.tripId).filter((x): x is string => !!x))]
  const tripRows = tripIds.length
    ? await db.query.trips.findMany({
        where: (t, { inArray }) => inArray(t.id, tripIds),
        with: { client: { columns: { name: true } }, route: { columns: { code: true } } },
      })
    : []
  const tripMap = new Map(tripRows.map(t => [t.id, t]))

  return rows.map(r => {
    const trip = r.tripId ? tripMap.get(r.tripId) : undefined
    return {
      id:           r.id,
      type:         r.type,
      severity:     r.severity,
      status:       r.status,
      tripId:       r.tripId ?? '',
      tripCode:     r.trip?.code ?? '',
      driverId:     r.driverId ?? '',
      driverName:   r.driver?.name ?? '',
      driverPhoto:  r.driver?.photoUrl ?? undefined,
      plate:        r.vehicle?.plate ?? '',
      clientName:   trip?.client?.name ?? '',
      routeCode:    trip?.route?.code ?? '',
      title:        r.title,
      description:  r.description ?? '',
      source:       r.source ?? 'GPS',
      lat:          r.lat ? Number(r.lat) : undefined,
      lng:          r.lng ? Number(r.lng) : undefined,
      delayMinutes: r.delayMinutes ?? undefined,
      deviationKm:  r.deviationKm ? Number(r.deviationKm) : undefined,
      occurredAt:   r.occurredAt,
      slaDeadline:  r.slaDeadline ?? undefined,
      assignedTo:   r.assignedTo ?? undefined,
      resolvedAt:   r.resolvedAt ?? undefined,
    }
  })
}

export async function assignAlert(alertId: string, userId: string) {
  const [updated] = await db.update(alerts)
    .set({ assignedTo: userId, status: 'em_tratativa' })
    .where(eq(alerts.id, alertId))
    .returning()
  return updated ?? null
}

export async function addTreatment(alertId: string, operatorId: string, body: { actionType?: string; notes?: string; outcome?: string }) {
  const alertRow = await db.query.alerts.findFirst({ where: eq(alerts.id, alertId) })
  if (!alertRow) return null
  const [t] = await db.insert(treatments).values({
    alertId,
    tripId:     alertRow.tripId,
    operatorId,
    actionType: body.actionType,
    notes:      body.notes,
    outcome:    body.outcome,
  }).returning()
  return t
}

export async function resolveAlert(alertId: string) {
  const [r] = await db.update(alerts)
    .set({ status: 'resolvido', resolvedAt: new Date() })
    .where(eq(alerts.id, alertId))
    .returning()
  return r ?? null
}

export async function getAlertStats() {
  const all = await db.select().from(alerts)
  const startToday = new Date(); startToday.setHours(0,0,0,0)
  const criticos = all.filter(a => a.severity === 'critico' && a.status !== 'resolvido').length
  const abertos = all.filter(a => a.status === 'aberto').length
  const resolvidosHoje = all.filter(a => a.resolvedAt && a.resolvedAt >= startToday).length
  // SLA: % of resolved alerts where resolvedAt <= slaDeadline
  const closed = all.filter(a => a.status === 'resolvido' && a.slaDeadline && a.resolvedAt)
  const onTime = closed.filter(a => a.resolvedAt! <= a.slaDeadline!).length
  const slaPct = closed.length ? Math.round((onTime / closed.length) * 100) : 100

  return {
    criticos:       { count: criticos },
    abertos:        { count: abertos },
    resolvidosHoje: { count: resolvidosHoje },
    slaTratativas:  { pct: slaPct },
  }
}
```

**api/src/modules/alerts/alerts.plugin.ts**:
```typescript
import { Elysia, t } from 'elysia'
import { authGuard } from '../../lib/rbac'
import { listAlerts, assignAlert, addTreatment, resolveAlert, getAlertStats } from './alerts.service'

const severity = t.Union([t.Literal('critico'), t.Literal('medio'), t.Literal('baixo')])
const alertStatus = t.Union([t.Literal('aberto'), t.Literal('em_tratativa'), t.Literal('resolvido')])
const period = t.Union([t.Literal('today'), t.Literal('7d'), t.Literal('30d')])

export const alertsPlugin = new Elysia({ name: 'alerts' })
  .use(authGuard)
  .group('/api/alerts', (app) =>
    app
      .get('/stats', () => getAlertStats(), { detail: { tags: ['alerts'], summary: 'KPIAlertas' } })
      .get('/', ({ query }) => listAlerts({
        severity:   query.severity,
        status:     query.status,
        type:       query.type,
        clientName: query.clientName,
        routeCode:  query.routeCode,
        assignedTo: query.assignedTo,
        period:     query.period,
        search:     query.search,
      }), {
        query: t.Object({
          severity:   t.Optional(severity),
          status:     t.Optional(alertStatus),
          type:       t.Optional(t.String()),
          clientName: t.Optional(t.String()),
          routeCode:  t.Optional(t.String()),
          assignedTo: t.Optional(t.String({ format: 'uuid' })),
          period:     t.Optional(period),
          search:     t.Optional(t.String()),
        }),
      })
      .patch('/:id/assign', async ({ params, user, set }) => {
        const r = await assignAlert(params.id, user.id)
        if (!r) { set.status = 404; return { error: 'Alert not found' } }
        return r
      }, {
        params: t.Object({ id: t.String({ format: 'uuid' }) }),
        detail: { tags: ['alerts'], summary: 'Assign alert to current user' },
      })
      .post('/:id/treatments', async ({ params, body, user, set }) => {
        const r = await addTreatment(params.id, user.id, body)
        if (!r) { set.status = 404; return { error: 'Alert not found' } }
        return r
      }, {
        params: t.Object({ id: t.String({ format: 'uuid' }) }),
        body: t.Object({
          actionType: t.Optional(t.String()),
          notes:      t.Optional(t.String()),
          outcome:    t.Optional(t.Union([t.Literal('pendente'), t.Literal('resolvido'), t.Literal('escalado')])),
        }),
        detail: { tags: ['alerts'], summary: 'Add treatment record' },
      })
      .patch('/:id/resolve', async ({ params, set }) => {
        const r = await resolveAlert(params.id)
        if (!r) { set.status = 404; return { error: 'Alert not found' } }
        return r
      }, {
        params: t.Object({ id: t.String({ format: 'uuid' }) }),
        detail: { tags: ['alerts'], summary: 'Mark alert resolved' },
      })
  )
```
  </action>
  <verify>
    <automated>cd api && test -f src/modules/alerts/alerts.service.ts && test -f src/modules/alerts/alerts.plugin.ts && grep -q "listAlerts" src/modules/alerts/alerts.service.ts && grep -q "/assign" src/modules/alerts/alerts.plugin.ts && grep -q "/treatments" src/modules/alerts/alerts.plugin.ts && grep -q "/resolve" src/modules/alerts/alerts.plugin.ts && grep -q "period" src/modules/alerts/alerts.plugin.ts && bun --bun tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - All 8 filter keys (severity, status, type, clientName, routeCode, assignedTo, period, search) accepted
    - `period` supports 'today' | '7d' | '30d' (matches AlertFilters)
    - PATCH /:id/assign sets status='em_tratativa' and assignedTo=user.id
    - POST /:id/treatments inserts a treatment row linked to current user
    - PATCH /:id/resolve sets status='resolvido' and resolvedAt=NOW
    - GET /stats returns KPIAlertas shape
    - All endpoints `.use(authGuard)`
    - `bun --bun tsc --noEmit` passes
  </acceptance_criteria>
  <done>Alerts module fully wired with operational actions (assign/treatments/resolve).</done>
</task>

<task type="auto">
  <name>Task 4: Dashboard module with Redis 30s KPI cache</name>
  <read_first>
    - api/src/redis/client.ts (Plan 03)
    - api/src/lib/rbac.ts (Plan 04)
    - torre-de-controle/src/data/types.ts (KPIDashboard, KPITorre)
    - .planning/ARCHITECTURE.md (section "Redis Key Schema" — `kpi:trips_today` EX 30)
  </read_first>
  <files>api/src/modules/dashboard/dashboard.service.ts, api/src/modules/dashboard/dashboard.plugin.ts</files>
  <action>
**api/src/modules/dashboard/dashboard.service.ts**:
```typescript
import { db } from '../../db/client'
import { redis } from '../../redis/client'
import { trips } from '../../db/schema/trips'
import { alerts } from '../../db/schema/alerts'
import { drivers } from '../../db/schema/drivers'

const KPI_CACHE_KEY = 'kpi:dashboard'
const KPI_CACHE_TTL = 30  // seconds, per ARCHITECTURE.md

export async function getDashboardKpis() {
  const cached = await redis.get(KPI_CACHE_KEY)
  if (cached) {
    try { return JSON.parse(cached) } catch { /* fall through */ }
  }

  const [allTrips, allAlerts, allDrivers] = await Promise.all([
    db.select().from(trips),
    db.select().from(alerts),
    db.select().from(drivers),
  ])

  const today = new Date(); today.setHours(0,0,0,0)
  const todayTrips = allTrips.filter(t => t.windowStart >= today || (t.arrivedAt && t.arrivedAt >= today))
  const onTime = todayTrips.filter(t => t.slaStatus === 'no_prazo' || (t.status === 'completed' && t.arrivedAt! <= t.windowEnd)).length
  const totalToday = todayTrips.length
  const slaPct = totalToday > 0 ? Math.round((onTime / totalToday) * 100) : 100

  const motoristasEmRiscoCount = allDrivers.filter(d => d.avgDelayMinutes > 10 && d.status === 'on_route').length
  const atrasosCriticosCount = allAlerts.filter(a => a.type === 'atraso_critico' && a.status !== 'resolvido').length
  const paradasCount = allAlerts.filter(a => a.type === 'parada_nao_planejada' && a.status !== 'resolvido').length

  // sparkline: last-7-days bucket — simple stub (real ETL is Phase 6)
  const sparkline = Array.from({ length: 7 }, () => Math.max(0, Math.round(Math.random() * 10)))

  const kpis = {
    entregas: { onTime, total: totalToday, pct: slaPct },
    sla:      { pct: slaPct, meta: 95 },
    motoristasEmRisco:    { count: motoristasEmRiscoCount, total: allDrivers.length, sparkline: [...sparkline] },
    atrasosCriticos:      { count: atrasosCriticosCount,   total: allAlerts.length,  sparkline: [...sparkline] },
    paradasNaoPlanejadas: { count: paradasCount,           total: allAlerts.length,  sparkline: [...sparkline] },
  }

  await redis.set(KPI_CACHE_KEY, JSON.stringify(kpis), 'EX', KPI_CACHE_TTL)
  return kpis
}
```

**api/src/modules/dashboard/dashboard.plugin.ts**:
```typescript
import { Elysia } from 'elysia'
import { authGuard } from '../../lib/rbac'
import { getDashboardKpis } from './dashboard.service'

export const dashboardPlugin = new Elysia({ name: 'dashboard' })
  .use(authGuard)
  .group('/api/dashboard', (app) =>
    app.get('/kpis', () => getDashboardKpis(), { detail: { tags: ['dashboard'], summary: 'KPIDashboard with 30s Redis cache' } })
  )
```
  </action>
  <verify>
    <automated>cd api && test -f src/modules/dashboard/dashboard.service.ts && test -f src/modules/dashboard/dashboard.plugin.ts && grep -q "kpi:dashboard" src/modules/dashboard/dashboard.service.ts && grep -q "EX', 30" src/modules/dashboard/dashboard.service.ts || grep -q "KPI_CACHE_TTL" src/modules/dashboard/dashboard.service.ts && grep -q "authGuard" src/modules/dashboard/dashboard.plugin.ts && bun --bun tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `dashboard.service.ts` defines `getDashboardKpis` returning shape compatible with `KPIDashboard` (entregas, sla, motoristasEmRisco, atrasosCriticos, paradasNaoPlanejadas)
    - Reads Redis `kpi:dashboard` first; falls back to DB query
    - Writes cache with `EX 30` (30 second TTL)
    - Endpoint protected by authGuard
    - `bun --bun tsc --noEmit` passes
  </acceptance_criteria>
  <done>Dashboard KPI endpoint with Redis cache wired.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /api/{trips,drivers,alerts,vehicles,dashboard} | Authenticated requests; cookie + authGuard |
| API → Postgres | Trusted; Drizzle parameterized |
| API → Redis (KPI cache) | Trusted internal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05-01 | Tampering | SQL injection via filter query params | mitigate | All filter values pass through Drizzle helpers (`eq`, `ilike`, `inArray`); raw `sql\`...\`` only used with parameter placeholders, never with user-controlled string concatenation. |
| T-02-05-02 | Tampering | LIKE-pattern injection (`%` chars in search) | accept | `ilike(col, '%${search}%')` is parameterized — `%` inside the search becomes part of the LIKE pattern, expanding matches. Acceptable for search UX; not a security issue (no escape). |
| T-02-05-03 | Information Disclosure | Unauthenticated access to data endpoints | mitigate | Every plugin uses `.use(authGuard)`. Validation: any 401 test against `/api/trips` must succeed. |
| T-02-05-04 | Elevation of Privilege | Mass assignment via PATCH/POST bodies | mitigate | TypeBox validators on every body explicitly declare allowed fields. Requests with extra fields are rejected by Elysia (default behavior). |
| T-02-05-05 | Information Disclosure | UUID format leakage hints at user enumeration | accept | `t.String({ format: 'uuid' })` returns 400 for malformed; 404 for not-found. Both reveal "not a valid id" — acceptable for an internal app. |
| T-02-05-06 | Tampering | IDOR — analyst views other-tenant data | accept | MVP has single-tenant operation; client filters are name-based, not user-bound. Multi-tenant isolation deferred to Phase 6. Documented. |
| T-02-05-07 | Denial of Service | Unbounded LIMIT on /api/trips | mitigate | Query validator caps `limit` at 500; default 100. Prevents pathological requests. |
| T-02-05-08 | Information Disclosure | Sensitive driver data (phone, address) exposed to viewer role | accept | All authenticated roles currently see all driver fields. Granular field-level RBAC deferred. Documented. |
</threat_model>

<verification>
- All 10 files compile (`bun --bun tsc --noEmit`)
- Every plugin has `.use(authGuard)`
- Every list endpoint has TypeBox query validation
- Every PATCH/POST has TypeBox body validation
- Filter param names match frontend hook contracts exactly (verified by grep)

Smoke testing happens in Plan 06 once app composed:
- `curl -c cookie.txt -X POST localhost:3000/api/auth/login -H "Content-Type: application/json" -d '{"email":"admin@torre.fic","password":"senha123"}'`
- `curl -b cookie.txt localhost:3000/api/trips | jq 'length'` returns >=50
- `curl -b cookie.txt localhost:3000/api/trips/stats | jq` returns KPIViagens shape
- `curl localhost:3000/api/trips` (no cookie) returns 401
</verification>

<success_criteria>
- 10 module files (5 service + 5 plugin) all compile
- Filter param names exactly match the frontend hook contracts (param-clean Phase 3 transition)
- KPI endpoints return shapes matching types.ts KPI* interfaces
- All endpoints behind authGuard
- Dashboard cache writes/reads Redis with 30s TTL
</success_criteria>

<output>
After completion, create `.planning/phases/02-backend-core-auth-api-foundation/02-05-SUMMARY.md` listing:
- Final route inventory (path + method)
- Filter param mappings (frontend hook → API query) confirmed
- Any deviations from useTrips/useDrivers/useAlerts contract (none expected)
- Note for Plan 06: plugins to compose in `api/src/index.ts`: tripsPlugin, driversPlugin, alertsPlugin, vehiclesPlugin, dashboardPlugin (+ authPlugin from Plan 04)
</output>
</content>
</invoke>