---
phase: 09-ranking-escrita-auditoria-write-flows
plan: 06
type: execute
wave: 5
depends_on: [05]
files_modified:
  - torre-de-controle/src/app/pages/ranking/components/EvaluationFormDialog.tsx
  - torre-de-controle/src/app/pages/ranking/components/BloqueiosTab.tsx
  - torre-de-controle/src/app/pages/ranking/components/ViagensTab.tsx
autonomous: true
requirements: [D-09-01, D-09-02, D-09-09, D-09-10]
must_haves:
  truths:
    - "An admin/supervisor sees an ENABLED Salvar in EvaluationFormDialog; submitting calls useEvaluateTrip and on success closes the dialog + the ranking refetches (D-09-10)"
    - "Checking NO-SHOW (atendeu=false) and saving triggers the server-side auto-block; the UI reflects the blocked driver after invalidation (D-09-02 surfaced)"
    - "An admin/supervisor sees an ENABLED Desbloquear in BloqueiosTab; clicking calls useUnblockDriver and the row leaves the active list after refetch"
    - "An admin/supervisor can create a MANUAL block from BloqueiosTab (driver_id/driver_name/motivo) via useBlockDriver"
    - "analyst/viewer see the write controls HIDDEN or DISABLED in all three components (read-only — D-09-10), driven by useCanWriteRanking"
  artifacts:
    - path: "torre-de-controle/src/app/pages/ranking/components/EvaluationFormDialog.tsx"
      provides: "Wired submit (useEvaluateTrip) gated by role; operador no longer sent (server-resolved)"
      contains: "useEvaluateTrip"
    - path: "torre-de-controle/src/app/pages/ranking/components/BloqueiosTab.tsx"
      provides: "Wired unblock (useUnblockDriver) + manual-block control (useBlockDriver), gated by role"
      contains: "useUnblockDriver"
    - path: "torre-de-controle/src/app/pages/ranking/components/ViagensTab.tsx"
      provides: "Avaliar/Editar trigger gated by role (read-only roles cannot open the write dialog as writable)"
      contains: "useCanWriteRanking"
  key_links:
    - from: "EvaluationFormDialog Salvar onClick"
      to: "useEvaluateTrip().mutate(payload)"
      via: "mutation + onSuccess close + invalidation (from 09-05)"
      pattern: "useEvaluateTrip"
    - from: "BloqueiosTab Desbloquear onClick"
      to: "useUnblockDriver().mutate({ id, driver_id, driver_name })"
      via: "row action → mutation"
      pattern: "useUnblockDriver"
    - from: "write controls"
      to: "useCanWriteRanking()"
      via: "conditional render/disable by role"
      pattern: "useCanWriteRanking"
---

<objective>
Wire the driver-lifecycle WRITE UI (D-09-10): make `EvaluationFormDialog` submit real evaluations (`useEvaluateTrip`), make `BloqueiosTab` unblock (`useUnblockDriver`) + create manual blocks (`useBlockDriver`), and gate every write control by role (`useCanWriteRanking` from 09-05) so analyst|viewer stay read-only. The `ViagensTab` "Avaliar/Editar" trigger is gated too (so read-only roles can't open the dialog as writable). All hooks + the role gate already exist (09-05) — this plan replaces the Phase 8 "disabled / Phase 9" placeholders with live, invalidating mutations.

Purpose: Turn the Phase 8 read-only shells into the working write surface for evaluations + blocks, with the role gate the user sees (hidden/disabled for read-only roles) — the backend requireRole (09-03) is the real enforcement; this is the UX layer. NO-SHOW auto-block (D-09-02) becomes visible: saving atendeu=false blocks the driver server-side and the invalidated reads reflect it.
Output: 3 wired components. `bun run build` (torre-de-controle) exit 0. LIVE: admin submit → 200 + dialog closes + table refetches; viewer sees no write button (grep + runtime).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-ranking-escrita-auditoria-write-flows/09-CONTEXT.md

<interfaces>
<!-- 09-05 hooks (Wave 4) + the existing shells this plan wires. -->

From torre-de-controle/src/hooks/useRanking.ts (09-05):
```typescript
useEvaluateTrip()    // { mutate, mutateAsync, isPending, isError, error } — POST /evaluations, invalidates trips/drivers/stats/blocks/logs
useBlockDriver()     // POST /blocks (manual)
useUnblockDriver()   // PATCH /blocks/:id
useCanWriteRanking(): boolean   // true iff role admin|supervisor
// EvaluateTripInput: { trip_id, driver_id, driver_name, comunicacao, atendeu, desvio_rota, postura, ajuste_manual, observacao }
// BlockDriverInput:  { driver_id, driver_name, motivo }
// UnblockDriverInput:{ id, driver_id, driver_name }
```
From the shells (CURRENT state — Phase 8 placeholders to replace):
- EvaluationFormDialog.tsx: all fields are controlled local state (comunicacao/desvio/postura/noShow/ajuste/operador/observacao); the trip comes from `useRankingTrips().find(t => t.id === tripId)`; the Salvar + Bloquear buttons are `disabled title="Disponível na Phase 9"`; there is a `PHASE9_NOTICE` Lock banner. The `operador` field is a free-text Input.
- BloqueiosTab.tsx: DataTable of active DriverBlockRecord; the row "Desbloquear" button is `disabled title="Disponível na Phase 9"` with NO onClick; rows are mapped to `{...b, id: b.id ?? `${b.driver_id}-${i}`}`.
- ViagensTab.tsx: owns `<EvaluationFormDialog tripId open onOpenChange />`; the "Avaliar"/"Editar" row button calls `setEvaluatingTripId(row.original.id)` (always enabled today).

From torre-de-controle/src/components/ui/* : Dialog, Button, Input, Label, Select, Checkbox, DialogFooter all already imported in these shells. For a manual-block form in BloqueiosTab, reuse Dialog + Input + Button (same primitives EvaluationFormDialog uses).
</interfaces>

<role_gate_note>
useCanWriteRanking() reads useAuthStore role (populated on app boot by AuthGuard.me()). The role gate is UX ONLY — a read-only user who bypasses it still gets 403 from requireRole (09-03). For analyst|viewer: PREFER hiding the write control entirely (cleaner) OR disabling with a tooltip; either satisfies D-09-10. The existing Phase 8 "Disponível na Phase 9" disabled state is REPLACED — do not leave that placeholder text.
</role_gate_note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: wire EvaluationFormDialog submit (useEvaluateTrip) + gate by role; gate the ViagensTab trigger</name>
  <files>torre-de-controle/src/app/pages/ranking/components/EvaluationFormDialog.tsx, torre-de-controle/src/app/pages/ranking/components/ViagensTab.tsx</files>
  <read_first>
    - torre-de-controle/src/app/pages/ranking/components/EvaluationFormDialog.tsx (the FULL shell — all controlled fields + the disabled Salvar/Bloquear + PHASE9_NOTICE; this plan flips them to live)
    - torre-de-controle/src/app/pages/ranking/components/ViagensTab.tsx (owns the dialog + the Avaliar/Editar trigger button to gate)
    - torre-de-controle/src/hooks/useRanking.ts (09-05 — useEvaluateTrip signature + EvaluateTripInput field names + useCanWriteRanking)
    - C:\Users\antonio.magalhaes\Documents\Projetos\produção\ride-rank-buddy\src\components\EvaluationForm.tsx lines 39-50, 66-82 (the original submit payload mapping — confirm field names match EvaluateTripInput; the NO-SHOW checkbox maps to atendeu=false)
  </read_first>
  <action>
    EvaluationFormDialog.tsx:
      - Add `const evaluateTrip = useEvaluateTrip()` and `const canWrite = useCanWriteRanking()` (import from `@/hooks/useRanking`).
      - REMOVE the `operador` Input field and its state — operador is now resolved SERVER-SIDE from the JWT (09-03 resolveOperador); the front no longer sends or collects it. (If keeping the field is preferred for display, make it read-only and DO NOT include it in the payload.)
      - Build the submit handler `onSave`: assemble `EvaluateTripInput` = { trip_id: trip.id, driver_id: trip.driver_id, driver_name: trip.driverName, comunicacao, atendeu: !noShow (NO-SHOW checkbox checked ⇒ atendeu=false), desvio_rota: desvio, postura, ajuste_manual: ajuste, observacao }. Call `evaluateTrip.mutate(payload, { onSuccess: () => onOpenChange(false) })`. On NO-SHOW, the server auto-blocks (D-09-02) — no separate front call; the invalidation from the hook refreshes blocks/drivers.
      - Wire the Salvar button: `disabled={!canWrite || evaluateTrip.isPending}`, `onClick={onSave}`; show a pending label while isPending. Wire (or remove) the separate "Bloquear" button: the ride-rank "Bloquear" inside the eval form is the MANUAL block path — wire it to `useBlockDriver` (driver_id/driver_name from trip + a motivo, e.g. the observacao or a default) gated by canWrite; OR (simpler, and since BloqueiosTab gets the dedicated manual-block control in Task 2) REMOVE the in-dialog Bloquear button to avoid two manual-block entry points — document the choice. Either way, no disabled "Phase 9" placeholder remains.
      - Replace the PHASE9_NOTICE Lock banner: if `!canWrite`, show a "somente leitura (seu perfil não permite escrita)" notice and disable all inputs + Salvar (read-only view for analyst|viewer, D-09-10); if canWrite, remove the Phase 9 banner entirely.
      - Surface mutation errors: if `evaluateTrip.isError`, show `evaluateTrip.error?.message` inline (toast or a small error line — match any existing toast pattern in the app; if none, an inline text is fine).

    ViagensTab.tsx:
      - Add `const canWrite = useCanWriteRanking()`. Gate the row "Ação" button: if `!canWrite`, either hide the Avaliar/Editar button (render a muted "—" or nothing) OR render it disabled — so read-only roles cannot open the dialog in a writable state (D-09-10). The dialog itself already guards via canWrite (Task 1), but gating the trigger avoids a confusing writable-looking modal for viewers. Keep the dialog mount as-is.
  </action>
  <acceptance_criteria>
    - `cd torre-de-controle && npx tsc --noEmit` exits 0; `cd torre-de-controle && bun run build` exits 0.
    - `grep -n "useEvaluateTrip" torre-de-controle/src/app/pages/ranking/components/EvaluationFormDialog.tsx` returns the hook usage; `grep -n "\.mutate" ...` shows the submit calls it.
    - `grep -c "Disponível na Phase 9\|PHASE9_NOTICE\|disabled title=\"Disponível" torre-de-controle/src/app/pages/ranking/components/EvaluationFormDialog.tsx` returns 0 (the Phase 8 placeholder is fully removed).
    - `grep -n "useCanWriteRanking" torre-de-controle/src/app/pages/ranking/components/EvaluationFormDialog.tsx` AND `... ViagensTab.tsx` both return a line (role gate present in both — D-09-10).
    - `grep -n "operador" torre-de-controle/src/app/pages/ranking/components/EvaluationFormDialog.tsx` returns 0 in the SUBMIT payload (operador is server-resolved; not sent) — if the field is kept for display it must not be in the mutate() payload object.
    - atendeu mapping verified: `grep -nE "atendeu: ?!noShow|atendeu: ?!.*noShow" torre-de-controle/src/app/pages/ranking/components/EvaluationFormDialog.tsx` confirms NO-SHOW checkbox ⇒ atendeu=false (D-09-02 trigger).
    - LIVE (requires the app + API + a real cookie; if RANK_SUPABASE_SERVICE_KEY absent the 403/hidden-control checks still hold, the 200+refetch is part of the 09-03 Task-3 checkpoint scope): logged in as admin, open a trip's Avaliar dialog → Salvar is ENABLED; submit a valid evaluation → request 200, dialog closes, the Viagens row shows "Editar" (evaluated) after refetch. Logged in as viewer/analyst → the Avaliar button is hidden/disabled AND (if reachable) the dialog inputs + Salvar are disabled with the read-only notice.
  </acceptance_criteria>
  <verify>
    <automated>cd torre-de-controle && npx tsc --noEmit && bun run build</automated>
    <!-- Gate: build clean + grep checks in acceptance_criteria (useEvaluateTrip wired, no PHASE9 placeholder, useCanWriteRanking in both files, operador not in payload, atendeu:!noShow). LIVE admin-submit / viewer-read-only per acceptance_criteria. -->
  </verify>
  <done>EvaluationFormDialog submits via useEvaluateTrip (operador server-resolved, not sent), NO-SHOW maps to atendeu=false, role-gated with no Phase 9 placeholder; ViagensTab trigger gated by role; build + tsc exit 0; admin can submit, viewer is read-only.</done>
</task>

<task type="auto">
  <name>Task 2: wire BloqueiosTab unblock (useUnblockDriver) + manual-block control (useBlockDriver), gated by role</name>
  <files>torre-de-controle/src/app/pages/ranking/components/BloqueiosTab.tsx</files>
  <read_first>
    - torre-de-controle/src/app/pages/ranking/components/BloqueiosTab.tsx (the FULL shell — the disabled Desbloquear row action + the BlockRow mapping; this plan wires it)
    - torre-de-controle/src/app/pages/ranking/components/EvaluationFormDialog.tsx (AFTER Task 1 — reuse the same Dialog + Input + Button pattern for the manual-block form; consistent UX)
    - torre-de-controle/src/hooks/useRanking.ts (09-05 — useUnblockDriver { id, driver_id, driver_name }, useBlockDriver { driver_id, driver_name, motivo }, useCanWriteRanking)
    - C:\Users\antonio.magalhaes\Documents\Projetos\produção\ride-rank-buddy\src\components\BlocksList.tsx (the original unblock action — for parity on labels/confirmation)
  </read_first>
  <action>
    BloqueiosTab.tsx:
      - Add `const unblockDriver = useUnblockDriver()`, `const blockDriver = useBlockDriver()`, `const canWrite = useCanWriteRanking()`.
      - Wire the row "Desbloquear" action: replace the disabled button with an ENABLED one when `canWrite` (hidden/disabled otherwise — D-09-10). onClick → `unblockDriver.mutate({ id: row.original.id, driver_id: row.original.driver_id, driver_name: row.original.driver_name })`. Disable the button while `unblockDriver.isPending`. The hook invalidation removes the row from the active list after the PATCH. Remove the "Disponível na Phase 9" title. Optionally a confirm() before unblocking (match ride-rank if it confirms).
      - Add a MANUAL-BLOCK control: a toolbar button "Bloqueio manual" (rendered only when `canWrite`) that opens a small Dialog with inputs for driver_id, driver_name, and motivo (all required; motivo maxLength ~500 to match the backend). On submit → `blockDriver.mutate({ driver_id, driver_name, motivo }, { onSuccess: () => closeDialog() })`. After success the invalidation refreshes the blocks table (the new MANUAL block appears). Pass the toolbar via the DataTable `toolbar` prop (same mechanism RotasTab uses for its toolbar button). Surface blockDriver.isError inline.
      - For analyst|viewer (`!canWrite`): the Desbloquear column action is hidden/disabled AND the "Bloqueio manual" toolbar button is not rendered (read-only — D-09-10).
  </action>
  <acceptance_criteria>
    - `cd torre-de-controle && npx tsc --noEmit` exits 0; `bun run build` exits 0.
    - `grep -n "useUnblockDriver" torre-de-controle/src/app/pages/ranking/components/BloqueiosTab.tsx` AND `grep -n "useBlockDriver" ...` both return a line; `grep -c "\.mutate" ...` returns >= 2 (unblock + manual block).
    - `grep -n "useCanWriteRanking" torre-de-controle/src/app/pages/ranking/components/BloqueiosTab.tsx` returns a line gating both controls (D-09-10).
    - `grep -c "Disponível na Phase 9" torre-de-controle/src/app/pages/ranking/components/BloqueiosTab.tsx` returns 0 (placeholder removed).
    - `grep -n "motivo" torre-de-controle/src/app/pages/ranking/components/BloqueiosTab.tsx` confirms the manual-block form collects motivo (BlockDriverInput).
    - LIVE (with cookie; DB-effect part shares the 09-03 Task-3 checkpoint when the secret is absent): admin → "Desbloquear" enabled, clicking unblocks a driver (row leaves the active list after refetch); "Bloqueio manual" opens a dialog, submitting creates a MANUAL block that appears in the table. viewer/analyst → no Desbloquear action and no "Bloqueio manual" button visible.
  </acceptance_criteria>
  <verify>
    <automated>cd torre-de-controle && npx tsc --noEmit && bun run build</automated>
    <!-- Gate: build clean + grep checks (useUnblockDriver + useBlockDriver wired, >=2 .mutate, useCanWriteRanking gate, no PHASE9 placeholder, motivo collected). LIVE unblock + manual-block / viewer-read-only per acceptance_criteria. -->
  </verify>
  <done>BloqueiosTab unblock wired via useUnblockDriver + manual-block dialog via useBlockDriver, both role-gated with no Phase 9 placeholder; build + tsc exit 0; admin can unblock + manually block, viewer is read-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Ranking write UI (EvaluationFormDialog/BloqueiosTab/ViagensTab) → useRanking mutation hooks → /api/ranking writes | User-entered evaluation/block data leaves the UI for the requireRole-gated endpoints. The role gate shown here (useCanWriteRanking) is UX; the backend requireRole is the real control. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-22 | Elevation | client role gate (useCanWriteRanking) hiding controls | accept (mitigated server-side) | Hiding/disabling the Salvar/Desbloquear/Bloqueio-manual controls for analyst|viewer is UX only; a tampered client still hits `requireRole('admin','supervisor')` (09-03) → 403. DO NOT rely on the front gate for security — it is defense-in-depth + correct UX (D-09-10). The backend is authoritative. (T1) |
| T-09-23 | Spoofing | operador entered in the UI | mitigate | The operador free-text field is REMOVED from the submit payload — operador is resolved server-side from the JWT (09-03 resolveOperador), so the user cannot stamp an audit log under someone else's name. (T-09-12) |
| T-09-24 | Tampering | bad evaluation/block values from the form | mitigate | The backend typebox (09-03) rejects bad enums / out-of-range ajuste_manual / overlong motivo with 422; the hooks surface the error inline. Front controls (Select for enums, range slider clamped -20..20) reduce accidental bad input. (T5) |
| T-09-25 | Repudiation | a write with no audit trail | mitigate | The audit log is written server-side in the same handler (09-03) — the UI cannot skip it; the invalidation of ['ranking','logs'] makes the new audit row visible in LogsTab (09-07). (T4) |
</threat_model>

<verification>
- `cd torre-de-controle && npx tsc --noEmit` → exit 0; `bun run build` → exit 0.
- No "Disponível na Phase 9" / PHASE9_NOTICE placeholders remain in the 3 components.
- useCanWriteRanking gates write controls in all 3 (grep-verified); operador not sent from the eval form.
- LIVE: admin can submit evaluation (200 + dialog close + refetch) and unblock/manually-block; viewer/analyst see no write controls.
</verification>

<success_criteria>
The driver-lifecycle write UI is live and role-gated: admin|supervisor evaluate (with the NO-SHOW auto-block surfaced via invalidation), unblock, and manually block; analyst|viewer get a clean read-only view (no write controls). operador is server-resolved (not collected in the form). Mutations invalidate ['ranking',...] so the page reflects writes immediately. The Phase 8 "Phase 9" placeholders are gone.
</success_criteria>

<output>
After completion, create `.planning/phases/09-ranking-escrita-auditoria-write-flows/09-06-SUMMARY.md` — record whether the in-dialog Bloquear button was wired or removed, and the manual-block UX chosen.
</output>
