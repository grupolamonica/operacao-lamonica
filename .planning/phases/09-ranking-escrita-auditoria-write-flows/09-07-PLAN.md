---
phase: 09-ranking-escrita-auditoria-write-flows
plan: 07
type: execute
wave: 5
depends_on: [05]
files_modified:
  - torre-de-controle/src/app/pages/ranking/components/RotasTab.tsx
  - torre-de-controle/src/app/pages/ranking/components/LogsTab.tsx
autonomous: true
requirements: [D-09-01, D-09-05, D-09-09, D-09-10]
must_haves:
  truths:
    - "An admin/supervisor can create, edit, and delete a route-score from RotasTab via useCreateRouteScore/useUpdateRouteScore/useDeleteRouteScore; the table refetches after each (D-09-10)"
    - "analyst/viewer see RotasTab read-only — no Nova Pontuação / edit / delete controls (D-09-10)"
    - "LogsTab consumes useRankingLogs (GET /api/ranking/logs) and renders real audit rows with the existing antes/depois renderDiff (no more logs=[])"
  artifacts:
    - path: "torre-de-controle/src/app/pages/ranking/components/RotasTab.tsx"
      provides: "Route-score CRUD (create/update/delete) via mutation hooks, role-gated"
      contains: "useCreateRouteScore"
    - path: "torre-de-controle/src/app/pages/ranking/components/LogsTab.tsx"
      provides: "Live audit feed from useRankingLogs (replaces the const logs = [] shell)"
      contains: "useRankingLogs"
  key_links:
    - from: "RotasTab CRUD controls"
      to: "useCreateRouteScore / useUpdateRouteScore / useDeleteRouteScore + useCanWriteRanking"
      via: "dialog form + row actions, gated by role"
      pattern: "useCreateRouteScore"
    - from: "LogsTab"
      to: "useRankingLogs (09-05) → GET /api/ranking/logs (09-02)"
      via: "hook replaces the hardcoded empty array"
      pattern: "useRankingLogs"
---

<objective>
Wire the remaining WRITE/READ UI (D-09-10): give `RotasTab` full route-score CRUD (`useCreateRouteScore`/`useUpdateRouteScore`/`useDeleteRouteScore`) gated by role, and switch `LogsTab` from its `const logs = []` shell to the live `useRankingLogs` feed (09-02's `GET /logs`). All hooks exist (09-05); this plan replaces the Phase 8 placeholders (the disabled "Nova Pontuação" button; the empty logs array) with working, role-gated controls and a real audit table.

Purpose: Complete the write surface UI (route config) and close the audit loop — the LogsTab finally shows the antes/depois diffs that every Phase 9 mutation writes. RotasTab edits bust the cache server-side (09-04) and invalidate ['ranking',...] so derived scores refresh. Read-only roles see RotasTab without CRUD controls; LogsTab is read-only by nature (any role).
Output: 2 wired components. `bun run build` (torre-de-controle) exit 0. LIVE: admin creates/edits/deletes a route-score (table refetches); LogsTab lists real audit rows; viewer sees RotasTab read-only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-ranking-escrita-auditoria-write-flows/09-CONTEXT.md

<interfaces>
<!-- 09-05 hooks (Wave 4) + the existing shells this plan wires. -->

From torre-de-controle/src/hooks/useRanking.ts (09-05):
```typescript
useRankingLogs()        // { data: EvaluationLogRecord[], isLoading, isError, error, refetch } — GET /logs
useCreateRouteScore()   // { mutate, isPending, isError, error } — POST /route-scores, invalidates route-scores/trips/drivers/stats/logs
useUpdateRouteScore()   // PATCH /route-scores/:id
useDeleteRouteScore()   // DELETE /route-scores/:id
useCanWriteRanking(): boolean
// EvaluationLogRecord re-exported from useRanking (09-05) — import from '@/hooks/useRanking'
// RouteScoreCreateInput: { origin_code, destination_code, pontuacao, data_inicio, data_fim, observacao }
// RouteScoreUpdateInput: { id, ...partial of the above }
// delete: id: string
```
From the shells (CURRENT state — Phase 8 placeholders to replace):
- RotasTab.tsx: DataTable of RouteScoreRecord (Origem/Destino/Viagens/Pontuação/Período/Obs.); a `toolbar` with a DISABLED "Nova Pontuação" button `title="Disponível na Phase 9"`; rows mapped to `{...rs, id: rs.id ?? `${rs.origin_code}-${rs.destination_code}-${rs.data_inicio}`}`. No row actions today.
- LogsTab.tsx: full DataTable + `ActionBadge` + `renderDiff(dados_antes, dados_depois)` + `formatLogDate` ALREADY BUILT; it imports `EvaluationLogRecord` by deep relative path and uses `const logs: EvaluationLogRecord[] = []` with a "habilitada na Phase 9" subtitle/empty message. The 08-08 SUMMARY explicitly says Phase 9 "troca 1 linha por um hook".

From torre-de-controle/src/components/ui/* : Dialog/Input/Label/Button/Select already available (RotasTab imports Button/Badge; add Dialog/Input/Label for the CRUD form). The DataTable `toolbar` prop is already used by RotasTab.
</interfaces>

<role_gate_note>
useCanWriteRanking() (09-05) is the UX gate (admin|supervisor). It is NOT security — the backend requireRole (09-04) enforces. For analyst|viewer: hide the "Nova Pontuação" button + the row edit/delete actions (read-only RotasTab, D-09-10). LogsTab has NO write controls and is reachable by ANY authenticated role (GET /logs is authGuard-only, D-09-01) — no gate needed there; just swap the data source.
</role_gate_note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: LogsTab — consume useRankingLogs (replace the const logs = [] shell)</name>
  <files>torre-de-controle/src/app/pages/ranking/components/LogsTab.tsx</files>
  <read_first>
    - torre-de-controle/src/app/pages/ranking/components/LogsTab.tsx (the FULL component — the table/columns/renderDiff/ActionBadge are DONE; only `const logs = []` + the "Phase 9" subtitle/empty message change. Note line ~10 imports EvaluationLogRecord by deep path and line ~148 sets the empty array)
    - torre-de-controle/src/hooks/useRanking.ts (09-05 — useRankingLogs returns { data, isLoading, isError, error }; EvaluationLogRecord is re-exported here)
  </read_first>
  <action>
    LogsTab.tsx — minimal swap (08-08 designed this as a 1-line change):
      - Replace `const logs: EvaluationLogRecord[] = []` with `const { data: logs, isLoading, isError } = useRankingLogs()` (import useRankingLogs from `@/hooks/useRanking`). Prefer importing `EvaluationLogRecord` from `@/hooks/useRanking` (09-05 re-export) instead of the deep relative path — update the import; if leaving the deep path is simpler, that is acceptable as long as it still typechecks.
      - Update the DataTable `subtitle` + `emptyMessage`: drop the "Auditoria será habilitada na Phase 9" copy. subtitle → isLoading ? 'Carregando…' : isError ? 'Falha ao carregar' : `${logs.length} registros`. emptyMessage → isLoading ? 'Carregando auditoria…' : isError ? 'Não foi possível carregar a auditoria.' : 'Nenhum registro de auditoria ainda.'
      - Leave columns/renderDiff/ActionBadge/formatLogDate untouched — they already handle CRIAÇÃO/EDIÇÃO/DESBLOQUEIO and the antes/depois diff (and tolerate the new BLOQUEIO_MANUAL/BLOQUEIO_NO_SHOW/ROTA_* acoes via the default ActionBadge branch). The ActionBadge already matches accented + ASCII variants.
  </action>
  <acceptance_criteria>
    - `cd torre-de-controle && npx tsc --noEmit` exits 0; `bun run build` exits 0.
    - `grep -n "useRankingLogs" torre-de-controle/src/app/pages/ranking/components/LogsTab.tsx` returns the hook usage.
    - `grep -c "const logs.*= \[\]" torre-de-controle/src/app/pages/ranking/components/LogsTab.tsx` returns 0 (the hardcoded empty array is gone).
    - `grep -c "habilitada na Phase 9\|Phase 9" torre-de-controle/src/app/pages/ranking/components/LogsTab.tsx` returns 0 (placeholder copy removed).
    - renderDiff/ActionBadge preserved: `grep -n "renderDiff" ...` AND `grep -n "ActionBadge" ...` still return lines (the diff renderer was NOT removed).
    - LIVE (any authenticated role; needs the API + a real session — the audit ROWS require RANK_SUPABASE_SERVICE_KEY, shared with the 09-03 Task-3 checkpoint, but the WIRING/empty-state is verifiable without it): logged in, open /ranking → Logs tab → the table loads from GET /api/ranking/logs (shows rows if any exist; clean empty state otherwise; no Phase 9 copy). After any Phase 9 write (09-06) the new audit row appears once ['ranking','logs'] is invalidated.
  </acceptance_criteria>
  <verify>
    <automated>cd torre-de-controle && npx tsc --noEmit && bun run build</automated>
    <!-- Gate: build clean + grep checks (useRankingLogs wired, no `const logs = []`, no Phase 9 copy, renderDiff/ActionBadge preserved). LIVE Logs tab loads from GET /logs per acceptance_criteria. -->
  </verify>
  <done>LogsTab reads from useRankingLogs (no more logs=[]), with loading/error/empty states and the Phase 9 copy removed; the existing renderDiff/ActionBadge are preserved; build + tsc exit 0.</done>
</task>

<task type="auto">
  <name>Task 2: RotasTab — route-score CRUD (create/update/delete) via mutation hooks, role-gated</name>
  <files>torre-de-controle/src/app/pages/ranking/components/RotasTab.tsx</files>
  <read_first>
    - torre-de-controle/src/app/pages/ranking/components/RotasTab.tsx (the FULL shell — the disabled "Nova Pontuação" toolbar button + the columns + the RouteRow id mapping; this plan adds the form + row actions)
    - torre-de-controle/src/app/pages/ranking/components/EvaluationFormDialog.tsx (the Dialog + controlled-field pattern to mirror for the route-score form — Select/Input/Label/Button; keep the Torre Argon styling)
    - torre-de-controle/src/hooks/useRanking.ts (09-05 — useCreateRouteScore/useUpdateRouteScore/useDeleteRouteScore signatures + RouteScoreCreateInput/UpdateInput fields + useCanWriteRanking)
    - C:\Users\antonio.magalhaes\Documents\Projetos\produção\ride-rank-buddy\src\components\RouteScores.tsx (the original CRUD UI — for parity on the form fields: origin_code, destination_code, pontuacao, data_inicio, data_fim, observacao)
  </read_first>
  <action>
    RotasTab.tsx:
      - Add `const createRoute = useCreateRouteScore()`, `const updateRoute = useUpdateRouteScore()`, `const deleteRoute = useDeleteRouteScore()`, `const canWrite = useCanWriteRanking()` (import from `@/hooks/useRanking`).
      - Replace the disabled "Nova Pontuação" toolbar button: render it ENABLED only when `canWrite` (hidden otherwise — D-09-10); onClick opens a route-score Dialog in CREATE mode. Remove the "Disponível na Phase 9" title.
      - Build a route-score Dialog (mirror EvaluationFormDialog's Dialog structure) with controlled inputs: origin_code (text), destination_code (text), pontuacao (number input), data_inicio (text/date — match the string format used by the backend; the existing column shows raw data_inicio strings), data_fim (optional text/date, nullable → empty = null), observacao (optional textarea/text). The dialog serves both CREATE (empty) and EDIT (prefilled from the row).
      - CREATE submit → `createRoute.mutate(payload as RouteScoreCreateInput, { onSuccess: close })`. EDIT submit → `updateRoute.mutate({ id, ...patch }, { onSuccess: close })`. On success the hook invalidation refreshes the table + derived scores.
      - Add row actions (a new "Ação" column) rendered only when `canWrite`: an "Editar" button (opens the dialog prefilled with that RouteRow) and a "Remover" button → `deleteRoute.mutate(row.original.id, ...)` (guard with a confirm()). For rows whose `id` is the deterministic fallback (`origin-destination-data_inicio`, i.e. the backend returned no real id), DISABLE edit/delete and title it "registro sem id — recarregue" (you cannot PATCH/DELETE by a synthetic id). Prefer using `rs.id` (real uuid) for the row key when present so the actions target the real row.
      - For analyst|viewer (`!canWrite`): no "Nova Pontuação" button, no row actions column — RotasTab stays exactly the read-only table it is today (D-09-10).
      - Surface mutation errors inline (createRoute/updateRoute/deleteRoute .isError → .error?.message).
  </action>
  <acceptance_criteria>
    - `cd torre-de-controle && npx tsc --noEmit` exits 0; `bun run build` exits 0.
    - `grep -cE "useCreateRouteScore|useUpdateRouteScore|useDeleteRouteScore" torre-de-controle/src/app/pages/ranking/components/RotasTab.tsx` returns 3; `grep -c "\.mutate" ...` returns >= 3.
    - `grep -n "useCanWriteRanking" torre-de-controle/src/app/pages/ranking/components/RotasTab.tsx` returns a line gating the toolbar button + row actions (D-09-10).
    - `grep -c "Disponível na Phase 9" torre-de-controle/src/app/pages/ranking/components/RotasTab.tsx` returns 0 (placeholder removed).
    - The synthetic-id guard exists: `grep -n "rs.id" torre-de-controle/src/app/pages/ranking/components/RotasTab.tsx` (or equivalent) shows edit/delete prefer/require the real backend id, not the deterministic fallback — so PATCH/DELETE never fire with a synthetic key.
    - LIVE (admin cookie; DB-effect shares the 09-03 Task-3 checkpoint when the secret is absent): admin → "Nova Pontuação" opens a form, submitting creates a route-score that appears in the table after refetch; "Editar" updates pontuacao (table refetches); "Remover" deletes the row. viewer/analyst → no "Nova Pontuação", no edit/delete actions (read-only table).
  </acceptance_criteria>
  <verify>
    <automated>cd torre-de-controle && npx tsc --noEmit && bun run build</automated>
    <!-- Gate: build clean + grep checks (3 route-score mutation hooks, >=3 .mutate, useCanWriteRanking gate, no PHASE9 placeholder, synthetic-id guard on rs.id). LIVE admin CRUD / viewer-read-only per acceptance_criteria. -->
  </verify>
  <done>RotasTab has full route-score CRUD (create/update/delete) via the 09-05 hooks, role-gated with no Phase 9 placeholder, with a synthetic-id guard on edit/delete; build + tsc exit 0; admin can manage route-scores, viewer is read-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RotasTab CRUD UI → route-score mutation hooks → /api/ranking/route-scores writes | User-entered route config leaves the UI for the requireRole-gated endpoints; the role gate here is UX, the backend requireRole is the control. |
| LogsTab → GET /api/ranking/logs | Read-only audit feed; reachable by any authenticated role (authGuard) — no write path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-26 | Elevation | RotasTab client role gate | accept (mitigated server-side) | Hiding CRUD controls for analyst|viewer is UX only; a tampered client still hits `requireRole('admin','supervisor')` (09-04) → 403. The backend is authoritative. (T1) |
| T-09-27 | Tampering | bad route-score values (pontuacao non-numeric, etc.) | mitigate | Backend typebox (09-04) rejects with 422; the number input + the hooks surface the error. (T5) |
| T-09-28 | Tampering | PATCH/DELETE with a synthetic row id | mitigate | Edit/delete are disabled for rows lacking a real backend uuid (synthetic `origin-dest-date` fallback), so a mutation never targets a non-existent id (would 404). |
| T-09-29 | Info-disclosure (XSS) | rendering audit dados_antes/depois in LogsTab | mitigate | renderDiff (08-08) renders values as escaped JSX text (no dangerouslySetInnerHTML — T-08-15 carry-over); real audit data now flows through the same safe renderer. |
| T-09-30 | Repudiation | route edits without audit | mitigate | Each route mutation logs ROTA_* server-side (09-04); LogsTab surfaces it after ['ranking','logs'] invalidation. (T4) |
</threat_model>

<verification>
- `cd torre-de-controle && npx tsc --noEmit` → exit 0; `bun run build` → exit 0.
- LogsTab: no `logs = []`, no Phase 9 copy, renderDiff/ActionBadge preserved, reads useRankingLogs.
- RotasTab: 3 route-score mutation hooks wired, role-gated, synthetic-id guard, no Phase 9 placeholder.
- LIVE: admin CRUDs route-scores (table refetches) + LogsTab shows real audit rows; viewer sees RotasTab read-only.
</verification>

<success_criteria>
The route-config write UI and the audit read UI are complete: admin|supervisor manage route-scores (create/edit/delete, each audited + cache-busted server-side, table refetched), analyst|viewer see RotasTab read-only, and LogsTab shows the live antes/depois audit feed from GET /logs (the last Phase 8 shell placeholder removed). With 09-06, all D-09-10 write controls are wired and gated.
</success_criteria>

<output>
After completion, create `.planning/phases/09-ranking-escrita-auditoria-write-flows/09-07-SUMMARY.md`
</output>
