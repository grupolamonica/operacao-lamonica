---
phase: 09-ranking-escrita-auditoria-write-flows
plan: 04
type: execute
wave: 3
depends_on: [01, 03]
files_modified:
  - api/src/modules/ranking/ranking.write.service.ts
  - api/src/modules/ranking/ranking.write.plugin.ts
  - api/src/index.ts   # only if 09-03 took the inline-in-index.ts fallback (Elysia 1.4.28 body bug); then the 3 route-scores routes go in that SAME inline block instead of the plugin. Default: untouched (plugin path).
autonomous: true
requirements: [D-09-01, D-09-04, D-09-05, D-09-07]
must_haves:
  truths:
    - "POST /api/ranking/route-scores (admin/supervisor) creates a route_scores row + audit log + busts cache"
    - "PATCH /api/ranking/route-scores/:id (admin/supervisor) updates a route_scores row + audit log + busts cache"
    - "DELETE /api/ranking/route-scores/:id (admin/supervisor) deletes a route_scores row + audit log + busts cache"
    - "analyst/viewer (or no cookie) get 403/401 on every route-scores write endpoint"
    - "Each route-scores mutation writes an evaluation_logs row with an analogous acao taxonomy (ROTA_CRIACAO/ROTA_EDICAO/ROTA_REMOCAO) + server-resolved operador (D-09-05)"
  artifacts:
    - path: "api/src/modules/ranking/ranking.write.service.ts"
      provides: "createRouteScoreLogged / updateRouteScoreLogged / deleteRouteScoreLogged orchestrators"
      contains: "RouteScoreLogged"
    - path: "api/src/modules/ranking/ranking.write.plugin.ts"
      provides: "POST/PATCH/DELETE /route-scores behind requireRole('admin','supervisor')"
      contains: "route-scores"
  key_links:
    - from: "ranking.write.plugin.ts route-scores routes"
      to: "requireRole('admin','supervisor') (plugin-level, inherited)"
      via: "same write plugin scope as 09-03"
      pattern: "route-scores"
    - from: "route-scores orchestrators"
      to: "createEvaluationLog (audit) + bustRankingCache"
      via: "log + bust after each mutation"
      pattern: "createEvaluationLog"
---

<objective>
Complete the write surface with route-scores CRUD: `POST /api/ranking/route-scores`, `PATCH /api/ranking/route-scores/:id`, `DELETE /api/ranking/route-scores/:id` — all behind `requireRole('admin','supervisor')` (D-09-01/07), each logging an audit row (analogous taxonomy, D-09-05) and busting the ranking cache (D-09-04). These ADD to the SAME `ranking.write.plugin.ts` + `ranking.write.service.ts` created in 09-03 (hence the dependency — shared files, sequential wave). The Supabase CRUD functions already exist from 09-01 (`createRouteScore`/`updateRouteScore`/`deleteRouteScore`).

Purpose: Route base-points drive the derived score (getRouteBasePoints in ranking.routes.ts feeds transformTrips). Editing them must be admin|supervisor-only, audited, and must bust the cache so the next read recomputes scores with the new base points (D-09-04). Closes the last of the D-09-07 endpoints.
Output: 3 route-scores routes added to the existing write plugin + 3 logged orchestrators. LIVE: admin → 200 + row mutated + log; analyst → 403; no cookie → 401. `bun --bun tsc --noEmit` exit 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-ranking-escrita-auditoria-write-flows/09-CONTEXT.md

<interfaces>
<!-- 09-01 + 09-03 outputs the executor builds on. -->

From api/src/modules/ranking/ranking.writes.ts (09-01):
```typescript
createRouteScore(r: Omit<RouteScoreRecord,'id'|'created_at'|'updated_at'>): Promise<RouteScoreRecord>
updateRouteScore(id: string, patch: Partial<RouteScoreRecord>): Promise<RouteScoreRecord | null>
deleteRouteScore(id: string): Promise<void>
```
From api/src/modules/ranking/ranking.audit.ts (09-01): `createEvaluationLog(...)`.
From api/src/modules/ranking/ranking.cache.ts (09-01): `bustRankingCache()`.
From api/src/modules/ranking/ranking.write.service.ts (09-03): `resolveOperador(userId): Promise<string>` — REUSE it (do not redefine).
From api/src/modules/ranking/ranking.write.plugin.ts (09-03): the `rankingWritePlugin` (or inline-fallback block) already chains `requireRole('admin','supervisor')` + `.group('/api/ranking', ...)`; ADD the route-scores routes into the SAME chain.

From api/src/modules/ranking/ranking.types.ts:
```typescript
export interface RouteScoreRecord {
  id?: string; origin_code: string; destination_code: string; pontuacao: number;
  data_inicio: string; data_fim: string | null; observacao: string | null;
}
```
</interfaces>

<ride_rank_parity>
<!-- From routeScoreService.ts — the CRUD shapes (audit was added by ride-rank's UI layer; here we centralize it server-side). -->
createRouteScore(record): insert {origin_code, destination_code, pontuacao, data_inicio, data_fim, observacao}
updateRouteScore(id, patch): update {...patch, updated_at: now} where id
deleteRouteScore(id): delete where id
Audit taxonomy (D-09-05 "taxonomia análoga"): ROTA_CRIACAO / ROTA_EDICAO / ROTA_REMOCAO (no trip_id/driver_id — these logs carry the route identity in dados_*).
</ride_rank_parity>
</context>

<tasks>

<task type="auto">
  <name>Task 1: route-scores logged orchestrators in ranking.write.service.ts</name>
  <files>api/src/modules/ranking/ranking.write.service.ts</files>
  <read_first>
    - api/src/modules/ranking/ranking.write.service.ts (09-03 — match the existing orchestrator style; REUSE resolveOperador; mirror how blockDriverManual logs + busts)
    - C:\Users\antonio.magalhaes\Documents\Projetos\produção\ride-rank-buddy\src\services\routeScoreService.ts lines 25-41 (createRouteScore/updateRouteScore/deleteRouteScore — the CRUD being wrapped)
    - api/src/modules/ranking/ranking.writes.ts (09-01 — the createRouteScore/updateRouteScore/deleteRouteScore signatures this wraps)
  </read_first>
  <action>
    Add 3 orchestrators to `ranking.write.service.ts` (append; do not touch the 09-03 functions), reusing the existing `resolveOperador`, `createEvaluationLog`, `bustRankingCache` imports (add createRouteScore/updateRouteScore/deleteRouteScore to the existing import from `./ranking.writes`):

    `createRouteScoreLogged(input: Omit<RouteScoreRecord,'id'|'created_at'|'updated_at'>, userId: string): Promise<RouteScoreRecord>`:
      - operador = resolveOperador(userId).
      - const row = await createRouteScore(input).
      - await createEvaluationLog({ operador, acao:'ROTA_CRIACAO', dados_antes:null, dados_depois: { ...input } as Record<string,unknown> }).
      - await bustRankingCache()  (new base points change derived scores — D-09-04).
      - return row.

    `updateRouteScoreLogged(id: string, patch: Partial<RouteScoreRecord>, userId: string): Promise<RouteScoreRecord | null>`:
      - operador = resolveOperador(userId).
      - const row = await updateRouteScore(id, patch).
      - await createEvaluationLog({ operador, acao:'ROTA_EDICAO', dados_antes: { id } as Record<string,unknown>, dados_depois: { id, ...patch } as Record<string,unknown> }).
        (dados_antes carries at least the id; the full pre-image is not re-fetched to keep it one query — document this minor divergence: route edits log the patch + id, not a full before-snapshot.)
      - await bustRankingCache().
      - return row.

    `deleteRouteScoreLogged(id: string, userId: string): Promise<void>`:
      - operador = resolveOperador(userId).
      - await deleteRouteScore(id).
      - await createEvaluationLog({ operador, acao:'ROTA_REMOCAO', dados_antes: { id } as Record<string,unknown>, dados_depois: null }).
      - await bustRankingCache().

    Header/comment: route-score mutations use the analogous audit taxonomy ROTA_CRIACAO/ROTA_EDICAO/ROTA_REMOCAO (D-09-05) and bust the cache because base points feed the derived score (D-09-04). operador server-resolved (T-09-12). No driver/trip ids on these logs — the route identity lives in dados_*.
  </action>
  <acceptance_criteria>
    - `bun --bun tsc --noEmit` (from api/) exits 0.
    - `grep -cE "ROTA_(CRIACAO|EDICAO|REMOCAO)" api/src/modules/ranking/ranking.write.service.ts` returns 3.
    - `grep -c "createRouteScoreLogged\|updateRouteScoreLogged\|deleteRouteScoreLogged" api/src/modules/ranking/ranking.write.service.ts` returns 3 export sites.
    - `grep -c "bustRankingCache" api/src/modules/ranking/ranking.write.service.ts` increased by 3 vs after 09-03 (now >= 6 total).
    - `grep -c "resolveOperador" api/src/modules/ranking/ranking.write.service.ts` shows it is REUSED (still ONE definition — not redefined; route orchestrators call it).
    - `bun test src/modules/ranking/` (dummy envs) → 25 pass (no regression; import-safe).
  </acceptance_criteria>
  <done>createRouteScoreLogged/updateRouteScoreLogged/deleteRouteScoreLogged added, each logging ROTA_* audit + busting cache + reusing resolveOperador; tsc exit 0; 25 tests green.</done>
</task>

<task type="auto">
  <name>Task 2: route-scores endpoints in ranking.write.plugin.ts (same requireRole scope)</name>
  <files>api/src/modules/ranking/ranking.write.plugin.ts, api/src/index.ts (only if 09-03 used the inline fallback)</files>
  <read_first>
    - api/src/modules/ranking/ranking.write.plugin.ts (09-03 — ADD into the SAME `.group('/api/ranking', app => app...)` chain under the same plugin-level requireRole; copy the POST /evaluations route shape for body+detail)
    - .planning/phases/09-ranking-escrita-auditoria-write-flows/09-03-SUMMARY.md (CHECK FIRST — it records whether 09-03 took the plugin path or the inline-in-index.ts fallback; this decides which file gets the 3 route-scores routes)
    - api/src/index.ts lines 56-67 of ranking.plugin.ts is the read /route-scores GET — note the hyphen segment; for the write plugin use the same string literal `/route-scores` in `.post('/route-scores', ...)` (Elysia route strings accept hyphens; the hyphen only constrains Eden Treaty dot-access on the front, handled in 09-05)
    - api/src/modules/users/users.plugin.ts lines 117-158 (PATCH/:id + DELETE/:id with params t.Object — copy for the /route-scores/:id routes)
  </read_first>
  <action>
    Add 3 routes to the existing write plugin's `.group('/api/ranking', ...)` chain (after the 09-03 PATCH /blocks/:id), all inheriting the plugin-level `requireRole('admin','supervisor')` (no per-route gate needed — D-09-01). Import the 3 new orchestrators from `./ranking.write.service`.

      1. `POST /route-scores`: body `t.Object({ origin_code: t.String({minLength:1}), destination_code: t.String({minLength:1}), pontuacao: t.Integer(), data_inicio: t.String({minLength:1}), data_fim: t.Union([t.String(), t.Null()]), observacao: t.Union([t.String({maxLength:500}), t.Null()]) })`. Handler `{ body, user }`: `const row = await createRouteScoreLogged(body, user.id); return { ok:true, id: row.id }`. detail tags ['ranking'].
      2. `PATCH /route-scores/:id`: params `t.Object({ id: t.String() })`, body `t.Object({ origin_code: t.Optional(t.String()), destination_code: t.Optional(t.String()), pontuacao: t.Optional(t.Integer()), data_inicio: t.Optional(t.String()), data_fim: t.Optional(t.Union([t.String(), t.Null()])), observacao: t.Optional(t.Union([t.String({maxLength:500}), t.Null()])) })`. Handler: `const row = await updateRouteScoreLogged(params.id, body, user.id); if (!row) { set.status = 404; return { error:'Route score not found' } } return { ok:true }`.
      3. `DELETE /route-scores/:id`: params `t.Object({ id: t.String() })`. Handler: `await deleteRouteScoreLogged(params.id, user.id); set.status = 204; return ''` (mirror users.plugin DELETE 204 shape).

    IF 09-03's SUMMARY records the inline-in-index.ts fallback path (Elysia body bug), add these 3 routes to that SAME inline `requireRole('admin','supervisor')` block in index.ts instead of the plugin — and note in THIS plan's SUMMARY that index.ts was the file touched (the frontmatter already lists index.ts as conditional). Default assumption: 09-03 used the plugin and index.ts is NOT touched here.
  </action>
  <acceptance_criteria>
    - `bun --bun tsc --noEmit` (from api/) exits 0.
    - `grep -cE "'/route-scores'|'/route-scores/:id'" api/src/modules/ranking/ranking.write.plugin.ts` returns 3 (POST + PATCH + DELETE) — OR the same 3 in `api/src/index.ts` if 09-03's SUMMARY recorded the inline fallback.
    - `grep -c "RouteScoreLogged" api/src/modules/ranking/ranking.write.plugin.ts` (or index.ts on the fallback path) returns 3 (handlers call the orchestrators).
    - LIVE — RBAC (no secret needed; requireRole fires before Supabase): boot api, `POST /api/ranking/route-scores` with NO cookie → 401; with analyst cookie → 403; admin cookie + invalid body (missing origin_code) → 422.
    - LIVE — DB effect (requires RANK_SUPABASE_SERVICE_KEY; deferred to 09-03's Task 3 checkpoint scope if absent): admin `POST /route-scores` valid → 200 {ok:true,id}; the row appears in `GET /api/ranking/route-scores`; a ROTA_CRIACAO log appears in `GET /api/ranking/logs`. `PATCH /route-scores/:id {pontuacao: 5}` → 200; the row's pontuacao updates; ROTA_EDICAO log. `DELETE /route-scores/:id` → 204; row gone from `GET /api/ranking/route-scores`; ROTA_REMOCAO log.
    - The 6 read endpoints + the 3 driver-lifecycle write endpoints (09-03) still behave (401 without cookie); wsPlugin still last.
  </acceptance_criteria>
  <done>POST/PATCH/DELETE /route-scores added under the same requireRole('admin','supervisor') scope (plugin, or index.ts inline block iff 09-03 used the fallback); handlers call the logged orchestrators; LIVE 401/403/422 without secret; DB effect + ROTA_* audit with secret; no regression to existing endpoints; tsc exit 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → POST/PATCH/DELETE /api/ranking/route-scores → ride-rank Supabase | Untrusted route-config input crosses into the OPEN-RLS DB; requireRole + typebox are the only controls. Base points here feed the derived score, so a bad value silently affects ranking until corrected. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-15 | Elevation | route-scores write endpoints | mitigate | Inherit plugin-level `requireRole('admin','supervisor')` → 403 analyst/viewer, 401 no cookie. Verified live. (T1) |
| T-09-16 | Tampering | malformed route-score body (e.g. pontuacao as string) | mitigate | typebox `t.Integer()` on pontuacao, `t.String({minLength})` on codes, maxLength on observacao → 422. Verified live. (T5) |
| T-09-17 | Repudiation | route edit without audit | mitigate | Each route mutation calls createEvaluationLog (ROTA_CRIACAO/EDICAO/REMOCAO) with server-resolved operador in the same handler. (T4/T-09-12) |
| T-09-18 | Tampering | stale derived score after base-point change | mitigate | bustRankingCache() after each route mutation forces the next read to recompute transformTrips with the new base points (D-09-04). |
| T-09-14 | — (RLS open) | direct DB route-score write | accept | Same accepted risk as 09-03 (key server-side only). |
</threat_model>

<verification>
- `cd api && bun --bun tsc --noEmit` → exit 0.
- LIVE without secret: route-scores POST/PATCH/DELETE → 401 (no cookie) / 403 (analyst) / 422 (bad body). MUST pass autonomously.
- LIVE with secret: 200/204 + row created/updated/deleted + ROTA_* audit + cache busted.
- No regression to the 6 reads or the 3 driver-lifecycle writes; wsPlugin last.
</verification>

<success_criteria>
The route-scores CRUD write surface is live and admin|supervisor-only, each operation audited with the ROTA_* taxonomy and busting the cache so derived scores recompute. All D-09-07 endpoints now exist. RBAC + validation proven without the secret; DB effect proven with it.
</success_criteria>

<output>
After completion, create `.planning/phases/09-ranking-escrita-auditoria-write-flows/09-04-SUMMARY.md` — record which file received the routes (plugin vs index.ts inline, per 09-03's path).
</output>
