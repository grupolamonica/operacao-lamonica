---
phase: 09-ranking-escrita-auditoria-write-flows
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - api/src/modules/ranking/ranking.write.service.ts
  - api/src/modules/ranking/ranking.write.plugin.ts
  - api/src/index.ts
  - api/.env.example
autonomous: false
requirements: [D-09-01, D-09-02, D-09-03, D-09-04, D-09-05, D-09-06, D-09-07, D-09-08, D-09-09]
must_haves:
  truths:
    - "POST /api/ranking/evaluations with an admin/supervisor cookie upserts evaluations by trip_id and writes an evaluation_logs row CRIAÇÃO (new) or EDIÇÃO (existing)"
    - "An evaluation with atendeu=false ALSO inserts a driver_blocks row tipo=NO_SHOW + an evaluation_logs row BLOQUEIO_NO_SHOW, server-side, in the same handler (D-09-02)"
    - "POST /api/ranking/blocks (admin/supervisor) inserts a MANUAL block + log BLOQUEIO_MANUAL; PATCH /api/ranking/blocks/:id unblocks (ativo=false, data_fim=now, manual_override=true) + log DESBLOQUEIO"
    - "analyst/viewer (or no cookie) get 403/401 on every write endpoint (requireRole('admin','supervisor'))"
    - "operador on every write + log = the authenticated Torre user's name (fallback email/id), resolved server-side from the JWT user id"
    - "After every successful write the ranking sheets Redis cache is busted (D-09-04/09)"
    - "comunicacao/desvio_rota/postura are validated as enums; ajuste_manual is clamped to [-20,20]; observacao length-bounded (D-09-06, T5)"
  artifacts:
    - path: "api/src/modules/ranking/ranking.write.service.ts"
      provides: "evaluateTrip (upsert+log+autoblock), blockDriverManual, unblockDriver orchestrators + resolveOperador + bust"
      contains: "evaluateTrip"
    - path: "api/src/modules/ranking/ranking.write.plugin.ts"
      provides: "POST /evaluations, POST /blocks, PATCH /blocks/:id behind requireRole('admin','supervisor') with typebox bodies"
      contains: "requireRole"
    - path: "api/src/index.ts"
      provides: "rankingWritePlugin wired before wsPlugin (or inline fallback if body bug)"
      contains: "rankingWrite"
  key_links:
    - from: "ranking.write.plugin.ts"
      to: "requireRole('admin','supervisor')"
      via: ".use(requireRole(...)) at plugin level"
      pattern: "requireRole\\('admin', ?'supervisor'\\)"
    - from: "ranking.write.service.ts evaluateTrip"
      to: "upsertEvaluation + createEvaluationLog + insertDriverBlock + bustRankingCache"
      via: "sequential awaits in one handler"
      pattern: "createEvaluationLog"
    - from: "evaluateTrip (atendeu=false)"
      to: "insertDriverBlock tipo=NO_SHOW + log BLOQUEIO_NO_SHOW"
      via: "conditional auto-block inside evaluate"
      pattern: "NO_SHOW"
    - from: "ranking.write.service.ts"
      to: "Torre users table (operador resolution)"
      via: "db.select name/email from users where id = user.id"
      pattern: "resolveOperador"
---

<objective>
Ship the driver-lifecycle WRITE endpoints — the core of Phase 9: `POST /api/ranking/evaluations` (upsert + audit + auto-block NO_SHOW), `POST /api/ranking/blocks` (manual block + audit), `PATCH /api/ranking/blocks/:id` (unblock + audit) — all behind `requireRole('admin','supervisor')` (D-09-01/07), replicating the ride-rank `DataContext.evaluateTrip` / `unblockDriver` flows EXACTLY (D-09-02/05/06). Each write resolves `operador` from the authenticated Torre user, writes its audit log in the same handler (T4), and busts the ranking cache (D-09-04/09). New write orchestrators live in `ranking.write.service.ts`; the HTTP surface in `ranking.write.plugin.ts` (mirroring the proven `users.plugin` POST-with-body pattern), with an inline-in-index.ts fallback documented for the Elysia 1.4.28 body bug (D-09-08).

Purpose: This is the write heart of the milestone — avaliar → pontuar (derived) → auto-bloquear, plus manual block/unblock, with non-bypassable RBAC and atomic auditoria. It must mirror ride-rank so the existing data + the Phase 7 read composition stay consistent.
Output: 2 new ranking files + index.ts wiring + .env.example note. LIVE: admin cookie → 200 + DB row + log row; analyst → 403; no cookie → 401; atendeu=false → block row + 2 logs. `bun --bun tsc --noEmit` exit 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/09-ranking-escrita-auditoria-write-flows/09-CONTEXT.md

<interfaces>
<!-- Contracts the executor builds against. 09-01 outputs + existing Torre patterns. -->

From api/src/modules/ranking/ranking.writes.ts (09-01 — Wave 1 dependency):
```typescript
upsertEvaluation(e: Omit<EvaluationRecord,'id'|'created_at'|'updated_at'>): Promise<{ record: EvaluationRecord; existed: boolean; before: EvaluationRecord | null }>
insertDriverBlock(b: Omit<DriverBlockRecord,'id'|'updated_at'>): Promise<void>
unblockDriverBlocks(driverId: string): Promise<void>   // ativo=false, data_fim=now, manual_override=true, where ativo=true
```
From api/src/modules/ranking/ranking.audit.ts (09-01):
```typescript
createEvaluationLog(log: Omit<EvaluationLogRecord,'id'|'created_at'>): Promise<void>
```
From api/src/modules/ranking/ranking.cache.ts (09-01):
```typescript
bustRankingCache(): Promise<void>   // redis.del(SHEET_TRIPS_CACHE_KEY)
```
From api/src/lib/rbac.ts (EXISTS — the gate):
```typescript
export function requireRole(...roles: AuthUser['role'][]): Elysia   // .use(authGuard) + 403 if role mismatch
// authGuard injects user = { id, role, jti }  — NO name/email on the token
```
From api/src/db/schema/users.ts + api/src/db/client.ts (Torre Drizzle — for operador):
```typescript
users.id (uuid), users.name (varchar), users.email (varchar)   // db.select({name,email}).from(users).where(eq(users.id, ...))
```
From api/src/modules/users/users.plugin.ts (THE proven POST-with-body-in-plugin pattern — mirror it):
```
const writePlugin = new Elysia(...).use(requireRole('admin')).group('/api/users', app => app.post('/', handler, { body: t.Object({...}) }))
```
From api/src/index.ts (inline write fallback precedent — geofences L204-232: `.post('/api/geofences', async ({body,set}) => {...})` placed AFTER plugins; wire point for plugins is `.use(rankingPlugin).use(wsPlugin)` — wsPlugin MUST stay last).
</interfaces>

<ride_rank_parity>
<!-- The EXACT logic to replicate. From DataContext.tsx evaluateTrip (L220-285) + unblockDriverFn (L287-324). -->
evaluateTrip(tripId, driverId, driverName, {comunicacao, atendeu, desvio_rota, postura, ajuste_manual, observacao, operador}):
  1. existing = find evaluation by trip_id
  2. record = {trip_id, driver_id, driver_name, comunicacao, atendeu, desvio_rota, postura, ajuste_manual, observacao||'', operador}
  3. upsertEvaluation(record)
  4. createEvaluationLog({trip_id, driver_id, driver_name, operador, acao: existing?'EDIÇÃO':'CRIAÇÃO', dados_antes: existing??null, dados_depois: record})
  5. if (!atendeu): insertDriverBlock({driver_id, driver_name, tipo:'NO_SHOW', motivo:`No-Show na viagem ${tripId}`, ativo:true, manual_override:false, data_inicio: now ISO, data_fim:null, created_by: operador})
                   + createEvaluationLog({driver_id, driver_name, operador, acao:'BLOQUEIO_NO_SHOW', dados_antes:null, dados_depois:{status:'BLOQUEADO', motivo:'NO_SHOW', trip_id: tripId}})
unblock(driverId, driverName, operador):
  1. unblockDriverBlocks(driverId)   (update existing active blocks: ativo=false, manual_override=true, data_fim=now)
  2. insertDriverBlock({driver_id, driver_name, tipo:'MANUAL', motivo:'Desbloqueio manual pelo operador', ativo:false, manual_override:true, data_inicio: now, data_fim: now, created_by: operador})  (override record so score-blocks are overridden)
  3. createEvaluationLog({driver_id, driver_name, operador, acao:'DESBLOQUEIO', dados_antes:{status:'BLOQUEADO'}, dados_depois:{status:'ATIVO'}})
manual block (ride-rank EvaluationForm "Bloquear" path → blockDriver MANUAL):
  insertDriverBlock({driver_id, driver_name, tipo:'MANUAL', motivo:<from body>, ativo:true, manual_override:false, data_inicio: now, data_fim:null, created_by: operador})
  + createEvaluationLog({driver_id, driver_name, operador, acao:'BLOQUEIO_MANUAL', dados_antes:null, dados_depois:{status:'BLOQUEADO', motivo:<motivo>, tipo:'MANUAL'}})
</ride_rank_parity>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ranking.write.service.ts — operador resolution + evaluateTrip (with auto-block) + block/unblock orchestrators</name>
  <files>api/src/modules/ranking/ranking.write.service.ts</files>
  <read_first>
    - C:\Users\antonio.magalhaes\Documents\Projetos\produção\ride-rank-buddy\src\contexts\DataContext.tsx lines 220-324 (evaluateTrip + unblockDriverFn — THE flows to replicate; also in <ride_rank_parity> above)
    - api/src/modules/ranking/ranking.service.ts lines 224-276 (the existing async-orchestrator style to match — thin functions over the data layer)
    - api/src/modules/ranking/ranking.writes.ts + ranking.audit.ts + ranking.cache.ts (09-01 — the functions this service composes; signatures in <interfaces>)
    - api/src/db/client.ts + api/src/db/schema/users.ts (Torre Drizzle db + users table — for resolveOperador: db.select name/email by id)
    - api/src/modules/auth/auth.plugin.ts lines 75-79 (confirms /me returns only {id, role} — so the SERVER must resolve operador name from the users table, the token has no name)
  </read_first>
  <action>
    Create `ranking.write.service.ts` — pure async orchestrators (no Elysia), composing 09-01's functions + Torre Drizzle for operador. Replicate the ride-rank flows in <ride_rank_parity> EXACTLY.

    `resolveOperador(userId: string): Promise<string>` — `db.select({ name: users.name, email: users.email }).from(users).where(eq(users.id, userId)).limit(1)`; return `row?.name || row?.email || userId` (D-09-05 "nome se houver, senão email/id"). Import `db` from `../../db/client`, `users` from `../../db/schema/users`, `eq` from `drizzle-orm`. This is why operador is server-resolved: the JWT carries no name (auth.plugin /me = {id,role}).

    `evaluateTrip(input: { trip_id, driver_id, driver_name, comunicacao, atendeu, desvio_rota, postura, ajuste_manual, observacao }, userId: string): Promise<{ blocked: boolean }>`:
      - operador = await resolveOperador(userId).
      - record = { trip_id, driver_id, driver_name, comunicacao, atendeu, desvio_rota, postura, ajuste_manual, observacao: observacao ?? '', operador }.
      - const { existed, before } = await upsertEvaluation(record).
      - await createEvaluationLog({ trip_id, driver_id, driver_name, operador, acao: existed ? 'EDIÇÃO' : 'CRIAÇÃO', dados_antes: before, dados_depois: record as Record<string,unknown> }). USE THE EXACT em-dash strings 'EDIÇÃO'/'CRIAÇÃO' (the LogsTab ActionBadge matches both accented + ASCII, but the audit canon is accented — match ride-rank byte-for-byte).
      - if (!atendeu): await insertDriverBlock({ driver_id, driver_name, tipo:'NO_SHOW', motivo:`No-Show na viagem ${trip_id}`, ativo:true, manual_override:false, data_inicio:new Date().toISOString(), data_fim:null, created_by:operador }); then await createEvaluationLog({ driver_id, driver_name, operador, acao:'BLOQUEIO_NO_SHOW', dados_antes:null, dados_depois:{ status:'BLOQUEADO', motivo:'NO_SHOW', trip_id } }). Set blocked=true. (D-09-02 — server-side, inside evaluate, no threshold.)
      - await bustRankingCache() (D-09-04/09).
      - return { blocked }.
      AUDIT ATOMICITY (T4 — record the choice): the evaluation log (step 4) is awaited and its failure PROPAGATES (the whole request fails if the primary audit can't be written). The auto-block + its log run after; if the block fails, propagate too (the ride-rank swallowed block errors with console.error, but server-side we PROPAGATE so the operator sees the failure rather than a silent missing block — document this as the deliberate divergence from ride-rank's try/catch). Note: these are sequential Supabase calls (no real transaction across the REST API); document that "atomic" here means "same handler, fail-fast on first error, audit awaited before returning success".

    `blockDriverManual(input: { driver_id, driver_name, motivo }, userId: string): Promise<void>`:
      - operador = resolveOperador(userId).
      - insertDriverBlock({ driver_id, driver_name, tipo:'MANUAL', motivo: input.motivo, ativo:true, manual_override:false, data_inicio:now, data_fim:null, created_by:operador }).
      - createEvaluationLog({ driver_id, driver_name, operador, acao:'BLOQUEIO_MANUAL', dados_antes:null, dados_depois:{ status:'BLOQUEADO', motivo: input.motivo, tipo:'MANUAL' } }).
      - bustRankingCache().

    `unblockDriver(input: { driver_id, driver_name }, userId: string): Promise<void>`:
      - operador = resolveOperador(userId).
      - unblockDriverBlocks(input.driver_id)  (ativo=false, data_fim=now, manual_override=true on active rows).
      - insertDriverBlock({ driver_id, driver_name, tipo:'MANUAL', motivo:'Desbloqueio manual pelo operador', ativo:false, manual_override:true, data_inicio:now, data_fim:now, created_by:operador })  (the override record, ride-rank L294-304).
      - createEvaluationLog({ driver_id, driver_name, operador, acao:'DESBLOQUEIO', dados_antes:{status:'BLOQUEADO'}, dados_depois:{status:'ATIVO'} }).
      - bustRankingCache().

    Header doc: server-side replication of ride-rank DataContext write flows; operador resolved from Torre users (token has no name); auditoria via the single 09-01 helper; cache busted after each write (D-09-04). SECURITY: never log driver PII / evaluation contents.
  </action>
  <acceptance_criteria>
    - `bun --bun tsc --noEmit` (from api/) exits 0.
    - `grep -n "resolveOperador" api/src/modules/ranking/ranking.write.service.ts` shows it selects name/email from users by id and falls back to id.
    - `grep -c "createEvaluationLog" api/src/modules/ranking/ranking.write.service.ts` returns >= 4 (CRIAÇÃO/EDIÇÃO, BLOQUEIO_NO_SHOW, BLOQUEIO_MANUAL, DESBLOQUEIO).
    - `grep -n "BLOQUEIO_NO_SHOW" api/src/modules/ranking/ranking.write.service.ts` AND `grep -n "No-Show na viagem" ...` both return lines inside evaluateTrip's `!atendeu` branch (D-09-02 replica).
    - `grep -c "bustRankingCache" api/src/modules/ranking/ranking.write.service.ts` returns >= 3 (evaluate, manual block, unblock).
    - `grep -n "manual_override: true" api/src/modules/ranking/ranking.write.service.ts` returns a line in unblockDriver's override insert.
    - CRIAÇÃO/EDIÇÃO logic verified WITHOUT the live secret (autonomous structural assertion): `grep -n "existed" api/src/modules/ranking/ranking.write.service.ts` confirms evaluateTrip destructures the `existed` flag from `upsertEvaluation` (`grep -n "{ existed, before }" ...` or `grep -n "existed, before" ...` confirms the destructure); `grep -nE "existed \\?" ...` confirms `existed` drives the acao ternary.
    - The exact em-dash audit literals exist (NOT the ASCII variants): `grep -n "'CRIAÇÃO'" api/src/modules/ranking/ranking.write.service.ts` AND `grep -n "'EDIÇÃO'" ...` both return a line; `grep -c "CRIACAO\|EDICAO" ...` returns 0 (no ASCII fallbacks introduced — byte-for-byte parity with ride-rank).
    - dados_antes snapshot wired WITHOUT the secret: `grep -n "before" api/src/modules/ranking/ranking.write.service.ts` shows `before` (the pre-image returned by upsertEvaluation) is threaded into the CRIAÇÃO/EDIÇÃO log as `dados_antes` — i.e. `grep -nE "dados_antes: ?before" ...` (or the multi-line equivalent) confirms the snapshot path exists in code, proving the EDIÇÃO before/after diff is captured even when the live DB cannot be reached this run.
    - `bun test src/modules/ranking/` (dummy envs) → 25 pass (no regression; the service is imported but pure-import-safe — no top-level db/redis/supabase access).
  </acceptance_criteria>
  <done>ranking.write.service.ts exports resolveOperador + evaluateTrip (upsert+log+conditional NO_SHOW auto-block+bust), blockDriverManual (MANUAL+log+bust), unblockDriver (unblock+override+log+bust) — all replicating ride-rank, operador server-resolved, audit awaited fail-fast; the existed flag drives CRIAÇÃO/EDIÇÃO and before→dados_antes is wired (verified structurally without the secret); tsc exit 0; 25 tests green.</done>
</task>

<task type="auto">
  <name>Task 2: ranking.write.plugin.ts — 3 write endpoints behind requireRole + typebox validation; wire in index.ts (inline fallback if body bug)</name>
  <files>api/src/modules/ranking/ranking.write.plugin.ts, api/src/index.ts, api/.env.example</files>
  <read_first>
    - api/src/modules/users/users.plugin.ts (THE proven pattern — `.use(requireRole(...)).group('/api/...', app => app.post('/', handler, { body: t.Object({...}) }))`; copy the structure incl. how `user` is read in the handler and how body typebox + params t.Object({id: t.String()}) are declared)
    - api/src/index.ts lines 155-172 (plugin wiring order — write plugin goes with the other plugins, BEFORE `.use(wsPlugin)`) AND lines 204-236 (the geofences INLINE write fallback shape — only if the plugin body parse breaks)
    - api/src/lib/rbac.ts (requireRole — how it chains authGuard + injects user; the handler reads `user.id` for operador and `user.role` is already enforced)
    - api/.env.example lines 40-49 (the RANK_* block + the existing comment that already anticipates Phase 9 — update it to confirm the anon key writes because RLS is open per D-09-03)
  </read_first>
  <action>
    Create `ranking.write.plugin.ts` mirroring `users.plugin.ts` adminPlugin: `export const rankingWritePlugin = new Elysia({ name: 'ranking-write' }).use(requireRole('admin','supervisor')).group('/api/ranking', app => app...)`. requireRole at PLUGIN level applies to all 3 routes (D-09-01: admin|supervisor only; analyst/viewer → 403). Define typebox enums once (T5, D-09-06):
      - `comunicacaoSchema = t.Union([t.Literal('BOA'), t.Literal('REGULAR'), t.Literal('RUIM')])`
      - `desvioSchema = t.Union([t.Literal('NENHUM'), t.Literal('LEVE'), t.Literal('GRAVE')])`
      - `posturaSchema = t.Union([t.Literal('OK'), t.Literal('RUIM')])`

    Routes (all read `{ body, user, set }`):
      1. `POST /evaluations`: body `t.Object({ trip_id: t.String({minLength:1}), driver_id: t.String({minLength:1}), driver_name: t.String(), comunicacao: comunicacaoSchema, atendeu: t.Boolean(), desvio_rota: desvioSchema, postura: posturaSchema, ajuste_manual: t.Integer({ minimum:-20, maximum:20 }), observacao: t.Optional(t.String({ maxLength: 1000 })) })`. Handler: `const r = await evaluateTrip(body, user.id); return { ok:true, blocked: r.blocked }`. (ajuste_manual clamp via t.Integer min/max — out-of-range → 422 by the global onError VALIDATION branch; this IS the clamp per D-09-06/T5.) detail tags ['ranking'].
      2. `POST /blocks`: body `t.Object({ driver_id: t.String({minLength:1}), driver_name: t.String(), motivo: t.String({minLength:1, maxLength:500}) })`. Handler: `await blockDriverManual(body, user.id); return { ok:true }`.
      3. `PATCH /blocks/:id`: params `t.Object({ id: t.String() })`, body `t.Object({ driver_id: t.String({minLength:1}), driver_name: t.String() })`. Handler: `await unblockDriver(body, user.id); return { ok:true }`. (The :id is the block row id for REST shape; the unblock operates by driver_id per ride-rank — document that :id identifies the displayed block but the DB update keys on driver_id+ativo, matching the ride-rank unblockDriver.)
    Import evaluateTrip/blockDriverManual/unblockDriver from `./ranking.write.service`, requireRole from `../../lib/rbac`, Elysia+t from 'elysia'.

    Wire in `index.ts`: import `rankingWritePlugin` and add `.use(rankingWritePlugin)` immediately AFTER `.use(rankingPlugin)` and BEFORE `.use(wsPlugin)` (wsPlugin MUST remain last — Elysia 1.4 rule, see the existing comment at L169-171). Add the `ranking` tag already exists in swagger — no new tag needed.

    ELYSIA 1.4.28 BODY BUG (D-09-08) — decision protocol: FIRST attempt the plugin above (users.plugin proves POST-with-body works in a plugin when chained via requireRole + group). RUN the live acceptance test. IF the body arrives `undefined`/parse fails at runtime (the geofences/telemetry symptom), THEN fall back: move the 3 routes INLINE into index.ts AFTER the plugin chain (mirror geofences L204-236 exactly — `.post('/api/ranking/evaluations', async ({ body, set }) => {...})`), keeping the SAME typebox via inline `set.status` role checks IS NOT acceptable — instead, for the inline fallback, gate each inline route by reading the auth cookie is wrong; INSTEAD keep requireRole by registering an inline sub-Elysia `.use(requireRole('admin','supervisor'))` block before the routes, OR (simpler, matching geofences which are unauthenticated) — NO: writes MUST keep requireRole. So the inline fallback is: a dedicated `const rankingWriteInline = new Elysia().use(requireRole('admin','supervisor')).post('/api/ranking/evaluations', ...).post('/api/ranking/blocks',...).patch('/api/ranking/blocks/:id',...)` mounted via `.use()` AFTER wsPlugin? No — wsPlugin last. Mount it BEFORE wsPlugin too. The fallback differs from the plugin ONLY by not using `.group()` (the group prefix is the suspected body-parse trigger in 1.4.28). Document whichever path was taken in the SUMMARY. THE SECURITY INVARIANT IS NON-NEGOTIABLE: every write route resolves through `requireRole('admin','supervisor')` in BOTH paths (T1).

    Update `.env.example` RANK_SUPABASE_SERVICE_KEY comment: confirm per D-09-03 the anon key WRITES (RLS open on all 5 tables, verified via MCP); server-side only; the prior "trocar por service_role se o RLS de escrita bloquear" line can stay as a note but add "Phase 9: writes confirmed working with the anon key — RLS is open (D-09-03)".
  </action>
  <acceptance_criteria>
    - `bun --bun tsc --noEmit` (from api/) exits 0.
    - `grep -nE "requireRole\('admin', ?'supervisor'\)" api/src/modules/ranking/ranking.write.plugin.ts` returns a line at plugin level (T1). `grep -n "rankingWrite" api/src/index.ts` shows the import + `.use(...)` (or the inline fallback block) wired before wsPlugin.
    - `grep -n "t.Integer" api/src/modules/ranking/ranking.write.plugin.ts` shows `minimum: -20, maximum: 20` on ajuste_manual (clamp, D-09-06/T5); `grep -cE "t.Union\(\[" ...` returns >= 3 (the 3 enums).
    - LIVE — RBAC + DB effect + audit (REQUIRES a real cookie; if RANK_SUPABASE_SERVICE_KEY unset, see fallback note below). Boot api (kill bun procs first), authenticate as an admin to capture the access_token cookie, then:
        a. `POST /api/ranking/evaluations` with a valid body + admin cookie → HTTP 200 `{ok:true}`; verify a row exists in `evaluations` for that trip_id and a row in `evaluation_logs` with acao CRIAÇÃO (or EDIÇÃO on a 2nd call) — confirm via `GET /api/ranking/logs` (09-02) returning the new log at the top.
        b. Same POST with an analyst cookie → HTTP 403. With NO cookie → HTTP 401.
        c. `POST /api/ranking/evaluations` with `atendeu:false` + admin cookie → 200 `{ok:true, blocked:true}`; verify a `driver_blocks` row tipo=NO_SHOW (via `GET /api/ranking/blocks`) AND a BLOQUEIO_NO_SHOW log (via `GET /api/ranking/logs`).
        d. `POST /api/ranking/blocks` admin → 200; block visible in `GET /api/ranking/blocks` + BLOQUEIO_MANUAL log. `PATCH /api/ranking/blocks/:id` admin → 200; the driver's active block now ativo=false (drops out of `GET /api/ranking/blocks`) + DESBLOQUEIO log.
        e. `POST /api/ranking/evaluations` with `ajuste_manual: 999` → HTTP 422 (typebox clamp/validation, T5); with `comunicacao:'XYZ'` → 422.
      [If RANK_SUPABASE_SERVICE_KEY is NOT available in the run, the user-setup gate applies: the 401/403/422 checks (b, e) DO NOT need the key (requireRole + typebox fire before any Supabase call) and MUST pass; the DB-effect checks (a,c,d) become a checkpoint:human-verify in Task 3 against the key, exactly like Phase 7's parity checkpoint. Run and record b+e regardless.]
    - The 6 read endpoints (5 Phase 7 + /logs) still return 401 without cookie (no regression).
  </acceptance_criteria>
  <done>3 write endpoints live under requireRole('admin','supervisor') with typebox enum+clamp bodies; wired in index.ts before wsPlugin (plugin or documented inline fallback); LIVE 401/403/422 confirmed without the secret; DB-effect + audit confirmed with the secret (or deferred to the Task 3 checkpoint); reads unaffected; tsc exit 0.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: human-verify — live write + DB effect + audit against the real ride-rank DB</name>
  <what-built>
    POST /api/ranking/evaluations (+ auto-block NO_SHOW), POST /api/ranking/blocks, PATCH /api/ranking/blocks/:id — all behind requireRole('admin','supervisor'), each writing an evaluation_logs audit row and busting the ranking cache. This checkpoint confirms the DB-effect path that requires the user-setup secret `RANK_SUPABASE_SERVICE_KEY` (the anon key of Lamonica Ranking, qbwazymqhfunlhnikbla) — the same secret class as the Phase 7 Task-4 checkpoint. The autonomous 401/403/422 checks already passed in Task 2 without it.
  </what-built>
  <how-to-verify>
    1. Set in api/.env (dev) and the VPS .env: `RANK_SUPABASE_URL=https://qbwazymqhfunlhnikbla.supabase.co`, `RANK_SUPABASE_SERVICE_KEY=<anon key of Lamonica Ranking — Dashboard → Settings → API → anon/public; SERVER-SIDE ONLY, never VITE_/logs>`, plus the existing RANK_SHEET_ID/RANK_SHEET_TAB.
    2. Kill all bun procs (`taskkill //F //IM bun.exe`), then `cd api && bun run dev`. Log into the Torre as an admin; capture the access_token cookie.
    3. POST /api/ranking/evaluations with a real FECHADA trip_id (pick one from `GET /api/ranking/trips`), driver_id/driver_name from that trip, comunicacao 'BOA', atendeu true, desvio_rota 'NENHUM', postura 'OK', ajuste_manual 0, observacao 'teste fase 9' → expect 200 {ok:true}. Then `GET /api/ranking/logs` → the newest row is acao CRIAÇÃO with operador = your Torre name and dados_depois carrying the evaluation. Repeat the SAME trip_id → 200, newest log acao EDIÇÃO with dados_antes = the prior record.
    4. POST the same trip with atendeu:false → 200 {ok:true, blocked:true}. `GET /api/ranking/blocks` → a tipo NO_SHOW block for that driver; `GET /api/ranking/logs` → a BLOQUEIO_NO_SHOW row. `GET /api/ranking/drivers` → that driver now status BLOQUEADO, rank null.
    5. POST /api/ranking/blocks (driver_id, driver_name, motivo 'teste manual') → 200; block + BLOQUEIO_MANUAL log appear. PATCH /api/ranking/blocks/<that block id> with {driver_id, driver_name} → 200; the driver's active block drops from `GET /api/ranking/blocks`, a DESBLOQUEIO log appears, and `GET /api/ranking/drivers` shows the driver ATIVO again.
    6. Confirm operador on every new log = your Torre user's name (not id), and that re-reading after each write reflects the change immediately (cache busted).
  </how-to-verify>
  <resume-signal>Type "aprovado" if all writes land in the DB, the 4 audit acoes (CRIAÇÃO/EDIÇÃO, BLOQUEIO_NO_SHOW, BLOQUEIO_MANUAL, DESBLOQUEIO) appear with the correct operador, and status flips ATIVO↔BLOQUEADO as expected — or report the exact divergence (endpoint, expected vs returned, missing log/row).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → POST/PATCH /api/ranking/{evaluations,blocks} → ride-rank Supabase | Untrusted client input crosses into a DB with OPEN RLS. The ONLY access control is requireRole at this plugin; the ONLY input validation is typebox here. |
| ranking.write.service → Torre users table | Server reads the authenticated user's name/email to stamp operador (no client-supplied operador is trusted). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | Elevation | write endpoints | mitigate | `requireRole('admin','supervisor')` at plugin (and inline-fallback) level → 403 for analyst/viewer, 401 for no cookie. authGuard validates JWT + Redis blacklist. Verified live (b). (T1) |
| T-09-05 | Tampering | enum/range injection in evaluation body | mitigate | typebox `t.Union` literals for comunicacao/desvio_rota/postura; `t.Integer({minimum:-20,maximum:20})` clamps ajuste_manual; `maxLength` on observacao/motivo → 422 via global onError. Verified live (e). (T5) |
| T-09-04 | Repudiation | a mutation without an audit trail | mitigate | Every write calls the single createEvaluationLog (09-01) in the same handler; the primary log is awaited and its failure propagates (request fails rather than silently mutating without audit). operador is server-resolved (not spoofable from body). (T4) |
| T-09-12 | Spoofing | client forging operador | mitigate | operador is NOT read from the body — resolveOperador derives it from `user.id` (JWT) against the Torre users table. The body has no operador field. |
| T-09-02 | Info-disclosure | anon key / PII leak | mitigate | Phase 7 posture preserved: key only from env via lazy rankSupabase, never logged/VITE_; handlers never log rows; global onError masks 500s. (T2) |
| T-09-13 | DoS | unbounded observacao/motivo | mitigate | maxLength bounds on string bodies. |
| T-09-14 | — (RLS open) | direct DB write bypassing Torre | accept | D-09-03 documented risk: anyone with the anon key writes directly. Mitigation = key stays server-side (never VITE_/bundle); Torre requireRole is the app-level control. Not closed (ride-rank depends on open RLS). |
</threat_model>

<verification>
- `cd api && bun --bun tsc --noEmit` → exit 0.
- LIVE without secret: POST evaluations → 401 (no cookie) / 403 (analyst) / 422 (bad enum or ajuste_manual out of [-20,20]). MUST pass autonomously.
- LIVE with secret (Task 3 checkpoint): 200 + evaluations row + correct audit acao; atendeu=false → NO_SHOW block + BLOQUEIO_NO_SHOW log; manual block/unblock → rows + BLOQUEIO_MANUAL/DESBLOQUEIO logs; status flips; reads reflect immediately.
- 6 read endpoints unaffected; wsPlugin remains last.
</verification>

<success_criteria>
The driver-lifecycle write surface is live and non-bypassable: evaluate (with replicated NO_SHOW auto-block), manual block, and unblock — all admin|supervisor-only, enum/clamp-validated, each writing the correct audit acao with a server-resolved operador and busting the ranking cache. Mirrors ride-rank DataContext exactly. RBAC + validation proven without the secret; DB effect proven with it.
</success_criteria>

<output>
After completion, create `.planning/phases/09-ranking-escrita-auditoria-write-flows/09-03-SUMMARY.md` — record which body-bug path was taken (plugin vs inline) and the checkpoint status.
</output>
