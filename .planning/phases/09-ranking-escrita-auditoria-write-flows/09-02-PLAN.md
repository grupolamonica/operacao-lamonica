---
phase: 09-ranking-escrita-auditoria-write-flows
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - api/src/modules/ranking/ranking.plugin.ts
  - api/src/modules/ranking/ranking.service.ts
autonomous: true
requirements: [D-09-01, D-09-07]
must_haves:
  truths:
    - "GET /api/ranking/logs returns evaluation_logs ordered by created_at desc (the LogsTab data source)"
    - "GET /api/ranking/logs is reachable by ANY authenticated role (authGuard only), not gated by requireRole"
    - "An unauthenticated request to GET /api/ranking/logs returns 401"
  artifacts:
    - path: "api/src/modules/ranking/ranking.plugin.ts"
      provides: "6th GET endpoint /logs on the existing read plugin"
      contains: "/logs"
    - path: "api/src/modules/ranking/ranking.service.ts"
      provides: "getRankingLogs thin orchestrator wrapping fetchEvaluationLogs"
      contains: "getRankingLogs"
  key_links:
    - from: "GET /api/ranking/logs"
      to: "fetchEvaluationLogs (ranking.reads.ts — already exists)"
      via: "getRankingLogs orchestrator returning EvaluationLogRecord[]"
      pattern: "getRankingLogs"
---

<objective>
Add the read endpoint `GET /api/ranking/logs` to the EXISTING `ranking.plugin.ts` (the Phase 7 read plugin, behind `authGuard`). It returns `evaluation_logs` ordered desc — the data source the Phase 8 LogsTab shell is waiting for (D-09-07 `GET /logs` under authGuard; D-09-01 any role reads). `fetchEvaluationLogs` ALREADY exists in `ranking.reads.ts` (Phase 7) — this plan just exposes it via a thin orchestrator.

Purpose: Complete the audit READ path so the LogsTab (wired in 09-07) shows real antes/depois diffs. This is a pure read — no body, so it carries ZERO Elysia 1.4.28 body-bug risk and stays on the existing read plugin (correct gate: authGuard, not requireRole).
Output: `ranking.plugin.ts` gains a 6th GET + `ranking.service.ts` gains `getRankingLogs`. `/swagger/json` lists `/api/ranking/logs`; smoke 401 without cookie. `bun --bun tsc --noEmit` exit 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-ranking-escrita-auditoria-write-flows/09-CONTEXT.md

<interfaces>
<!-- Already-shipped contracts — use directly. -->

From api/src/modules/ranking/ranking.reads.ts (Phase 7 — ALREADY EXISTS, do not recreate):
```typescript
export async function fetchEvaluationLogs(): Promise<EvaluationLogRecord[]>
// already: .from('evaluation_logs').select('*').order('created_at', {ascending:false}).limit(200)
```

From api/src/modules/ranking/ranking.types.ts:
```typescript
export interface EvaluationLogRecord {
  id?: string; trip_id?: string; driver_id?: string; driver_name?: string;
  operador: string; acao: string;
  dados_antes?: Record<string, unknown> | null;
  dados_depois?: Record<string, unknown> | null;
  created_at?: string;
}
```

From api/src/modules/ranking/ranking.plugin.ts (the plugin to extend — pattern: `.use(authGuard).group('/api/ranking', app => app.get(...))`, each `.get` has `detail: { tags: ['ranking'], summary }`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: add GET /api/ranking/logs to the read plugin + a thin service orchestrator</name>
  <files>api/src/modules/ranking/ranking.plugin.ts, api/src/modules/ranking/ranking.service.ts</files>
  <read_first>
    - api/src/modules/ranking/ranking.plugin.ts (the FULL plugin — copy the exact `.get('/route-scores', () => getRankingRouteScores(), { detail: {...} })` shape for the new /logs route; it is the closest analog: a no-arg read returning an array)
    - api/src/modules/ranking/ranking.service.ts lines 261-276 (the thin orchestrators getRankingBlocks/getRankingRouteScores/getRankingStats — add getRankingLogs in the same one-liner style that just awaits a read fn)
    - api/src/modules/ranking/ranking.reads.ts lines 55-62 (fetchEvaluationLogs — the read it wraps; ALREADY desc-ordered + limit 200, so no logic to add)
  </read_first>
  <action>
    In `ranking.service.ts`, add `export async function getRankingLogs(): Promise<EvaluationLogRecord[]> { return fetchEvaluationLogs() }` next to the other thin orchestrators (after getRankingStats). Add `fetchEvaluationLogs` to the existing import from `./ranking.reads` and `EvaluationLogRecord` to the existing type import from `./ranking.types`. Keep it a one-liner passthrough — `fetchEvaluationLogs` already orders desc + limits 200 (D-09-07 "ordenado desc").

    In `ranking.plugin.ts`, add a 6th `.get('/logs', () => getRankingLogs(), { detail: { tags: ['ranking'], summary: 'Log de auditoria (evaluation_logs), ordenado desc' } })` inside the SAME `.group('/api/ranking', ...)` chain, after `/stats`. Import `getRankingLogs` from `./ranking.service`. The endpoint inherits the plugin-level `authGuard` (D-09-01: any authenticated role reads — do NOT wrap in requireRole). No body, no query schema. Update the plugin header doc to list /logs as the 6th endpoint and note it is read-only auditoria added in Phase 9 but lives on the READ plugin because it is a read under authGuard (writes are the separate write plugin, 09-03).
  </action>
  <acceptance_criteria>
    - `bun --bun tsc --noEmit` (from api/) exits 0.
    - `grep -n "'/logs'" api/src/modules/ranking/ranking.plugin.ts` returns a line; `grep -n "getRankingLogs" api/src/modules/ranking/ranking.service.ts` returns the new orchestrator.
    - LIVE smoke (no cookie): start API with dummy envs (kill all bun procs first per STATE known-issue), then `curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/api/ranking/logs` returns `401` (authGuard blocks, T-09-09). [Boot: `cd api && REDIS_URL="redis://127.0.0.1:6399" RANK_SUPABASE_URL="https://dummy.supabase.co" RANK_SUPABASE_SERVICE_KEY="dummy" bun run dev` in background; curl; then taskkill //F //IM bun.exe.]
    - LIVE swagger: `curl -s http://localhost:3000/swagger/json` contains `/api/ranking/logs` (endpoint registered).
    - The 5 existing GET still return 401 without cookie (no regression to the read contract).
  </acceptance_criteria>
  <done>GET /api/ranking/logs registered on the read plugin under authGuard; getRankingLogs orchestrator wraps fetchEvaluationLogs (desc, limit 200); tsc exit 0; live 401 without cookie + swagger lists the path; 5 prior endpoints unaffected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → GET /api/ranking/logs → ride-rank Supabase | An authenticated Torre request reads audit rows; authGuard is the boundary (valid JWT + Redis blacklist) before any Supabase query. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-09 | Spoofing | unauthenticated read of audit log | mitigate | Endpoint inherits plugin-level `authGuard`; no cookie → 401 before query. Verified by the live 401 acceptance check. (T1) |
| T-09-10 | Info-disclosure | audit rows / service errors leaking PII | mitigate | Reuse Phase 7: errors propagate to global onError which masks 500s and never leaks the key/rows; the handler never logs the returned rows. (T2) |
| T-09-11 | Elevation | logs gated too tightly (requireRole) | accept | D-09-01 deliberately puts /logs under authGuard ONLY (any role audits). Not a security gap — read of non-mutating audit data; matches decision. |
</threat_model>

<verification>
- `cd api && bun --bun tsc --noEmit` → exit 0.
- Live boot with dummy envs → `GET /api/ranking/logs` 401 without cookie; `/swagger/json` lists `/api/ranking/logs`.
- Only `ranking.plugin.ts` + `ranking.service.ts` changed; no write code, no UI.
</verification>

<success_criteria>
The audit read path is live: `GET /api/ranking/logs` exists under authGuard, returns evaluation_logs desc, is in swagger, and 401s without a session. The Phase 8 LogsTab now has a real endpoint to consume (wired in 09-07 via the hook from 09-05).
</success_criteria>

<output>
After completion, create `.planning/phases/09-ranking-escrita-auditoria-write-flows/09-02-SUMMARY.md`
</output>
