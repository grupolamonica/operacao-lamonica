---
phase: 09-ranking-escrita-auditoria-write-flows
plan: 05
type: execute
wave: 4
depends_on: [02, 03, 04]
files_modified:
  - torre-de-controle/src/hooks/useRanking.ts
autonomous: true
requirements: [D-09-09, D-09-10]
must_haves:
  truths:
    - "useRankingLogs() fetches GET /api/ranking/logs and exposes EvaluationLogRecord[] for the LogsTab"
    - "useEvaluateTrip / useBlockDriver / useUnblockDriver / route-score create/update/delete mutation hooks call the Phase 9 write endpoints via Eden Treaty and invalidate the relevant ['ranking', ...] query keys (D-09-09)"
    - "useCanWriteRanking() returns true only for admin|supervisor (from useAuthStore role), driving the UI gate (D-09-10)"
  artifacts:
    - path: "torre-de-controle/src/hooks/useRanking.ts"
      provides: "5 read hooks (existing) + useRankingLogs + 6 mutation hooks + useCanWriteRanking + re-exported EvaluationLogRecord type"
      contains: "useEvaluateTrip"
  key_links:
    - from: "mutation hooks onSuccess"
      to: "queryClient.invalidateQueries(['ranking', ...])"
      via: "useQueryClient invalidation per action"
      pattern: "invalidateQueries"
    - from: "useCanWriteRanking"
      to: "useAuthStore user.role"
      via: "role === 'admin' || role === 'supervisor'"
      pattern: "useAuthStore"
    - from: "useRankingLogs"
      to: "GET /api/ranking/logs (09-02)"
      via: "Eden Treaty api.api.ranking.logs.get()"
      pattern: "ranking.*logs"
---

<objective>
Add the front-end WRITE layer to `useRanking.ts`: the `useRankingLogs` read hook (consumes 09-02's `GET /logs`), the mutation hooks (`useEvaluateTrip`, `useBlockDriver`, `useUnblockDriver`, `useCreateRouteScore`, `useUpdateRouteScore`, `useDeleteRouteScore`) calling the Phase 9 write endpoints via Eden Treaty with `['ranking', ...]` invalidation (D-09-09), and `useCanWriteRanking()` (role gate from `useAuthStore`, D-09-10). All in the single hooks file so the existing 5 read hooks + the new ones share one query namespace. NO component edits here (those are 09-06/07) — this is the hook contract the UI wiring builds on.

Purpose: Centralize every ranking mutation + the audit read + the role check in one place, mirroring the read-hook house style (Eden Treaty + TanStack, `data/isLoading/isError/error/refetch` shape). Invalidation keeps the derived ranking fresh after writes (the backend already busted Redis; the front must refetch).
Output: `useRanking.ts` extended (existing exports untouched). `bun run build` (torre-de-controle) exit 0; `tsc` clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-ranking-escrita-auditoria-write-flows/09-CONTEXT.md

<interfaces>
<!-- Existing front-end contracts + the Phase 9 backend endpoints (now in the App type via 02/03/04). -->

From torre-de-controle/src/hooks/useRanking.ts (EXISTING — extend, don't rewrite):
```typescript
// 5 read hooks return: { data, isLoading, isError, error, refetch }
export function useRankingDrivers() / useRankingTrips(filters?) / useRankingBlocks() / useRankingRouteScores() / useRankingStats()
// already re-exports RankedDriver, RankingStats, Driver, Trip, DriverBlockRecord, RouteScoreRecord, StatusMetrics
// import { api } from '@/lib/api'  — Eden Treaty client (credentials:'include')
```
From torre-de-controle/src/stores/useAuthStore.ts:
```typescript
export const useAuthStore = create(...)   // state.user = { id, name, email, role } | null
```
From api/src/modules/ranking/ranking.types.ts (re-export the log type for LogsTab):
```typescript
export interface EvaluationLogRecord { id?; trip_id?; driver_id?; driver_name?; operador; acao; dados_antes?; dados_depois?; created_at? }
```
Eden Treaty access patterns (from the existing file — hyphenated segments need bracket-notation `as any`):
```typescript
api.api.ranking.drivers.get()
await (api.api.ranking as any)['route-scores'].get()           // hyphen → bracket + as any
// Phase 9 writes (now typed via App):
api.api.ranking.evaluations.post({ ...body })
api.api.ranking.blocks.post({ ...body })
await (api.api.ranking.blocks as any)({ id }).patch({ ...body })  // path param + hyphen-free
await (api.api.ranking as any)['route-scores'].post({ ...body })
await (api.api.ranking as any)['route-scores']({ id }).patch({ ...body })
await (api.api.ranking as any)['route-scores']({ id }).delete()
```
(The EXACT treaty call shape may vary by Eden version — match the established `useInsights`/`useRanking` `as any` casting where TS dot-access fails; the runtime path is `/api/ranking/...`. Verify against `@/lib/api` App type at build time.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: useRankingLogs + useCanWriteRanking + re-export EvaluationLogRecord</name>
  <files>torre-de-controle/src/hooks/useRanking.ts</files>
  <read_first>
    - torre-de-controle/src/hooks/useRanking.ts (the FULL file — match the read-hook shape exactly: useQuery, queryKey ['ranking', x], staleTime 30_000, return {data,isLoading,isError,error,refetch}; copy useRankingRouteScores for the hyphen/bracket pattern)
    - torre-de-controle/src/stores/useAuthStore.ts (user.role source for the gate)
    - torre-de-controle/src/app/pages/ranking/components/LogsTab.tsx lines 10, 145-148 (LogsTab imports EvaluationLogRecord by deep relative path and uses `const logs = []`; the re-export here lets 09-07 swap to `useRankingLogs` cleanly)
  </read_first>
  <action>
    Extend `useRanking.ts` (append; leave the 5 read hooks + existing re-exports intact):

    Add `EvaluationLogRecord` to the existing `export type { ... } from '../../../api/src/modules/ranking/ranking.types'` re-export block (so the LogsTab can import it from `@/hooks/useRanking` instead of the deep path — optional convenience; keep the deep-path type too if simpler).

    `useRankingLogs()` — copy `useRankingRouteScores` exactly but for `/logs`: `queryKey: ['ranking','logs']`, `queryFn` calls `api.api.ranking.logs.get()` (or `(api.api.ranking as any).logs.get()` if dot-access fails), returns `(data ?? []) as EvaluationLogRecord[]`, staleTime 30_000, return the standard `{data,isLoading,isError,error,refetch}` shape with `data ?? []` fallback.

    `useCanWriteRanking(): boolean` — `const role = useAuthStore(s => s.user?.role); return role === 'admin' || role === 'supervisor'` (D-09-10 gate — admin|supervisor write; analyst|viewer read-only). Import `useAuthStore` from `@/stores/useAuthStore`. Header comment: this is the single source for the UI write gate; the BACKEND requireRole is the real control (this only hides/disables controls — T-09-19 defense-in-depth note).
  </action>
  <acceptance_criteria>
    - `cd torre-de-controle && npx tsc --noEmit` (or the project's typecheck) exits 0.
    - `grep -n "useRankingLogs" torre-de-controle/src/hooks/useRanking.ts` returns the hook; its queryKey is `['ranking', 'logs']` (`grep -n "'ranking', 'logs'" ...`).
    - `grep -n "useCanWriteRanking" torre-de-controle/src/hooks/useRanking.ts` returns the hook; `grep -n "useAuthStore" ...` confirms it reads role; `grep -n "supervisor" ...` confirms admin|supervisor logic.
    - `grep -n "EvaluationLogRecord" torre-de-controle/src/hooks/useRanking.ts` confirms the type is available (re-exported or imported).
    - The 5 existing read hooks remain exported unchanged (`grep -c "export function useRanking" ...` >= 6 now: 5 reads + logs).
  </acceptance_criteria>
  <done>useRankingLogs (GET /logs, ['ranking','logs']) + useCanWriteRanking (admin|supervisor from useAuthStore) added; EvaluationLogRecord re-exported; existing read hooks intact; tsc exit 0.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 6 mutation hooks with ['ranking', ...] invalidation</name>
  <files>torre-de-controle/src/hooks/useRanking.ts</files>
  <behavior>
    - useEvaluateTrip().mutate(payload) → POST /api/ranking/evaluations → on success invalidates ['ranking','trips'], ['ranking','drivers'], ['ranking','stats'], ['ranking','blocks'] (atendeu=false may have created a block), ['ranking','logs'].
    - useBlockDriver().mutate(payload) → POST /api/ranking/blocks → invalidates ['ranking','blocks'], ['ranking','drivers'], ['ranking','stats'], ['ranking','logs'].
    - useUnblockDriver().mutate({id, driver_id, driver_name}) → PATCH /api/ranking/blocks/:id → invalidates ['ranking','blocks'], ['ranking','drivers'], ['ranking','stats'], ['ranking','logs'].
    - useCreateRouteScore / useUpdateRouteScore({id,...}) / useDeleteRouteScore(id) → route-scores endpoints → invalidate ['ranking','route-scores'], ['ranking','trips'], ['ranking','drivers'], ['ranking','stats'], ['ranking','logs'] (base points change derived scores).
    - Each hook surfaces { mutate, mutateAsync, isPending, isError, error } from useMutation; throws on non-2xx (reads Eden `error` like the read hooks do).
  </behavior>
  <read_first>
    - torre-de-controle/src/hooks/useRanking.ts (read-hook error handling: `if (error) throw new Error((error.value as any)?.error ?? '...')` — mutations reuse this; AND the EXACT bracket-cast call shape in useRankingRouteScores: `await (api.api.ranking as any)['route-scores'].get()` — the write hooks MUST mirror this casting)
    - torre-de-controle/src/app/pages/configuracoes/tabs/UsersTab.tsx OR any existing file using useMutation + useQueryClient in this app (find the established mutation+invalidate pattern; if none, use the standard `@tanstack/react-query` useMutation with onSuccess → queryClient.invalidateQueries({ queryKey: ['ranking', x] }))
    - api/src/modules/ranking/ranking.write.plugin.ts (09-03/04 — the exact endpoint paths + body field names the hooks must send: evaluations{trip_id,driver_id,driver_name,comunicacao,atendeu,desvio_rota,postura,ajuste_manual,observacao}; blocks{driver_id,driver_name,motivo}; blocks/:id PATCH{driver_id,driver_name}; route-scores{origin_code,destination_code,pontuacao,data_inicio,data_fim,observacao})
  </read_first>
  <action>
    Add 6 mutation hooks using `useMutation` + `useQueryClient` from '@tanstack/react-query' (add the import). Each: `const qc = useQueryClient(); return useMutation({ mutationFn: async (payload) => { const { data, error } = await <treaty call>; if (error) throw new Error((error.value as any)?.error ?? 'Falha na operação'); return data }, onSuccess: () => { ['ranking','trips'|'drivers'|'stats'|'blocks'|'route-scores'|'logs'].forEach... qc.invalidateQueries({ queryKey: ['ranking', key] }) } })`.

    Payload types: define small input interfaces matching the backend bodies (EvaluateTripInput, BlockDriverInput, UnblockDriverInput {id,driver_id,driver_name}, RouteScoreCreateInput, RouteScoreUpdateInput {id, patch...}, id:string for delete). Reuse the re-exported domain enums where natural (comunicacao/desvio_rota/postura as string unions matching the backend literals).

    Treaty calls — use the `as any` bracket casting established for hyphenated/parameterized segments (see <interfaces>); the runtime paths are `/api/ranking/evaluations`, `/api/ranking/blocks`, `/api/ranking/blocks/:id`, `/api/ranking/route-scores`, `/api/ranking/route-scores/:id`. Specifically: `blocks/:id` and ALL `route-scores` calls MUST go through the bracket-cast form (`(api.api.ranking as any)['route-scores']...` and the param-call `({ id }).patch(...)` shape) because the hyphen breaks dot-access and the App-type param inference is unreliable across the monorepo Eden version mismatch — mirror exactly how `useRankingRouteScores` already casts. Match whatever the App type exposes after 02/03/04 — if dot-access typechecks for evaluations/blocks POST, keep it; the hyphenated/parameterized ones default to the cast. credentials are already included via `@/lib/api`.

    Invalidation per D-09-09: invalidate the keys listed in <behavior> for each action. Do NOT do optimistic updates (out of scope per 09-CONTEXT deferred). Header comment: mutations call the requireRole-gated Phase 9 endpoints; on success they invalidate ['ranking',...] so the derived ranking refetches (backend already busted the 60s sheets cache).
  </action>
  <acceptance_criteria>
    - `cd torre-de-controle && npx tsc --noEmit` exits 0; `cd torre-de-controle && bun run build` (or npm/pnpm per project) exits 0.
    - `grep -cE "export function use(EvaluateTrip|BlockDriver|UnblockDriver|CreateRouteScore|UpdateRouteScore|DeleteRouteScore)" torre-de-controle/src/hooks/useRanking.ts` returns 6.
    - `grep -c "invalidateQueries" torre-de-controle/src/hooks/useRanking.ts` returns >= 6 (each hook invalidates).
    - `grep -n "useMutation" torre-de-controle/src/hooks/useRanking.ts` and `grep -n "useQueryClient" ...` both present.
    - `grep -n "evaluations" torre-de-controle/src/hooks/useRanking.ts` + `grep -n "blocks" ...` + `grep -n "route-scores" ...` confirm the 4 endpoint families are called.
    - Each mutation invalidates `['ranking','logs']` (audit refresh): `grep -c "'ranking', 'logs'" torre-de-controle/src/hooks/useRanking.ts` >= 6 (useRankingLogs queryKey + each mutation's invalidation) — confirm logs invalidation is wired so the LogsTab updates after a write.
    - EDEN TREATY CALL-SHAPE FORMALLY VERIFIED (this is the contract 09-06/07 copy; do not leave it implicit): `grep -nE "\(api\.api\.ranking as any\)\['route-scores'\]" torre-de-controle/src/hooks/useRanking.ts` returns the route-scores write call sites (POST/PATCH/DELETE using the bracket-cast path — confirms the hyphen segment is handled, not dot-accessed). `grep -nE "\)\.patch\(|\.delete\(\)" torre-de-controle/src/hooks/useRanking.ts` confirms a `.patch(` and a `.delete()` call exist (the route-scores/:id + blocks/:id verbs). `grep -nE "blocks.*\.patch\(|\{ ?id ?\}\)\.patch" torre-de-controle/src/hooks/useRanking.ts` confirms the `blocks/:id` PATCH uses the path-param call form. These greps make the exact treaty shapes a build-gate, not just a SUMMARY note.
  </acceptance_criteria>
  <done>6 mutation hooks (evaluate/block/unblock/route-score CRUD) call the Phase 9 endpoints via Eden Treaty using the verified bracket/param call shapes (route-scores bracket-cast + :id .patch/.delete), throw on error, and invalidate the correct ['ranking',...] keys incl. 'logs'; build + tsc exit 0; read hooks intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| React UI → useRanking mutation hooks → /api/ranking writes | The hooks send user-entered evaluation/block/route data to the requireRole-gated endpoints. The front-end role gate (useCanWriteRanking) is convenience only — the backend is authoritative. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-19 | Elevation | client-side role gate bypass | accept (mitigated server-side) | useCanWriteRanking only hides/disables UI; a user editing the bundle to call a mutation still hits `requireRole('admin','supervisor')` (09-03/04) → 403. The real control is the backend. Documented defense-in-depth. (T1) |
| T-09-20 | Tampering | bad payload from the client | mitigate | typebox validation on the backend (09-03/04) rejects bad enums/ranges with 422; the hooks surface the error. The hook payload types reduce accidental shape drift. (T5) |
| T-09-21 | Info-disclosure | error.value leaking internals to the UI | mitigate | Backend global onError masks 500s to `{error:'Internal server error'}`; hooks display only `error.value.error`. No stack/DB detail reaches the UI. |
</threat_model>

<verification>
- `cd torre-de-controle && npx tsc --noEmit` → exit 0; `bun run build` → exit 0.
- useRanking.ts exports the 5 reads (unchanged) + useRankingLogs + 6 mutations + useCanWriteRanking; EvaluationLogRecord re-exported.
- The route-scores bracket-cast + `blocks/:id` param-call treaty shapes are present (grep-verified) — the call-shape contract for 09-06/07 is a build gate.
- Only useRanking.ts changed; no component touched.
</verification>

<success_criteria>
The hook contract for the UI write wiring is complete: a logs read hook, six invalidating mutation hooks against the Phase 9 endpoints (with the exact, grep-verified Eden Treaty call shapes), and a role gate — all in the single shared hooks file, build-clean, with the read hooks untouched. 09-06 and 09-07 can now wire the shells with zero backend knowledge.
</success_criteria>

<output>
After completion, create `.planning/phases/09-ranking-escrita-auditoria-write-flows/09-05-SUMMARY.md` — record the exact Eden Treaty call shapes that typechecked (for 09-06/07 to copy verbatim).
</output>
