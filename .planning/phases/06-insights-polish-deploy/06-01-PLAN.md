---
phase: 06-insights-polish-deploy
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - api/package.json
  - api/src/db/schema/push-subscriptions.ts
  - api/src/db/schema/alert-thresholds.ts
  - api/src/db/schema/gps-providers.ts
  - api/src/db/schema/users.ts
  - api/src/db/schema/relations.ts
  - api/src/db/schema/index.ts
  - api/src/db/seed/index.ts
  - api/src/lib/scrub.ts
  - api/src/lib/sentry.ts
  - api/src/lib/vapid.ts
  - api/.env.example
  - torre-de-controle/package.json
  - torre-de-controle/src/components/ui/form.tsx
  - torre-de-controle/src/lib/scrub.ts
  - torre-de-controle/src/lib/sentry.ts
  - torre-de-controle/src/main.tsx
  - torre-de-controle/.env.example
autonomous: true
requirements:
  - PHASE6-WEB-PUSH-VAPID
  - PHASE6-SENTRY-BACKEND
  - PHASE6-SENTRY-FRONTEND
user_setup: []
must_haves:
  truths:
    - "Drizzle schema export inclui pushSubscriptions, alertThresholds, gpsProviders"
    - "users table tem coluna notification_preferences JSONB com default {critico:true, medio:false, baixo:false}"
    - "api/src/lib/sentry.ts side-effect inicializa @sentry/node se SENTRY_DSN presente"
    - "api/src/lib/vapid.ts exporta vapidPublicKey + isVapidConfigured baseado em env"
    - "torre-de-controle/src/lib/sentry.ts exporta initSentry() chamado em main.tsx antes de createRoot"
    - "scrub.ts em backend e frontend remove keys [password, authorization, cookie, email, phone, lat, lng, address, token, jwt] recursivamente com max depth 8"
    - "shadcn form.tsx instalado em torre-de-controle/src/components/ui/form.tsx"
    - "package.json backend inclui web-push@3.6.7, @sentry/node@10.55.0, @types/web-push"
    - "package.json frontend inclui @sentry/react@10.55.0, react-hook-form@7.76.1, zod@4.4.3, @hookform/resolvers, @sentry/vite-plugin"
    - ".env.example backend e frontend documentam VAPID_PUBLIC_KEY, VAPID_PRIVATE_KEY, VAPID_SUBJECT, SENTRY_DSN, VITE_SENTRY_DSN, VITE_VAPID_PUBLIC_KEY"
  artifacts:
    - path: "api/src/db/schema/push-subscriptions.ts"
      provides: "pushSubscriptions table (id, userId FK CASCADE, endpoint UNIQUE, p256dh, auth, createdAt)"
      exports: ["pushSubscriptions", "SelectPushSubscription", "InsertPushSubscription"]
    - path: "api/src/db/schema/alert-thresholds.ts"
      provides: "alertThresholds table (type VARCHAR PK, value INT, updatedBy FK, updatedAt)"
      exports: ["alertThresholds", "SelectAlertThreshold"]
    - path: "api/src/db/schema/gps-providers.ts"
      provides: "gpsProviders table (id, name, baseUrl, apiKey, isActive, createdAt)"
      exports: ["gpsProviders", "SelectGpsProvider", "InsertGpsProvider"]
    - path: "api/src/db/schema/users.ts"
      provides: "users table modified — adds notificationPreferences JSONB"
      contains: "jsonb('notification_preferences')"
    - path: "api/src/db/schema/relations.ts"
      provides: "pushSubscriptions relation to users (many); user relation extended"
      contains: "pushSubscriptionsRelations"
    - path: "api/src/db/schema/index.ts"
      provides: "barrel export — adds push-subscriptions, alert-thresholds, gps-providers"
      contains: "push-subscriptions"
    - path: "api/src/db/seed/index.ts"
      provides: "seed inserts alert_thresholds defaults (atraso_critico_minutes=30, desvio_km_threshold=2, stop_duration_minutes=15)"
      contains: "alertThresholds"
    - path: "api/src/lib/scrub.ts"
      provides: "scrubRecursive(obj, depth) recursive PII scrubber"
      exports: ["scrubRecursive", "SCRUB_KEYS"]
    - path: "api/src/lib/sentry.ts"
      provides: "Side-effect init @sentry/node with beforeSend scrub"
      contains: "Sentry.init"
    - path: "api/src/lib/vapid.ts"
      provides: "VAPID env loader + webpush.setVapidDetails call"
      exports: ["isVapidConfigured", "vapidPublicKey"]
    - path: "torre-de-controle/src/components/ui/form.tsx"
      provides: "shadcn Form/FormField/FormItem/FormLabel/FormControl/FormMessage wrappers"
      exports: ["Form", "FormField", "FormItem", "FormLabel", "FormControl", "FormMessage"]
    - path: "torre-de-controle/src/lib/scrub.ts"
      provides: "Frontend version of recursive PII scrubber (identical to backend)"
      exports: ["scrubRecursive"]
    - path: "torre-de-controle/src/lib/sentry.ts"
      provides: "initSentry() — @sentry/react init with beforeSend scrub"
      exports: ["initSentry"]
    - path: "torre-de-controle/src/main.tsx"
      provides: "Calls initSentry() before createRoot"
      contains: "initSentry()"
    - path: "api/.env.example"
      provides: "Documents VAPID + SENTRY env vars"
      contains: "VAPID_PUBLIC_KEY"
    - path: "torre-de-controle/.env.example"
      provides: "Documents VITE_API_URL, VITE_SENTRY_DSN, VITE_VAPID_PUBLIC_KEY"
      contains: "VITE_VAPID_PUBLIC_KEY"
  key_links:
    - from: "api/src/db/schema/push-subscriptions.ts"
      to: "api/src/db/schema/users.ts"
      via: "references(() => users.id, { onDelete: 'cascade' })"
      pattern: "references.*users\\.id.*cascade"
    - from: "api/src/db/schema/index.ts"
      to: "push-subscriptions, alert-thresholds, gps-providers"
      via: "export *"
      pattern: "export \\* from './push-subscriptions'"
    - from: "torre-de-controle/src/main.tsx"
      to: "initSentry"
      via: "import + call before createRoot"
      pattern: "initSentry\\(\\)"
    - from: "api/src/lib/sentry.ts"
      to: "api/src/lib/scrub.ts"
      via: "scrubRecursive in beforeSend"
      pattern: "scrubRecursive\\(event\\)"
---

<objective>
Wave 0 scaffold: instalar dependências novas (web-push, @sentry/node, @sentry/react, react-hook-form, zod, shadcn form), criar 3 schemas Drizzle novos (push_subscriptions, alert_thresholds, gps_providers), adicionar coluna JSONB notification_preferences em users, criar utilities compartilhadas (scrub.ts, sentry.ts, vapid.ts), atualizar .env.example backend + criar frontend .env.example, seed defaults de thresholds.

Purpose: Wave 1 (backend modules) e Wave 2 (frontend) precisam dessas dependências e schemas existindo para serem implementáveis em paralelo.

Output: Schemas Drizzle + libs init + dependencies prontos. Drizzle push acontecerá apenas no Wave 3 (deploy plan) contra DATABASE_URL_PROD.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ARCHITECTURE.md
@.planning/STACK.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/06-insights-polish-deploy/06-CONTEXT.md
@.planning/phases/06-insights-polish-deploy/06-RESEARCH.md
@.planning/phases/06-insights-polish-deploy/06-PATTERNS.md
@api/src/db/schema/users.ts
@api/src/db/schema/index.ts
@api/src/db/schema/relations.ts
@api/src/db/schema/alerts.ts
@api/src/db/schema/geofences.ts
@api/src/db/seed/index.ts
@api/src/lib/logger.ts
@api/.env.example
@torre-de-controle/src/main.tsx
@torre-de-controle/package.json

<interfaces>
<!-- Existing Drizzle pattern (from api/src/db/schema/users.ts lines 1-15) -->
```typescript
import { pgTable, uuid, varchar, text, boolean, timestamp } from 'drizzle-orm/pg-core'
import { sql } from 'drizzle-orm'

export const users = pgTable('users', {
  id:           uuid('id').primaryKey().default(sql`gen_random_uuid()`),
  name:         varchar('name', { length: 100 }).notNull(),
  email:        varchar('email', { length: 150 }).unique().notNull(),
  passwordHash: text('password_hash').notNull(),
  role:         varchar('role', { length: 20 }).notNull(),
  isActive:     boolean('is_active').default(true).notNull(),
  createdAt:    timestamp('created_at', { withTimezone: true }).defaultNow().notNull(),
})

export type SelectUser = typeof users.$inferSelect
export type InsertUser = typeof users.$inferInsert
```

<!-- FK with cascade (from api/src/db/schema/alerts.ts) -->
`tripId: uuid('trip_id').references(() => trips.id)` — replace with `.references(() => users.id, { onDelete: 'cascade' })` for push_subscriptions

<!-- Logger pattern (from api/src/lib/logger.ts) -->
```typescript
import pino from 'pino'
export const logger = pino({ level: process.env.LOG_LEVEL ?? 'info', base: { service: 'torre-api' } })
```

<!-- Relations pattern (from api/src/db/schema/relations.ts) -->
```typescript
export const usersRelations = relations(users, ({ many }) => ({
  assignedAlerts: many(alerts),
  treatments:     many(treatments),
}))
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Install backend + frontend dependencies + scaffold scrub/sentry/vapid libs</name>
  <files>
    api/package.json,
    torre-de-controle/package.json,
    torre-de-controle/src/components/ui/form.tsx,
    api/src/lib/scrub.ts,
    api/src/lib/sentry.ts,
    api/src/lib/vapid.ts,
    torre-de-controle/src/lib/scrub.ts,
    torre-de-controle/src/lib/sentry.ts,
    torre-de-controle/src/main.tsx,
    api/.env.example,
    torre-de-controle/.env.example
  </files>
  <read_first>
    - api/package.json (current deps)
    - torre-de-controle/package.json (current deps)
    - api/src/lib/logger.ts (env-based init pattern — PATTERNS.md lines 909-919)
    - api/.env.example (current envs)
    - torre-de-controle/src/main.tsx (createRoot location)
    - 06-RESEARCH.md lines 42-90 (Standard Stack table with locked versions)
    - 06-RESEARCH.md lines 622-683 (Pattern 5 Sentry beforeSend recursive scrub — full code)
    - 06-PATTERNS.md lines 1500-1541 (scrubRecursive frontend impl — copy as-is)
    - 06-CONTEXT.md D-38, D-39, D-40 (Sentry init + scrub + sample rate)
    - 06-CONTEXT.md D-11 (VAPID self-hosted)
  </read_first>
  <action>
    **Step A — Backend deps** (run via `docker compose exec api`):
    ```
    docker compose exec api bun add web-push@3.6.7 @sentry/node@10.55.0
    docker compose exec api bun add -d @types/web-push
    ```
    Verify package.json contains these exact versions. NÃO usar @sentry/bun — per CONTEXT locked: "@sentry/node em Bun runtime (NÃO existe @sentry/bun como pacote separado oficial; @sentry/node funciona)".

    **Step B — Frontend deps** (in torre-de-controle/):
    ```
    npm install @sentry/react@10.55.0 react-hook-form@7.76.1 zod@4.4.3 @hookform/resolvers
    npm install -D @sentry/vite-plugin
    npx shadcn@latest add form
    ```
    Confirms `form.tsx` lands in `torre-de-controle/src/components/ui/form.tsx`. shadcn CLI also pulls react-hook-form transitively — already installed explicitly above is fine.

    **Step C — scrub.ts (backend + frontend, identical content)**:
    Create `api/src/lib/scrub.ts` and `torre-de-controle/src/lib/scrub.ts` with the recursive scrubber from PATTERNS.md lines 1504-1528. SCRUB_KEYS array exact: `password, passwordhash, authorization, cookie, cookies, email, phone, lat, lng, latitude, longitude, address, token, jwt, access_token, refresh_token`. MAX_DEPTH=8. Match keys case-insensitive via `.toLowerCase()` + `.includes()`. Detect Bearer tokens in string values via `/Bearer\s+\S+/i`. Frontend file uses no Node-only imports.

    **Step D — Backend sentry.ts (side-effect module per D-38)**:
    `api/src/lib/sentry.ts`: import `* as Sentry from '@sentry/node'`, import `scrubRecursive` from `./scrub`, only call `Sentry.init({ dsn: process.env.SENTRY_DSN, environment: process.env.NODE_ENV ?? 'development', tracesSampleRate: 0.1, beforeSend(event) { return scrubRecursive(event) } })` if `process.env.SENTRY_DSN` truthy. No exports needed (side-effect only). Reference PATTERNS.md lines 922-934.

    **Step E — Backend vapid.ts**:
    `api/src/lib/vapid.ts`: import `webpush from 'web-push'` and logger. Read `VAPID_PUBLIC_KEY`, `VAPID_PRIVATE_KEY`, `VAPID_SUBJECT` from env (subject default `mailto:admin@torredecontrole.com`). If both public+private present, call `webpush.setVapidDetails(subject, publicKey, privateKey)` + `logger.info({ subject }, 'vapid keys configured')`. Else `logger.warn('VAPID keys missing — push notifications disabled')`. Export `isVapidConfigured: boolean` and `vapidPublicKey: string | undefined`. PATTERNS.md lines 945-961.

    **Step F — Frontend sentry.ts**:
    `torre-de-controle/src/lib/sentry.ts`: import `* as Sentry from '@sentry/react'` and scrubRecursive. Export `initSentry()` function that returns early if `!import.meta.env.VITE_SENTRY_DSN`, otherwise `Sentry.init({ dsn: ..., environment: import.meta.env.MODE, tracesSampleRate: 0.1, replaysSessionSampleRate: 0, replaysOnErrorSampleRate: 0, beforeSend(event) { return scrubRecursive(event) } })`. PATTERNS.md lines 1530-1540.

    **Step G — main.tsx wire initSentry**:
    Edit `torre-de-controle/src/main.tsx`: add `import { initSentry } from './lib/sentry'` and call `initSentry()` BEFORE `createRoot(...)` line. PATTERNS.md lines 1546-1548.

    **Step H — .env.example updates**:
    Append to `api/.env.example` (preserve existing):
    ```
    # Web Push (Phase 6) — gerar com: bunx web-push generate-vapid-keys --json
    VAPID_PUBLIC_KEY=
    VAPID_PRIVATE_KEY=
    VAPID_SUBJECT=mailto:admin@torredecontrole.com

    # Sentry (Phase 6) — opcional em dev, obrigatório em prod
    SENTRY_DSN=
    SENTRY_ENVIRONMENT=development
    ```
    Create `torre-de-controle/.env.example` (NEW file, see PATTERNS.md lines 1848-1862):
    ```
    # Backend API URL
    VITE_API_URL=http://localhost:3000

    # Sentry (Phase 6) — opcional em dev
    VITE_SENTRY_DSN=

    # Web Push (Phase 6) — public key (safe to expose)
    VITE_VAPID_PUBLIC_KEY=

    # For build CI only (DO NOT commit values):
    # SENTRY_AUTH_TOKEN=
    # SENTRY_ORG=
    # SENTRY_PROJECT=torre-frontend
    ```
  </action>
  <verify>
    <automated>
      cd "C:/Users/antonio.magalhaes/Documents/Projetos/argon-dashboard/.claude/worktrees/elastic-napier-5559df" && \
      grep -q '"web-push"' api/package.json && \
      grep -q '"@sentry/node"' api/package.json && \
      grep -q '"@sentry/react"' torre-de-controle/package.json && \
      grep -q '"react-hook-form"' torre-de-controle/package.json && \
      grep -q '"zod"' torre-de-controle/package.json && \
      grep -q '"@sentry/vite-plugin"' torre-de-controle/package.json && \
      test -f torre-de-controle/src/components/ui/form.tsx && \
      test -f api/src/lib/scrub.ts && \
      test -f api/src/lib/sentry.ts && \
      test -f api/src/lib/vapid.ts && \
      test -f torre-de-controle/src/lib/scrub.ts && \
      test -f torre-de-controle/src/lib/sentry.ts && \
      grep -q 'initSentry' torre-de-controle/src/main.tsx && \
      grep -q 'VAPID_PUBLIC_KEY' api/.env.example && \
      grep -q 'VITE_VAPID_PUBLIC_KEY' torre-de-controle/.env.example
    </automated>
  </verify>
  <acceptance_criteria>
    - All listed deps present in respective package.json with exact versions from RESEARCH.md Standard Stack table
    - `api/src/lib/scrub.ts` exports `scrubRecursive` function that returns `<scrubbed>` for keys in SCRUB_KEYS list
    - Both backend and frontend scrub.ts have identical SCRUB_KEYS list (case-insensitive matching)
    - `api/src/lib/sentry.ts` checks `process.env.SENTRY_DSN` truthiness before calling `Sentry.init`
    - `torre-de-controle/src/lib/sentry.ts` checks `import.meta.env.VITE_SENTRY_DSN` truthiness
    - `main.tsx` calls `initSentry()` before any other side effects
    - shadcn `form.tsx` exports `Form, FormField, FormItem, FormLabel, FormControl, FormMessage` (verify via grep)
  </acceptance_criteria>
  <done>
    Dependencies installed and visible in package.json. Scrub/sentry/vapid utilities created with correct PII scrub keys. Frontend main.tsx initializes Sentry on app boot. .env.example documents new envs for both backend and frontend.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create 3 Drizzle schemas + modify users (notificationPreferences) + wire barrel/relations + seed thresholds defaults</name>
  <files>
    api/src/db/schema/push-subscriptions.ts,
    api/src/db/schema/alert-thresholds.ts,
    api/src/db/schema/gps-providers.ts,
    api/src/db/schema/users.ts,
    api/src/db/schema/relations.ts,
    api/src/db/schema/index.ts,
    api/src/db/seed/index.ts
  </files>
  <read_first>
    - api/src/db/schema/users.ts (current shape — exact column types)
    - api/src/db/schema/alerts.ts (FK + index pattern)
    - api/src/db/schema/geofences.ts (defaultRandom + isActive pattern)
    - api/src/db/schema/index.ts (barrel export pattern — 11 lines)
    - api/src/db/schema/relations.ts (relations() pattern, lines 60-63 for usersRelations)
    - api/src/db/seed/index.ts (insert pattern — users seed lines 40-46)
    - 06-RESEARCH.md lines 1213-1278 (Drizzle schema examples for new tables — copy structure)
    - 06-PATTERNS.md lines 91-227 (Pattern Group A — Drizzle Schema Files mapping)
    - 06-CONTEXT.md D-13, D-14, D-19, D-20 (table semantics)
    - 06-CONTEXT.md "Claude's Discretion" — push_subscriptions PK/FK/indexes
  </read_first>
  <action>
    **File 1 — api/src/db/schema/push-subscriptions.ts** (NEW, per RESEARCH lines 1213-1230):
    Imports `pgTable, uuid, text, timestamp` from 'drizzle-orm/pg-core', `sql` from 'drizzle-orm', `users` from './users'. Define `pushSubscriptions = pgTable('push_subscriptions', { ... })` columns: `id` UUID PK default `sql\`gen_random_uuid()\``, `userId` UUID notNull references users.id with `onDelete: 'cascade'`, `endpoint` text UNIQUE notNull (dedupe per CONTEXT D-13 "Claude's Discretion"), `p256dh` text notNull, `auth` text notNull, `createdAt` timestamp withTimezone defaultNow notNull. Export `SelectPushSubscription = typeof pushSubscriptions.$inferSelect`, `InsertPushSubscription = typeof pushSubscriptions.$inferInsert`.

    **File 2 — api/src/db/schema/alert-thresholds.ts** (NEW, per RESEARCH lines 1234-1246):
    Imports `pgTable, varchar, integer, timestamp, uuid` from 'drizzle-orm/pg-core', `users` from './users'. Define `alertThresholds = pgTable('alert_thresholds', { ... })`: `type` varchar(50) PRIMARY KEY (key per D-19), `value` integer notNull, `updatedBy` uuid references users.id (nullable for seed inserts), `updatedAt` timestamp withTimezone defaultNow notNull. Export `SelectAlertThreshold = typeof alertThresholds.$inferSelect`.

    **File 3 — api/src/db/schema/gps-providers.ts** (NEW):
    Imports `pgTable, uuid, varchar, text, boolean, timestamp` from 'drizzle-orm/pg-core'. Use `.defaultRandom()` for id (matches geofences.ts pattern per PATTERNS.md lines 156-168). Columns: `id` UUID PK defaultRandom, `name` varchar(100) notNull, `baseUrl` text (nullable — stub), `apiKey` text (nullable — stub per D-20), `isActive` boolean notNull default true, `createdAt` timestamp withTimezone defaultNow notNull. Export `SelectGpsProvider, InsertGpsProvider`.

    **File 4 — api/src/db/schema/users.ts** (MODIFY):
    Add `jsonb` to the import from 'drizzle-orm/pg-core' (existing imports: pgTable, uuid, varchar, text, boolean, timestamp — add `, jsonb`). Inside `users = pgTable('users', { ... })` add column: `notificationPreferences: jsonb('notification_preferences').default({ critico: true, medio: false, baixo: false })`. Place between `isActive` and `createdAt`. Default uses literal object — Drizzle serializes to PostgreSQL DEFAULT. DO NOT add notNull (allow null for legacy rows). Reference RESEARCH lines 1264-1278.

    **File 5 — api/src/db/schema/relations.ts** (MODIFY):
    Add import: `import { pushSubscriptions } from './push-subscriptions'`. Modify `usersRelations` to add `pushSubscriptions: many(pushSubscriptions)` to the returned object. Append at end of file:
    `export const pushSubscriptionsRelations = relations(pushSubscriptions, ({ one }) => ({ user: one(users, { fields: [pushSubscriptions.userId], references: [users.id] }) }))`
    Reference PATTERNS.md lines 199-207.

    **File 6 — api/src/db/schema/index.ts** (MODIFY):
    Add three lines BEFORE the `export * from './relations'` line (relations must come last):
    ```
    export * from './push-subscriptions'
    export * from './alert-thresholds'
    export * from './gps-providers'
    ```

    **File 7 — api/src/db/seed/index.ts** (MODIFY):
    Add import: `import { alertThresholds } from '../schema/alert-thresholds'`. After existing user seed insert (or anywhere safe — sequencing doesn't matter for this), insert defaults:
    ```typescript
    await db.insert(alertThresholds).values([
      { type: 'atraso_critico_minutes', value: 30 },
      { type: 'desvio_km_threshold',    value: 2  },
      { type: 'stop_duration_minutes',  value: 15 },
    ]).onConflictDoNothing()
    console.log('[seed] alert_thresholds: 3 defaults')
    ```
    Values from D-19 locked. `onConflictDoNothing()` makes seed idempotent.

    **DO NOT execute `drizzle-kit push`** — push happens in Wave 3 deploy plan (06-08-PLAN.md) against DATABASE_URL_PROD with --strict --verbose. For local dev verification only, executor may optionally run `docker compose exec api bunx drizzle-kit push --strict --verbose --dry-run` to preview SQL but MUST NOT apply it without explicit user approval (per 06-CONTEXT D-37 risk and 06-RESEARCH Pitfall #1).
  </action>
  <verify>
    <automated>
      cd "C:/Users/antonio.magalhaes/Documents/Projetos/argon-dashboard/.claude/worktrees/elastic-napier-5559df" && \
      grep -q "pgTable('push_subscriptions'" api/src/db/schema/push-subscriptions.ts && \
      grep -q "onDelete: 'cascade'" api/src/db/schema/push-subscriptions.ts && \
      grep -q "endpoint" api/src/db/schema/push-subscriptions.ts && \
      grep -q "unique" api/src/db/schema/push-subscriptions.ts && \
      grep -q "pgTable('alert_thresholds'" api/src/db/schema/alert-thresholds.ts && \
      grep -q "pgTable('gps_providers'" api/src/db/schema/gps-providers.ts && \
      grep -q "jsonb('notification_preferences')" api/src/db/schema/users.ts && \
      grep -q "critico: true" api/src/db/schema/users.ts && \
      grep -q "pushSubscriptionsRelations" api/src/db/schema/relations.ts && \
      grep -q "push-subscriptions" api/src/db/schema/index.ts && \
      grep -q "alert-thresholds" api/src/db/schema/index.ts && \
      grep -q "gps-providers" api/src/db/schema/index.ts && \
      grep -q "atraso_critico_minutes" api/src/db/seed/index.ts && \
      docker compose exec -T api bunx tsc --noEmit
    </automated>
  </verify>
  <acceptance_criteria>
    - All 3 new schema files compile without errors (`bunx tsc --noEmit` returns 0)
    - users.ts notification_preferences default literal equals `{ critico: true, medio: false, baixo: false }` (verified via grep "critico: true")
    - push_subscriptions.endpoint has `.unique()` modifier
    - push_subscriptions.userId references users.id with `onDelete: 'cascade'`
    - relations.ts exports `pushSubscriptionsRelations` AND `usersRelations` includes `pushSubscriptions: many(pushSubscriptions)`
    - Barrel `index.ts` exports all 3 new schemas BEFORE `relations` export (Drizzle resolves relations after tables exist)
    - Seed inserts 3 threshold rows with values 30/2/15 with onConflictDoNothing
    - No drizzle-kit push executed — schema applied via Wave 3 deploy plan
  </acceptance_criteria>
  <done>
    Drizzle schemas committed to repo, type-checks pass. Barrel and relations wired. Seed adds thresholds defaults (idempotent). Push deferred to Wave 3 deploy plan.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local dev → schema/.env files | Developer commits — no untrusted input here |
| Sentry SDK → external Sentry service | Backend/frontend SDKs send event data; PII scrubbing required before send |
| web-push library → push services (FCM/Mozilla autopush) | Backend sends notifications to vendor endpoints; uses VAPID keys |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.01-01 | Information disclosure | api/src/lib/sentry.ts beforeSend | mitigate | scrubRecursive removes PII keys (password, email, phone, lat, lng, address, JWT, cookies) before event sent to Sentry (ASVS V7.3.4) |
| T-06.01-02 | Information disclosure | torre-de-controle/src/lib/sentry.ts beforeSend | mitigate | Same scrubRecursive shared logic on frontend |
| T-06.01-03 | Information disclosure | .env.example files | mitigate | Empty values shipped; real secrets only in Railway/CF Pages env stores (D-44). Document that `.env.production` MUST be in .gitignore |
| T-06.01-04 | Tampering | push_subscriptions.endpoint column | mitigate | UNIQUE constraint prevents duplicate subscriptions per endpoint; CASCADE delete on user deletion ensures cleanup |
| T-06.01-05 | Spoofing | api/src/lib/vapid.ts VAPID key handling | mitigate | Private key only loaded from env (never code); subject hardcoded as mailto: per RFC 8292 |
| T-06.01-06 | DoS | scrubRecursive infinite loop | mitigate | MAX_DEPTH=8 caps recursion (PATTERNS.md line 1509) |
</threat_model>

<verification>
After both tasks:
1. `docker compose exec api bunx tsc --noEmit` — passes with 0 errors
2. `cd torre-de-controle && npm run build` — Vite builds successfully (Sentry plugin no-op since SENTRY_AUTH_TOKEN missing in dev)
3. Confirm `.env.example` files updated; new VAPID/Sentry vars listed
4. Confirm schemas exported from barrel; `import { pushSubscriptions, alertThresholds, gpsProviders } from './db/schema'` resolves
5. No drizzle-kit push executed against prod DB
</verification>

<success_criteria>
- All schema files compile + barrel + relations + seed updated
- Backend + frontend deps installed at locked versions
- scrub/sentry/vapid utilities created
- main.tsx initializes Sentry on boot
- .env.example documents all new envs
- Tasks 1+2 enable Wave 1 (backend modules) and Wave 2 (frontend) to run in parallel
</success_criteria>

<output>
After completion, create `.planning/phases/06-insights-polish-deploy/06-01-SUMMARY.md` documenting deps installed, schemas created, libs scaffolded, env vars added.
</output>
