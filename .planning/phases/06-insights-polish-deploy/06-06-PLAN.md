---
phase: 06-insights-polish-deploy
plan: 06
type: execute
wave: 3
depends_on: [06-04]
files_modified:
  - torre-de-controle/src/hooks/useUsers.ts
  - torre-de-controle/src/hooks/useThresholds.ts
  - torre-de-controle/src/hooks/useGpsProviders.ts
  - torre-de-controle/src/hooks/usePushSubscription.ts
  - torre-de-controle/public/sw.js
  - torre-de-controle/src/app/pages/configuracoes/ConfiguracoesPage.tsx
  - torre-de-controle/src/app/pages/configuracoes/tabs/UsersTab.tsx
  - torre-de-controle/src/app/pages/configuracoes/tabs/AlertThresholdsTab.tsx
  - torre-de-controle/src/app/pages/configuracoes/tabs/NotificationsTab.tsx
  - torre-de-controle/src/app/pages/configuracoes/tabs/GpsProvidersTab.tsx
autonomous: true
requirements:
  - PHASE6-CONFIG-USERS-TAB
  - PHASE6-CONFIG-THRESHOLDS-TAB
  - PHASE6-CONFIG-GPS-PROVIDERS-TAB
  - PHASE6-CONFIG-NOTIFICATIONS-TAB
  - PHASE6-WEB-PUSH-SUBSCRIBE-UI
  - PHASE6-WEB-PUSH-SERVICE-WORKER
user_setup: []
must_haves:
  truths:
    - "Página /configuracoes renderiza 4 tabs: Usuários, Alertas, Notificações, Integrações GPS (D-17)"
    - "Aba Usuários: lista usuarios, criar, editar (role/isActive), soft delete — visivel só para admin"
    - "Aba Alertas: form RHF + Zod para 3 thresholds (atraso_critico_minutes, desvio_km_threshold, stop_duration_minutes)"
    - "Aba Notificações: botão 'Ativar notificações' chama enablePush + toggles per-severity (critico/medio/baixo)"
    - "Aba GPS: lista providers + form criar/editar (name, baseUrl, apiKey, isActive)"
    - "public/sw.js handle 'push' event mostra notification + 'notificationclick' abre URL"
    - "usePushSubscription registra SW com scope:'/' + chama PushManager.subscribe + POST /api/push/subscribe"
    - "useUsers/useThresholds/useGpsProviders contratos { data, isLoading, ... } + mutations com invalidateQueries"
    - "Push subscribe button mostra estado: idle / enabling / enabled / error (per usePushSubscription state)"
    - "VAPID_PUBLIC_KEY lida via import.meta.env.VITE_VAPID_PUBLIC_KEY"
  artifacts:
    - path: "torre-de-controle/src/hooks/useUsers.ts"
      provides: "useUsers, useCreateUser, useUpdateUser, useDeactivateUser, useUpdateMyPreferences"
      exports: ["useUsers", "useCreateUser", "useUpdateUser", "useDeactivateUser", "useUpdateMyPreferences"]
    - path: "torre-de-controle/src/hooks/useThresholds.ts"
      provides: "useThresholds, useUpdateThreshold"
      exports: ["useThresholds", "useUpdateThreshold"]
    - path: "torre-de-controle/src/hooks/useGpsProviders.ts"
      provides: "useGpsProviders, useCreateGpsProvider, useUpdateGpsProvider, useDeleteGpsProvider"
      exports: ["useGpsProviders", "useCreateGpsProvider", "useUpdateGpsProvider", "useDeleteGpsProvider"]
    - path: "torre-de-controle/src/hooks/usePushSubscription.ts"
      provides: "hook returning { status, error, enablePush, disablePush }"
      exports: ["usePushSubscription"]
    - path: "torre-de-controle/public/sw.js"
      provides: "Service Worker handler para push event + notificationclick"
      contains: "showNotification"
    - path: "torre-de-controle/src/app/pages/configuracoes/ConfiguracoesPage.tsx"
      provides: "Shell page com 4 Tabs (shadcn)"
      exports: ["ConfiguracoesPage"]
    - path: "torre-de-controle/src/app/pages/configuracoes/tabs/UsersTab.tsx"
      provides: "Tabela + dialog form create/edit"
      exports: ["UsersTab"]
    - path: "torre-de-controle/src/app/pages/configuracoes/tabs/AlertThresholdsTab.tsx"
      provides: "RHF form com Zod schema para 3 thresholds"
      exports: ["AlertThresholdsTab"]
    - path: "torre-de-controle/src/app/pages/configuracoes/tabs/NotificationsTab.tsx"
      provides: "Botão Ativar + checkboxes per severity + Save"
      exports: ["NotificationsTab"]
    - path: "torre-de-controle/src/app/pages/configuracoes/tabs/GpsProvidersTab.tsx"
      provides: "Tabela + form CRUD para providers"
      exports: ["GpsProvidersTab"]
  key_links:
    - from: "torre-de-controle/src/hooks/usePushSubscription.ts"
      to: "navigator.serviceWorker.register('/sw.js', { scope: '/' })"
      via: "registration explícita com scope root"
      pattern: "navigator\\.serviceWorker\\.register\\('/sw\\.js'"
    - from: "torre-de-controle/src/hooks/usePushSubscription.ts"
      to: "/api/push/subscribe"
      via: "(api.api.push as any).subscribe.post(...)"
      pattern: "push.*subscribe.*post"
    - from: "torre-de-controle/public/sw.js"
      to: "showNotification + openWindow"
      via: "push event handler + notificationclick handler"
      pattern: "showNotification"
    - from: "AlertThresholdsTab"
      to: "react-hook-form + zod + shadcn Form (form.tsx from 06-01)"
      via: "useForm({ resolver: zodResolver(schema) }) + <Form {...form}>"
      pattern: "zodResolver"
---

<objective>
Wave 3 frontend: implementar página `/configuracoes` com 4 tabs (Usuários, Thresholds Alertas, Notificações com Push opt-in, GPS Providers), hooks CRUD, Service Worker, hook usePushSubscription. Substitui o stub atual (torre-de-controle/src/app/pages/configuracoes/ConfiguracoesPage.tsx).

Purpose: Admin gerencia usuários e thresholds. Qualquer user habilita push notifications. UI prepara terreno para integração GPS futura.

Output: 4 hooks + 1 SW + 1 page + 4 tabs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-insights-polish-deploy/06-CONTEXT.md
@.planning/phases/06-insights-polish-deploy/06-RESEARCH.md
@.planning/phases/06-insights-polish-deploy/06-PATTERNS.md
@.planning/phases/06-insights-polish-deploy/06-04-PLAN.md
@torre-de-controle/src/hooks/useGeofences.ts
@torre-de-controle/src/components/ui/tabs.tsx
@torre-de-controle/src/components/ui/form.tsx
@torre-de-controle/src/lib/api.ts
@torre-de-controle/src/components/domain/DataTable.tsx
@torre-de-controle/src/app/pages/configuracoes/ConfiguracoesPage.tsx

<interfaces>
<!-- useGeofences CRUD pattern (torre-de-controle/src/hooks/useGeofences.ts lines 1-49) -->
```typescript
import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query'
export function useGeofences() {
  const q = useQuery({ queryKey: ['geofences'], queryFn: async () => { ... } })
  return { data: q.data ?? [], isLoading: q.isLoading, refetch: q.refetch }
}
export function useCreateGeofence() {
  const qc = useQueryClient()
  return useMutation({ mutationFn: async (body) => { ... }, onSuccess: () => qc.invalidateQueries({ queryKey: ['geofences'] }) })
}
```

<!-- shadcn Tabs (torre-de-controle/src/components/ui/tabs.tsx) -->
```typescript
import { Tabs, TabsContent, TabsList, TabsTrigger } from '@/components/ui/tabs'
<Tabs defaultValue="users">
  <TabsList><TabsTrigger value="users">Usuários</TabsTrigger>...</TabsList>
  <TabsContent value="users"><UsersTab /></TabsContent>
</Tabs>
```

<!-- shadcn Form (from 06-01) -->
```typescript
import { Form, FormField, FormItem, FormLabel, FormControl, FormMessage } from '@/components/ui/form'
import { useForm } from 'react-hook-form'
import { zodResolver } from '@hookform/resolvers/zod'
import { z } from 'zod'
```

<!-- Service Worker registration with explicit scope (06-RESEARCH lines 525-541) -->
```typescript
const reg = await navigator.serviceWorker.register('/sw.js', { scope: '/' })
// CRITICAL: scope:'/' makes SW visible to all routes per RESEARCH Pitfall #2
```

<!-- urlBase64ToUint8Array (VAPID public key conversion) -->
```typescript
function urlBase64ToUint8Array(base64String: string): Uint8Array {
  const padding = '='.repeat((4 - base64String.length % 4) % 4)
  const base64 = (base64String + padding).replace(/-/g, '+').replace(/_/g, '/')
  const raw = atob(base64)
  return new Uint8Array([...raw].map(c => c.charCodeAt(0)))
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 4 CRUD hooks (useUsers, useThresholds, useGpsProviders, usePushSubscription) + Service Worker public/sw.js</name>
  <files>
    torre-de-controle/src/hooks/useUsers.ts,
    torre-de-controle/src/hooks/useThresholds.ts,
    torre-de-controle/src/hooks/useGpsProviders.ts,
    torre-de-controle/src/hooks/usePushSubscription.ts,
    torre-de-controle/public/sw.js
  </files>
  <read_first>
    - torre-de-controle/src/hooks/useGeofences.ts (CRUD with useQuery + useMutation + invalidateQueries — CORE analog)
    - torre-de-controle/src/lib/api.ts (Eden Treaty api import)
    - 06-RESEARCH.md lines 473-507 (Service Worker complete code)
    - 06-RESEARCH.md lines 512-541 (usePushSubscription complete code)
    - 06-PATTERNS.md lines 1051-1090 (useUsers/Thresholds/GpsProviders CRUD pattern)
    - 06-PATTERNS.md lines 1100-1167 (usePushSubscription pattern)
    - 06-PATTERNS.md lines 1612-1641 (Service Worker pattern)
    - 06-CONTEXT.md D-11..D-16 (full push semantics)
  </read_first>
  <action>
    **File 1 — torre-de-controle/src/hooks/useUsers.ts**:
    Pattern from useGeofences. Define types `User` (without passwordHash), `CreateUserInput`, `UpdateUserInput`.
    ```typescript
    import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query'
    import { api } from '@/lib/api'

    export type UserRole = 'admin' | 'supervisor' | 'analyst' | 'viewer'
    export type User = {
      id: string; name: string; email: string; role: UserRole
      isActive: boolean
      notificationPreferences: { critico?: boolean; medio?: boolean; baixo?: boolean } | null
      createdAt: string
    }

    export function useUsers() {
      const q = useQuery({
        queryKey: ['users'],
        queryFn: async () => {
          const { data, error } = await (api.api.users as any).get()
          if (error) throw new Error((error.value as any)?.error ?? 'Failed to fetch users')
          return (data ?? []) as User[]
        },
      })
      return { data: q.data ?? [], isLoading: q.isLoading, isError: q.isError, error: q.error, refetch: q.refetch }
    }

    export function useCreateUser() {
      const qc = useQueryClient()
      return useMutation({
        mutationFn: async (input: { name: string; email: string; role: UserRole; password: string }) => {
          const { data, error } = await (api.api.users as any).post(input)
          if (error) {
            const msg = (error.value as any)?.error ?? 'Create failed'
            throw new Error(msg)
          }
          return data as User
        },
        onSuccess: () => qc.invalidateQueries({ queryKey: ['users'] }),
      })
    }

    export function useUpdateUser() {
      const qc = useQueryClient()
      return useMutation({
        mutationFn: async ({ id, patch }: { id: string; patch: { role?: UserRole; isActive?: boolean; notificationPreferences?: any } }) => {
          const { data, error } = await (api.api.users as any)[id].patch(patch)
          if (error) throw new Error('Update failed')
          return data as User
        },
        onSuccess: () => qc.invalidateQueries({ queryKey: ['users'] }),
      })
    }

    export function useDeactivateUser() {
      const qc = useQueryClient()
      return useMutation({
        mutationFn: async (id: string) => {
          const { error } = await (api.api.users as any)[id].delete()
          if (error) throw new Error('Deactivate failed')
        },
        onSuccess: () => qc.invalidateQueries({ queryKey: ['users'] }),
      })
    }

    export function useUpdateMyPreferences() {
      const qc = useQueryClient()
      return useMutation({
        mutationFn: async (prefs: { critico?: boolean; medio?: boolean; baixo?: boolean }) => {
          const { data, error } = await (api.api.users as any).me['notification-preferences'].patch(prefs)
          if (error) throw new Error('Update preferences failed')
          return data
        },
        onSuccess: () => qc.invalidateQueries({ queryKey: ['users'] }),
      })
    }
    ```

    **File 2 — torre-de-controle/src/hooks/useThresholds.ts**:
    ```typescript
    import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query'
    import { api } from '@/lib/api'

    export type Thresholds = Record<string, number>

    export function useThresholds() {
      const q = useQuery({
        queryKey: ['thresholds'],
        queryFn: async () => {
          const { data, error } = await (api.api.thresholds as any).get()
          if (error) throw new Error('Failed to fetch thresholds')
          return (data ?? {}) as Thresholds
        },
      })
      return { data: q.data ?? {} as Thresholds, isLoading: q.isLoading, isError: q.isError, error: q.error, refetch: q.refetch }
    }

    export function useUpdateThreshold() {
      const qc = useQueryClient()
      return useMutation({
        mutationFn: async ({ type, value }: { type: string; value: number }) => {
          const { error } = await (api.api.thresholds as any)[type].patch({ value })
          if (error) throw new Error('Update threshold failed')
        },
        onSuccess: () => qc.invalidateQueries({ queryKey: ['thresholds'] }),
      })
    }
    ```

    **File 3 — torre-de-controle/src/hooks/useGpsProviders.ts**:
    Same shape as useUsers (useQuery + 3 mutations: create/update/delete). Types: `GpsProvider = { id, name, baseUrl?, apiKey?, isActive, createdAt }`. Endpoints `(api.api['gps-providers'] as any).get() / .post() / [id].patch() / [id].delete()`.

    **File 4 — torre-de-controle/src/hooks/usePushSubscription.ts** (RESEARCH lines 511-541, PATTERNS lines 1116-1167):
    ```typescript
    import { useEffect, useState } from 'react'
    import { api } from '@/lib/api'

    const VAPID_PUBLIC_KEY = import.meta.env.VITE_VAPID_PUBLIC_KEY as string | undefined

    function urlBase64ToUint8Array(base64String: string): Uint8Array {
      const padding = '='.repeat((4 - base64String.length % 4) % 4)
      const base64 = (base64String + padding).replace(/-/g, '+').replace(/_/g, '/')
      const raw = atob(base64)
      return new Uint8Array([...raw].map(c => c.charCodeAt(0)))
    }

    type PushStatus = 'idle' | 'enabling' | 'enabled' | 'denied' | 'unsupported' | 'error'

    export function usePushSubscription() {
      const [status, setStatus] = useState<PushStatus>('idle')
      const [error, setError] = useState<string | null>(null)

      // Check current subscription state on mount
      useEffect(() => {
        if (!('serviceWorker' in navigator) || !('PushManager' in window)) {
          setStatus('unsupported')
          return
        }
        if (Notification.permission === 'denied') {
          setStatus('denied')
          return
        }
        navigator.serviceWorker.getRegistration('/sw.js').then(async reg => {
          if (!reg) return
          const sub = await reg.pushManager.getSubscription()
          if (sub) setStatus('enabled')
        }).catch(() => { /* ignore */ })
      }, [])

      async function enablePush() {
        setStatus('enabling'); setError(null)
        try {
          if (!('serviceWorker' in navigator) || !('PushManager' in window)) {
            throw new Error('Push notifications não suportadas neste browser')
          }
          if (!VAPID_PUBLIC_KEY) {
            throw new Error('VAPID public key não configurada (VITE_VAPID_PUBLIC_KEY missing)')
          }

          const reg = await navigator.serviceWorker.register('/sw.js', { scope: '/' })
          await navigator.serviceWorker.ready

          const permission = await Notification.requestPermission()
          if (permission !== 'granted') {
            setStatus('denied')
            throw new Error('Permissão de notificação negada')
          }

          let subscription = await reg.pushManager.getSubscription()
          if (!subscription) {
            subscription = await reg.pushManager.subscribe({
              userVisibleOnly: true,
              applicationServerKey: urlBase64ToUint8Array(VAPID_PUBLIC_KEY),
            })
          }

          const json = subscription.toJSON()
          const { error: apiError } = await (api.api.push as any).subscribe.post({
            endpoint: subscription.endpoint,
            keys:     json.keys,
          })
          if (apiError) throw new Error('Falha ao registrar subscription no servidor')

          setStatus('enabled')
        } catch (e: any) {
          setStatus('error')
          setError(e?.message ?? 'Erro desconhecido')
        }
      }

      async function disablePush() {
        try {
          const reg = await navigator.serviceWorker.getRegistration('/sw.js')
          if (!reg) { setStatus('idle'); return }
          const sub = await reg.pushManager.getSubscription()
          if (sub) {
            const endpoint = sub.endpoint
            await sub.unsubscribe()
            await (api.api.push as any).unsubscribe.post({ endpoint })
          }
          setStatus('idle')
        } catch (e: any) {
          setStatus('error')
          setError(e?.message ?? 'Erro ao desativar')
        }
      }

      return { status, error, enablePush, disablePush }
    }
    ```

    **File 5 — torre-de-controle/public/sw.js** (RESEARCH lines 474-507, PATTERNS lines 1612-1641):
    ```javascript
    // Service Worker for Torre de Controle — Web Push notifications
    // Scope: '/' (root) — registered explicitly from frontend
    self.addEventListener('install', () => self.skipWaiting())
    self.addEventListener('activate', (e) => e.waitUntil(self.clients.claim()))

    self.addEventListener('push', (event) => {
      if (!event.data) return
      let payload
      try { payload = event.data.json() }
      catch { payload = { title: 'Torre de Controle', body: event.data.text(), url: '/' } }

      event.waitUntil(
        self.registration.showNotification(payload.title, {
          body:               payload.body,
          icon:               '/icon-192.png',
          badge:              '/icon-192.png',
          data:               { url: payload.url || '/' },
          tag:                payload.url || 'torre-default',
          requireInteraction: true,
        })
      )
    })

    self.addEventListener('notificationclick', (event) => {
      event.notification.close()
      const targetUrl = (event.notification.data && event.notification.data.url) || '/'
      event.waitUntil(
        self.clients.matchAll({ type: 'window', includeUncontrolled: true }).then((clients) => {
          for (const client of clients) {
            if (client.url.includes(targetUrl) && 'focus' in client) return client.focus()
          }
          return self.clients.openWindow(targetUrl)
        })
      )
    })
    ```

    Nota: `icon-192.png` e `icon-512.png` em `public/` são opcionais nesta fase. Service Worker degrades graciously sem icon. README/SUMMARY deve documentar essa pendência operacional (não bloqueia push).
  </action>
  <verify>
    <automated>
      cd "C:/Users/antonio.magalhaes/Documents/Projetos/argon-dashboard/.claude/worktrees/elastic-napier-5559df" && \
      grep -q "useUsers" torre-de-controle/src/hooks/useUsers.ts && \
      grep -q "useCreateUser" torre-de-controle/src/hooks/useUsers.ts && \
      grep -q "useDeactivateUser" torre-de-controle/src/hooks/useUsers.ts && \
      grep -q "useUpdateMyPreferences" torre-de-controle/src/hooks/useUsers.ts && \
      grep -q "useThresholds" torre-de-controle/src/hooks/useThresholds.ts && \
      grep -q "useUpdateThreshold" torre-de-controle/src/hooks/useThresholds.ts && \
      grep -q "useGpsProviders" torre-de-controle/src/hooks/useGpsProviders.ts && \
      grep -q "usePushSubscription" torre-de-controle/src/hooks/usePushSubscription.ts && \
      grep -q "navigator.serviceWorker.register('/sw.js'" torre-de-controle/src/hooks/usePushSubscription.ts && \
      grep -q "scope: '/'" torre-de-controle/src/hooks/usePushSubscription.ts && \
      grep -q "VITE_VAPID_PUBLIC_KEY" torre-de-controle/src/hooks/usePushSubscription.ts && \
      test -f torre-de-controle/public/sw.js && \
      grep -q "showNotification" torre-de-controle/public/sw.js && \
      grep -q "notificationclick" torre-de-controle/public/sw.js && \
      cd torre-de-controle && npx tsc --noEmit
    </automated>
  </verify>
  <acceptance_criteria>
    - All 4 hooks export the listed functions
    - useUsers calls (api.api.users as any).get/post/[id].patch/[id].delete with onSuccess invalidating ['users']
    - useUpdateMyPreferences targets /api/users/me/notification-preferences
    - useThresholds.get returns Record<string, number>
    - usePushSubscription:
      - Detects unsupported, denied, idle, enabling, enabled, error states
      - Uses explicit `{ scope: '/' }` in register call
      - Uses VITE_VAPID_PUBLIC_KEY env
      - Persists to backend via /api/push/subscribe with { endpoint, keys }
      - disablePush calls /api/push/unsubscribe with endpoint
    - public/sw.js exists with push and notificationclick handlers; uses event.waitUntil; reuses existing window via matchAll/focus
    - tsc passes
  </acceptance_criteria>
  <done>
    Hooks + SW prontos para consumo pela ConfiguracoesPage. Push end-to-end estará funcional após VAPID env set em deploy.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: ConfiguracoesPage shell + 4 tabs (UsersTab, AlertThresholdsTab with RHF/Zod, NotificationsTab with push opt-in, GpsProvidersTab)</name>
  <files>
    torre-de-controle/src/app/pages/configuracoes/ConfiguracoesPage.tsx,
    torre-de-controle/src/app/pages/configuracoes/tabs/UsersTab.tsx,
    torre-de-controle/src/app/pages/configuracoes/tabs/AlertThresholdsTab.tsx,
    torre-de-controle/src/app/pages/configuracoes/tabs/NotificationsTab.tsx,
    torre-de-controle/src/app/pages/configuracoes/tabs/GpsProvidersTab.tsx
  </files>
  <read_first>
    - torre-de-controle/src/app/pages/configuracoes/ConfiguracoesPage.tsx (current stub — to REPLACE entirely)
    - torre-de-controle/src/components/ui/tabs.tsx (shadcn Tabs API)
    - torre-de-controle/src/components/ui/form.tsx (created in 06-01 — Form/FormField API)
    - torre-de-controle/src/components/ui/dialog.tsx (if exists — for user create/edit modal; check existence)
    - torre-de-controle/src/components/ui/input.tsx (Input component)
    - torre-de-controle/src/components/ui/button.tsx
    - torre-de-controle/src/components/ui/badge.tsx
    - torre-de-controle/src/components/domain/DataTable.tsx
    - torre-de-controle/src/app/pages/motoristas/components/MotoristasTable.tsx (CRUD pattern with toolbar lines 39-88)
    - 06-RESEARCH.md lines 1023-1071 (RHF + Zod form full code — copy for AlertThresholdsTab)
    - 06-PATTERNS.md lines 1353-1391 (Configuracoes tabs)
    - 06-CONTEXT.md D-17, D-18, D-19, D-20 (tabs + semantics)
    - 06-CONTEXT.md D-14 (notification_preferences default { critico: true, medio: false, baixo: false })
  </read_first>
  <action>
    **File 1 — torre-de-controle/src/app/pages/configuracoes/ConfiguracoesPage.tsx** (FULL REPLACEMENT):
    ```typescript
    import { Tabs, TabsContent, TabsList, TabsTrigger } from '@/components/ui/tabs'
    import { UsersTab } from './tabs/UsersTab'
    import { AlertThresholdsTab } from './tabs/AlertThresholdsTab'
    import { NotificationsTab } from './tabs/NotificationsTab'
    import { GpsProvidersTab } from './tabs/GpsProvidersTab'

    export function ConfiguracoesPage() {
      return (
        <div className="space-y-5">
          <header className="pb-4">
            <h1 className="text-2xl font-bold text-white">Configurações</h1>
            <p className="text-sm text-white/70">Usuários, regras de alerta, notificações e integrações</p>
          </header>

          <Tabs defaultValue="users" className="space-y-4">
            <TabsList>
              <TabsTrigger value="users">Usuários</TabsTrigger>
              <TabsTrigger value="thresholds">Alertas</TabsTrigger>
              <TabsTrigger value="notifications">Notificações</TabsTrigger>
              <TabsTrigger value="gps">Integrações GPS</TabsTrigger>
            </TabsList>
            <TabsContent value="users"><UsersTab /></TabsContent>
            <TabsContent value="thresholds"><AlertThresholdsTab /></TabsContent>
            <TabsContent value="notifications"><NotificationsTab /></TabsContent>
            <TabsContent value="gps"><GpsProvidersTab /></TabsContent>
          </Tabs>
        </div>
      )
    }
    ```

    **File 2 — UsersTab.tsx**:
    Hooks: `useUsers()`, `useCreateUser()`, `useUpdateUser()`, `useDeactivateUser()`. Use `DataTable` with columns: name, email, role (Badge), isActive (Badge "ativo"/"inativo"), createdAt (formatted date), actions ("Editar", "Desativar").

    Add toolbar with "Novo usuário" button. Use shadcn `Dialog` if exists (check). If Dialog not present, use inline form below table. RHF + Zod for create/edit:
    ```typescript
    const createSchema = z.object({
      name:     z.string().min(1).max(100),
      email:    z.string().email(),
      role:     z.enum(['admin','supervisor','analyst','viewer']),
      password: z.string().min(6),
    })
    type CreateFormData = z.infer<typeof createSchema>
    ```

    onSubmit calls `createMutation.mutate(data)` → on success closes dialog. Edit uses smaller schema (no password). Desativar calls `deactivate.mutate(id)`. Display error from mutation in form via Sonner toast (if exists) OR alert state.

    **File 3 — AlertThresholdsTab.tsx** (RHF + Zod, RESEARCH lines 1037-1071):
    ```typescript
    import { useForm } from 'react-hook-form'
    import { zodResolver } from '@hookform/resolvers/zod'
    import { z } from 'zod'
    import { useEffect } from 'react'
    import { Form, FormControl, FormField, FormItem, FormLabel, FormMessage } from '@/components/ui/form'
    import { Input } from '@/components/ui/input'
    import { Button } from '@/components/ui/button'
    import { Card } from '@/components/ui/card'
    import { useThresholds, useUpdateThreshold } from '@/hooks/useThresholds'

    const schema = z.object({
      atrasoCriticoMinutes: z.coerce.number().int().positive().max(300),
      desvioKmThreshold:    z.coerce.number().positive().max(50),
      stopDurationMinutes:  z.coerce.number().int().positive().max(120),
    })
    type FormData = z.infer<typeof schema>

    export function AlertThresholdsTab() {
      const { data, isLoading } = useThresholds()
      const updateMutation = useUpdateThreshold()

      const form = useForm<FormData>({
        resolver: zodResolver(schema),
        defaultValues: { atrasoCriticoMinutes: 30, desvioKmThreshold: 2, stopDurationMinutes: 15 },
      })

      // Sync form with server data when loaded
      useEffect(() => {
        if (!isLoading && data) {
          form.reset({
            atrasoCriticoMinutes: data['atraso_critico_minutes'] ?? 30,
            desvioKmThreshold:    data['desvio_km_threshold'] ?? 2,
            stopDurationMinutes:  data['stop_duration_minutes'] ?? 15,
          })
        }
      }, [data, isLoading])

      async function onSubmit(values: FormData) {
        await Promise.all([
          updateMutation.mutateAsync({ type: 'atraso_critico_minutes', value: values.atrasoCriticoMinutes }),
          updateMutation.mutateAsync({ type: 'desvio_km_threshold',    value: values.desvioKmThreshold }),
          updateMutation.mutateAsync({ type: 'stop_duration_minutes',  value: values.stopDurationMinutes }),
        ])
      }

      return (
        <Card className="p-5 bg-card">
          <header className="mb-4">
            <h3 className="text-sm font-semibold text-foreground">Limiares de Alerta</h3>
            <p className="text-xs text-muted-foreground">Configurados globalmente. Apenas admin pode salvar.</p>
          </header>
          <Form {...form}>
            <form onSubmit={form.handleSubmit(onSubmit)} className="grid gap-4 sm:grid-cols-3">
              <FormField control={form.control} name="atrasoCriticoMinutes" render={({ field }) => (
                <FormItem><FormLabel>Atraso crítico (min)</FormLabel><FormControl><Input type="number" {...field} /></FormControl><FormMessage /></FormItem>
              )} />
              <FormField control={form.control} name="desvioKmThreshold" render={({ field }) => (
                <FormItem><FormLabel>Desvio (km)</FormLabel><FormControl><Input type="number" step="0.1" {...field} /></FormControl><FormMessage /></FormItem>
              )} />
              <FormField control={form.control} name="stopDurationMinutes" render={({ field }) => (
                <FormItem><FormLabel>Tempo parado (min)</FormLabel><FormControl><Input type="number" {...field} /></FormControl><FormMessage /></FormItem>
              )} />
              <div className="sm:col-span-3 flex justify-end">
                <Button type="submit" disabled={form.formState.isSubmitting || updateMutation.isPending}>Salvar</Button>
              </div>
            </form>
          </Form>
        </Card>
      )
    }
    ```

    **File 4 — NotificationsTab.tsx**:
    Combines push opt-in + per-severity prefs. Hooks: `usePushSubscription()`, `useUpdateMyPreferences()`. Need current user prefs — read from `useUsers().data` filtered by current user OR via new `useMe()` hook (use existing auth store if available).
    ```typescript
    import { useState } from 'react'
    import { Card } from '@/components/ui/card'
    import { Button } from '@/components/ui/button'
    import { Checkbox } from '@/components/ui/checkbox'
    import { usePushSubscription } from '@/hooks/usePushSubscription'
    import { useUpdateMyPreferences } from '@/hooks/useUsers'
    // For current user prefs, may import from useAuthStore if exists; otherwise fetch /api/auth/me

    export function NotificationsTab() {
      const push = usePushSubscription()
      const updatePrefs = useUpdateMyPreferences()
      const [prefs, setPrefs] = useState<{ critico: boolean; medio: boolean; baixo: boolean }>({ critico: true, medio: false, baixo: false })

      // Optional: load current prefs from /api/auth/me (if hook exists) on mount

      async function savePrefs() {
        await updatePrefs.mutateAsync(prefs)
      }

      const statusLabel: Record<typeof push.status, string> = {
        idle:        'Não ativadas',
        enabling:    'Ativando...',
        enabled:     'Ativadas',
        denied:      'Permissão negada — habilite nas configurações do navegador',
        unsupported: 'Não suportado neste navegador',
        error:       'Erro ao ativar',
      }

      return (
        <Card className="p-5 bg-card space-y-6">
          <section>
            <header className="mb-3">
              <h3 className="text-sm font-semibold text-foreground">Notificações no navegador</h3>
              <p className="text-xs text-muted-foreground">Receba alertas críticos mesmo com a aba em background.</p>
            </header>
            <div className="flex items-center gap-3">
              <Button onClick={push.enablePush} disabled={push.status === 'enabling' || push.status === 'enabled' || push.status === 'unsupported'}>
                {push.status === 'enabled' ? 'Notificações Ativadas' : 'Ativar Notificações'}
              </Button>
              {push.status === 'enabled' && (
                <Button variant="outline" onClick={push.disablePush}>Desativar</Button>
              )}
              <span className="text-xs text-muted-foreground">{statusLabel[push.status]}</span>
            </div>
            {push.error && <p className="mt-2 text-xs text-destructive">{push.error}</p>}
            <p className="mt-3 text-xs text-muted-foreground">No iOS Safari, instale como App na tela inicial para receber notificações (Web Push API limitation).</p>
          </section>

          <section>
            <header className="mb-3">
              <h3 className="text-sm font-semibold text-foreground">Preferências por severidade</h3>
              <p className="text-xs text-muted-foreground">Escolha quais severidades disparam notificações.</p>
            </header>
            <div className="space-y-2">
              <label className="flex items-center gap-2 text-sm">
                <Checkbox checked={prefs.critico} onCheckedChange={(v) => setPrefs(p => ({ ...p, critico: Boolean(v) }))} />
                <span className="text-foreground">Críticos</span>
              </label>
              <label className="flex items-center gap-2 text-sm">
                <Checkbox checked={prefs.medio} onCheckedChange={(v) => setPrefs(p => ({ ...p, medio: Boolean(v) }))} />
                <span className="text-foreground">Médios</span>
              </label>
              <label className="flex items-center gap-2 text-sm">
                <Checkbox checked={prefs.baixo} onCheckedChange={(v) => setPrefs(p => ({ ...p, baixo: Boolean(v) }))} />
                <span className="text-foreground">Baixos</span>
              </label>
            </div>
            <Button className="mt-4" onClick={savePrefs} disabled={updatePrefs.isPending}>Salvar preferências</Button>
          </section>
        </Card>
      )
    }
    ```

    Se shadcn `Checkbox` não existir, instalar: `npx shadcn@latest add checkbox`. Executor verifica.

    **File 5 — GpsProvidersTab.tsx**:
    Pattern from UsersTab simplified. Hooks `useGpsProviders/useCreateGpsProvider/useUpdateGpsProvider/useDeleteGpsProvider`. DataTable: name, baseUrl, isActive, createdAt, actions. Form (RHF + Zod):
    ```typescript
    const schema = z.object({
      name:     z.string().min(1).max(100),
      baseUrl:  z.string().url().or(z.literal('')).optional(),
      apiKey:   z.string().optional(),
      isActive: z.boolean().default(true),
    })
    ```
    Display warning banner: "Este formulário registra a configuração mas não conecta efetivamente com o provider — integração futura (Phase 7+)."
  </action>
  <verify>
    <automated>
      cd "C:/Users/antonio.magalhaes/Documents/Projetos/argon-dashboard/.claude/worktrees/elastic-napier-5559df" && \
      ! grep -q "Construction\|Disponível em Phase 6" torre-de-controle/src/app/pages/configuracoes/ConfiguracoesPage.tsx && \
      grep -q "<Tabs defaultValue=\"users\">" torre-de-controle/src/app/pages/configuracoes/ConfiguracoesPage.tsx && \
      grep -q "UsersTab" torre-de-controle/src/app/pages/configuracoes/ConfiguracoesPage.tsx && \
      grep -q "AlertThresholdsTab" torre-de-controle/src/app/pages/configuracoes/ConfiguracoesPage.tsx && \
      grep -q "NotificationsTab" torre-de-controle/src/app/pages/configuracoes/ConfiguracoesPage.tsx && \
      grep -q "GpsProvidersTab" torre-de-controle/src/app/pages/configuracoes/ConfiguracoesPage.tsx && \
      grep -q "zodResolver" torre-de-controle/src/app/pages/configuracoes/tabs/AlertThresholdsTab.tsx && \
      grep -q "useThresholds" torre-de-controle/src/app/pages/configuracoes/tabs/AlertThresholdsTab.tsx && \
      grep -q "useUpdateThreshold" torre-de-controle/src/app/pages/configuracoes/tabs/AlertThresholdsTab.tsx && \
      grep -q "usePushSubscription" torre-de-controle/src/app/pages/configuracoes/tabs/NotificationsTab.tsx && \
      grep -q "enablePush" torre-de-controle/src/app/pages/configuracoes/tabs/NotificationsTab.tsx && \
      grep -q "useUsers" torre-de-controle/src/app/pages/configuracoes/tabs/UsersTab.tsx && \
      grep -q "useGpsProviders" torre-de-controle/src/app/pages/configuracoes/tabs/GpsProvidersTab.tsx && \
      cd torre-de-controle && npx tsc --noEmit && npm run build
    </automated>
  </verify>
  <acceptance_criteria>
    - ConfiguracoesPage stub fully replaced with 4 tabs
    - UsersTab renders DataTable + create/edit/deactivate flow
    - AlertThresholdsTab uses RHF + zodResolver with 3 numeric fields, calls useUpdateThreshold for each
    - NotificationsTab has Ativar/Desativar button reflecting push.status; severity checkboxes; Save button calls useUpdateMyPreferences
    - GpsProvidersTab renders providers + form CRUD with stub warning banner
    - All 5 files compile; tsc --noEmit + build pass
  </acceptance_criteria>
  <done>
    Página Configurações operacional. Admin CRUDa users, edita thresholds (afeta alert engine via 60s cache invalidation), Notifications tab habilita push end-to-end.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /api/users/* (admin only) | Frontend doesn't enforce — backend requireRole('admin') is the source of truth |
| Browser → /api/push/subscribe | Subscription endpoint stored in DB tied to current user (server JWT) |
| Service Worker → Push services | Browser-native delivery channel; SW receives via push event |
| Notification click → window navigation | URL from payload.data.url, controlled by backend dispatcher |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.06-01 | Spoofing | usePushSubscription bypass (subscribe other user) | mitigate | Backend authGuard derives userId from JWT cookie; body only carries subscription details — no userId in client payload (06-04 plan threat T-06.04-01 covers) |
| T-06.06-02 | Tampering | SW scope mismatch | mitigate | Explicit `{ scope: '/' }` in register call (Pitfall #2). SW file at `public/sw.js` (root). |
| T-06.06-03 | XSS via notification body | Notification payload body rendered by browser | mitigate | Browser native notification doesn't execute HTML/JS. Payload is text-only. |
| T-06.06-04 | Open redirect | notificationclick.url from arbitrary input | mitigate | URL constructed by backend dispatcher (Plan 04) as `/alertas/${alert.id}` literal — no user-provided URL. SW openWindow trusts payload.data.url; document that backend MUST validate. |
| T-06.06-05 | Information disclosure | Notifications visible on lock screen | accept | Operating system behavior; user opts in explicitly. Sensitive PII (lat/lng) NOT in notification body — only alert title + description. |
| T-06.06-06 | Information disclosure | UsersTab exposes email of all users | accept | Admin role only (server-enforced). Internal operational data. |
| T-06.06-07 | Tampering | UpdateThreshold race condition | mitigate | In-memory cache invalidation post-update (06-03 plan); 60s window where alert engine might use stale value. Risk acceptable — thresholds change rarely |
| T-06.06-08 | XSS | DataTable cells (driver names, route codes) | mitigate | React JSX auto-escapes |
</threat_model>

<verification>
After both tasks:
1. `npx tsc --noEmit` passes 0 errors
2. `npm run build` succeeds — proves bundle composes with new deps (RHF, zod, sentry, web-push types)
3. (Manual smoke in final plan) Visit /configuracoes — all 4 tabs render, AlertThresholdsTab form submits, NotificationsTab subscribe button works in dev (without VAPID env → shows error)
4. Verify `public/sw.js` is in build output `dist/sw.js` (Vite copies public/* as-is)
</verification>

<success_criteria>
- ConfiguracoesPage stub replaced
- 4 tabs functional, hooks wired
- Push opt-in via Service Worker + subscribe API
- Threshold updates trigger backend cache invalidation
- All Type-checks + builds pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-insights-polish-deploy/06-06-SUMMARY.md` documenting page structure, push flow, RHF schemas, RBAC enforcement points.
</output>
