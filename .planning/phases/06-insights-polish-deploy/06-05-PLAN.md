---
phase: 06-insights-polish-deploy
plan: 05
type: execute
wave: 3
depends_on: [06-04]
files_modified:
  - torre-de-controle/src/hooks/useInsights.ts
  - torre-de-controle/src/app/pages/insights/InsightsPage.tsx
  - torre-de-controle/src/app/pages/insights/components/DateRangePicker.tsx
  - torre-de-controle/src/app/pages/insights/components/SlaHistoricoChart.tsx
  - torre-de-controle/src/app/pages/insights/components/MotoristasRankingChart.tsx
  - torre-de-controle/src/app/pages/insights/components/RotasProblematicasTable.tsx
  - torre-de-controle/src/app/pages/insights/components/AlertasDistribuicaoChart.tsx
autonomous: true
requirements:
  - PHASE6-INSIGHTS-PAGE
  - PHASE6-INSIGHTS-CHARTS
  - PHASE6-INSIGHTS-DRILL-DOWN
user_setup: []
must_haves:
  truths:
    - "Página /insights renderiza 4 cards: SLA histórico (line chart), Ranking motoristas (bar chart), Rotas problemáticas (table), Distribuição alertas (donut chart)"
    - "DateRangePicker apresenta presets 7d / 30d (default) / 90d e propaga para os 4 cards"
    - "Range persistido em URL query string (?range=30d) via useSearchParams (D-02)"
    - "Click em ponto SLA histórico atualiza dateFilter state e propaga para outros 3 cards (cross-filter D-04)"
    - "Click em motorista navega para /motoristas/:id (D-05)"
    - "Click em rota navega para /viagens?route=ROUTE_CODE (D-05)"
    - "Charts re-mount em troca de tema via key={isDark} (Phase 1b pattern)"
    - "Hooks useSlaHistory/useDriversRanking/useProblematicRoutes/useAlertsDistribution todos com staleTime 30s (D-29)"
    - "Grid responsivo: lg:grid-cols-2 xl:grid-cols-4 (D-25 stack vertical em <1280px)"
    - "Hook contract { data, isLoading, isError, error, refetch } (Phase 1b convention)"
  artifacts:
    - path: "torre-de-controle/src/hooks/useInsights.ts"
      provides: "useSlaHistory, useDriversRanking, useProblematicRoutes, useAlertsDistribution (4 sub-hooks)"
      exports: ["useSlaHistory", "useDriversRanking", "useProblematicRoutes", "useAlertsDistribution"]
    - path: "torre-de-controle/src/app/pages/insights/InsightsPage.tsx"
      provides: "Page com state range + dateFilter + 4 cards no grid"
      exports: ["InsightsPage"]
    - path: "torre-de-controle/src/app/pages/insights/components/DateRangePicker.tsx"
      provides: "Toolbar com Select preset (7d/30d/90d)"
      exports: ["DateRangePicker"]
    - path: "torre-de-controle/src/app/pages/insights/components/SlaHistoricoChart.tsx"
      provides: "Line chart com onPointClick"
      exports: ["SlaHistoricoChart"]
    - path: "torre-de-controle/src/app/pages/insights/components/MotoristasRankingChart.tsx"
      provides: "Bar chart com onBarClick navegando /motoristas/:id"
      exports: ["MotoristasRankingChart"]
    - path: "torre-de-controle/src/app/pages/insights/components/RotasProblematicasTable.tsx"
      provides: "Table com rotas + click navega /viagens?route="
      exports: ["RotasProblematicasTable"]
    - path: "torre-de-controle/src/app/pages/insights/components/AlertasDistribuicaoChart.tsx"
      provides: "Doughnut chart"
      exports: ["AlertasDistribuicaoChart"]
  key_links:
    - from: "torre-de-controle/src/app/pages/insights/InsightsPage.tsx"
      to: "useSlaHistory/useDriversRanking/useProblematicRoutes/useAlertsDistribution"
      via: "useInsights hooks import + render"
      pattern: "useSlaHistory"
    - from: "Insights charts"
      to: "useThemeStore (isDark)"
      via: "key={isDark} force re-mount on theme switch (Phase 1b pattern)"
      pattern: "key=\\{`\\$\\{isDark\\}"
    - from: "SlaHistoricoChart"
      to: "InsightsPage dateFilter state"
      via: "onPointClick prop → setDateFilter (cross-filter D-04)"
      pattern: "onPointClick"
    - from: "MotoristasRankingChart / RotasProblematicasTable"
      to: "react-router-dom useNavigate"
      via: "navigate('/motoristas/' + driverId) / navigate('/viagens?route=' + code)"
      pattern: "useNavigate"
---

<objective>
Wave 3 frontend: implementar página `/insights` completa com 4 cards Chart.js + cross-filter + drill-down navigation. Substitui o stub Construction placeholder atual (torre-de-controle/src/app/pages/insights/InsightsPage.tsx).

Purpose: Operador acessa analytics histórico (SLA, ranking, rotas, distribuição alertas) via /insights. Clica num dia ruim → outros 3 cards filtram. Clica num motorista → navega para detalhe. Clica numa rota → vai para viagens daquela rota.

Output: 1 hook composto + 1 page + 5 componentes de chart/table/picker.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-insights-polish-deploy/06-CONTEXT.md
@.planning/phases/06-insights-polish-deploy/06-RESEARCH.md
@.planning/phases/06-insights-polish-deploy/06-PATTERNS.md
@.planning/phases/06-insights-polish-deploy/06-04-PLAN.md
@torre-de-controle/src/hooks/useDashboardKPIs.ts
@torre-de-controle/src/hooks/useTrips.ts
@torre-de-controle/src/components/domain/SparklineChart.tsx
@torre-de-controle/src/components/domain/DataTable.tsx
@torre-de-controle/src/app/pages/alertas/AlertasPage.tsx
@torre-de-controle/src/app/pages/motoristas/components/MotoristasTable.tsx
@torre-de-controle/src/app/pages/insights/InsightsPage.tsx
@torre-de-controle/src/stores/useThemeStore.ts
@torre-de-controle/src/lib/api.ts

<interfaces>
<!-- TanStack Query hook pattern (torre-de-controle/src/hooks/useDashboardKPIs.ts) -->
```typescript
export function useDashboardKPIs() {
  const q = useQuery({
    queryKey: ['dashboard-kpis'],
    queryFn: async () => {
      const { data, error } = await api.api.dashboard.kpis.get()
      if (error) throw new Error('...')
      return data
    },
    staleTime: 30_000,
  })
  return { data: q.data ?? defaultValue, isLoading: q.isLoading, isError: q.isError, error: q.error, refetch: q.refetch }
}
```

<!-- Theme switching pattern (torre-de-controle/src/components/domain/SparklineChart.tsx) -->
```typescript
import { useThemeStore } from '@/stores/useThemeStore'
const { isDark } = useThemeStore()
<Line key={`${isDark}-${lineColor}`} ... />
```

<!-- Chart.js components to register -->
```typescript
// Line chart
import { Line } from 'react-chartjs-2'
import { Chart as ChartJS, CategoryScale, LinearScale, PointElement, LineElement, Filler, Tooltip, Legend } from 'chart.js'
ChartJS.register(CategoryScale, LinearScale, PointElement, LineElement, Filler, Tooltip, Legend)

// Bar chart
import { Bar } from 'react-chartjs-2'
import { Chart as ChartJS, BarElement, CategoryScale, LinearScale, Tooltip, Legend } from 'chart.js'

// Doughnut
import { Doughnut } from 'react-chartjs-2'
import { Chart as ChartJS, ArcElement, Tooltip, Legend } from 'chart.js'
```

<!-- Chart.js onClick element extraction -->
```typescript
onClick: (_, elements, chart) => {
  if (elements.length) {
    const i = elements[0].index
    onPointClick?.(labels[i])
  }
}
```

<!-- Page layout / header (torre-de-controle/src/app/pages/alertas/AlertasPage.tsx) -->
```typescript
<header className="pb-4">
  <h1 className="text-2xl font-bold text-white">Insights</h1>
  <p className="text-sm text-white/70">...</p>
</header>
```

<!-- API endpoint shapes (from 06-02-PLAN insights service) -->
```typescript
// GET /api/insights/sla-history → [{ date: string, total: number, onTime: number, sla: number }]
// GET /api/insights/drivers-ranking → [{ driverId, name, code, score, slaPercent, avgDelayMin, totalTrips }]
// GET /api/insights/problematic-routes → [{ routeId, code, name, alerts, avgDelay, slaPercent }]
// GET /api/insights/alerts-distribution → [{ type: string, count: number }]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: useInsights composite hook (4 sub-hooks) + DateRangePicker component</name>
  <files>
    torre-de-controle/src/hooks/useInsights.ts,
    torre-de-controle/src/app/pages/insights/components/DateRangePicker.tsx
  </files>
  <read_first>
    - torre-de-controle/src/hooks/useDashboardKPIs.ts (queryFn + hook contract — copy shape)
    - torre-de-controle/src/hooks/useGeofences.ts (Eden Treaty `(api.api.geofences as any).post(...)` pattern for typed endpoints)
    - torre-de-controle/src/lib/api.ts (Eden Treaty `api` import path)
    - torre-de-controle/src/components/ui/select.tsx (if exists — for preset selector)
    - 06-PATTERNS.md lines 992-1041 (useInsights hooks pattern)
    - 06-CONTEXT.md D-02 (range presets, URL persistence opcional)
    - 06-CONTEXT.md D-29 (staleTime 30s)
  </read_first>
  <action>
    **File 1 — torre-de-controle/src/hooks/useInsights.ts**:
    ```typescript
    import { useQuery } from '@tanstack/react-query'
    import { api } from '@/lib/api'

    type Range = '7d' | '30d' | '90d'

    type SlaPoint = { date: string; total: number; onTime: number; sla: number }
    type DriverRank = { driverId: string; name: string; code?: string | null; score: number; slaPercent: number; avgDelayMin: number | null; totalTrips: number }
    type ProblematicRoute = { routeId: string; code: string; name: string; alerts: number; avgDelay: number | null; slaPercent: number }
    type AlertDist = { type: string; count: number }

    function emptyOnError<T>(d: T): T { return d }

    export function useSlaHistory(range: Range = '30d') {
      const q = useQuery({
        queryKey: ['insights', 'sla-history', range],
        queryFn: async (): Promise<SlaPoint[]> => {
          const { data, error } = await (api.api.insights as any)['sla-history'].get({ query: { range } })
          if (error) throw new Error('Failed to fetch SLA history')
          return (data ?? []) as SlaPoint[]
        },
        staleTime: 30_000,
      })
      return { data: q.data ?? [], isLoading: q.isLoading, isError: q.isError, error: q.error, refetch: q.refetch }
    }

    export function useDriversRanking(range: Range = '30d', limit: number = 10) {
      const q = useQuery({
        queryKey: ['insights', 'drivers-ranking', range, limit],
        queryFn: async (): Promise<DriverRank[]> => {
          const { data, error } = await (api.api.insights as any)['drivers-ranking'].get({ query: { range, limit } })
          if (error) throw new Error('Failed to fetch drivers ranking')
          return (data ?? []) as DriverRank[]
        },
        staleTime: 30_000,
      })
      return { data: q.data ?? [], isLoading: q.isLoading, isError: q.isError, error: q.error, refetch: q.refetch }
    }

    export function useProblematicRoutes(range: Range = '30d') {
      const q = useQuery({
        queryKey: ['insights', 'problematic-routes', range],
        queryFn: async (): Promise<ProblematicRoute[]> => {
          const { data, error } = await (api.api.insights as any)['problematic-routes'].get({ query: { range } })
          if (error) throw new Error('Failed to fetch problematic routes')
          return (data ?? []) as ProblematicRoute[]
        },
        staleTime: 30_000,
      })
      return { data: q.data ?? [], isLoading: q.isLoading, isError: q.isError, error: q.error, refetch: q.refetch }
    }

    export function useAlertsDistribution(range: Range = '30d') {
      const q = useQuery({
        queryKey: ['insights', 'alerts-distribution', range],
        queryFn: async (): Promise<AlertDist[]> => {
          const { data, error } = await (api.api.insights as any)['alerts-distribution'].get({ query: { range } })
          if (error) throw new Error('Failed to fetch alerts distribution')
          return (data ?? []) as AlertDist[]
        },
        staleTime: 30_000,
      })
      return { data: q.data ?? [], isLoading: q.isLoading, isError: q.isError, error: q.error, refetch: q.refetch }
    }

    export type { Range, SlaPoint, DriverRank, ProblematicRoute, AlertDist }
    ```

    Note: usar `(api.api.insights as any)['sla-history']` porque o segmento contém hífen (TypeScript não permite dot-notation com `-`). Eden Treaty resolve via bracket. Cast `as any` aceito por já estar no padrão dos hooks existentes (useGeofences usa mesmo cast).

    **File 2 — torre-de-controle/src/app/pages/insights/components/DateRangePicker.tsx**:
    Verifica existência de `torre-de-controle/src/components/ui/select.tsx` (shadcn). Se existe, usa Select; senão, ToggleGroup (`torre-de-controle/src/components/ui/toggle-group.tsx` se existir) ou native `<select>`.

    Componente:
    ```typescript
    import { type Range } from '@/hooks/useInsights'
    import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select'

    interface Props {
      value: Range
      onChange: (range: Range) => void
    }

    export function DateRangePicker({ value, onChange }: Props) {
      return (
        <Select value={value} onValueChange={(v) => onChange(v as Range)}>
          <SelectTrigger className="w-[140px]">
            <SelectValue />
          </SelectTrigger>
          <SelectContent>
            <SelectItem value="7d">Últimos 7 dias</SelectItem>
            <SelectItem value="30d">Últimos 30 dias</SelectItem>
            <SelectItem value="90d">Últimos 90 dias</SelectItem>
          </SelectContent>
        </Select>
      )
    }
    ```

    Se shadcn Select não existe, executor instala via `npx shadcn@latest add select` antes de criar este componente — adicionar verificação no Step antes do create.
  </action>
  <verify>
    <automated>
      cd "C:/Users/antonio.magalhaes/Documents/Projetos/argon-dashboard/.claude/worktrees/elastic-napier-5559df" && \
      grep -q "useSlaHistory" torre-de-controle/src/hooks/useInsights.ts && \
      grep -q "useDriversRanking" torre-de-controle/src/hooks/useInsights.ts && \
      grep -q "useProblematicRoutes" torre-de-controle/src/hooks/useInsights.ts && \
      grep -q "useAlertsDistribution" torre-de-controle/src/hooks/useInsights.ts && \
      grep -q "staleTime: 30_000" torre-de-controle/src/hooks/useInsights.ts && \
      test -f torre-de-controle/src/app/pages/insights/components/DateRangePicker.tsx && \
      grep -q "DateRangePicker" torre-de-controle/src/app/pages/insights/components/DateRangePicker.tsx && \
      cd torre-de-controle && npx tsc --noEmit
    </automated>
  </verify>
  <acceptance_criteria>
    - 4 hooks exported with consistent contract { data, isLoading, isError, error, refetch }
    - All hooks have staleTime: 30_000
    - QueryKey arrays include 'insights' namespace prefix + endpoint name + range
    - DateRangePicker renders Select with 3 presets and calls onChange with Range type
    - tsc passes 0 errors
  </acceptance_criteria>
  <done>
    Hooks and picker ready for InsightsPage consumption.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 4 chart/table components + InsightsPage assembling cross-filter and drill-down</name>
  <files>
    torre-de-controle/src/app/pages/insights/components/SlaHistoricoChart.tsx,
    torre-de-controle/src/app/pages/insights/components/MotoristasRankingChart.tsx,
    torre-de-controle/src/app/pages/insights/components/RotasProblematicasTable.tsx,
    torre-de-controle/src/app/pages/insights/components/AlertasDistribuicaoChart.tsx,
    torre-de-controle/src/app/pages/insights/InsightsPage.tsx
  </files>
  <read_first>
    - torre-de-controle/src/components/domain/SparklineChart.tsx (Chart.js + theme key pattern — CORE analog)
    - torre-de-controle/src/components/domain/DataTable.tsx (TanStack Table wrapper)
    - torre-de-controle/src/app/pages/motoristas/components/MotoristasTable.tsx (column def lines 39-88)
    - torre-de-controle/src/app/pages/alertas/AlertasPage.tsx (header + filter state pattern)
    - torre-de-controle/src/stores/useThemeStore.ts (isDark flag)
    - torre-de-controle/src/app/pages/insights/InsightsPage.tsx (current stub — to be REPLACED entirely)
    - 06-RESEARCH.md lines 553-612 (SlaHistoricoChart full code)
    - 06-PATTERNS.md lines 1174-1245 (chart components + table mapping)
    - 06-PATTERNS.md lines 1278-1342 (InsightsPage with cross-filter)
    - 06-CONTEXT.md D-04 (cross-filter — click point filters others), D-05 (click motorista/rota navigate)
  </read_first>
  <action>
    **File 1 — torre-de-controle/src/app/pages/insights/components/SlaHistoricoChart.tsx**:
    ```typescript
    import { Line } from 'react-chartjs-2'
    import {
      Chart as ChartJS,
      CategoryScale, LinearScale, PointElement, LineElement, Filler, Tooltip, Legend,
    } from 'chart.js'
    import { useThemeStore } from '@/stores/useThemeStore'
    import { Card } from '@/components/ui/card'
    import type { SlaPoint } from '@/hooks/useInsights'

    ChartJS.register(CategoryScale, LinearScale, PointElement, LineElement, Filler, Tooltip, Legend)

    function cssVar(name: string): string {
      if (typeof window === 'undefined') return '#000'
      return getComputedStyle(document.documentElement).getPropertyValue(name).trim() || '#000'
    }

    interface Props {
      data: SlaPoint[]
      onPointClick?: (date: string) => void
      isLoading?: boolean
    }

    export function SlaHistoricoChart({ data, onPointClick, isLoading }: Props) {
      const { isDark } = useThemeStore()
      const labels = data.map(d => d.date)
      const slaPercent = data.map(d => d.sla * 100)
      const successColor = cssVar('--success') || '#2dce89'

      return (
        <Card className="p-5 bg-card">
          <header className="mb-3 flex items-baseline justify-between">
            <h3 className="text-sm font-semibold text-foreground">SLA Histórico</h3>
            <span className="text-xs text-muted-foreground">% no prazo</span>
          </header>
          <div style={{ height: 260, width: '100%' }}>
            {isLoading ? (
              <div className="flex h-full items-center justify-center text-sm text-muted-foreground">Carregando...</div>
            ) : data.length === 0 ? (
              <div className="flex h-full items-center justify-center text-sm text-muted-foreground">Sem dados no período</div>
            ) : (
              <Line
                key={`${isDark}-sla`}
                data={{
                  labels,
                  datasets: [{
                    label: 'SLA %',
                    data: slaPercent,
                    borderColor: successColor,
                    backgroundColor: successColor + '33',
                    tension: 0.3,
                    fill: true,
                    pointRadius: 4,
                    pointHoverRadius: 6,
                  }],
                }}
                options={{
                  responsive: true,
                  maintainAspectRatio: false,
                  onClick: (_, elements) => {
                    if (elements.length && onPointClick) {
                      onPointClick(labels[elements[0].index])
                    }
                  },
                  plugins: { legend: { display: false } },
                  scales: {
                    y: { min: 0, max: 100, ticks: { color: isDark ? '#fff' : '#000' } },
                    x: { ticks: { color: isDark ? '#fff' : '#000' } },
                  },
                }}
              />
            )}
          </div>
        </Card>
      )
    }
    ```

    **File 2 — torre-de-controle/src/app/pages/insights/components/MotoristasRankingChart.tsx**:
    Pattern: `Bar` (horizontal), labels = driver names, data = slaPercent. Click → navigate `/motoristas/:driverId`. Register `BarElement` (RESEARCH lines 1237-1241).
    ```typescript
    import { Bar } from 'react-chartjs-2'
    import { Chart as ChartJS, BarElement, CategoryScale, LinearScale, Tooltip, Legend } from 'chart.js'
    import { useNavigate } from 'react-router-dom'
    import { useThemeStore } from '@/stores/useThemeStore'
    import { Card } from '@/components/ui/card'
    import type { DriverRank } from '@/hooks/useInsights'

    ChartJS.register(BarElement, CategoryScale, LinearScale, Tooltip, Legend)

    interface Props { data: DriverRank[]; isLoading?: boolean; dateFilter?: string | null }

    export function MotoristasRankingChart({ data, isLoading }: Props) {
      const { isDark } = useThemeStore()
      const navigate = useNavigate()
      // ... render Bar chart, onClick element → navigate('/motoristas/' + data[i].driverId)
    }
    ```
    Card header "Ranking Motoristas — % no prazo".

    **File 3 — torre-de-controle/src/app/pages/insights/components/RotasProblematicasTable.tsx**:
    Reusa DataTable. Columns: code + name (combined cell per PATTERNS lines 1257-1265), alerts (numérico), avgDelay (min), slaPercent (formatted as %).
    ```typescript
    import { useNavigate } from 'react-router-dom'
    import { type ColumnDef } from '@tanstack/react-table'
    import { DataTable } from '@/components/domain/DataTable'
    import { Card } from '@/components/ui/card'
    import type { ProblematicRoute } from '@/hooks/useInsights'

    interface Props { data: ProblematicRoute[]; isLoading?: boolean; dateFilter?: string | null }

    export function RotasProblematicasTable({ data, isLoading }: Props) {
      const navigate = useNavigate()
      const columns: ColumnDef<ProblematicRoute>[] = [
        {
          id: 'route', header: 'Rota', size: 220,
          cell: ({ row }) => (
            <button
              className="text-left cursor-pointer hover:underline"
              onClick={() => navigate(`/viagens?route=${encodeURIComponent(row.original.code)}`)}
            >
              <p className="text-sm font-medium text-foreground">{row.original.code}</p>
              <p className="text-xs text-muted-foreground">{row.original.name}</p>
            </button>
          ),
        },
        { accessorKey: 'alerts', header: 'Alertas', cell: (i) => <span className="tabular-nums">{i.getValue<number>()}</span> },
        { accessorKey: 'avgDelay', header: 'Atraso médio (min)', cell: (i) => <span className="tabular-nums">{i.getValue<number>() ?? '—'}</span> },
        { accessorKey: 'slaPercent', header: 'SLA', cell: (i) => <span className="tabular-nums">{(i.getValue<number>() * 100).toFixed(1)}%</span> },
      ]
      return (
        <Card className="p-5 bg-card">
          <header className="mb-3"><h3 className="text-sm font-semibold text-foreground">Rotas Problemáticas</h3></header>
          {isLoading ? <p className="text-sm text-muted-foreground">Carregando...</p> : <DataTable data={data} columns={columns} pageSize={10} emptyMessage="Sem dados no período." />}
        </Card>
      )
    }
    ```

    **File 4 — torre-de-controle/src/app/pages/insights/components/AlertasDistribuicaoChart.tsx**:
    Doughnut chart, labels = alert types, data = counts. Cores: paleta multi-color usando CSS vars `--danger, --warning, --info, --success` + fallback.
    ```typescript
    import { Doughnut } from 'react-chartjs-2'
    import { Chart as ChartJS, ArcElement, Tooltip, Legend } from 'chart.js'
    import { useThemeStore } from '@/stores/useThemeStore'
    import { Card } from '@/components/ui/card'
    import type { AlertDist } from '@/hooks/useInsights'

    ChartJS.register(ArcElement, Tooltip, Legend)

    const PALETTE = ['#f5365c', '#fb6340', '#fbb140', '#2dce89', '#11cdef', '#5e72e4', '#8965e0', '#f3a4b5']

    interface Props { data: AlertDist[]; isLoading?: boolean; dateFilter?: string | null }

    export function AlertasDistribuicaoChart({ data, isLoading }: Props) {
      const { isDark } = useThemeStore()
      // Map types to friendly names if needed; render Doughnut
      // key={`${isDark}-dist`}
    }
    ```

    **File 5 — torre-de-controle/src/app/pages/insights/InsightsPage.tsx** (FULL REPLACEMENT — remove Construction stub):
    ```typescript
    import { useState } from 'react'
    import { useSearchParams } from 'react-router-dom'
    import { useSlaHistory, useDriversRanking, useProblematicRoutes, useAlertsDistribution, type Range } from '@/hooks/useInsights'
    import { DateRangePicker } from './components/DateRangePicker'
    import { SlaHistoricoChart } from './components/SlaHistoricoChart'
    import { MotoristasRankingChart } from './components/MotoristasRankingChart'
    import { RotasProblematicasTable } from './components/RotasProblematicasTable'
    import { AlertasDistribuicaoChart } from './components/AlertasDistribuicaoChart'

    function isValidRange(s: string | null): s is Range {
      return s === '7d' || s === '30d' || s === '90d'
    }

    export function InsightsPage() {
      const [searchParams, setSearchParams] = useSearchParams()
      const rawRange = searchParams.get('range')
      const range: Range = isValidRange(rawRange) ? rawRange : '30d'
      const [dateFilter, setDateFilter] = useState<string | null>(null)

      const sla        = useSlaHistory(range)
      const ranking    = useDriversRanking(range)
      const routes     = useProblematicRoutes(range)
      const dist       = useAlertsDistribution(range)

      function setRange(r: Range) {
        setSearchParams({ range: r })
        setDateFilter(null) // reset cross-filter on range change
      }

      return (
        <div className="space-y-5">
          <header className="pb-4 flex items-start gap-3">
            <div>
              <h1 className="text-2xl font-bold text-white">Insights</h1>
              <p className="text-sm text-white/70">Analytics, tendências de SLA e ranking operacional</p>
            </div>
            <div className="ml-auto">
              <DateRangePicker value={range} onChange={setRange} />
            </div>
          </header>

          {dateFilter && (
            <div className="flex items-center gap-2 rounded-md border border-border bg-muted px-3 py-2">
              <span className="text-xs text-muted-foreground">Filtrado por:</span>
              <span className="text-xs font-medium text-foreground">{dateFilter}</span>
              <button className="ml-auto text-xs text-primary hover:underline" onClick={() => setDateFilter(null)}>Limpar</button>
            </div>
          )}

          <div className="grid gap-5 lg:grid-cols-2 xl:grid-cols-4">
            <SlaHistoricoChart data={sla.data} isLoading={sla.isLoading} onPointClick={setDateFilter} />
            <MotoristasRankingChart data={ranking.data} isLoading={ranking.isLoading} dateFilter={dateFilter} />
            <RotasProblematicasTable data={routes.data} isLoading={routes.isLoading} dateFilter={dateFilter} />
            <AlertasDistribuicaoChart data={dist.data} isLoading={dist.isLoading} dateFilter={dateFilter} />
          </div>
        </div>
      )
    }
    ```

    Cross-filter D-04 NOTE: dateFilter currently visual indicator only — Insights API doesn't accept day filter param (CONTEXT D-04 trade-off: complexidade vs valor — implementação MVP: setDateFilter armazena, banner mostra "Filtrado por: 2026-05-15", cards individuais decide se aplicam (MotoristasRankingChart pode filtrar localmente pelo dia se data houver, ou simplesmente exibir indicador). RESEARCH lines 1085-1087 confirm: "cross-filter via URL não MVP, state local + visual indicator suficiente". Plan executor pode opcionalmente filtrar arrays localmente — atomic decision.
  </action>
  <verify>
    <automated>
      cd "C:/Users/antonio.magalhaes/Documents/Projetos/argon-dashboard/.claude/worktrees/elastic-napier-5559df" && \
      test -f torre-de-controle/src/app/pages/insights/components/SlaHistoricoChart.tsx && \
      test -f torre-de-controle/src/app/pages/insights/components/MotoristasRankingChart.tsx && \
      test -f torre-de-controle/src/app/pages/insights/components/RotasProblematicasTable.tsx && \
      test -f torre-de-controle/src/app/pages/insights/components/AlertasDistribuicaoChart.tsx && \
      grep -q "useSlaHistory" torre-de-controle/src/app/pages/insights/InsightsPage.tsx && \
      grep -q "dateFilter" torre-de-controle/src/app/pages/insights/InsightsPage.tsx && \
      grep -q "lg:grid-cols-2 xl:grid-cols-4" torre-de-controle/src/app/pages/insights/InsightsPage.tsx && \
      grep -q "useSearchParams" torre-de-controle/src/app/pages/insights/InsightsPage.tsx && \
      ! grep -q "Construction" torre-de-controle/src/app/pages/insights/InsightsPage.tsx && \
      grep -q "key=" torre-de-controle/src/app/pages/insights/components/SlaHistoricoChart.tsx && \
      grep -q "useNavigate" torre-de-controle/src/app/pages/insights/components/MotoristasRankingChart.tsx && \
      grep -q "useNavigate" torre-de-controle/src/app/pages/insights/components/RotasProblematicasTable.tsx && \
      cd torre-de-controle && npx tsc --noEmit && npm run build
    </automated>
  </verify>
  <acceptance_criteria>
    - InsightsPage.tsx Construction stub fully removed
    - InsightsPage renders 4 cards in responsive grid (lg:grid-cols-2 xl:grid-cols-4)
    - range state persisted in URL via useSearchParams (D-02)
    - Each chart component uses `key={\`${isDark}-...\`}` to force re-mount on theme switch
    - SlaHistoricoChart accepts onPointClick prop and fires for clicked element
    - MotoristasRankingChart and RotasProblematicasTable use useNavigate to drill-down to /motoristas/:id and /viagens?route=CODE
    - dateFilter banner appears when set, with "Limpar" action
    - All 4 hooks invoked from InsightsPage with correct range
    - tsc passes 0 errors AND `npm run build` succeeds (proves Vite + Chart.js types reconcile)
  </acceptance_criteria>
  <done>
    Página Insights operacional. Operador escolhe range, clica em ponto SLA → cross-filter ativado, clica em motorista/rota → navega. Charts respondem a theme switch.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /api/insights/* via cookie | authGuard validates session; data is read-only aggregates |
| Chart.js DOM rendering | Tooltip uses textContent (Chart.js default, not innerHTML) |
| URL query string (range) | Validated via isValidRange whitelist |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.05-01 | Tampering | URL ?range= value | mitigate | isValidRange validates literal whitelist; falls back to '30d' if invalid |
| T-06.05-02 | XSS | Chart.js tooltips render driver name / route name | mitigate | Chart.js default tooltips use textContent (not innerHTML); React JSX auto-escapes when names rendered in DataTable columns |
| T-06.05-03 | DoS | Page renders >10k rows (Drivers ranking with no limit) | mitigate | Backend limit=10 default + max=50 (06-02 plugin TypeBox); frontend table pageSize=10 |
| T-06.05-04 | Information disclosure | Aggregated SLA data exposed to all auth users | accept | All authenticated operators legitimately see aggregates per CONTEXT (no per-team filter in scope) |
</threat_model>

<verification>
After both tasks:
1. `cd torre-de-controle && npx tsc --noEmit` passes
2. `npm run build` succeeds — proves Vite + Chart.js + react-chartjs-2 + Tailwind v4 compile
3. (Manual smoke in next plan with running backend) Visit /insights, see 4 cards, switch range, click point — expected behaviors
</verification>

<success_criteria>
- Insights page replaces stub with operational 4-card dashboard
- Range persisted in URL
- Cross-filter visual indicator via dateFilter state
- Drill-down navigation to /motoristas/:id and /viagens?route= works
- Theme switching forces chart re-mount
- All charts respect Loading + Empty states
- tsc + build pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-insights-polish-deploy/06-05-SUMMARY.md` documenting components created, cross-filter approach, drill-down navigation paths.
</output>
