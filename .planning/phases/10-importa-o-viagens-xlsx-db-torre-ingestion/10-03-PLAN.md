---
phase: 10-importa-o-viagens-xlsx-db-torre-ingestion
plan: 03
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - api/src/modules/positions/geocoder.ts
  - api/src/modules/positions/geocoder.test.ts
autonomous: true
requirements: [D-10-01, D-10-08]
user_setup: []

must_haves:
  truths:
    - "Geocoder consulta o geocode_cache ANTES de bater no Nominatim (cache-first)"
    - "Em cache-hit, o geocoder NÃO faz request HTTP ao Nominatim"
    - "Requests ao Nominatim são sequenciais com rate-limit ~1 req/s e User-Agent custom"
    - "Geocode falho é best-effort: retorna geocoded=false + lat/lng null, sem lançar erro"
    - "lat/lng fora de faixa (lat∉[-90,90] ou lng∉[-180,180]) são rejeitados antes de virar resultado válido"
  artifacts:
    - path: "api/src/modules/positions/geocoder.ts"
      provides: "geocodeText(query): GeocodeResult com cache-first + rate-limit + best-effort + validação de faixa"
      exports: ["geocodeText", "GeocodeResult"]
    - path: "api/src/modules/positions/geocoder.test.ts"
      provides: "Testes: cache-hit não-HTTP, validação de faixa, best-effort em falha"
  key_links:
    - from: "api/src/modules/positions/geocoder.ts"
      to: "geocode_cache (DB Torre)"
      via: "SELECT por query antes do fetch; INSERT após resolver (ON CONFLICT DO NOTHING)"
      pattern: "geocodeCache"
    - from: "api/src/modules/positions/geocoder.ts"
      to: "nominatim.openstreetmap.org"
      via: "fetch com header User-Agent + rate-limit sequencial 1/s"
      pattern: "User-Agent"
---

<objective>
Cria o módulo de geocoding (D-10-01 + D-10-08): recebe o texto livre da Posição, consulta `geocode_cache` PRIMEIRO (cache-first), e só em miss bate no Nominatim/OSM — sequencial, rate-limited ~1 req/s, com User-Agent custom (ToS, T3). Extrai cidade/UF/lat/lng da resposta estruturada (countrycodes=br, limit=1, addressdetails=1), valida faixa de lat/lng (T4), grava no cache (incl. miss), e é best-effort: falha de geocode NÃO derruba o import (retorna geocoded=false + null).

Purpose: Isola a parte lenta e sujeita a ToS/falha. ~125 rows ≈ 2min no 1º import (rate-limit), ~0 no 2º (tudo em cache). O endpoint (Plan 04) chama `geocodeText(posicaoRaw)` por linha e usa o resultado p/ preencher cidade/uf/lat/lng/geom (ou geom NULL se geocoded=false).
Output: `geocoder.ts` + teste; consome a tabela `geocode_cache` (Plan 01).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/10-importa-o-viagens-xlsx-db-torre-ingestion/10-CONTEXT.md

<interfaces>
<!-- Depende do Plan 01: tabela geocode_cache (query PK, lat, lng, cidade, uf, display_name, provider, created_at). -->

DB client do Torre (api/src/db/client.ts) — postgres.js + Drizzle:
  import { db } from '../../db/client'
  import { geocodeCache } from '../../db/schema'   // exportado pelo barrel (Plan 01)
  import { eq } from 'drizzle-orm'
  // SELECT: await db.select().from(geocodeCache).where(eq(geocodeCache.query, q))
  // INSERT idempotente: await db.insert(geocodeCache).values({...}).onConflictDoNothing()

Nominatim (10-CONTEXT canonical_refs):
  GET https://nominatim.openstreetmap.org/search?q=<texto>&format=json&countrycodes=br&limit=1&addressdetails=1
  Header OBRIGATÓRIO: 'User-Agent': 'TorreDeControle/1.0 (contato interno Lamonica)'  // ToS T3
  Resposta: array; item[0] = { lat, lon, display_name, address: { city|town|village|municipality, state, ... } }
  UF: do address.state (nome do estado) → mapear p/ sigla 2-letras, OU usar address['ISO3166-2-lvl4'] (ex. "BR-BA") → sufixo. Cidade: address.city ?? town ?? village ?? municipality.

Rate-limit sequencial: um módulo-singleton que serializa chamadas (fila/await encadeado) garantindo >=1000ms entre requests HTTP ao Nominatim. Cache-hit NÃO consome o slot de rate-limit.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Módulo geocoder.ts (cache-first + Nominatim rate-limited + best-effort)</name>
  <files>api/src/modules/positions/geocoder.ts</files>
  <read_first>
    - .planning/phases/10-importa-o-viagens-xlsx-db-torre-ingestion/10-CONTEXT.md (D-10-01 geocoding+cache+rate-limit+best-effort; D-10-08 geocode_cache; T3/T4 do threat_model)
    - api/src/db/schema/geocode-cache.ts (colunas exatas — produzido no Plan 01)
    - api/src/db/client.ts (db + postgres.js; padrão de import do schema)
    - api/src/index.ts (linhas ~209-218 — padrão de uso do db.insert/db.execute no Torre; estilo async/erro)
  </read_first>
  <behavior>
    - geocodeText('POSTO J REIS - ENTRE RIOS BA') com cache contendo essa query → retorna { geocoded:true, lat, lng, cidade, uf } SEM chamar fetch (verificável por spy/mock em fetch: 0 chamadas).
    - geocodeText(query nova) com fetch mockado retornando lat=-11.94, lon=-38.06, address.state='Bahia' → retorna geocoded:true, uf='BA', cidade preenchida, lat/lng numéricos; grava no geocode_cache.
    - fetch mockado retornando [] (sem match) → geocoded:false, lat/lng null; grava miss no cache (não re-bate no 2º call).
    - fetch que lança (rede) → geocoded:false, lat/lng null, NÃO propaga throw (best-effort).
    - Resposta com lat=999 (fora de faixa) → tratada como miss (geocoded:false), nunca produz coordenada inválida (T4).
  </behavior>
  <action>
Cria `api/src/modules/positions/geocoder.ts`:
- Exporta `interface GeocodeResult { geocoded: boolean; lat: number | null; lng: number | null; cidade: string | null; uf: string | null; displayName: string | null }`.
- Exporta `async function geocodeText(query: string): Promise<GeocodeResult>`:
  1. Normaliza a query (trim). Se vazia → retorna result vazio geocoded:false sem tocar cache/rede.
  2. **Cache-first:** `db.select().from(geocodeCache).where(eq(geocodeCache.query, q))`. Se hit → monta GeocodeResult a partir da row (geocoded = lat/lng não-null) e RETORNA (sem fetch). Cobre D-10-08 + T3 (não bate no Nominatim).
  3. **Miss:** chama `await rateLimitedFetch(url)` (ver rate-limiter abaixo) com a URL Nominatim (`format=json&countrycodes=br&limit=1&addressdetails=1`, query URL-encoded) e header `User-Agent` custom (T3). Best-effort: tudo dentro de try/catch; qualquer erro/timeout → trata como miss.
  4. Parse: se array vazio → miss. Senão extrai lat=`parseFloat(item.lat)`, lng=`parseFloat(item.lon)`, cidade (city??town??village??municipality), uf (do address.state mapeado p/ sigla, ou sufixo de `ISO3166-2-lvl4`), display_name.
  5. **Validação de faixa (T4):** se lat ∉ [-90,90] ou lng ∉ [-180,180] ou NaN → degrada p/ miss (geocoded:false, lat/lng null). Coordenada inválida NUNCA vira resultado válido.
  6. **Grava cache** (hit OU miss): `db.insert(geocodeCache).values({ query: q, lat, lng, cidade, uf, displayName, provider: 'nominatim' }).onConflictDoNothing()` — idempotente, inclusive miss (lat/lng null) p/ não re-bater (D-10-08).
  7. Retorna o GeocodeResult.
- Rate-limiter: implementa um singleton module-level que serializa as chamadas HTTP ao Nominatim garantindo >=1000ms entre requests (ex.: encadeia uma `Promise` `lastCall` + `await sleep(gap)`); o cache-hit (passo 2) NÃO passa pelo rate-limiter. Sequencial (D-10-01, T3 — sem bulk paralelo).
NÃO logue o conteúdo das queries/coords (T5) além de contadores/erro genérico. `process.env` p/ User-Agent opcional, mas com fallback literal embutido (não precisa de user-setup).
  </action>
  <verify>
    <automated>cd api && bunx tsc --noEmit 2>&1 | grep -v '^#' | grep -c "error TS" | grep -q '^0$' && grep -q "User-Agent" src/modules/positions/geocoder.ts && echo "OK tsc + User-Agent present"</automated>
  </verify>
  <acceptance_criteria>
    - `geocoder.ts` exporta `geocodeText` e `GeocodeResult`.
    - Cache-first: SELECT em `geocodeCache` antes de qualquer fetch (grep `geocodeCache` + ordem no código).
    - Header `User-Agent` presente no fetch ao Nominatim; URL com `countrycodes=br&limit=1&addressdetails=1`.
    - Rate-limiter serializa HTTP com gap >=1000ms; cache-hit fora do rate-limiter.
    - Validação lat∈[-90,90]/lng∈[-180,180]; inválido → miss.
    - best-effort: nenhum caminho lança erro pra fora de geocodeText.
    - `bunx tsc --noEmit` exit 0.
  </acceptance_criteria>
  <done>Geocoder cache-first + rate-limited + best-effort + range-validated compila; consome geocode_cache.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Teste do geocoder (cache-hit sem HTTP, faixa, best-effort)</name>
  <files>api/src/modules/positions/geocoder.test.ts</files>
  <read_first>
    - api/src/modules/positions/geocoder.ts (assinaturas — Task 1)
    - api/src/modules/ranking/ranking.scoring.test.ts (padrão bun:test do projeto)
  </read_first>
  <behavior>
    - Com `db.select` mockado p/ retornar uma row de cache e `globalThis.fetch` espionado: geocodeText(query) retorna geocoded:true da row e fetch foi chamado 0 vezes (cache-first provado).
    - Com cache vazio + fetch mock retornando item lat=-11.94/lon=-38.06/state='Bahia': geocoded:true, uf==='BA', lat≈-11.94; e db.insert chamado (cache write).
    - Com cache vazio + fetch mock retornando lat='999': geocoded:false, lat null (validação de faixa T4).
    - Com cache vazio + fetch que rejeita (throw): geocoded:false, sem throw propagado (best-effort).
  </behavior>
  <action>
Cria `api/src/modules/positions/geocoder.test.ts` (`bun:test`). Mocka o `db` (via `mock.module('../../db/client', ...)` ou injetando spies em `db.select`/`db.insert` conforme o padrão do projeto) e `globalThis.fetch` (substitui por `mock(() => ...)`). Casos do bloco <behavior>: cache-hit sem HTTP (assert `fetch` 0 chamadas), miss→parse+uf+cache-write, faixa inválida→miss, fetch-throw→best-effort sem propagar. Restaura os mocks no `afterEach`. NÃO bate na rede real nem no DB real.
  </action>
  <verify>
    <automated>cd api && bun test src/modules/positions/geocoder.test.ts 2>&1 | grep -v '^#' | grep -qE "(pass|[0-9]+ pass)" && echo "OK geocoder tests pass"</automated>
  </verify>
  <acceptance_criteria>
    - `bun test src/modules/positions/geocoder.test.ts` passa.
    - Caso cache-hit prova fetch=0 (cache-first).
    - Casos provam validação de faixa e best-effort (sem throw).
    - Teste roda sem rede/DB reais (tudo mockado).
  </acceptance_criteria>
  <done>Suite do geocoder verde, provando cache-first, faixa e best-effort sem I/O real.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| geocoder → Nominatim (OSM) | serviço externo público com ToS |
| Nominatim → geocoder | resposta externa (coords não confiáveis até validar) |
| geocoder → DB Torre | escreve no geocode_cache |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | DoS/abuso de ToS (Nominatim) | geocoder.ts | mitigate | rate-limit sequencial >=1s + User-Agent custom + cache-first (só bate em miss) + sem bulk paralelo; falha = best-effort (não retenta em loop) (D-10-01, T3) |
| T-10-04b | Tampering (geo injection / coord inválida) | geocoder.ts | mitigate | valida lat∈[-90,90]/lng∈[-180,180] e NaN antes de aceitar; inválido vira miss → geom nunca recebe coord fora de faixa (T4) |
| T-10-05 | Information Disclosure (PII) | geocoder.ts | accept | geocode_cache guarda só query→coord (sem nome de motorista); sem log do conteúdo das queries (T5, D-10-08) |
</threat_model>

<verification>
- `bunx tsc --noEmit` do api inteiro exit 0.
- `bun test geocoder.test.ts` verde, incluindo cache-hit sem HTTP.
- `User-Agent` + parâmetros Nominatim + validação de faixa presentes.
</verification>

<success_criteria>
- Geocoder cache-first, ToS-friendly (rate-limit + User-Agent), best-effort (falha não derruba import), com validação de faixa — pronto para o endpoint (Plan 04).
- Segundo import não bate no Nominatim (tudo em cache).
</success_criteria>

<output>
After completion, create `.planning/phases/10-importa-o-viagens-xlsx-db-torre-ingestion/10-03-SUMMARY.md`
</output>
