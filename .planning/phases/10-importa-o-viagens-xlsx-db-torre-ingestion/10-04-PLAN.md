---
phase: 10-importa-o-viagens-xlsx-db-torre-ingestion
plan: 04
type: execute
wave: 3
depends_on: [10-01, 10-02, 10-03]
files_modified:
  - api/src/index.ts
autonomous: false
requirements: [D-10-01, D-10-03, D-10-04, D-10-07]
user_setup: []

must_haves:
  truths:
    - "POST /api/positions/import exige role admin (403 sem; 401 sem sessão)"
    - "Upload de um .xlsx válido grava as linhas com motorista no driver_positions (geocodadas best-effort)"
    - "Re-upload do MESMO arquivo grava 0 linhas novas (ON CONFLICT DO NOTHING via UNIQUE motorista_norm+data_posicao)"
    - "Linha geocodada recebe geom via ST_SetSRID(ST_MakePoint(lng,lat),4326); linha sem geocode fica geom NULL + geocoded=false"
    - "Resposta é { inserted, skipped, failed, total, sample[] }"
  artifacts:
    - path: "api/src/index.ts"
      provides: "Rota inline POST /api/positions/import (multipart, requireRole admin) orquestrando parse→geocode→upsert"
      contains: "/api/positions/import"
  key_links:
    - from: "api/src/index.ts (import endpoint)"
      to: "parseViagensXlsx + geocodeText + db.insert(driverPositions)"
      via: "orquestração: parse buffer → geocode por linha → upsert ON CONFLICT DO NOTHING"
      pattern: "parseViagensXlsx"
    - from: "api/src/index.ts (import endpoint)"
      to: "driver_positions.geom"
      via: "UPDATE ... SET geom = ST_SetSRID(ST_MakePoint($lng,$lat),4326) p/ linhas geocodadas"
      pattern: "ST_MakePoint"
---

<objective>
Cria o endpoint de ingestão (D-10-07): `POST /api/positions/import` multipart `file`, atrás de `requireRole('admin')` (D-10-03, T1), INLINE no `index.ts` (fallback do bug Elysia 1.4.28 body/multipart — padrão geofences/telemetry). Orquestra: lê o buffer → `parseViagensXlsx` (Plan 02) → por linha `geocodeText(posicaoRaw)` (Plan 03, sequencial best-effort) → upsert em `driver_positions` (Plan 01) com `ON CONFLICT (motorista_norm, data_posicao) DO NOTHING` (D-10-04 idempotência + D-10-03 histórico) → `geom` via `ST_SetSRID(ST_MakePoint($lng,$lat),4326)` só p/ geocodadas (T4). Aplica caps de tamanho/linhas + valida extensão/mime (T2). Retorna `{ inserted, skipped, failed, total, sample[] }`.

A 2ª task é um checkpoint:human-verify do import LIVE com a planilha real (`C:\Users\antonio.magalhaes\Downloads\Viagens.xlsx`) contra o DB Torre — paridade de contagem (~125) + re-upload idempotente (0 dup) + 2º import não bate no Nominatim (cache). Padrão dos checkpoints 09-03/07-04 (autonomous:false).
Output: rota no `index.ts`; ingestão ponta-a-ponta provada live.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/10-importa-o-viagens-xlsx-db-torre-ingestion/10-CONTEXT.md

<interfaces>
<!-- Padrão de write INLINE no index.ts (fallback do bug Elysia 1.4.28 — geofences/telemetry). -->

api/src/index.ts — geofences POST inline (~209-219), insere e depois preenche geom via raw SQL:
```typescript
.post('/api/geofences', async ({ body, set }) => {
  const b = body as any
  const [fence] = await db.insert(geofences).values({ ... }).returning()
  const wkt = 'POLYGON((' + coords + '))'
  await db.execute(sql`UPDATE geofences SET geom = ST_GeomFromText(${wkt}, 4326) WHERE id = ${fence.id}`)
  return fence
})
```
Telemetry inline (~179) valida header e lê `body as any`. Já importa `sql` e `eq` de 'drizzle-orm', `db` de './db/client'.

RBAC (api/src/lib/rbac.ts): `requireRole('admin')` → plugin Elysia. Mas o padrão INLINE (geofences/telemetry) NÃO passa por plugin (bug body). Para gate de role inline neste endpoint: replicar a verificação do authGuard+role manualmente — ler `cookie.access_token`, verificar JWT via jwtPlugin/jwt, checar blacklist no redis, e exigir `payload.role === 'admin'` (403 caso contrário; 401 sem token/inválido). Espelha a lógica de `authGuard` + `requireRole('admin')` do rbac.ts dentro do handler inline. (Decisão de implementação do executor: se conseguir compor `requireRole('admin')` como plugin SEM o bug body p/ multipart, pode usar plugin; senão, gate inline espelhando rbac.ts. O endpoint NÃO pode ficar sem o gate admin — isso é HIGH/bloqueia.)

Multipart no Elysia 1.4: `body` traz o arquivo como `File`/`Blob`; obter o buffer com `await (body.file as File).arrayBuffer()`. Validar `file.size` (cap, ex. 10MB) e nome/tipo (`.xlsx` / mime spreadsheet) ANTES de parsear (T2).

Drizzle imports já presentes no index.ts: `sql`, `eq`, `and`, `desc`, `db`. Adicionar imports: `driverPositions` de './db/schema', `parseViagensXlsx` de './modules/positions/viagens.parser', `geocodeText` de './modules/positions/geocoder'.

Upsert + geom:
```typescript
// por linha (sequencial — geocode é rate-limited):
const g = await geocodeText(row.posicaoRaw)
const [ins] = await db.insert(driverPositions).values({
  motorista: row.motorista, motoristaNorm: row.motoristaNorm,
  dataPosicao: row.dataPosicao, posicaoRaw: row.posicaoRaw, veiculo: row.veiculo,
  cidade: g.cidade, uf: g.uf, lat: g.lat?.toString(), lng: g.lng?.toString(), geocoded: g.geocoded,
}).onConflictDoNothing({ target: [driverPositions.motoristaNorm, driverPositions.dataPosicao] }).returning({ id: driverPositions.id })
if (ins && g.geocoded && g.lat != null && g.lng != null) {
  await db.execute(sql`UPDATE driver_positions SET geom = ST_SetSRID(ST_MakePoint(${g.lng}, ${g.lat}), 4326) WHERE id = ${ins.id}`)
}
// ins === undefined ⇒ conflito (skipped); contar.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rota inline POST /api/positions/import (admin, parse→geocode→upsert→geom)</name>
  <files>api/src/index.ts</files>
  <read_first>
    - api/src/index.ts (padrão write inline geofences ~209-241 + telemetry ~179-202; imports de sql/eq/db; onError ~136-143; ordem de registro dos plugins/rotas)
    - api/src/lib/rbac.ts (authGuard derive + requireRole — lógica a espelhar no gate; 401/403)
    - api/src/modules/positions/viagens.parser.ts (parseViagensXlsx + ParsedPosition — Plan 02)
    - api/src/modules/positions/geocoder.ts (geocodeText + GeocodeResult — Plan 03)
    - api/src/db/schema/driver-positions.ts (driverPositions + colunas + UNIQUE target — Plan 01)
    - .planning/phases/10-importa-o-viagens-xlsx-db-torre-ingestion/10-CONTEXT.md (D-10-03/04/07 + threat_model T1/T2/T4)
  </read_first>
  <behavior>
    - POST /api/positions/import sem cookie de sessão → 401.
    - POST com cookie de role != admin → 403 (T1).
    - POST admin com arquivo > cap (ex. >10MB) ou extensão/mime não-xlsx → 4xx, sem parsear (T2).
    - POST admin com .xlsx válido (3 linhas-motorista distintas) → 200 com { inserted:3, skipped:0, failed:0, total:3, sample:[...] }; 3 rows em driver_positions; linhas geocodadas têm geom não-null, não-geocodadas geom null + geocoded=false.
    - Re-POST do MESMO arquivo → { inserted:0, skipped:3, ... } (ON CONFLICT DO NOTHING; D-10-04).
  </behavior>
  <action>
Adiciona ao `api/src/index.ts` a rota INLINE `.post('/api/positions/import', async ({ body, cookie, set, ... }) => {...})`, posicionada junto às outras rotas inline de write (após os `.use(...plugin)` e perto das rotas geofences/telemetry inline — NÃO dentro de um plugin, p/ evitar o bug body/multipart do Elysia 1.4.28). Imports no topo: `driverPositions` de './db/schema', `parseViagensXlsx` de './modules/positions/viagens.parser', `geocodeText` de './modules/positions/geocoder'.

Fluxo do handler:
1. **Gate admin (T1, D-10-03):** espelha `authGuard` + `requireRole('admin')` do rbac.ts — lê `cookie.access_token?.value`; sem token → 401; verifica JWT (via o mesmo jwt usado no authGuard) → inválido → 401; checa `redis.get('session:blacklist:'+jti)` → revogado → 401; `payload.role !== 'admin'` → 403. (Ou plugin requireRole('admin') se compor sem o bug body p/ multipart — mas o gate admin é OBRIGATÓRIO.)
2. **Validação do upload (T2):** extrai o `File` do multipart (`body.file`); se ausente → 400. Valida `file.size <= 10*1024*1024` (cap) senão 413/400; valida nome termina em `.xlsx` e/ou mime spreadsheet senão 400. `const buf = Buffer.from(await file.arrayBuffer())`.
3. **Parse:** `const rows = parseViagensXlsx(buf)`. `total = rows.length`. Cap defensivo de linhas processadas (ex. <= 5000) p/ T2.
4. **Orquestra sequencial** (geocode é rate-limited — NÃO Promise.all): para cada row, `try { const g = await geocodeText(row.posicaoRaw); const [ins] = await db.insert(driverPositions).values({... incl. geocoded:g.geocoded, lat:g.lat?.toString(), lng:g.lng?.toString()}).onConflictDoNothing({ target: [driverPositions.motoristaNorm, driverPositions.dataPosicao] }).returning({ id }); if (!ins) skipped++ else { inserted++; if (g.geocoded && g.lat!=null && g.lng!=null) await db.execute(sql\`UPDATE driver_positions SET geom = ST_SetSRID(ST_MakePoint(${g.lng}, ${g.lat}), 4326) WHERE id = ${ins.id}\`) } } catch { failed++ }`. ST_MakePoint(lng,lat) — ordem X,Y (T4); valores via bind (sql template), nunca string-concat.
5. **Resposta (D-10-07):** `{ inserted, skipped, failed, total, sample }` onde `sample` = primeiros ~5 registros inseridos (motorista, dataPosicao, cidade, uf, geocoded) — sem PII em log (T5), mas ok no body de resposta ao admin autenticado.
NÃO logue valores das linhas (T5). NÃO concatene SQL (binds só). Mantém o registro do wsPlugin como último .use (regra Elysia 1.4) — a rota inline `.post` fica fora dos plugins, como as de geofences/telemetry.
  </action>
  <verify>
    <automated>cd api && bunx tsc --noEmit 2>&1 | grep -v '^#' | grep -c "error TS" | grep -q '^0$' && grep -q "/api/positions/import" src/index.ts && grep -q "ST_MakePoint" src/index.ts && echo "OK tsc + route + ST_MakePoint present"</automated>
  </verify>
  <acceptance_criteria>
    - `index.ts` tem a rota `POST /api/positions/import` inline (fora de plugin) com gate admin (401/403) espelhando rbac.ts.
    - Valida cap de tamanho + extensão/mime antes de parsear (T2).
    - Orquestra parse→geocode (sequencial)→`db.insert(driverPositions).onConflictDoNothing({ target:[motoristaNorm, dataPosicao] })`; geom via `ST_SetSRID(ST_MakePoint(...),4326)` só p/ geocodadas, com binds.
    - Resposta `{ inserted, skipped, failed, total, sample }`.
    - `bunx tsc --noEmit` exit 0; `App` type (Eden Treaty) ainda exportado.
  </acceptance_criteria>
  <done>Endpoint compila, gate admin presente, idempotência+geom+resposta no contrato D-10-07; tsc limpo.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Import LIVE da planilha real + paridade ~125 + idempotência + cache</name>
  <files>api/drizzle/postgis-driver-positions.sql (aplicar ao DB Torre se ainda pendente do Plan 01 Task 3); sem código novo — verificação live do endpoint da Task 1</files>
  <action>
PRÉ-PASSO (somente se a camada DB NÃO foi aplicada autônoma no Plan 01 Task 3): aplicar ao DB Torre — `CREATE TABLE IF NOT EXISTS driver_positions` + `geocode_cache` (espelhando o schema Drizzle do Plan 01, sem geom) e depois `psql "$DATABASE_URL_DIRECT" -f api/drizzle/postgis-driver-positions.sql` (coluna geom Point + GIST). NUNCA `drizzle-kit push` (dropa geom — STATE known issue). Em seguida subir o api e EXECUTAR o roteiro de <how-to-verify> com a planilha real, apresentando ao usuário a saída dos curl + counts do psql para aprovação. Este é um checkpoint de VERIFICAÇÃO: nenhum código novo é escrito — só aplica a migração (se pendente) e prova o fluxo live ponta-a-ponta.
  </action>
  <what-built>
Endpoint `POST /api/positions/import` (admin) que parseia o `Viagens.xlsx`, geocoda best-effort (Nominatim + cache) e grava em `driver_positions` (DB Torre) com idempotência (UNIQUE motorista_norm+data_posicao) e geom PostGIS. A migração PostGIS (Plan 01 Task 3) é aplicada aqui SE não tiver sido aplicada autônoma (psql/Supabase MCP com `DATABASE_URL`/`DATABASE_URL_DIRECT` do Torre) — incluindo o `CREATE TABLE driver_positions/geocode_cache` base e o `api/drizzle/postgis-driver-positions.sql`.
  </what-built>
  <how-to-verify>
Pré: garantir que `driver_positions` + `geocode_cache` existem no DB Torre e que `postgis-driver-positions.sql` foi aplicado (`psql "$DATABASE_URL_DIRECT" -c "\d driver_positions"` deve listar a coluna `geom` e o índice GIST). Subir o api (`bun run dev` no diretório api, matando bun.exe antigos antes — STATE known issue: `taskkill //F //IM bun.exe`) e autenticar como admin (cookie de sessão).

1. **Migração aplicada:** `psql "$DATABASE_URL_DIRECT" -c "\d driver_positions"` mostra `geom | geometry(Point,4326)` + `idx_driver_positions_geom` (GIST). Confirmar.
2. **1º import (paridade ~125):** upload do arquivo real:
   `curl -s -b cookies.txt -F "file=@C:/Users/antonio.magalhaes/Downloads/Viagens.xlsx" http://localhost:3000/api/positions/import`
   Esperado: JSON `{ inserted: ~125, skipped: 0, failed: <baixo>, total: ~125, sample:[...] }`. Confirmar `inserted + skipped + failed === total` e `total` ≈ 125 (paridade com as 125 linhas-motorista da planilha).
   `psql "$DATABASE_URL_DIRECT" -c "SELECT count(*) FROM driver_positions;"` ≈ 125.
   `psql ... -c "SELECT count(*) FROM driver_positions WHERE geocoded = true AND geom IS NOT NULL;"` > 0 (maioria geocodada).
3. **Idempotência (re-upload = 0 dup, D-10-04):** repetir o MESMO curl. Esperado `{ inserted: 0, skipped: ~125, ... }`. `count(*)` em driver_positions permanece ≈ 125 (NÃO dobrou).
4. **Cache (D-10-08, T3):** no 2º import, confirmar que NÃO houve novas chamadas ao Nominatim — sinais: 2º import retorna quase instantâneo (sem o ~2min de rate-limit do 1º) E/OU `psql ... -c "SELECT count(*) FROM geocode_cache;"` não cresce entre o 2º e um 3º import. (O 2º import é skip total por idempotência; p/ provar o cache de geocode isolado, opcionalmente apagar 1 linha de driver_positions e re-importar — deve re-inserir sem bater no Nominatim, pois a query já está no geocode_cache.)
5. **RBAC (T1):** `curl` SEM cookie → 401; com cookie de role não-admin → 403.
  </how-to-verify>
  <verify>
    <automated>MANUAL — checkpoint:human-verify. Verificação live no roteiro de how-to-verify (psql \d driver_positions mostra geom+GIST; curl do 1º import ≈125; re-upload = 0 dup; sem-cookie/não-admin = 401/403). Não há comando autônomo: requer DB Torre + planilha real fora do repo.</automated>
  </verify>
  <resume-signal>Digite "approved" se: \d mostra geom+GIST, 1º import ≈125 linhas geocodadas, re-upload = 0 dup, 2º import não bate no Nominatim, e sem-cookie/não-admin = 401/403. Caso contrário, descreva o que divergiu (contagem, geom null em massa, duplicou, 200 sem admin).</resume-signal>
  <done>Import live aprovado pelo usuário: geom+GIST aplicados, ~125 posições geocodadas gravadas, re-upload idempotente (0 dup), 2º import sem Nominatim, RBAC 401/403 confirmados.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente → API (/positions/import) | upload multipart não confiável de um usuário autenticado |
| API → DB Torre | inserts + DDL geom no Postgres de produção |
| API → Nominatim | (via geocoder) serviço externo com ToS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Spoofing/Elevation (import não-autorizado) | POST /api/positions/import | mitigate | gate `requireRole('admin')` espelhando authGuard+role do rbac.ts (401 sem sessão, 403 sem admin) — endpoint sem o gate admin é HIGH/bloqueia (D-10-03, T1) |
| T-10-02 | Tampering/DoS (xlsx malicioso) | handler de upload | mitigate | cap de tamanho (10MB) + valida extensão/mime ANTES de parsear + cap de linhas processadas + parser com `cellFormula:false` (Plan 02) — sem avaliar fórmula (T2) |
| T-10-04 | Tampering (SQL/geo injection) | upsert + geom | mitigate | inserts parametrizados (Drizzle) + `ST_SetSRID(ST_MakePoint($lng,$lat),4326)` via sql binds (nunca concat); lat/lng já validados em faixa pelo geocoder (Plan 03, T4) |
| T-10-04b | Tampering (idempotência) | upsert | mitigate | `onConflictDoNothing({ target:[motoristaNorm, dataPosicao] })` sobre o UNIQUE (Plan 01) → re-upload não duplica (D-10-04) |
| T-10-03 | DoS de ToS (Nominatim) | orquestração | mitigate | geocode chamado SEQUENCIAL (não Promise.all) + cache-first do geocoder; falha de linha = best-effort (failed++) não derruba o import (D-10-01, T3) |
| T-10-05 | Information Disclosure (PII) | handler/logs | accept | não loga valores de linha (nomes/posições); `sample` retorna só no body ao admin autenticado (T5) |
</threat_model>

<verification>
- `bunx tsc --noEmit` do api inteiro exit 0; rota presente; `ST_MakePoint` presente.
- Checkpoint live: \d mostra geom+GIST; 1º import ≈125 geocodadas; re-upload 0 dup; 2º import sem Nominatim; 401/403 sem admin.
</verification>

<success_criteria>
- `POST /api/positions/import` (admin) grava ~125 posições geocodadas no DB Torre; re-upload idempotente (0 dup); 2º import não bate no Nominatim (cache).
- Goal da Phase 10 atingido ponta-a-ponta (upload → linhas geocodadas → idempotente).
</success_criteria>

<output>
After completion, create `.planning/phases/10-importa-o-viagens-xlsx-db-torre-ingestion/10-04-SUMMARY.md`
</output>
