---
phase: 10-importa-o-viagens-xlsx-db-torre-ingestion
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - api/package.json
  - api/src/modules/positions/viagens.parser.ts
  - api/src/modules/positions/viagens.parser.test.ts
autonomous: true
requirements: [D-10-05, D-10-06]
user_setup: []

must_haves:
  truths:
    - "Parser lê um buffer .xlsx e retorna só as linhas com Motorista não-vazio (~125)"
    - "Parser parseia data_posicao 'dd/MM/yyyy HH:mm:ss' → Date (TZ America/Sao_Paulo) correto"
    - "Parser produz motorista_norm (upper + trim + strip-acentos) para o join futuro"
    - "Parser NÃO avalia fórmulas (cellFormula:false) — defesa contra formula injection"
  artifacts:
    - path: "api/src/modules/positions/viagens.parser.ts"
      provides: "parseViagensXlsx(buffer): ParsedPosition[] — parse + normalização das linhas com motorista"
      exports: ["parseViagensXlsx", "normalizeMotorista"]
    - path: "api/src/modules/positions/viagens.parser.test.ts"
      provides: "Testes unitários: only-Motorista, parse de data, motorista_norm, sem fórmula"
    - path: "api/package.json"
      provides: "Dependência xlsx (SheetJS) adicionada"
      contains: "xlsx"
  key_links:
    - from: "api/src/modules/positions/viagens.parser.ts"
      to: "xlsx (SheetJS)"
      via: "XLSX.read(buffer, { type:'buffer', cellFormula:false })"
      pattern: "cellFormula"
    - from: "api/src/modules/positions/viagens.parser.ts"
      to: "motorista_norm"
      via: "normalizeMotorista (upper+trim+strip-acentos)"
      pattern: "normalizeMotorista"
---

<objective>
Cria o parser do `Viagens.xlsx`: lê um buffer, extrai SÓ as linhas com Motorista (D-10-05 — ~125 das 1540), parseia o timestamp `dd/MM/yyyy HH:mm:ss` para `Date` no fuso America/Sao_Paulo, e normaliza o nome (`motorista_norm` = upper+trim+strip-acentos, D-10-06) para habilitar o join futuro com o ranking. Parse `data_only` semântico SEM avaliar fórmulas (`cellFormula:false`) — defesa contra formula/CSV injection (T2).

Purpose: Camada de leitura pura e testável, isolada do endpoint. O endpoint (Plan 04) chama `parseViagensXlsx(buffer)` e recebe registros prontos para geocodar + persistir.
Output: módulo `viagens.parser.ts` + teste unitário; dep `xlsx` em `api/package.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/10-importa-o-viagens-xlsx-db-torre-ingestion/10-CONTEXT.md
@api/src/modules/ranking/ranking.types.ts

<interfaces>
<!-- Estrutura REAL da planilha (inspecionada via openpyxl, ver 10-CONTEXT) -->
<!-- Aba "Página1", 1540 linhas, 68 colunas, header na primeira linha. -->
<!-- Colunas-chave (1-indexadas no openpyxl): -->
<!--   col12 = Veículo (placa)          col15 = Motorista (nome) -->
<!--   col16 = Data Posição (dd/MM/yyyy HH:mm:ss)   col18 = Posição (texto livre, cidade+UF no fim) -->
<!--   col19 = Estado (VAZIO — UF sai do texto da Posição) -->
<!-- Só ~125 linhas têm Motorista não-vazio; o resto é contexto da viagem (fora de escopo). -->

SheetJS (xlsx) — leitura de buffer SEM avaliar fórmula:
  import * as XLSX from 'xlsx'
  const wb = XLSX.read(buffer, { type: 'buffer', cellFormula: false, cellHTML: false })
  const sheet = wb.Sheets[wb.SheetNames[0]]
  const rows: string[][] = XLSX.utils.sheet_to_json(sheet, { header: 1, raw: false, defval: '' })
  // header:1 → array-of-arrays (0-indexado); rows[0] = header. Acessar col15 = rows[i][14].

Nomes do ranking (api/src/modules/ranking/ranking.types.ts → Driver.nome): ex. "ADAUTO SANTOS COSTA" (já upper, sem acento na amostra). normalizeMotorista deve produzir a MESMA forma a partir de qualquer caixa/acento da planilha.

strip-acentos canônico em TS: `s.normalize('NFD').replace(/[̀-ͯ]/g, '')`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Adicionar dep xlsx + parser viagens.parser.ts</name>
  <files>api/package.json, api/src/modules/positions/viagens.parser.ts</files>
  <read_first>
    - .planning/phases/10-importa-o-viagens-xlsx-db-torre-ingestion/10-CONTEXT.md (D-10-05 estrutura da planilha: cols 12/15/16/18, só linhas com Motorista, parse de data TZ; D-10-06 motorista_norm)
    - api/package.json (formato das deps — versões pinadas, ex. drizzle-orm "0.45.2")
    - api/src/modules/ranking/ranking.types.ts (Driver.nome — formato alvo do motorista_norm)
  </read_first>
  <behavior>
    - parseViagensXlsx(buffer de planilha com 3 linhas-motorista + 5 linhas sem motorista) → retorna SÓ as 3 com motorista.
    - Linha com Motorista "Adauto  Santos  Costá" → motoristaNorm === "ADAUTO SANTOS COSTA" (upper, trim, espaços colapsados, acentos removidos).
    - data_posicao "31/05/2026 14:30:00" → Date cujo instante corresponde a 14:30 em America/Sao_Paulo (UTC-03 → 17:30:00Z). Verificar via getTime()/toISOString().
    - posicao_raw preservado as-is (texto livre da col18, ex. "0.03 Km - POSTO J REIS - ENTRE RIOS BA").
    - veiculo (placa, col12) extraído; ausente → null/undefined sem quebrar.
    - Linha com Motorista vazio/whitespace → EXCLUÍDA (não entra no resultado).
  </behavior>
  <action>
Adiciona `"xlsx"` às dependencies de `api/package.json` (SheetJS — D-10-05; versão estável recente pinada, sem caret se o padrão do projeto for pin exato) e instala (`bun install` no diretório api).

Cria `api/src/modules/positions/viagens.parser.ts`:
- Exporta `interface ParsedPosition { motorista: string; motoristaNorm: string; dataPosicao: Date; posicaoRaw: string; veiculo: string | null }`.
- Exporta `normalizeMotorista(nome: string): string` = trim → colapsa espaços internos (`replace(/\s+/g, ' ')`) → strip-acentos (`normalize('NFD').replace(/[̀-ͯ]/g,'')`) → `toUpperCase()`. Forma alvo bate com `Driver.nome` do ranking (D-10-06).
- Exporta `parseViagensXlsx(buffer: Buffer | ArrayBuffer): ParsedPosition[]`:
  - `XLSX.read(buffer, { type: 'buffer', cellFormula: false, cellHTML: false })` (cellFormula:false = NÃO avalia fórmulas, T2).
  - Primeira sheet; `sheet_to_json(sheet, { header: 1, raw: false, defval: '' })` → array-of-arrays.
  - Itera linhas a partir da segunda (pula header); lê col15 (índice 14) = motorista, col16 (15) = data, col18 (17) = posição, col12 (11) = veículo.
  - Pula linha se motorista (após trim) for vazio (D-10-05 — só ~125).
  - Parse de `dd/MM/yyyy HH:mm:ss` → `Date` no fuso America/Sao_Paulo: faça parse manual dos componentes (regex `^(\d{2})\/(\d{2})\/(\d{4})\s+(\d{2}):(\d{2}):(\d{2})$`) e construa o instante UTC aplicando offset -03:00 (Brasil não tem mais DST desde 2019 — offset fixo aceitável p/ granularidade cidade). Linha com data inválida: pula a linha (best-effort) e conta como descartada (NÃO derruba o parse).
  - `veiculo`: string trimada ou `null` se vazio.
NÃO logue valores de linha (T5). NÃO use eval/fórmula. Retorna o array.
  </action>
  <verify>
    <automated>cd api && bunx tsc --noEmit 2>&1 | grep -v '^#' | grep -c "error TS" | grep -q '^0$' && echo "OK tsc"</automated>
  </verify>
  <acceptance_criteria>
    - `api/package.json` lista `xlsx` em dependencies e `bun install` rodou (node_modules/xlsx presente).
    - `viagens.parser.ts` exporta `parseViagensXlsx`, `normalizeMotorista`, `ParsedPosition`.
    - `XLSX.read` usa `cellFormula: false` (grep encontra `cellFormula`).
    - `bunx tsc --noEmit` exit 0.
  </acceptance_criteria>
  <done>Parser compila, dep instalada, fórmula desligada, normalização e parse de data implementados.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Teste unitário do parser (only-Motorista, data TZ, norm, sem fórmula)</name>
  <files>api/src/modules/positions/viagens.parser.test.ts</files>
  <read_first>
    - api/src/modules/positions/viagens.parser.ts (assinaturas a testar — produzido na Task 1)
    - api/src/modules/ranking/ranking.scoring.test.ts (padrão de teste do projeto: `bun test`, describe/it/expect — ver imports e estilo)
  </read_first>
  <behavior>
    - Constrói um workbook em memória com `XLSX.utils` (aoa_to_sheet com header + 3 linhas-motorista + 2 linhas sem motorista nas posições de coluna corretas: motorista=col15, data=col16, posição=col18, veículo=col12) e serializa p/ buffer (`XLSX.write(wb, { type:'buffer', bookType:'xlsx' })`).
    - parseViagensXlsx(buffer) retorna length === 3 (só as com motorista).
    - normalizeMotorista('  Adáuto   santos COSTA ') === 'ADAUTO SANTOS COSTA'.
    - data '31/05/2026 14:30:00' → result[i].dataPosicao.toISOString() === '2026-05-31T17:30:00.000Z' (UTC-03).
    - posicaoRaw preservado exatamente.
  </behavior>
  <action>
Cria `api/src/modules/positions/viagens.parser.test.ts` no padrão `bun test` do projeto (espelha o estilo de `ranking.scoring.test.ts` — `import { describe, it, expect } from 'bun:test'`). Monta a fixture in-memory com `XLSX.utils.aoa_to_sheet` posicionando as colunas certas (índices 11/14/15/17), `book_new` + `book_append_sheet`, `XLSX.write({ type:'buffer' })`. Casos cobrindo o bloco <behavior>: contagem only-Motorista, `normalizeMotorista` (acentos+caixa+espaços), parse de data em America/Sao_Paulo (assert no `toISOString`), preservação de `posicaoRaw`, e que linha com data inválida é descartada sem throw. NÃO depende de rede/DB/Redis (parser é puro).
  </action>
  <verify>
    <automated>cd api && bun test src/modules/positions/viagens.parser.test.ts 2>&1 | grep -v '^#' | grep -qE "(pass|[0-9]+ pass)" && echo "OK parser tests pass"</automated>
  </verify>
  <acceptance_criteria>
    - `bun test src/modules/positions/viagens.parser.test.ts` passa todos os casos.
    - Teste prova: only-Motorista (3 de 5), motorista_norm canônico, data em UTC-03, posicaoRaw intacto, data inválida descartada.
    - Teste roda sem rede/DB.
  </acceptance_criteria>
  <done>Suite do parser verde, provando D-10-05 + D-10-06 sem I/O externo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| arquivo .xlsx → parser | conteúdo de planilha (não confiável) entra no processo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02 | Tampering/Elevation (formula/CSV injection) | viagens.parser.ts | mitigate | `XLSX.read(..., { cellFormula:false, cellHTML:false })` — NÃO avalia fórmulas; `sheet_to_json` lê valores semânticos (data_only), sem executar conteúdo (D-10-05, T2) |
| T-10-05 | Information Disclosure (PII) | viagens.parser.ts | accept | parser não loga valores de linha (nomes/posições); dados internos do sistema (T5) |
| T-10-02b | Denial of Service (xlsx gigante) | parser | transfer | cap de tamanho/linhas é aplicado no endpoint (Plan 04, T-10-02 do endpoint) ANTES de chamar o parser; o parser só processa o buffer recebido |
</threat_model>

<verification>
- `bunx tsc --noEmit` do api inteiro exit 0.
- `bun test viagens.parser.test.ts` verde.
- `cellFormula: false` presente; `normalizeMotorista` exportado.
</verification>

<success_criteria>
- Parser puro e testado que entrega `ParsedPosition[]` só das linhas com motorista, com `motoristaNorm` e `dataPosicao` (TZ correto), sem avaliar fórmulas.
- Pronto para o endpoint (Plan 04) consumir.
</success_criteria>

<output>
After completion, create `.planning/phases/10-importa-o-viagens-xlsx-db-torre-ingestion/10-02-SUMMARY.md`
</output>
