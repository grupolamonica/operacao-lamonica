---
phase: 10-importa-o-viagens-xlsx-db-torre-ingestion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/src/db/schema/driver-positions.ts
  - api/src/db/schema/geocode-cache.ts
  - api/src/db/schema/index.ts
  - api/drizzle/postgis-driver-positions.sql
autonomous: true
requirements: [D-10-02, D-10-03, D-10-04, D-10-06, D-10-08]
user_setup: []

must_haves:
  truths:
    - "Tabela driver_positions existe no DB Torre com coluna geom geometry(Point,4326)"
    - "Tabela geocode_cache existe no DB Torre keyed pela query string"
    - "Constraint UNIQUE(motorista_norm, data_posicao) impede posição duplicada do mesmo motorista no mesmo timestamp"
    - "geom NÃO é gerenciado pelo Drizzle (aplicado via SQL manual, igual geofences)"
  artifacts:
    - path: "api/src/db/schema/driver-positions.ts"
      provides: "Definição Drizzle da tabela driver_positions (colunas não-geom) + UNIQUE(motorista_norm, data_posicao)"
      contains: "driverPositions"
    - path: "api/src/db/schema/geocode-cache.ts"
      provides: "Definição Drizzle da tabela geocode_cache (query PK)"
      contains: "geocodeCache"
    - path: "api/drizzle/postgis-driver-positions.sql"
      provides: "SQL manual idempotente: coluna geom geometry(Point,4326) + GIST index em driver_positions"
      contains: "geometry(Point, 4326)"
    - path: "api/src/db/schema/index.ts"
      provides: "Barrel export das duas tabelas novas"
      contains: "driver-positions"
  key_links:
    - from: "api/src/db/schema/index.ts"
      to: "api/src/db/schema/driver-positions.ts"
      via: "export * from"
      pattern: "export \\* from './driver-positions'"
    - from: "api/drizzle/postgis-driver-positions.sql"
      to: "driver_positions.geom"
      via: "ALTER TABLE ADD COLUMN geom + CREATE INDEX USING GIST"
      pattern: "USING GIST"
---

<objective>
Cria a camada de dados da ingestão: as tabelas `driver_positions` (posições importadas com histórico) e `geocode_cache` (cache de geocoding), seguindo o padrão Drizzle do projeto, e aplica a coluna PostGIS `geom` via SQL MANUAL (réplica de `api/drizzle/postgis-manual.sql`), porque `drizzle-kit push` DROPA a coluna geom (known issue / MEMORY).

Purpose: Toda a ingestão (parser, geocoder, endpoint) depende deste schema. A idempotência (D-10-04) e o histórico (D-10-03) vivem na constraint `UNIQUE(motorista_norm, data_posicao)`. O `motorista_norm` (D-10-06) habilita o join futuro com o ranking (Phase 11). O cache (D-10-08) torna o re-import rápido e ToS-friendly.
Output: `driver-positions.ts`, `geocode-cache.ts`, barrel atualizado, `postgis-driver-positions.sql` aplicado ao DB Torre.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/10-importa-o-viagens-xlsx-db-torre-ingestion/10-CONTEXT.md

<interfaces>
<!-- Padrões a SEGUIR — extraídos do codebase. Executor usa diretamente, sem explorar. -->

Padrão de tabela Drizzle COM índice/constraint (api/src/db/schema/push-subscriptions.ts):
```typescript
import { pgTable, uuid, text, timestamp, index } from 'drizzle-orm/pg-core'
import { sql } from 'drizzle-orm'

export const pushSubscriptions = pgTable('push_subscriptions', {
  id:        uuid('id').primaryKey().default(sql`gen_random_uuid()`),
  endpoint:  text('endpoint').notNull().unique(),
  createdAt: timestamp('created_at', { withTimezone: true }).defaultNow().notNull(),
}, (t) => ({
  userIdIdx: index('idx_push_subscriptions_user_id').on(t.userId),
}))

export type SelectPushSubscription = typeof pushSubscriptions.$inferSelect
export type InsertPushSubscription = typeof pushSubscriptions.$inferInsert
```

Padrão PostGIS MANUAL (api/drizzle/postgis-manual.sql) — NÃO drizzle-managed, idempotente:
```sql
CREATE EXTENSION IF NOT EXISTS postgis;
ALTER TABLE geofences ADD COLUMN IF NOT EXISTS geom geometry(Polygon, 4326);
CREATE INDEX IF NOT EXISTS idx_geofences_geom ON geofences USING GIST (geom);
```

Barrel atual (api/src/db/schema/index.ts) — adicionar as 2 linhas novas ANTES de `export * from './relations'`:
```typescript
export * from './geofences'
export * from './push-subscriptions'
...
export * from './relations'
```

`unique()` composto em Drizzle (para UNIQUE(motorista_norm, data_posicao)) usa o callback `(t) => ({...})` com `unique('nome_constraint').on(t.motoristaNorm, t.dataPosicao)` (import `unique` de 'drizzle-orm/pg-core').
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Definir tabelas driver_positions + geocode_cache (Drizzle, sem geom)</name>
  <files>api/src/db/schema/driver-positions.ts, api/src/db/schema/geocode-cache.ts</files>
  <read_first>
    - api/src/db/schema/push-subscriptions.ts (padrão pgTable + index + unique + $inferSelect/$inferInsert)
    - api/src/db/schema/geofences.ts (padrão de tabela com colunas geo — lat/lng como varchar no events, mas aqui use numeric/text conforme action)
    - .planning/phases/10-importa-o-viagens-xlsx-db-torre-ingestion/10-CONTEXT.md (D-10-02, D-10-04, D-10-06, D-10-08 — colunas exatas)
    - api/src/modules/ranking/ranking.types.ts (Driver.nome — formato dos nomes p/ motorista_norm)
  </read_first>
  <action>
Cria `api/src/db/schema/driver-positions.ts` exportando `driverPositions = pgTable('driver_positions', {...})` com EXATAMENTE estas colunas:
- `id` uuid primaryKey default gen_random_uuid()
- `motorista` text notNull — nome as-is da planilha (col15)
- `motoristaNorm` text('motorista_norm') notNull — nome normalizado (upper+trim+strip-acentos), preenchido pelo parser (D-10-06)
- `dataPosicao` timestamp('data_posicao', { withTimezone: true }) notNull — timestamp da posição (col16, parseado TZ America/Sao_Paulo)
- `posicaoRaw` text('posicao_raw') notNull — texto livre da Posição (col18), SEMPRE gravado mesmo sem geocode
- `veiculo` text — placa (col12), nullable
- `cidade` text — extraída do geocode, nullable
- `uf` varchar('uf', { length: 2 }) — extraída do geocode, nullable
- `lat` numeric('lat', { precision: 10, scale: 7 }) — redundante p/ leitura simples no front (D-10-02), nullable
- `lng` numeric('lng', { precision: 10, scale: 7 }) — redundante, nullable
- `geocoded` boolean notNull default(false) — flag best-effort (D-10-01); true só quando geocode resolveu
- `createdAt` timestamp('created_at', { withTimezone: true }) notNull defaultNow()

NÃO declare a coluna `geom` no Drizzle (vem do SQL manual, Task 3 — igual geofences). Adiciona o callback `(t) => ({ ... })` com `unique('driver_positions_motorista_norm_data_posicao_unique').on(t.motoristaNorm, t.dataPosicao)` (D-10-04 — idempotência; import `unique` de 'drizzle-orm/pg-core') e um `index('idx_driver_positions_motorista_norm').on(t.motoristaNorm)` p/ o join futuro. Exporta `type SelectDriverPosition` e `type InsertDriverPosition`.

Cria `api/src/db/schema/geocode-cache.ts` exportando `geocodeCache = pgTable('geocode_cache', {...})` (D-10-08) com:
- `query` text primaryKey — a query string normalizada usada no Nominatim (PK = idempotência de geocoding)
- `lat` numeric('lat', { precision: 10, scale: 7 }) — nullable (cache de miss = NULL lat/lng)
- `lng` numeric('lng', { precision: 10, scale: 7 }) — nullable
- `cidade` text nullable
- `uf` varchar('uf', { length: 2 }) nullable
- `displayName` text('display_name') nullable
- `provider` text notNull default('nominatim')
- `createdAt` timestamp('created_at', { withTimezone: true }) notNull defaultNow()
Exporta `type SelectGeocodeCache` e `type InsertGeocodeCache`.

Não loga PII (T5) — schema só, sem console.
  </action>
  <verify>
    <automated>cd api && bunx tsc --noEmit 2>&1 | grep -E "driver-positions|geocode-cache" | grep -v '^#' | wc -l | grep -q '^0$' && echo "OK tsc clean for new schema files"</automated>
  </verify>
  <acceptance_criteria>
    - `api/src/db/schema/driver-positions.ts` exporta `driverPositions` com as 12 colunas listadas (sem `geom`) + UNIQUE(motorista_norm, data_posicao) no callback.
    - `api/src/db/schema/geocode-cache.ts` exporta `geocodeCache` com `query` como primaryKey.
    - Ambos exportam os types `$inferSelect`/`$inferInsert`.
    - `bunx tsc --noEmit` não acusa erro nesses 2 arquivos.
  </acceptance_criteria>
  <done>Tabelas definidas em Drizzle (sem geom), types exportados, tsc limpo nos arquivos novos.</done>
</task>

<task type="auto">
  <name>Task 2: Exportar tabelas novas no barrel do schema</name>
  <files>api/src/db/schema/index.ts</files>
  <read_first>
    - api/src/db/schema/index.ts (ordem dos exports — relations por último)
  </read_first>
  <action>
Edita `api/src/db/schema/index.ts`: adiciona `export * from './driver-positions'` e `export * from './geocode-cache'` IMEDIATAMENTE ANTES de `export * from './relations'` (mantendo relations como última linha, padrão do arquivo). Isso registra as tabelas no `drizzle(queryClient, { schema })` do `db/client.ts`, habilitando `db.insert(driverPositions)` e `db.query` nos módulos downstream.
  </action>
  <verify>
    <automated>cd api && bunx tsc --noEmit 2>&1 | grep -v '^#' | grep -c "error TS" | grep -q '^0$' && echo "OK full tsc clean"</automated>
  </verify>
  <acceptance_criteria>
    - `index.ts` contém `export * from './driver-positions'` e `export * from './geocode-cache'` antes de `./relations`.
    - `bunx tsc --noEmit` (projeto inteiro do api) exit 0.
  </acceptance_criteria>
  <done>Barrel exporta as 2 tabelas; tsc do api inteiro limpo.</done>
</task>

<task type="auto">
  <name>Task 3: SQL manual PostGIS (geom + GIST) e aplicar ao DB Torre</name>
  <files>api/drizzle/postgis-driver-positions.sql</files>
  <read_first>
    - api/drizzle/postgis-manual.sql (padrão EXATO: CREATE EXTENSION IF NOT EXISTS postgis + ALTER TABLE ADD COLUMN IF NOT EXISTS geom + CREATE INDEX IF NOT EXISTS ... USING GIST; comentário de WHY MANUAL)
    - .planning/STATE.md (Known issue: DB local drift — aplicar via SQL manual, NUNCA db:push; geom seria dropado)
    - .planning/phases/10-importa-o-viagens-xlsx-db-torre-ingestion/10-CONTEXT.md (D-10-02 + canonical_refs: Torre Supabase ocgifdytaqlubuokjkwv TEM PostGIS)
  </read_first>
  <action>
Cria `api/drizzle/postgis-driver-positions.sql` replicando o padrão de `postgis-manual.sql` (idempotente, comentário no topo explicando WHY MANUAL — drizzle-kit push DROPA geom; aplicar AFTER drizzle criar a tabela base; exemplo de invocação `psql "$DATABASE_URL_DIRECT" -f api/drizzle/postgis-driver-positions.sql`). Conteúdo SQL:
- `CREATE EXTENSION IF NOT EXISTS postgis;`
- `ALTER TABLE driver_positions ADD COLUMN IF NOT EXISTS geom geometry(Point, 4326);` (Point, NÃO Polygon — D-10-02)
- `CREATE INDEX IF NOT EXISTS idx_driver_positions_geom ON driver_positions USING GIST (geom);`

Aplicar ao DB Torre: o executor primeiro garante que a tabela base `driver_positions` exista no DB. Se houver Supabase MCP disponível para o projeto Torre `ocgifdytaqlubuokjkwv`, use-o para (a) criar a tabela base a partir do schema Drizzle via SQL `CREATE TABLE IF NOT EXISTS driver_positions (...)` espelhando as colunas da Task 1 (sem geom) + `geocode_cache`, e (b) aplicar o conteúdo de `postgis-driver-positions.sql`. NÃO rode `drizzle-kit push` (dropa geom). Se NEM psql NEM Supabase MCP estiverem disponíveis no ambiente autônomo, deixe o SQL pronto no arquivo e registre em deferred/SUMMARY que a aplicação ao DB Torre roda no checkpoint live do Plan 04 (10-04 Task 2) — a verificação estrutural abaixo cobre só a forma/idempotência do SQL.
  </action>
  <verify>
    <automated>cd api && grep -v '^--' drizzle/postgis-driver-positions.sql | grep -c "geometry(Point, 4326)" | grep -qv '^0$' && grep -v '^--' drizzle/postgis-driver-positions.sql | grep -q "USING GIST" && echo "OK postgis sql shape (Point + GIST)"</automated>
  </verify>
  <acceptance_criteria>
    - `api/drizzle/postgis-driver-positions.sql` existe, idempotente (IF NOT EXISTS em extension/coluna/índice), com `geometry(Point, 4326)` e `USING GIST`.
    - Comentário no topo explica WHY MANUAL (drizzle push dropa geom).
    - SE psql/Supabase MCP disponível: `\d driver_positions` (ou query equivalente ao information_schema/geometry_columns) mostra a coluna `geom` e o índice GIST aplicados ao DB Torre. SE indisponível autônomo: arquivo pronto + nota no SUMMARY de que a aplicação roda no checkpoint live do 10-04.
  </acceptance_criteria>
  <done>SQL PostGIS manual criado (Point+GIST, idempotente); aplicado ao DB Torre se conexão disponível, senão pronto p/ o checkpoint live do Plan 04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| código → DB Torre | DDL aplicado ao Postgres de produção (Supabase ocgifdytaqlubuokjkwv) |
| drizzle-kit → schema geo | drizzle-kit NÃO pode gerenciar `geom` (dropa a coluna) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | schema geo / `geom` | mitigate | geom aplicado via SQL MANUAL idempotente (postgis-driver-positions.sql), NÃO drizzle-kit push — push dropa a coluna (STATE known issue) |
| T-10-04 | Tampering (idempotência) | driver_positions | mitigate | UNIQUE(motorista_norm, data_posicao) na DDL → re-import não duplica (D-10-04); upsert ON CONFLICT no Plan 04 |
| T-10-05 | Information Disclosure (PII) | schema files | accept | sem logs de valores; nomes/posições são dados internos; geocode_cache guarda só query→coord (sem PII) (D-10-08, T5) |
</threat_model>

<verification>
- `bunx tsc --noEmit` do api inteiro exit 0.
- `driver-positions.ts` e `geocode-cache.ts` exportam as tabelas + types; barrel atualizado.
- `postgis-driver-positions.sql` idempotente com Point+GIST; aplicado ao Torre (ou pronto p/ checkpoint live).
</verification>

<success_criteria>
- Tabelas `driver_positions` (com UNIQUE motorista_norm+data_posicao) e `geocode_cache` definidas em Drizzle e exportadas.
- Coluna `geom geometry(Point,4326)` + índice GIST entregues via SQL manual (não Drizzle).
- Camada de dados pronta para parser (Plan 02), geocoder (Plan 03) e endpoint (Plan 04).
</success_criteria>

<output>
After completion, create `.planning/phases/10-importa-o-viagens-xlsx-db-torre-ingestion/10-01-SUMMARY.md`
</output>
