---
phase: 08-ranking-ui
plan: 05
type: execute
wave: 3
depends_on: ["08-01", "08-02", "08-03"]
files_modified:
  - torre-de-controle/src/app/pages/ranking/components/ViagensTab.tsx
  - torre-de-controle/src/app/pages/ranking/components/EvaluationFormDialog.tsx
autonomous: true
requirements: [PHASE8-TAB-VIAGENS, PHASE8-MODAIS-SHELL]
must_haves:
  truths:
    - "A aba Viagens lista as viagens reais FECHADA de /api/ranking/trips numa DataTable"
    - "Colunas: ID viagem, motorista, vinculo, rota (origem->destino), data, ETA origem, ETA destino, ocorrencia, pontuacao"
    - "O botao 'Avaliar' por linha abre o EvaluationFormDialog em modo shell (UI montada, submit DESABILITADO — Phase 9)"
    - "Nenhum POST/PATCH e disparado nesta fase"
  artifacts:
    - path: "torre-de-controle/src/app/pages/ranking/components/ViagensTab.tsx"
      provides: "DataTable de Trip (FECHADA) com colunas e botao avaliar que abre o form shell"
      min_lines: 70
      exports: ["ViagensTab"]
    - path: "torre-de-controle/src/app/pages/ranking/components/EvaluationFormDialog.tsx"
      provides: "Modal shell do formulario de avaliacao (campos montados, submit disabled/TODO Phase 9)"
      min_lines: 60
      exports: ["EvaluationFormDialog"]
  key_links:
    - from: "torre-de-controle/src/app/pages/ranking/components/ViagensTab.tsx"
      to: "useRankingTrips"
      via: "hook import"
      pattern: "useRankingTrips"
    - from: "torre-de-controle/src/app/pages/ranking/components/ViagensTab.tsx"
      to: "EvaluationFormDialog"
      via: "onEvaluate -> setEvaluatingTrip"
      pattern: "EvaluationFormDialog"
---

<objective>
Preencher a aba Viagens: uma DataTable (padrao Torre) com as viagens reais FECHADA de `/api/ranking/trips`, com colunas equivalentes ao TripList do ride-rank. O botao "Avaliar" por linha abre o `EvaluationFormDialog` em modo SHELL — todos os campos montados (comunicacao, desvio, postura, no-show, ajuste manual, operador, observacao), mas o submit/bloqueio fica DESABILITADO com aviso "Disponivel na Phase 9" (READ-ONLY, sem POST/PATCH).

Purpose: recriar a tabela de viagens e a UI do formulario de avaliacao, deixando a escrita (Phase 9) pronta para plugar sem retrabalho de UI.
Output: ViagensTab.tsx + EvaluationFormDialog.tsx (substituindo o stub do 08-03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-ranking-ui/08-CONTEXT.md

<interfaces>
Hooks (08-01, torre-de-controle/src/hooks/useRanking.ts):
```typescript
useRankingTrips(filters?: { from?: string; to?: string }): { data: Trip[]; isLoading; ... }   // FECHADA only
useRankingRouteScores(): { data: RouteScoreRecord[]; ... }   // para base de pontos (max score) — opcional
// Trip campos relevantes: id, driver_id, driverName, data, origin_code, destination_code, status_eta, status_eta_destino, ocorrencia, ocorrencia_count, score_final, evaluated, vinculo? (vem no driver, nao no trip)
```
DataTable (torre-de-controle/src/components/domain/DataTable.tsx) — generico, embrulha PanelCard, props { data, columns, onRowClick?, title?, subtitle?, toolbar? }.
shadcn disponiveis no Torre: dialog, select, label, input, button, badge. NAO existem: switch, slider, textarea (ver acao para substitutos nativos no shell).
fixMojibake (08-02): aplicar em driverName e observacao na exibicao.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: ViagensTab — DataTable de Trip (FECHADA)</name>
  <files>torre-de-controle/src/app/pages/ranking/components/ViagensTab.tsx</files>
  <read_first>
    - ride-rank: C:\Users\antonio.magalhaes\Documents\Projetos\produção\ride-rank-buddy\src\components\TripList.tsx (COLUNAS: ID Viagem, Motorista, Vinculo, Rota, Data, ETA Orig., ETA Dest., Ocorr., Pontuacao [score_final/maxScore], Acao [Avaliar/Editar]; StatusBadge ON TIME/EARLY/DELAY)
    - torre-de-controle/src/components/domain/DataTable.tsx (API)
    - torre-de-controle/src/components/domain/StatusBadge.tsx (badge ETA no padrao Torre)
    - torre-de-controle/src/hooks/useRanking.ts (useRankingTrips, useRankingRouteScores)
    - torre-de-controle/src/lib/mojibake.ts (fixMojibake)
  </read_first>
  <action>
    Substituir o stub `ViagensTab` (PHASE8-TAB-VIAGENS). Chamar `useRankingTrips()` (FECHADA only — o backend ja filtra). Montar `columns: ColumnDef<Trip, unknown>[]`:
    - id: header "ID Viagem", fonte mono, truncar.
    - driverName: header "Motorista", cell `fixMojibake(row.driverName)`.
    - rota: header "Rota", cell `${row.origin_code} -> ${row.destination_code}` (usar seta ASCII ou →).
    - data: header "Data".
    - status_eta: header "ETA Orig.", StatusBadge.
    - status_eta_destino: header "ETA Dest.", StatusBadge.
    - ocorrencia: header "Ocorr.", badge com `ocorrencia_count` quando `ocorrencia`, senao "-".
    - score_final: header "Pontuacao", numerico mono. (maxScore opcional via getRouteBasePoints — se for simples, exibir so score_final; senao replicar score/max usando route-scores. Documentar escolha.)
    - acao: header "Acao", botao "Avaliar" (ou "Editar" se `row.evaluated`) que chama `onEvaluate(row.id)`. Como e read-only, o clique abre o EvaluationFormDialog em modo shell (o botao em si fica habilitado para ABRIR o modal; o submit dentro do modal e que fica disabled).
    Estado local `evaluatingTripId: string | null`. Renderizar `<EvaluationFormDialog tripId={evaluatingTripId} open={!!evaluatingTripId} onOpenChange={(o)=>!o && setEvaluatingTripId(null)} />`.
    `title="Viagens"`, `subtitle={`${trips.length} viagens`}`.
    READ-ONLY: nenhuma mutacao na tabela.
  </action>
  <acceptance_criteria>
    - `cd torre-de-controle && npx tsc -b --noEmit` exit 0
    - grep confirma hook: `grep -c "useRankingTrips" torre-de-controle/src/app/pages/ranking/components/ViagensTab.tsx` >= 1
    - grep confirma DataTable: `grep -c "DataTable" torre-de-controle/src/app/pages/ranking/components/ViagensTab.tsx` >= 1
    - grep confirma mojibake: `grep -c "fixMojibake" torre-de-controle/src/app/pages/ranking/components/ViagensTab.tsx` >= 1
    - grep confirma modal montado: `grep "EvaluationFormDialog" torre-de-controle/src/app/pages/ranking/components/ViagensTab.tsx` retorna >= 1 linha
    - READ-ONLY: `grep -E "\.(post|patch|delete)\(" torre-de-controle/src/app/pages/ranking/components/ViagensTab.tsx` retorna 0 linhas
  </acceptance_criteria>
  <verify>
    <automated>cd torre-de-controle && npx tsc -b --noEmit</automated>
  </verify>
  <done>Aba Viagens lista as viagens FECHADA reais com colunas completas e StatusBadge ETA; botao Avaliar abre o form shell; nomes corrigidos; sem escrita.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: EvaluationFormDialog — shell do formulario (submit disabled)</name>
  <files>torre-de-controle/src/app/pages/ranking/components/EvaluationFormDialog.tsx</files>
  <read_first>
    - ride-rank: C:\Users\antonio.magalhaes\Documents\Projetos\produção\ride-rank-buddy\src\components\EvaluationForm.tsx (CAMPOS: Comunicacao [BOA+5/REGULAR 0/RUIM -10], Desvio de Rota [NENHUM/LEVE -10/GRAVE -20], Postura [OK/RUIM -10], NO-SHOW [toggle], Ajuste Manual [slider -20..+20], Operador [input], Observacao [textarea]; botoes Bloquear/Cancelar/Salvar)
    - torre-de-controle/src/components/ui/dialog.tsx (Dialog/DialogContent/DialogHeader/DialogTitle/DialogFooter)
    - torre-de-controle/src/components/ui/select.tsx, label.tsx, input.tsx, button.tsx (primitives existentes)
    - torre-de-controle/src/hooks/useRanking.ts (useRankingTrips — para achar a trip pelo tripId e mostrar motorista/data no header)
  </read_first>
  <action>
    Substituir o stub criando `EvaluationFormDialog` (PHASE8-MODAIS-SHELL, parte avaliacao). Props: `{ tripId: string | null; open: boolean; onOpenChange: (o:boolean)=>void }`. Quando `tripId` null, render minimo (Dialog fechado).
    Buscar a trip via `useRankingTrips()` e localizar por id para exibir `fixMojibake(trip.driverName)` + data no header.
    Montar os campos (UI completa, design Torre, controlados por useState local):
    - Comunicacao: shadcn Select com BOA/REGULAR/RUIM (labels com o delta entre parenteses).
    - Desvio de Rota: Select NENHUM/LEVE/GRAVE.
    - Postura: Select OK/RUIM.
    - NO-SHOW: como o Torre NAO tem shadcn `switch`, usar shadcn `checkbox` (existe: torre-de-controle/src/components/ui/checkbox.tsx) com Label.
    - Ajuste Manual: como o Torre NAO tem shadcn `slider`, usar `<input type="range" min={-20} max={20} step={1} />` nativo + exibir o valor.
    - Operador: shadcn Input.
    - Observacao: como o Torre NAO tem shadcn `textarea`, usar `<textarea>` nativo com classes Tailwind do padrao input (ou um Input multiline simples).
    Footer com botoes: "Bloquear", "Cancelar", "Salvar". Cancelar fecha o modal. **Bloquear e Salvar ficam `disabled` com `title="Disponivel na Phase 9"`** e um aviso visivel no rodape do form: "Edicao/avaliacao sera habilitada na Phase 9 (somente leitura nesta fase)." NENHUM handler de submit chama a API — sem POST/PATCH (READ-ONLY, D-V2-03 / 08-CONTEXT). Os onClick de Salvar/Bloquear ou sao ausentes (botao disabled) ou no-op documentado.
    Documentar no SUMMARY a substituicao de switch->checkbox, slider->input range, textarea->textarea nativo (decisao para nao introduzir 3 primitives shadcn so para o shell).
  </action>
  <acceptance_criteria>
    - `cd torre-de-controle && npx tsc -b --noEmit` exit 0
    - grep confirma submit desabilitado: `grep -c "disabled" torre-de-controle/src/app/pages/ranking/components/EvaluationFormDialog.tsx` >= 2 (Salvar e Bloquear)
    - grep confirma aviso de Phase 9: `grep -i "phase 9\|fase 9" torre-de-controle/src/app/pages/ranking/components/EvaluationFormDialog.tsx` retorna >= 1 linha
    - SEM escrita: `grep -E "\.(post|patch|delete)\(|evaluateTrip|blockDriver|createEvaluationLog" torre-de-controle/src/app/pages/ranking/components/EvaluationFormDialog.tsx` retorna 0 linhas
    - grep confirma os campos principais: cada um de "Comunica", "Desvio", "Postura", "NO-SHOW"|"No-show", "Ajuste", "Operador", "Observa" aparece
    - grep confirma export: `grep "export function EvaluationFormDialog" .../EvaluationFormDialog.tsx` retorna 1 linha
  </acceptance_criteria>
  <verify>
    <automated>cd torre-de-controle && npx tsc -b --noEmit && npm run build</automated>
  </verify>
  <done>Form de avaliacao montado com todos os campos no design Torre; Salvar/Bloquear desabilitados com aviso de Phase 9; nenhuma chamada de escrita; build passa.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dados de viagem/motorista -> render | driverName/observacao de fonte externa renderizados como texto React. |
| form de avaliacao -> backend | NESTA FASE nao ha submit; nenhum dado sai do form (sem escrita ate Phase 9). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-08 | Tampering (XSS) | render de driverName/observacao | mitigate | Render via JSX (auto-escape); proibido dangerouslySetInnerHTML; fixMojibake retorna texto puro. |
| T-08-09 | Repudiation | EvaluationFormDialog | accept | Sem escrita nesta fase — nada e persistido; auditoria/log e Phase 9 (ROADMAP). Submit desabilitado elimina acao não rastreada. |
</threat_model>

<verification>
- `cd torre-de-controle && npx tsc -b --noEmit` exit 0
- `cd torre-de-controle && npm run build` exit 0
- Aba Viagens + form shell prontos; submit/bloqueio desabilitados; zero POST/PATCH
</verification>

<success_criteria>
Aba Viagens lista as viagens FECHADA reais com colunas completas; o botao Avaliar abre o formulario com toda a UI montada porem Salvar/Bloquear desabilitados (Phase 9), sem qualquer escrita.
</success_criteria>

<output>
After completion, create `.planning/phases/08-ranking-ui/08-05-SUMMARY.md`
</output>
