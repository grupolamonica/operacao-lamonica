---
phase: 08-ranking-ui
plan: 06
type: execute
wave: 3
depends_on: ["08-01", "08-02", "08-03"]
files_modified:
  - torre-de-controle/src/app/pages/ranking/components/QualidadeTab.tsx
autonomous: true
requirements: [PHASE8-TAB-QUALIDADE]
must_haves:
  truths:
    - "A aba Qualidade mostra graficos Chart.js (NAO Recharts) com dados reais derivados de /api/ranking/{trips,drivers}"
    - "Ha um grafico de barras de % de viagens com penalidade por KPI (ETA orig delay, ETA dest delay, com ocorrencia)"
    - "Ha um grafico de distribuicao de pontuacao dos motoristas"
    - "Listas de motoristas destaque e motoristas que pedem atencao aparecem no padrao PanelCard"
  artifacts:
    - path: "torre-de-controle/src/app/pages/ranking/components/QualidadeTab.tsx"
      provides: "Charts Chart.js (bar) + listas de insight derivados de trips/drivers, dentro de PanelCard"
      min_lines: 90
      exports: ["QualidadeTab"]
  key_links:
    - from: "torre-de-controle/src/app/pages/ranking/components/QualidadeTab.tsx"
      to: "react-chartjs-2 Bar"
      via: "import { Bar } + ChartJS.register"
      pattern: "from 'react-chartjs-2'"
    - from: "torre-de-controle/src/app/pages/ranking/components/QualidadeTab.tsx"
      to: "useRankingTrips / useRankingDrivers"
      via: "hooks"
      pattern: "useRankingTrips"
---

<objective>
Preencher a aba Qualidade recriando os insights do QualityChart do ride-rank no design Torre, com Chart.js (substituindo Recharts) dentro de PanelCard. Calculos de display feitos no cliente a partir de `/api/ranking/trips` (FECHADA) e `/api/ranking/drivers` (ja computados).

Purpose: recriar a visao de qualidade operacional usando o stack de chart padronizado do Torre (Chart.js + tema isDark), nao Recharts.
Output: QualidadeTab.tsx (substituindo o stub do 08-03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-ranking-ui/08-CONTEXT.md

<interfaces>
Hooks (08-01, torre-de-controle/src/hooks/useRanking.ts):
```typescript
useRankingTrips(): { data: Trip[]; isLoading; ... }       // FECHADA; campos: status_eta, status_eta_destino, ocorrencia, origin_code, destination_code, score_final, driver_id, driverName
useRankingDrivers(): { data: RankedDriver[]; ... }         // campos: pontuacao, ocorrencias, etaOrigMetrics/etaDestMetrics, nome
```
Chart.js no Torre — padrao OBRIGATORIO (ver MotoristasRankingChart):
```typescript
import { Bar } from 'react-chartjs-2'
import { Chart as ChartJS, BarElement, CategoryScale, LinearScale, Tooltip, Legend } from 'chart.js'
ChartJS.register(BarElement, CategoryScale, LinearScale, Tooltip, Legend)
// tema: const { isDark } = useThemeStore(); <Bar key={`${isDark}-...`} ... /> com tickColor/gridColor por tema
```
PanelCard (torre-de-controle/src/components/domain/PanelCard.tsx) embrulha cada card. useThemeStore em torre-de-controle/src/stores/useThemeStore.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: QualidadeTab — charts Chart.js + listas de insight</name>
  <files>torre-de-controle/src/app/pages/ranking/components/QualidadeTab.tsx</files>
  <read_first>
    - ride-rank: C:\Users\antonio.magalhaes\Documents\Projetos\produção\ride-rank-buddy\src\components\QualityChart.tsx (COMPORTAMENTO/CALCULOS: kpiData [% delay ETA orig, % delay ETA dest, % com ocorrencia], scoreDistribution [buckets de pontuacao], listas de rotas early/delay/no-show, paineis "Motoristas destaque" e "Motoristas que pedem atencao". NOTA: usa Recharts + helpers de qualityInsights; RECRIAR em Chart.js e simplificar os helpers — ver acao)
    - torre-de-controle/src/app/pages/insights/components/MotoristasRankingChart.tsx (PADRAO EXATO Chart.js bar horizontal + theme key={isDark} + tooltip callbacks + scales tickColor/gridColor)
    - torre-de-controle/src/app/pages/insights/components/AlertasDistribuicaoChart.tsx (outro exemplo Chart.js no Torre)
    - torre-de-controle/src/components/domain/PanelCard.tsx
    - torre-de-controle/src/hooks/useRanking.ts (useRankingTrips, useRankingDrivers)
  </read_first>
  <action>
    Substituir o stub `QualidadeTab` (PHASE8-TAB-QUALIDADE). Chamar `useRankingTrips()` e `useRankingDrivers()`. Registrar Chart.js (BarElement/CategoryScale/LinearScale/Tooltip/Legend) uma vez no topo do modulo.
    Calcular no cliente (puro, a partir das trips/drivers — sem helpers do ride-rank fora de escopo):
    - kpiPenalidade: % de trips com `status_eta`=='DELAY', % com `status_eta_destino`=='DELAY', % com `ocorrencia===true` (sobre total de trips). Render como bar (horizontal, indexAxis 'y') num PanelCard "Penalidade por KPI" com escala 0..100 e sufixo %.
    - scoreDistribution: 5 buckets de `driver.pontuacao` (low..high) com contagem; bar vertical num PanelCard "Distribuicao de pontuacao".
    - (Opcional, se couber no budget) rotas com mais delay/early: agrupar trips por `origin->destination` e contar delay/early; mostrar como pequenas listas em PanelCard. Se exceder, omitir as listas de rota e manter as duas listas de motorista abaixo (documentar).
    - Lista "Motoristas destaque": top motoristas por `pontuacao` (ja desc) com baixa `ocorrencias` — exibir nome (`fixMojibake`), pontuacao, viagens, ocorrencias num PanelCard (lista simples, nao chart).
    - Lista "Motoristas que pedem atencao": motoristas com mais `ocorrencias` (ou maior `etaDestMetrics.delay`) — mesmo formato.
    Tema: usar `const { isDark } = useThemeStore()` e `<Bar key={`${isDark}-<nome>`} ... />` com tickColor/gridColor condicionais (igual MotoristasRankingChart). Cores: usar os hex Argon (#5e72e4 primary, #2dce89 success, #f5365c danger) ou o getBarColor por faixa.
    Layout: grid Argon responsivo (`grid grid-cols-1 lg:grid-cols-2 gap-5` para os 2 charts, e linhas abaixo para as listas). Loading: mensagem/Skeleton dentro do PanelCard (padrao MotoristasRankingChart).
    PROIBIDO Recharts (D-V2-03 / 08-CONTEXT): nao importar `recharts`.
  </action>
  <acceptance_criteria>
    - `cd torre-de-controle && npx tsc -b --noEmit` exit 0
    - `cd torre-de-controle && npm run build` exit 0
    - Chart.js usado, Recharts ausente: `grep -c "react-chartjs-2" .../QualidadeTab.tsx` >= 1 E `grep -c "recharts" .../QualidadeTab.tsx` === 0
    - grep confirma registro Chart.js: `grep "ChartJS.register" torre-de-controle/src/app/pages/ranking/components/QualidadeTab.tsx` retorna >= 1 linha
    - grep confirma tema isDark: `grep -c "isDark" torre-de-controle/src/app/pages/ranking/components/QualidadeTab.tsx` >= 1
    - grep confirma hooks: `grep -c "useRankingTrips\|useRankingDrivers" .../QualidadeTab.tsx` >= 2
    - grep confirma PanelCard: `grep -c "PanelCard" .../QualidadeTab.tsx` >= 2
    - grep confirma mojibake nas listas: `grep -c "fixMojibake" .../QualidadeTab.tsx` >= 1
  </acceptance_criteria>
  <verify>
    <automated>cd torre-de-controle && npx tsc -b --noEmit && npm run build</automated>
  </verify>
  <done>Aba Qualidade mostra os charts Chart.js (penalidade por KPI + distribuicao de pontuacao) e as listas de motoristas destaque/atencao com dados reais, no design Torre; sem Recharts; build passa.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dados de motorista/viagem -> chart/labels | Nomes/labels de fonte externa entram em labels de chart e listas (texto). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-11 | Tampering (XSS) | labels de chart / listas | mitigate | Chart.js renderiza em canvas (sem injecao de HTML); listas usam JSX text. Nomes via fixMojibake (texto puro). Sem dangerouslySetInnerHTML. |
| T-08-12 | Information Disclosure | QualidadeTab | accept | Agregados operacionais ja autorizados pelo authGuard; sem secret. |
</threat_model>

<verification>
- `cd torre-de-controle && npx tsc -b --noEmit` exit 0
- `cd torre-de-controle && npm run build` exit 0
- Charts Chart.js + listas com dados reais; Recharts ausente
</verification>

<success_criteria>
Aba Qualidade entrega os graficos (Chart.js) de penalidade por KPI e distribuicao de pontuacao + listas de motoristas destaque/atencao, todos com dados reais e no design Torre, sem Recharts.
</success_criteria>

<output>
After completion, create `.planning/phases/08-ranking-ui/08-06-SUMMARY.md`
</output>
