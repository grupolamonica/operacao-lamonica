---
phase: 08-ranking-ui
plan: 08
type: execute
wave: 3
depends_on: ["08-01", "08-02", "08-03"]
files_modified:
  - torre-de-controle/src/app/pages/ranking/components/LogsTab.tsx
autonomous: true
requirements: [PHASE8-TAB-LOGS]
must_haves:
  truths:
    - "A aba Logs renderiza no design Torre uma DataTable de auditoria com as colunas do EvaluationLogList (Data/Hora, Acao, Viagem, Motorista, Operador, Detalhes/diff)"
    - "Como o backend Phase 7 NAO expoe /api/ranking/logs (endpoint de logs e Phase 9), a aba mostra um estado vazio informativo + aviso de que os logs chegam na Phase 9 — SEM inventar endpoint nem chamar API inexistente"
    - "Existe uma funcao de render de diff JSON antes/depois pronta para quando os dados existirem (Phase 9)"
  artifacts:
    - path: "torre-de-controle/src/app/pages/ranking/components/LogsTab.tsx"
      provides: "DataTable shell de logs de auditoria (colunas + render de diff JSON) com estado vazio e aviso Phase 9"
      min_lines: 55
      exports: ["LogsTab"]
  key_links:
    - from: "torre-de-controle/src/app/pages/ranking/components/LogsTab.tsx"
      to: "DataTable / PanelCard (design Torre)"
      via: "render das colunas de auditoria"
      pattern: "DataTable\\|PanelCard"
---

<objective>
Preencher a aba Logs como SHELL read-only no design Torre, recriando as colunas de auditoria do EvaluationLogList do ride-rank (Data/Hora, Acao, Viagem, Motorista, Operador, Detalhes com diff antes/depois). 

RESTRICAO REAL (NAO e reducao de escopo): o modulo ranking do Phase 7 expoe apenas 5 endpoints — drivers, trips, blocks, route-scores, stats (ver ranking.plugin.ts / 07-04-SUMMARY). NAO existe `/api/ranking/logs`; a leitura de `evaluation_logs` esta planejada para a Phase 9 (MILESTONE-v2-ROADMAP linha 55: "Aba Logs lendo evaluation_logs" -> Phase 9). Portanto esta aba monta a tabela e o render de diff PRONTOS, exibe um estado vazio informativo e um aviso de que os logs serao habilitados na Phase 9 — sem chamar nenhum endpoint inexistente e sem inventar um hook de logs.

Purpose: entregar a aba Logs no design Torre (escopo D-V2-03 "todas as 6 abas") de forma honesta com o contrato disponivel, deixando o shell pronto para a Phase 9 plugar o hook de logs.
Output: LogsTab.tsx (substituindo o stub do 08-03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-ranking-ui/08-CONTEXT.md
@.planning/phases/07-ranking-backend/07-04-SUMMARY.md

<interfaces>
Tipo do log (ja existe no contrato, api/src/modules/ranking/ranking.types.ts EvaluationLogRecord) — re-exportar/importar para tipar a tabela e o diff:
```typescript
interface EvaluationLogRecord {
  id?: string; trip_id?: string; driver_id?: string; driver_name?: string;
  operador: string; acao: string;
  dados_antes?: Record<string, unknown> | null;
  dados_depois?: Record<string, unknown> | null;
  created_at?: string;
}
```
IMPORTANTE: NAO ha hook `useRankingLogs` (endpoint nao existe nesta fase). NAO criar um. A aba renderiza com `logs: EvaluationLogRecord[] = []` (vazio) ate a Phase 9.
DataTable (torre-de-controle/src/components/domain/DataTable.tsx), PanelCard, Badge (shadcn). fixMojibake (08-02) em driver_name.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: LogsTab — shell de auditoria (colunas + diff) com estado vazio + aviso Phase 9</name>
  <files>torre-de-controle/src/app/pages/ranking/components/LogsTab.tsx</files>
  <read_first>
    - ride-rank: C:\Users\antonio.magalhaes\Documents\Projetos\produção\ride-rank-buddy\src\components\EvaluationLogList.tsx (COLUNAS: Data/Hora [created_at formatado pt-BR], Acao [ActionBadge CRIACAO/EDICAO/DESBLOQUEIO], Viagem [trip_id], Motorista [driver_name], Operador, Detalhes [Object.entries(dados_depois) filtrando trip_id/driver_id/driver_name/operador])
    - .planning/phases/07-ranking-backend/07-04-SUMMARY.md (confirma que os endpoints sao 5 — drivers/trips/blocks/route-scores/stats; NAO ha /logs)
    - torre-de-controle/src/components/domain/DataTable.tsx + PanelCard.tsx
    - torre-de-controle/src/lib/mojibake.ts (fixMojibake)
    - api/src/modules/ranking/ranking.types.ts (EvaluationLogRecord)
  </read_first>
  <action>
    Substituir o stub `LogsTab` (PHASE8-TAB-LOGS). Importar/typar `EvaluationLogRecord` (re-exportar do contrato em useRanking.ts no 08-01 se conveniente, ou importar por path relativo de ranking.types como os outros tipos).
    Definir `const logs: EvaluationLogRecord[] = []` (vazio nesta fase — endpoint de logs e Phase 9; NAO criar hook nem chamar API).
    Montar a tabela no design Torre (DataTable se o shape `{id}` for facil — mapear `id: log.id ?? `${log.trip_id}-${i}``; ou um PanelCard + tabela simples). Colunas:
    - created_at: header "Data/Hora", formatar pt-BR (`new Date(created_at).toLocaleString('pt-BR')`), "-" quando ausente.
    - acao: header "Acao", Badge por tipo (CRIACAO=success, EDICAO=info/blue, DESBLOQUEIO=amber, default=outline) — recriar o ActionBadge no padrao Badge do Torre.
    - trip_id: header "Viagem", mono.
    - driver_name: header "Motorista", `fixMojibake`.
    - operador: header "Operador".
    - detalhes: header "Detalhes", render do DIFF antes/depois: criar uma funcao `renderDiff(dados_antes, dados_depois)` que, para cada chave relevante (excluindo trip_id/driver_id/driver_name/operador), mostra `chave: antes -> depois` (ou so `chave: valor` quando so ha dados_depois). Render como texto/lista compacta (sem dangerouslySetInnerHTML — JSON serializado/escapado).
    ESTADO VAZIO (obrigatorio): como `logs` esta vazio, a tabela mostra o emptyMessage e ADICIONAR, dentro do PanelCard (subtitle ou um banner), o aviso: "Auditoria sera habilitada na Phase 9 (leitura de evaluation_logs)." Deixar claro que a estrutura ja esta pronta.
    NAO inventar `/api/ranking/logs`, NAO criar `useRankingLogs`, NAO chamar fetch. Documentar no SUMMARY que o shell esta pronto para a Phase 9 plugar o hook.
    READ-ONLY por natureza (auditoria e so leitura).
  </action>
  <acceptance_criteria>
    - `cd torre-de-controle && npx tsc -b --noEmit` exit 0
    - `cd torre-de-controle && npm run build` exit 0
    - NAO chama endpoint de logs inexistente: `grep -E "ranking.*logs|useRankingLogs|/logs" torre-de-controle/src/app/pages/ranking/components/LogsTab.tsx` retorna 0 linhas
    - NAO faz fetch/treaty nesta aba: `grep -E "api\.api\.ranking|fetch\(" torre-de-controle/src/app/pages/ranking/components/LogsTab.tsx` retorna 0 linhas
    - grep confirma aviso Phase 9: `grep -i "phase 9\|fase 9" torre-de-controle/src/app/pages/ranking/components/LogsTab.tsx` retorna >= 1 linha
    - grep confirma colunas de auditoria: cada um de "Data/Hora"|"Data", "Acao"|"Ação", "Viagem", "Motorista", "Operador", "Detalhes" aparece
    - grep confirma render de diff: `grep -ci "diff\|dados_antes\|dados_depois" torre-de-controle/src/app/pages/ranking/components/LogsTab.tsx` >= 1
    - grep confirma export: `grep "export function LogsTab" .../LogsTab.tsx` retorna 1 linha
  </acceptance_criteria>
  <verify>
    <automated>cd torre-de-controle && npx tsc -b --noEmit && npm run build</automated>
  </verify>
  <done>Aba Logs renderiza o shell de auditoria no design Torre (colunas + render de diff) com estado vazio e aviso de Phase 9, sem inventar endpoint nem chamar API; build passa.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dados de log (Phase 9) -> render de diff | Quando existirem (Phase 9), os valores de dados_antes/depois sao de fonte externa; o render de diff deve escapar como texto. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-15 | Tampering (XSS) | renderDiff | mitigate | Diff renderizado como texto JSX (auto-escape) / JSON.stringify; PROIBIDO dangerouslySetInnerHTML. fixMojibake em driver_name (texto puro). |
| T-08-16 | Information Disclosure | LogsTab | accept | Nesta fase nao busca dado nenhum (shell vazio); na Phase 9 a leitura ficara atras do authGuard. |
</threat_model>

<verification>
- `cd torre-de-controle && npx tsc -b --noEmit` exit 0
- `cd torre-de-controle && npm run build` exit 0
- Shell de Logs no design Torre, sem endpoint inventado, com aviso de Phase 9
</verification>

<success_criteria>
Aba Logs entregue como shell honesto no design Torre — colunas de auditoria + render de diff prontos, estado vazio informativo e aviso de que a leitura de evaluation_logs chega na Phase 9 — sem chamar endpoint inexistente.
</success_criteria>

<output>
After completion, create `.planning/phases/08-ranking-ui/08-08-SUMMARY.md`
</output>
