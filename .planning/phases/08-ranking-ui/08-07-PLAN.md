---
phase: 08-ranking-ui
plan: 07
type: execute
wave: 3
depends_on: ["08-01", "08-02", "08-03"]
files_modified:
  - torre-de-controle/src/app/pages/ranking/components/BloqueiosTab.tsx
  - torre-de-controle/src/app/pages/ranking/components/RotasTab.tsx
autonomous: true
requirements: [PHASE8-TAB-BLOQUEIOS, PHASE8-TAB-ROTAS]
must_haves:
  truths:
    - "A aba Bloqueios lista os bloqueios ativos reais de /api/ranking/blocks numa DataTable"
    - "A acao 'Desbloquear' por linha aparece DESABILITADA (Phase 9), sem disparar nenhuma escrita"
    - "A aba Rotas lista as pontuacoes de rota reais de /api/ranking/route-scores numa tabela read"
    - "Acoes de escrita de rota (Nova/Editar/Remover) aparecem desabilitadas ou ausentes (Phase 9)"
  artifacts:
    - path: "torre-de-controle/src/app/pages/ranking/components/BloqueiosTab.tsx"
      provides: "DataTable de DriverBlockRecord (ativos) com acao desbloquear disabled"
      min_lines: 50
      exports: ["BloqueiosTab"]
    - path: "torre-de-controle/src/app/pages/ranking/components/RotasTab.tsx"
      provides: "Tabela read de RouteScoreRecord (origem, destino, pontuacao, periodo)"
      min_lines: 45
      exports: ["RotasTab"]
  key_links:
    - from: "torre-de-controle/src/app/pages/ranking/components/BloqueiosTab.tsx"
      to: "useRankingBlocks"
      via: "hook import"
      pattern: "useRankingBlocks"
    - from: "torre-de-controle/src/app/pages/ranking/components/RotasTab.tsx"
      to: "useRankingRouteScores"
      via: "hook import"
      pattern: "useRankingRouteScores"
---

<objective>
Preencher duas abas de tabela read-only: Bloqueios (`/api/ranking/blocks`, ativos) e Rotas (`/api/ranking/route-scores`). Ambas no design Torre (DataTable/PanelCard). As acoes de escrita do ride-rank (desbloquear motorista; criar/editar/remover pontuacao de rota) aparecem DESABILITADAS com aviso de Phase 9 ou sao omitidas — sem nenhum POST/PATCH/DELETE.

Purpose: recriar BlocksList e RouteScores do ride-rank em modo leitura, deixando os ganchos de escrita visiveis-porem-inertes para a Phase 9.
Output: BloqueiosTab.tsx + RotasTab.tsx (substituindo os stubs do 08-03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-ranking-ui/08-CONTEXT.md

<interfaces>
Hooks (08-01, torre-de-controle/src/hooks/useRanking.ts):
```typescript
useRankingBlocks(): { data: DriverBlockRecord[]; ... }   // ativos; campos: driver_id, driver_name, tipo, motivo, ativo, manual_override, data_inicio, data_fim, created_by
useRankingRouteScores(): { data: RouteScoreRecord[]; ... } // campos: origin_code, destination_code, pontuacao, data_inicio, data_fim, observacao
useRankingTrips(): { data: Trip[]; ... }  // opcional, para contar viagens por rota (origin->destination)
```
DataTable (torre-de-controle/src/components/domain/DataTable.tsx), PanelCard, StatusBadge, Badge (shadcn). fixMojibake (08-02) para driver_name/motivo/observacao.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: BloqueiosTab — DataTable de bloqueios ativos (acao disabled)</name>
  <files>torre-de-controle/src/app/pages/ranking/components/BloqueiosTab.tsx</files>
  <read_first>
    - ride-rank: C:\Users\antonio.magalhaes\Documents\Projetos\produção\ride-rank-buddy\src\components\BlocksList.tsx (COLUNAS: Motorista, Tipo [NO_SHOW/MANUAL], Motivo, Inicio, Status [BLOQUEADO/ENCERRADO], Criado por, Acao [Desbloquear]; so mostra ativos)
    - torre-de-controle/src/components/domain/DataTable.tsx + StatusBadge.tsx
    - torre-de-controle/src/hooks/useRanking.ts (useRankingBlocks)
    - torre-de-controle/src/lib/mojibake.ts (fixMojibake)
  </read_first>
  <action>
    Substituir o stub `BloqueiosTab` (PHASE8-TAB-BLOQUEIOS). Chamar `useRankingBlocks()` (ja retorna so ativos). Montar `columns: ColumnDef<DriverBlockRecord, unknown>[]` — NOTA: DriverBlockRecord nao tem `id` garantido (id?), e a DataTable exige `{ id: string }`. Mapear os dados para um shape com `id` estavel antes de passar: `data = blocks.map((b, i) => ({ ...b, id: b.id ?? `${b.driver_id}-${i}` }))`.
    Colunas:
    - driver_name: header "Motorista", `fixMojibake`.
    - tipo: header "Tipo", Badge (NO_SHOW / MANUAL).
    - motivo: header "Motivo", `fixMojibake`, truncar.
    - data_inicio: header "Inicio".
    - status: header "Status", StatusBadge "BLOQUEADO" (todos sao ativos).
    - created_by: header "Criado por".
    - acao: header "Acao", botao "Desbloquear" `disabled` com `title="Disponivel na Phase 9"`. NENHUM onClick que chame API.
    `title="Bloqueios"`, `subtitle={`${blocks.length} ativos`}`. Estado vazio: mensagem "Nenhum bloqueio ativo".
    READ-ONLY: sem mutacoes.
  </action>
  <acceptance_criteria>
    - `cd torre-de-controle && npx tsc -b --noEmit` exit 0
    - grep confirma hook: `grep -c "useRankingBlocks" torre-de-controle/src/app/pages/ranking/components/BloqueiosTab.tsx` >= 1
    - grep confirma acao desabilitada: `grep -c "disabled" torre-de-controle/src/app/pages/ranking/components/BloqueiosTab.tsx` >= 1
    - grep confirma aviso Phase 9: `grep -i "phase 9\|fase 9" torre-de-controle/src/app/pages/ranking/components/BloqueiosTab.tsx` retorna >= 1 linha
    - READ-ONLY: `grep -E "\.(post|patch|delete)\(|unblockDriver" torre-de-controle/src/app/pages/ranking/components/BloqueiosTab.tsx` retorna 0 linhas
  </acceptance_criteria>
  <verify>
    <automated>cd torre-de-controle && npx tsc -b --noEmit</automated>
  </verify>
  <done>Aba Bloqueios lista os bloqueios ativos reais com a acao Desbloquear desabilitada (Phase 9); nomes/motivos corrigidos; sem escrita.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: RotasTab — tabela read de route-scores</name>
  <files>torre-de-controle/src/app/pages/ranking/components/RotasTab.tsx</files>
  <read_first>
    - ride-rank: C:\Users\antonio.magalhaes\Documents\Projetos\produção\ride-rank-buddy\src\components\RouteScores.tsx (COLUNAS: Origem, Destino, Viagens, Pontuacao Atual, Periodo [data_inicio -> data_fim/atual], Acoes [Pontuar/Historico/Editar/Remover]. NOTA: o ride-rank tem CRUD + dialog de historico; aqui e SO LEITURA — ver acao)
    - torre-de-controle/src/components/domain/DataTable.tsx
    - torre-de-controle/src/hooks/useRanking.ts (useRankingRouteScores, useRankingTrips opcional)
    - torre-de-controle/src/lib/mojibake.ts (fixMojibake em observacao)
  </read_first>
  <action>
    Substituir o stub `RotasTab` (PHASE8-TAB-ROTAS). Chamar `useRankingRouteScores()`. Mapear para shape com `id` estavel (RouteScoreRecord.id? -> `id: rs.id ?? `${rs.origin_code}-${rs.destination_code}-${rs.data_inicio}``).
    Colunas (leitura):
    - origin_code: header "Origem", mono.
    - destination_code: header "Destino", mono.
    - pontuacao: header "Pontuacao", Badge `${pontuacao} pt`.
    - periodo: header "Periodo", cell `${data_inicio}${data_fim ? ' -> ' + data_fim : ' -> atual'}`.
    - observacao: header "Obs.", `fixMojibake`, truncar (mostrar "-" quando vazio).
    - (Opcional) viagens: contar trips por rota via `useRankingTrips()` agrupando `origin->destination`; se ficar simples, incluir coluna "Viagens"; senao omitir e documentar.
    NAO incluir o botao "Nova Pontuacao"/CRUD/historico-dialog do ride-rank (escrita = Phase 9). Se quiser sinalizar, um botao "Nova Pontuacao" pode aparecer `disabled` com `title="Disponivel na Phase 9"` no toolbar do DataTable — opcional.
    `title="Rotas"`, `subtitle={`${routeScores.length} rotas`}`. Estado vazio: "Nenhuma pontuacao de rota cadastrada."
    READ-ONLY: sem mutacoes.
  </action>
  <acceptance_criteria>
    - `cd torre-de-controle && npx tsc -b --noEmit` exit 0
    - `cd torre-de-controle && npm run build` exit 0
    - grep confirma hook: `grep -c "useRankingRouteScores" torre-de-controle/src/app/pages/ranking/components/RotasTab.tsx` >= 1
    - grep confirma DataTable/PanelCard: `grep -c "DataTable\|PanelCard" torre-de-controle/src/app/pages/ranking/components/RotasTab.tsx` >= 1
    - READ-ONLY: `grep -E "\.(post|patch|delete)\(|createRouteScore|updateRouteScore|deleteRouteScore" torre-de-controle/src/app/pages/ranking/components/RotasTab.tsx` retorna 0 linhas
    - grep confirma export: `grep "export function RotasTab" .../RotasTab.tsx` retorna 1 linha
  </acceptance_criteria>
  <verify>
    <automated>cd torre-de-controle && npx tsc -b --noEmit && npm run build</automated>
  </verify>
  <done>Aba Rotas lista as pontuacoes de rota reais (origem, destino, pontuacao, periodo, obs.) em modo leitura; CRUD ausente/disabled (Phase 9); build passa.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dados de bloqueio/rota -> render | driver_name/motivo/observacao de fonte externa renderizados como texto. |
| acoes de escrita | Desabilitadas nesta fase — nenhum endpoint de mutacao e atingido. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-13 | Tampering (XSS) | render de motivo/observacao | mitigate | JSX text + fixMojibake (texto puro); sem dangerouslySetInnerHTML. |
| T-08-14 | Elevation of Privilege | acao Desbloquear / CRUD rota | mitigate | Botoes disabled e sem handler de API nesta fase; a autorizacao real (RBAC) e Phase 9 (D-V2-06). Nada escreve. |
</threat_model>

<verification>
- `cd torre-de-controle && npx tsc -b --noEmit` exit 0
- `cd torre-de-controle && npm run build` exit 0
- Bloqueios + Rotas em leitura com dados reais; escrita inerte
</verification>

<success_criteria>
Abas Bloqueios e Rotas entregam as tabelas read-only com dados reais no design Torre; as acoes de escrita aparecem desabilitadas/ausentes (Phase 9) sem disparar mutacoes.
</success_criteria>

<output>
After completion, create `.planning/phases/08-ranking-ui/08-07-SUMMARY.md`
</output>
