---
phase: 08-ranking-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - torre-de-controle/src/hooks/useRanking.ts
autonomous: true
requirements: [PHASE8-RANKING-HOOKS]
must_haves:
  truths:
    - "Os 5 hooks de ranking buscam dados reais de /api/ranking/* via Eden Treaty e retornam tipados"
    - "Os tipos RankedDriver, Trip, DriverBlockRecord, RouteScoreRecord, RankingStats ficam disponíveis no front sem redefinição manual"
    - "Cada hook expõe o contrato { data, isLoading, isError, error, refetch } idêntico aos hooks v1 (useTrips/useInsights)"
  artifacts:
    - path: "torre-de-controle/src/hooks/useRanking.ts"
      provides: "5 hooks (useRankingDrivers/Trips/Blocks/RouteScores/Stats) + re-export dos tipos do contrato Phase 7"
      min_lines: 90
      exports: ["useRankingDrivers", "useRankingTrips", "useRankingBlocks", "useRankingRouteScores", "useRankingStats"]
  key_links:
    - from: "torre-de-controle/src/hooks/useRanking.ts"
      to: "api.api.ranking.*"
      via: "Eden Treaty client (@/lib/api)"
      pattern: "api\\.api\\.ranking"
---

<objective>
Criar os 5 hooks TanStack Query que consomem `/api/ranking/*` via Eden Treaty, replicando o padrão de `useInsights`/`useTrips`. Este é o fundamento de dados de todas as 6 abas (waves seguintes).

Purpose: centralizar o acesso read-only ao módulo ranking do Phase 7, tipado pelo `App` do Eden Treaty, com staleTime alinhado ao cache Redis curto do backend.
Output: `torre-de-controle/src/hooks/useRanking.ts` exportando 5 hooks + os tipos do contrato re-exportados para consumo das abas.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-ranking-ui/08-CONTEXT.md
@.planning/phases/07-ranking-backend/07-04-SUMMARY.md

<interfaces>
<!-- Contrato FIXO do Phase 7 — o executor consome estes tipos via Eden Treaty (App). NÃO redefinir manualmente. -->

Tipos exportados por api/src/modules/ranking/ranking.service.ts:
```typescript
export type RankedDriver = Driver & { rank: number | null }
export interface RankingStats { activeDrivers: number; top3Avg: number; totalTrips: number; activeBlocks: number }
```
Tipos em api/src/modules/ranking/ranking.types.ts (Driver, Trip, DriverBlockRecord, RouteScoreRecord, StatusMetrics):
```typescript
export interface Driver { id: string; nome: string; status: 'ATIVO'|'BLOQUEADO'; pontuacao: number; totalViagens: number; ocorrencias: number; created_at: string; etaOrigMetrics: StatusMetrics; etaDestMetrics: StatusMetrics; vinculo: string }
export interface StatusMetrics { onTime: number; early: number; delay: number }
// Trip: ver ranking.types.ts L44-70 (FECHADA only no contrato /trips)
// DriverBlockRecord: ver ranking.types.ts L169-181
// RouteScoreRecord: ver ranking.types.ts L139-149
```

Endpoints (api/src/modules/ranking/ranking.plugin.ts), todos atrás do authGuard (cookie do Torre):
```
GET /api/ranking/drivers       -> RankedDriver[]
GET /api/ranking/trips?from&to -> Trip[]   (FECHADA only)
GET /api/ranking/blocks        -> DriverBlockRecord[] (ativos)
GET /api/ranking/route-scores  -> RouteScoreRecord[]
GET /api/ranking/stats         -> RankingStats
```

Padrão Eden Treaty para segmento com hífen (route-scores): bracket-notation + cast `as any` — IDÊNTICO a useInsights `(api.api.insights as any)['sla-history'].get(...)`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: useRanking.ts — 5 hooks Eden Treaty + re-export de tipos</name>
  <files>torre-de-controle/src/hooks/useRanking.ts</files>
  <read_first>
    - torre-de-controle/src/hooks/useInsights.ts (TEMPLATE EXATO: queryKey namespace, staleTime 30_000, contrato de retorno { data, isLoading, isError, error, refetch }, bracket-notation `(api.api.X as any)['seg'].get(...)` para hífen, tratamento de error)
    - torre-de-controle/src/hooks/useTrips.ts (variação com query params via `.get({ query })`)
    - torre-de-controle/src/lib/api.ts (client treaty<App> com credentials:'include')
    - torre-de-controle/src/types/api.ts (re-export do App vindo de api/src/types/api.ts)
    - api/src/modules/ranking/ranking.service.ts (linhas 70-85 — RankedDriver, RankingStats)
    - api/src/modules/ranking/ranking.types.ts (Driver/Trip/DriverBlockRecord/RouteScoreRecord)
  </read_first>
  <action>
    Criar `torre-de-controle/src/hooks/useRanking.ts` com 5 hooks seguindo o padrão de useInsights (PHASE8-RANKING-HOOKS).
    Hooks e endpoints:
    - `useRankingDrivers()` -> `api.api.ranking.drivers.get()` -> `RankedDriver[]`. queryKey `['ranking','drivers']`.
    - `useRankingTrips(filters?: { from?: string; to?: string })` -> `api.api.ranking.trips.get({ query: filters ?? {} })` -> `Trip[]`. queryKey `['ranking','trips', filters]`.
    - `useRankingBlocks()` -> `api.api.ranking.blocks.get()` -> `DriverBlockRecord[]`. queryKey `['ranking','blocks']`.
    - `useRankingRouteScores()` -> `(api.api.ranking as any)['route-scores'].get()` -> `RouteScoreRecord[]` (bracket-notation por causa do hífen, idêntico a useInsights['sla-history']). queryKey `['ranking','route-scores']`.
    - `useRankingStats()` -> `api.api.ranking.stats.get()` -> `RankingStats`. queryKey `['ranking','stats']`.
    Cada hook: `staleTime: 30_000`; em erro `throw new Error((error.value as any)?.error ?? 'Failed to fetch ranking <x>')`; retorno `{ data: q.data ?? <default>, isLoading, isError, error, refetch }` (default `[]` para listas; para stats default `{ activeDrivers:0, top3Avg:0, totalTrips:0, activeBlocks:0 }`).
    Re-exportar os tipos do contrato para as abas: `export type { RankedDriver, RankingStats } from '../../../api/src/modules/ranking/ranking.service'` e `export type { Driver, Trip, DriverBlockRecord, RouteScoreRecord, StatusMetrics } from '../../../api/src/modules/ranking/ranking.types'`. (O front já importa o App por path relativo em types/api.ts — usar o mesmo estilo de path.)
    READ-ONLY: nenhum hook de mutação (POST/PATCH) — escrita é Phase 9.
    Lint: `@typescript-eslint/no-explicit-any` é warn (não bloqueia) — o cast `as any` no treaty segue o padrão estabelecido.
  </action>
  <acceptance_criteria>
    - `cd torre-de-controle && npx tsc -b --noEmit` exit 0
    - grep confirma os 5 exports: `grep -E "export function useRanking(Drivers|Trips|Blocks|RouteScores|Stats)" torre-de-controle/src/hooks/useRanking.ts` retorna 5 linhas
    - grep confirma re-export de tipos: `grep -c "export type" torre-de-controle/src/hooks/useRanking.ts` >= 2
    - grep confirma consumo do treaty: `grep -c "api.api.ranking" torre-de-controle/src/hooks/useRanking.ts` >= 5
    - grep confirma bracket-notation para route-scores: `grep "\['route-scores'\]" torre-de-controle/src/hooks/useRanking.ts` retorna >= 1 linha
    - Nenhum hook de escrita: `grep -E "\.(post|patch|delete)\(" torre-de-controle/src/hooks/useRanking.ts` retorna 0 linhas
  </acceptance_criteria>
  <verify>
    <automated>cd torre-de-controle && npx tsc -b --noEmit</automated>
  </verify>
  <done>useRanking.ts existe com os 5 hooks tipados consumindo /api/ranking/* via Eden Treaty, tipos re-exportados, tsc limpo, sem mutações.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /api/ranking | Mesma origem; cookie JWT do Torre é a única credencial. O service_role do ride-rank NUNCA chega ao browser (proxy server-side, D-V2-01). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Information Disclosure | useRanking.ts | accept | Hooks só leem /api/ranking same-origin com `credentials:'include'`; nenhuma secret é referenciada no front. service_role permanece no Elysia (T-07-11). |
| T-08-02 | Spoofing | Eden Treaty fetch | mitigate | authGuard do backend (já implementado, T-07-09) rejeita requisições sem cookie de sessão com 401; o hook propaga o erro. |
</threat_model>

<verification>
- `cd torre-de-controle && npx tsc -b --noEmit` exit 0 (projeto inteiro compila com os novos hooks)
- 5 hooks exportados, tipos re-exportados, consumo via `api.api.ranking.*`, zero mutações
</verification>

<success_criteria>
useRanking.ts pronto: 5 hooks read-only tipados pelo contrato Phase 7 via Eden Treaty, padrão useInsights respeitado (staleTime/return-shape), tipos disponíveis para as abas das waves seguintes.
</success_criteria>

<output>
After completion, create `.planning/phases/08-ranking-ui/08-01-SUMMARY.md`
</output>
