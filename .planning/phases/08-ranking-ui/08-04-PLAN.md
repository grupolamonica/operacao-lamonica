---
phase: 08-ranking-ui
plan: 04
type: execute
wave: 3
depends_on: ["08-01", "08-02", "08-03"]
files_modified:
  - torre-de-controle/src/app/pages/ranking/components/RankingTab.tsx
  - torre-de-controle/src/app/pages/ranking/components/DriverDetailsDialog.tsx
autonomous: true
requirements: [PHASE8-TAB-RANKING, PHASE8-MODAIS-SHELL]
must_haves:
  truths:
    - "A aba Ranking lista os motoristas reais de /api/ranking/drivers numa DataTable ordenavel"
    - "Colunas: rank, nome, pontuacao, vinculo, viagens, ocorrencias, status, e metricas ETA (origem/destino on-time/early/delay)"
    - "A tabela ordena por pontuacao desc por padrao e permite reordenar por colunas numericas"
    - "Clicar numa linha abre o DriverDetailsDialog em modo somente-leitura com o detalhamento do motorista"
  artifacts:
    - path: "torre-de-controle/src/app/pages/ranking/components/RankingTab.tsx"
      provides: "DataTable de RankedDriver com colunas e ordenacao + abertura do modal de detalhes"
      min_lines: 80
      exports: ["RankingTab"]
    - path: "torre-de-controle/src/app/pages/ranking/components/DriverDetailsDialog.tsx"
      provides: "Modal read-only de detalhes do motorista (KPIs + metricas ETA + viagens do motorista)"
      min_lines: 60
      exports: ["DriverDetailsDialog"]
  key_links:
    - from: "torre-de-controle/src/app/pages/ranking/components/RankingTab.tsx"
      to: "useRankingDrivers"
      via: "hook import"
      pattern: "useRankingDrivers"
    - from: "torre-de-controle/src/app/pages/ranking/components/RankingTab.tsx"
      to: "DriverDetailsDialog"
      via: "onRowClick -> setSelectedDriver"
      pattern: "DriverDetailsDialog"
---

<objective>
Preencher a aba Ranking: uma DataTable (TanStack Table, padrao Torre) com os motoristas reais de `/api/ranking/drivers`, ordenavel por pontuacao (desc default) e demais colunas numericas, com as metricas ETA. Clicar numa linha abre o `DriverDetailsDialog` (modal shadcn) em modo somente-leitura com o detalhamento do motorista e suas viagens.

Purpose: recriar o comportamento da tabela de ranking do ride-rank (DriverRanking.tsx) no design Torre, READ-ONLY (a edicao de nome / avaliacao e Phase 9).
Output: RankingTab.tsx + DriverDetailsDialog.tsx (substituindo o stub do 08-03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-ranking-ui/08-CONTEXT.md

<interfaces>
Hooks (08-01, torre-de-controle/src/hooks/useRanking.ts):
```typescript
useRankingDrivers(): { data: RankedDriver[]; isLoading; ... }
useRankingTrips(): { data: Trip[]; isLoading; ... }   // para listar as viagens do motorista no modal
// RankedDriver = Driver & { rank: number|null }
// Driver = { id, nome, status:'ATIVO'|'BLOQUEADO', pontuacao, totalViagens, ocorrencias, created_at, etaOrigMetrics:{onTime,early,delay}, etaDestMetrics:{...}, vinculo }
```
DataTable (torre-de-controle/src/components/domain/DataTable.tsx): generico `<T extends { id: string }>`, props `{ data, columns: ColumnDef<T,unknown>[], onRowClick?, selectedId?, pageSize?, title?, subtitle?, toolbar? }`. Ja embrulha em PanelCard. NOTA: a DataTable atual NAO tem getSortedRowModel — ver acao para habilitar ordenacao.
ColumnDef de @tanstack/react-table. shadcn Dialog (torre-de-controle/src/components/ui/dialog.tsx): Dialog/DialogContent/DialogHeader/DialogTitle/DialogDescription.
fixMojibake (08-02, torre-de-controle/src/lib/mojibake.ts): aplicar em nome e vinculo na exibicao.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: RankingTab — DataTable ordenavel de RankedDriver</name>
  <files>torre-de-controle/src/app/pages/ranking/components/RankingTab.tsx</files>
  <read_first>
    - ride-rank: C:\Users\antonio.magalhaes\Documents\Projetos\produção\ride-rank-buddy\src\components\DriverRanking.tsx (COLUNAS e comportamento: #/rank, Motorista, Pontos, Vinculo, Viagens, Ocorr., ETA Origem [On Time/Early/Delay], ETA Destino [On Time/Early/Delay]; linha clicavel abre detalhes; top-3 destacado)
    - torre-de-controle/src/components/domain/DataTable.tsx (API + como as colunas/headers/cells sao definidos; onRowClick/selectedId)
    - torre-de-controle/src/components/domain/StatusBadge.tsx (badge de status no padrao Torre — usar para ATIVO/BLOQUEADO)
    - torre-de-controle/src/hooks/useRanking.ts (useRankingDrivers)
    - torre-de-controle/src/lib/mojibake.ts (fixMojibake)
  </read_first>
  <action>
    Substituir o stub `RankingTab` (PHASE8-TAB-RANKING). Chamar `useRankingDrivers()`. Montar `columns: ColumnDef<RankedDriver, unknown>[]`:
    - rank: header "#", cell mostra `row.rank ?? '-'` (BLOQUEADO sem rank), destacar top-3 (ex.: cor de destaque quando rank<=3).
    - nome: header "Motorista", cell `fixMojibake(row.nome)`.
    - pontuacao: header "Pontos", numerico, alinhado a direita, fonte mono.
    - vinculo: header "Vinculo", cell `fixMojibake(row.vinculo)`.
    - totalViagens: header "Viagens", numerico.
    - ocorrencias: header "Ocorr.", numerico (destacar quando >0).
    - status: header "Status", usar StatusBadge (ATIVO=success, BLOQUEADO=danger).
    - etaOrigMetrics.onTime/early/delay e etaDestMetrics.onTime/early/delay: 6 colunas numericas com sufixo "%" (cell `value.toFixed(1)+'%'`), agrupadas visualmente (usar accessorFn `row.etaOrigMetrics.onTime` etc.).
    ORDENACAO: a DataTable do Torre hoje nao inclui `getSortedRowModel`. Para habilitar ordenacao ordenavel por colunas sem alterar a DataTable compartilhada (evitar overlap de arquivo), ordenar os dados ANTES de passar: manter um estado `sortBy`/`sortDir` local na RankingTab e passar `data={sortedDrivers}` ja ordenado (default por `pontuacao` desc). Cabecalhos das colunas numericas viram botoes que alternam o sort (onClick no header via `column.columnDef.header` como elemento clicavel). Alternativa aceita: usar `<DataTable>` apenas para layout e ordenar o array no componente. Documentar a escolha no SUMMARY. (NAO editar DataTable.tsx — pertence a outro escopo.)
    Passar `onRowClick={(d) => setSelectedDriver(d)}`, `selectedId={selectedDriver?.id ?? null}`, `title="Ranking de Motoristas"`, `subtitle={`${drivers.length} motoristas`}`.
    Renderizar `<DriverDetailsDialog driver={selectedDriver} open={!!selectedDriver} onOpenChange={(o)=>!o && setSelectedDriver(null)} />` (componente do Task 2).
    READ-ONLY: SEM botao de editar nome / avaliar (isso era escrita no ride-rank). Se quiser sinalizar, um botao "Avaliar" pode aparecer DISABLED com title "Disponivel na Phase 9" — opcional; o foco e leitura.
  </action>
  <acceptance_criteria>
    - `cd torre-de-controle && npx tsc -b --noEmit` exit 0
    - grep confirma hook: `grep -c "useRankingDrivers" torre-de-controle/src/app/pages/ranking/components/RankingTab.tsx` >= 1
    - grep confirma mojibake aplicado: `grep -c "fixMojibake" torre-de-controle/src/app/pages/ranking/components/RankingTab.tsx` >= 1
    - grep confirma DataTable usada: `grep -c "DataTable" torre-de-controle/src/app/pages/ranking/components/RankingTab.tsx` >= 1
    - grep confirma metricas ETA nas colunas: `grep -c "etaOrigMetrics\|etaDestMetrics" torre-de-controle/src/app/pages/ranking/components/RankingTab.tsx` >= 2
    - grep confirma modal montado: `grep "DriverDetailsDialog" torre-de-controle/src/app/pages/ranking/components/RankingTab.tsx` retorna >= 1 linha
    - READ-ONLY: nenhuma chamada de escrita: `grep -E "\.(post|patch|delete)\(|upsert|evaluateTrip|createRouteScore" torre-de-controle/src/app/pages/ranking/components/RankingTab.tsx` retorna 0 linhas
  </acceptance_criteria>
  <verify>
    <automated>cd torre-de-controle && npx tsc -b --noEmit</automated>
  </verify>
  <done>Aba Ranking lista motoristas reais com todas as colunas + metricas ETA, ordenavel (default pontuacao desc), nomes/vinculo com mojibake corrigido, linha clicavel abre detalhes; sem escrita.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: DriverDetailsDialog — modal read-only de detalhes</name>
  <files>torre-de-controle/src/app/pages/ranking/components/DriverDetailsDialog.tsx</files>
  <read_first>
    - ride-rank: C:\Users\antonio.magalhaes\Documents\Projetos\produção\ride-rank-buddy\src\components\DriverDetailsDialog.tsx (CONTEUDO: cards Rank/Pontuacao/Viagens/Ocorrencias/Rotas; metricas ETA Origem/Destino [On Time/Early/Delay]; lista de viagens do motorista. NOTA: o ride-rank usa Accordion + Tabs internas; SIMPLIFICAR — ver acao)
    - torre-de-controle/src/components/ui/dialog.tsx (Dialog/DialogContent/DialogHeader/DialogTitle/DialogDescription)
    - torre-de-controle/src/components/ui/tabs.tsx (se quiser sub-abas internas; Accordion NAO existe no Torre)
    - torre-de-controle/src/components/domain/DataTable.tsx (para a lista de viagens do motorista, opcional) e StatusBadge.tsx
    - torre-de-controle/src/lib/mojibake.ts (fixMojibake no nome/vinculo)
  </read_first>
  <action>
    Substituir o stub criando `DriverDetailsDialog` (PHASE8-MODAIS-SHELL, parte ranking). Props: `{ driver: RankedDriver | null; open: boolean; onOpenChange: (o:boolean)=>void }`. Quando `driver` for null, retornar null.
    Conteudo (somente leitura, design Torre):
    - DialogHeader: titulo `fixMojibake(driver.nome)` + DialogDescription curta; badges de status (StatusBadge) + vinculo (`fixMojibake`) + `ID {driver.id}` + `Rank {driver.rank ?? '-'}`.
    - Grid de cards/metricas (estilo KPI compacto ou DetailItem do Torre): Pontuacao, Viagens (`totalViagens`), Ocorrencias, Rank.
    - Dois blocos de metricas ETA: "ETA Origem" e "ETA Destino", cada um com On Time / Early / Delay em `%` (`driver.etaOrigMetrics.*` e `driver.etaDestMetrics.*`).
    - Lista das viagens do motorista: chamar `useRankingTrips()` e filtrar `trip.driver_id === driver.id`; mostrar numa pequena tabela (ID, data, rota origem->destino, status_eta, status_eta_destino, score_final). Se vazio, mensagem neutra.
    SIMPLIFICACAO vs ride-rank: NAO usar Accordion (inexistente no Torre) nem a "Analise da Lamonica" textual (depende de helpers do ride-rank fora de escopo). Entregar os KPIs + metricas ETA + lista de viagens — que sao os dados reais do contrato. Documentar a simplificacao no SUMMARY.
    READ-ONLY: o modal apenas exibe; nenhum form de edicao/avaliacao (isso e Phase 9).
  </action>
  <acceptance_criteria>
    - `cd torre-de-controle && npx tsc -b --noEmit` exit 0
    - grep confirma props read-only (sem submit): `grep -E "onSubmit|evaluateTrip|\.post\(|\.patch\(" torre-de-controle/src/app/pages/ranking/components/DriverDetailsDialog.tsx` retorna 0 linhas
    - grep confirma metricas ETA exibidas: `grep -c "etaOrigMetrics\|etaDestMetrics" torre-de-controle/src/app/pages/ranking/components/DriverDetailsDialog.tsx` >= 2
    - grep confirma Dialog shadcn: `grep -c "DialogContent" torre-de-controle/src/app/pages/ranking/components/DriverDetailsDialog.tsx` >= 1
    - grep confirma export: `grep "export function DriverDetailsDialog" torre-de-controle/src/app/pages/ranking/components/DriverDetailsDialog.tsx` retorna 1 linha
  </acceptance_criteria>
  <verify>
    <automated>cd torre-de-controle && npx tsc -b --noEmit && npm run build</automated>
  </verify>
  <done>Modal de detalhes abre em modo leitura com KPIs, metricas ETA origem/destino e viagens do motorista; nomes corrigidos; sem qualquer escrita; build passa.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dados de motorista (terceiros) -> render | Nome/vinculo/observacoes vem de fonte externa (ride-rank); renderizados como texto React (auto-escape). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07 | Information Disclosure | RankingTab / DriverDetailsDialog | accept | Exibe dados operacionais de motorista ja autorizados pelo authGuard; nenhum secret. |
| T-08-10 | Tampering (XSS) | render de nome/vinculo | mitigate | Renderizar SEMPRE via interpolacao JSX ({value}) — auto-escape do React. PROIBIDO dangerouslySetInnerHTML em qualquer celula/campo. fixMojibake retorna texto puro (sem HTML). |
</threat_model>

<verification>
- `cd torre-de-controle && npx tsc -b --noEmit` exit 0
- `cd torre-de-controle && npm run build` exit 0
- Aba Ranking + modal de detalhes funcionam com dados reais, read-only, sem dangerouslySetInnerHTML
</verification>

<success_criteria>
Aba Ranking entrega a tabela ordenavel completa (rank, nome, pontuacao, vinculo, viagens, ocorrencias, status, metricas ETA) com dados reais e abre o modal de detalhes read-only ao clicar numa linha. Zero escrita.
</success_criteria>

<output>
After completion, create `.planning/phases/08-ranking-ui/08-04-SUMMARY.md`
</output>
