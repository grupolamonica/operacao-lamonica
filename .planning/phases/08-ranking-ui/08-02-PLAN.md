---
phase: 08-ranking-ui
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - torre-de-controle/src/lib/mojibake.ts
  - torre-de-controle/src/lib/mojibake.test.ts
autonomous: true
requirements: [PHASE8-MOJIBAKE-DISPLAY]
must_haves:
  truths:
    - "Strings com mojibake conhecido (a sequencia citada na linha 19 do 08-CONTEXT) sao exibidas corrigidas (em-dash U+2014) na view"
    - "A correcao acontece SO na camada de apresentacao — nenhum dado do backend e alterado"
    - "Texto ja correto passa inalterado (idempotente, sem corromper acentos validos)"
  artifacts:
    - path: "torre-de-controle/src/lib/mojibake.ts"
      provides: "helper fixMojibake(s) que normaliza sequencias UTF-8-lidas-como-Latin1 comuns para exibicao"
      min_lines: 20
      exports: ["fixMojibake"]
    - path: "torre-de-controle/src/lib/mojibake.test.ts"
      provides: "casos de teste do helper (mojibake conhecido, idempotencia, null/undefined safe)"
  key_links:
    - from: "torre-de-controle/src/lib/mojibake.ts"
      to: "consumo nas abas (waves 3+)"
      via: "funcao pura exportada"
      pattern: "export function fixMojibake"
---

<objective>
Criar um helper de display puro `fixMojibake(s)` que normaliza mojibake comum (texto UTF-8 interpretado como Latin-1) APENAS na camada de view. As abas (waves seguintes) aplicam este helper ao renderizar campos de texto vindos do ride-rank (nome, vinculo, motivo, observacao) sem mexer no dado do backend (que permanece byte-a-byte, D-V2-01 / 08-CONTEXT secao mojibake).

Purpose: o ride-rank guarda strings com encoding inconsistente; o Torre so repassa os bytes. Corrigir no display evita reprocessar/migrar dados e mantem o backend como fonte fiel.
Output: `torre-de-controle/src/lib/mojibake.ts` (+ teste).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-ranking-ui/08-CONTEXT.md

<interfaces>
Padrao de helper puro do projeto: ver torre-de-controle/src/lib/formatters.ts (funcoes de formatacao de display, sem efeito colateral) e torre-de-controle/src/lib/utils.ts (cn). O helper novo segue o mesmo estilo (funcao exportada, sem React).

FONTE DO CASO CONCRETO: ler `.planning/phases/08-ranking-ui/08-CONTEXT.md` linha 19 — ela mostra um valor de `vinculo` com mojibake que deve aparecer como um travessao (em-dash, U+2014) na view. Copiar a sequencia mojibake EXATA daquela linha do CONTEXT para a tabela do helper (nao reescrever de memoria — evitar erro de transcricao de caracteres invisiveis).

TABELA DE MAPEAMENTO (descrita por destino para manter este plano ASCII-safe; o executor monta os pares lendo as fontes reais):
- Sequencias UTF-8-lidas-como-Latin1 -> caractere de pontuacao correto:
  - travessao (em-dash, U+2014), traco (en-dash, U+2013), apostrofo tipografico (U+2019), aspas curvas de abertura/fechamento (U+201C / U+201D).
- Sequencias de acentos pt-BR mais comuns -> letra acentuada correta:
  - minusculas: a-til, a-agudo, e-agudo, e-circunflexo, i-agudo, o-agudo, o-circunflexo, u-agudo, c-cedilha.
  - maiusculas: C-cedilha, E-agudo, A-grave.
- Remover o caractere orfao U+00C2 (A-circunflexo "fantasma") quando aparece antes de espaco/pontuacao.
FONTE PARA OS PARES EXATOS: a planilha/dados do ride-rank ja exibem esses casos; o conjunto canonico de mojibake UTF-8->Latin1 e padrao. O executor deve gerar os pares e cobri-los pelos testes do bloco <behavior> (que usam caracteres concretos digitados no arquivo de teste, nao neste plano).
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: fixMojibake helper (RED->GREEN->REFACTOR)</name>
  <files>torre-de-controle/src/lib/mojibake.ts, torre-de-controle/src/lib/mojibake.test.ts</files>
  <read_first>
    - torre-de-controle/src/lib/formatters.ts (estilo de helper puro de display do projeto)
    - .planning/phases/08-ranking-ui/08-CONTEXT.md (linha 19 — caso real do `vinculo` com mojibake que vira um em-dash; regra: corrigir so display)
  </read_first>
  <behavior>
    Escrever os testes em `mojibake.test.ts` digitando os caracteres concretos (a sequencia mojibake do CONTEXT no input, o caractere correto no esperado). Casos obrigatorios:
    - Test 1: a sequencia mojibake exata da linha 19 do 08-CONTEXT, isolada, deve virar um em-dash (U+2014). Este e o caso citado no escopo.
    - Test 2: um nome pt-BR com mojibake de a-til + o (ex.: a forma corrompida de "Joao da Silva") deve virar "Joao da Silva" com o a-til correto.
    - Test 3: texto puramente ASCII ("Atraso na portaria") passa inalterado.
    - Test 4: idempotencia — aplicar o helper duas vezes sobre o input do Test 1 produz o mesmo em-dash (rodar 2x nao corrompe).
    - Test 5: null/undefined/'' -> retorna '' e nunca lanca (`fixMojibake(null as any) === ''`, idem undefined e '').
    - Test 6: NAO corromper acentos ja corretos — "Operacao concluida" com c-cedilha/a-til/i-agudo validos passa inalterado (garante que a tabela so casa as sequencias mojibake, nunca letras acentuadas corretas).
  </behavior>
  <action>
    Implementar `export function fixMojibake(input: string | null | undefined): string` em `torre-de-controle/src/lib/mojibake.ts` (PHASE8-MOJIBAKE-DISPLAY).
    Estrategia: se input for null/undefined -> retornar ''. Caso contrario, aplicar substituicoes de sequencias mojibake conhecidas via uma tabela de pares aplicada em ORDEM (das sequencias mais longas para as mais curtas, para evitar substituicao parcial) com `replaceAll`/regex global. A tabela cobre os destinos descritos no bloco <interfaces> (pontuacao tipografica + acentos pt-BR minusculos e maiusculos + remocao do A-circunflexo orfao). Copiar a sequencia mojibake do caso real direto da linha 19 do 08-CONTEXT.
    CRITICO (idempotencia + nao-corrupcao): so substituir as SEQUENCIAS mojibake (que comecam por A-til/A-circunflexo seguidos de um segundo byte). Letras acentuadas ja corretas (a-til, e-agudo, c-cedilha isolados) NAO contem essas sequencias de dois caracteres, entao a tabela e segura — provar via Test 6.
    NAO exportar nada alem de `fixMojibake`. Funcao pura, sem React, sem I/O. Nenhuma chamada a backend (o dado permanece intacto — esta e so a view).
    Runner de teste: o front nao tem suite configurada (sem vitest). Escrever `mojibake.test.ts` como script de asserts usando `node:assert/strict`, executavel via `npx --yes tsx torre-de-controle/src/lib/mojibake.test.ts` (tsx resolve TS sem config). Garantir que o arquivo de teste seja salvo em UTF-8.
  </action>
  <acceptance_criteria>
    - RED primeiro: rodar o teste antes da implementacao falha; depois passa.
    - `npx --yes tsx torre-de-controle/src/lib/mojibake.test.ts` exit 0 (todos os asserts passam)
    - `cd torre-de-controle && npx tsc -b --noEmit` exit 0
    - grep confirma export unico: `grep -c "export " torre-de-controle/src/lib/mojibake.ts` === 1
    - grep confirma que o destino em-dash existe na tabela: `grep -c "u2014\|\\\\u2014" torre-de-controle/src/lib/mojibake.ts` >= 1 OU o em-dash literal aparece no arquivo (qualquer uma das formas conta)
    - Sem chamadas a backend/fetch no helper: `grep -E "fetch|api\." torre-de-controle/src/lib/mojibake.ts` retorna 0 linhas
  </acceptance_criteria>
  <verify>
    <automated>npx --yes tsx torre-de-controle/src/lib/mojibake.test.ts && cd torre-de-controle && npx tsc -b --noEmit</automated>
  </verify>
  <done>fixMojibake corrige mojibake conhecido na view, e idempotente e null-safe, nao corrompe acentos validos, nao toca o backend; teste verde e tsc limpo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dado do backend -> string renderizada | A transformacao e unidirecional e somente-display; o valor original (bytes) nunca e reescrito no backend. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | Tampering | fixMojibake | accept | Helper puro de apresentacao; nao persiste nada. O backend mantem byte-a-byte (D-V2-01). Risco de alteracao de dado = nulo. |
| T-08-04 | Information Disclosure | render de texto de terceiros | mitigate | A normalizacao nao introduz HTML; o consumo nas abas usa render de texto do React (auto-escape), nao dangerouslySetInnerHTML (ver threat_model das abas, T-08-10). |
</threat_model>

<verification>
- `npx --yes tsx torre-de-controle/src/lib/mojibake.test.ts` exit 0
- `cd torre-de-controle && npx tsc -b --noEmit` exit 0
- Helper puro, export unico, caso do CONTEXT coberto, zero acesso a backend
</verification>

<success_criteria>
Helper de display de mojibake pronto e testado, idempotente e seguro, disponivel para as abas aplicarem em campos de texto do ride-rank sem alterar o dado de origem.
</success_criteria>

<output>
After completion, create `.planning/phases/08-ranking-ui/08-02-SUMMARY.md`
</output>
