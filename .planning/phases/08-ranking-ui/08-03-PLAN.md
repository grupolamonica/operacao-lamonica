---
phase: 08-ranking-ui
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - torre-de-controle/src/app/router.tsx
  - torre-de-controle/src/app/layout/AppSidebar.tsx
  - torre-de-controle/src/app/pages/ranking/RankingPage.tsx
  - torre-de-controle/src/app/pages/ranking/components/StatsCards.tsx
  - torre-de-controle/src/app/pages/ranking/components/RankingTab.tsx
  - torre-de-controle/src/app/pages/ranking/components/ViagensTab.tsx
  - torre-de-controle/src/app/pages/ranking/components/QualidadeTab.tsx
  - torre-de-controle/src/app/pages/ranking/components/BloqueiosTab.tsx
  - torre-de-controle/src/app/pages/ranking/components/RotasTab.tsx
  - torre-de-controle/src/app/pages/ranking/components/LogsTab.tsx
autonomous: true
requirements: [PHASE8-RANKING-ROUTE-NAV, PHASE8-STATSCARDS]
must_haves:
  truths:
    - "Existe um item 'Ranking' no sidebar Argon flutuante que navega para /ranking"
    - "A rota /ranking carrega lazy (React.lazy) dentro do AppLayout autenticado, padrao 06-07"
    - "A RankingPage mostra um shell de 6 abas (shadcn Tabs) na ordem Ranking/Viagens/Qualidade/Bloqueios/Rotas/Logs"
    - "Os 4 StatsCards (Motoristas Ativos, Media Top 3, Total Viagens, Bloqueios) renderizam os numeros reais de /api/ranking/stats no padrao KPICard"
  artifacts:
    - path: "torre-de-controle/src/app/pages/ranking/RankingPage.tsx"
      provides: "Shell da pagina: header + StatsCards + Tabs montando os 6 componentes de aba"
      min_lines: 50
      exports: ["RankingPage"]
    - path: "torre-de-controle/src/app/pages/ranking/components/StatsCards.tsx"
      provides: "4 KPICards alimentados por useRankingStats"
      min_lines: 30
      exports: ["StatsCards"]
    - path: "torre-de-controle/src/app/pages/ranking/components/RankingTab.tsx"
      provides: "Stub inicial da aba Ranking (preenchido no 08-04)"
    - path: "torre-de-controle/src/app/pages/ranking/components/LogsTab.tsx"
      provides: "Stub inicial da aba Logs (preenchido no 08-08)"
  key_links:
    - from: "torre-de-controle/src/app/router.tsx"
      to: "ranking/RankingPage"
      via: "React.lazy + rota 'ranking' no AppLayout"
      pattern: "lazy\\(.*ranking/RankingPage"
    - from: "torre-de-controle/src/app/layout/AppSidebar.tsx"
      to: "/ranking"
      via: "navItems entry"
      pattern: "/ranking"
    - from: "torre-de-controle/src/app/pages/ranking/components/StatsCards.tsx"
      to: "useRankingStats"
      via: "hook import"
      pattern: "useRankingStats"
---

<objective>
Montar o esqueleto navegavel da feature: item de menu no sidebar Argon, rota lazy `/ranking`, a `RankingPage` com header + StatsCards + shell de 6 abas (shadcn Tabs) e os 4 KPIs reais. Para permitir que as abas sejam preenchidas em paralelo na Wave 3 SEM conflito de arquivo, este plano cria TAMBEM os 6 componentes de aba como STUBS ja importados e wirados nas Tabs (cada um em seu proprio arquivo). As waves seguintes so editam o arquivo do seu stub — RankingPage nao e tocada de novo.

Purpose: entregar a casca funcional (rota + nav + abas + KPIs) e fixar os contratos de arquivo para a Wave 3 paralela.
Output: rota lazy, item de sidebar, RankingPage, StatsCards e 6 stubs de aba.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-ranking-ui/08-CONTEXT.md

<interfaces>
Hooks disponiveis (criados no 08-01, torre-de-controle/src/hooks/useRanking.ts):
```typescript
useRankingStats(): { data: RankingStats; isLoading; isError; error; refetch }
// RankingStats = { activeDrivers: number; top3Avg: number; totalTrips: number; activeBlocks: number }
```
KPICard (torre-de-controle/src/components/domain/KPICard.tsx) props:
```typescript
{ title: string; value: string|number; subtitle?: string; total?: number; color?: 'green'|'blue'|'orange'|'red'|'purple'|'gray'; icon?: React.ElementType }
```
shadcn Tabs (torre-de-controle/src/components/ui/tabs.tsx): exporta Tabs, TabsList, TabsTrigger, TabsContent.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rota lazy /ranking + item no AppSidebar Argon</name>
  <files>torre-de-controle/src/app/router.tsx, torre-de-controle/src/app/layout/AppSidebar.tsx</files>
  <read_first>
    - torre-de-controle/src/app/router.tsx (padrao EXATO de lazy chunk: `const XPage = lazy(() => import('./pages/x/XPage').then(m => ({ default: m.XPage })))` e a rota dentro de AppLayout envolta por `<L>...</L>`)
    - torre-de-controle/src/app/layout/AppSidebar.tsx (array `navItems` com `{ to, label, icon }`; o item ativo usa gradiente Argon — so adicionar uma entrada)
  </read_first>
  <action>
    Em `router.tsx` (PHASE8-RANKING-ROUTE-NAV): adicionar `const RankingPage = lazy(() => import('./pages/ranking/RankingPage').then(m => ({ default: m.RankingPage })))` junto aos demais lazy chunks, e adicionar a rota `{ path: 'ranking', element: <L><RankingPage /></L> }` dentro do children do AppLayout (mesma posicao logica das outras, ex.: depois de 'insights').
    Em `AppSidebar.tsx`: adicionar ao array `navItems` a entrada `{ to: '/ranking', label: 'Ranking', icon: Trophy }` (importar `Trophy` de lucide-react). Posicionar de forma coerente (ex.: depois de 'insights', antes de 'configuracoes'). Nao alterar o estilo/estrutura do sidebar — so o array.
  </action>
  <acceptance_criteria>
    - grep confirma lazy import: `grep "ranking/RankingPage" torre-de-controle/src/app/router.tsx` retorna >= 1 linha contendo `lazy(`
    - grep confirma rota: `grep "path: 'ranking'" torre-de-controle/src/app/router.tsx` retorna 1 linha
    - grep confirma item de menu: `grep "to: '/ranking'" torre-de-controle/src/app/layout/AppSidebar.tsx` retorna 1 linha
    - grep confirma icone importado: `grep "Trophy" torre-de-controle/src/app/layout/AppSidebar.tsx` retorna >= 1 linha
  </acceptance_criteria>
  <verify>
    <automated>cd torre-de-controle && npx tsc -b --noEmit</automated>
  </verify>
  <done>/ranking existe como rota lazy autenticada e ha um item 'Ranking' no sidebar Argon navegando para ela.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: StatsCards (4 KPIs reais) + 6 stubs de aba</name>
  <files>torre-de-controle/src/app/pages/ranking/components/StatsCards.tsx, torre-de-controle/src/app/pages/ranking/components/RankingTab.tsx, torre-de-controle/src/app/pages/ranking/components/ViagensTab.tsx, torre-de-controle/src/app/pages/ranking/components/QualidadeTab.tsx, torre-de-controle/src/app/pages/ranking/components/BloqueiosTab.tsx, torre-de-controle/src/app/pages/ranking/components/RotasTab.tsx, torre-de-controle/src/app/pages/ranking/components/LogsTab.tsx</files>
  <read_first>
    - torre-de-controle/src/components/domain/KPICard.tsx (props e estilo do KPI Argon)
    - torre-de-controle/src/app/pages/viagens/components/ViagensKPIRow.tsx (se existir — exemplo de linha de KPIs alimentada por hook; senao usar o grid de KPIs do DashboardPage)
    - ride-rank: C:\Users\antonio.magalhaes\Documents\Projetos\produção\ride-rank-buddy\src\components\StatsCards.tsx (COMPORTAMENTO: 4 cards = Motoristas Ativos [value=ativos / total drivers], Media Top 3 [pts], Total Viagens, Bloqueios)
    - torre-de-controle/src/hooks/useRanking.ts (useRankingStats — criado no 08-01)
  </read_first>
  <action>
    StatsCards.tsx (PHASE8-STATSCARDS): componente `StatsCards` que chama `useRankingStats()` e renderiza 4 `KPICard` em grid `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-5` (mesmo grid Argon do dashboard):
      1. "Motoristas Ativos" value=`stats.activeDrivers` icon=Users color="blue"
      2. "Media Top 3" value=`stats.top3Avg` subtitle="pts" icon=BarChart3 color="green"
      3. "Total Viagens" value=`stats.totalTrips` icon=Route color="purple"
      4. "Bloqueios" value=`stats.activeBlocks` icon=ShieldAlert color="orange"
    (icones de lucide-react). Enquanto `isLoading`, renderizar os KPICards com value `'—'` (em-dash) ou um Skeleton — escolher e seguir o padrao do dashboard. Os numeros vem PRONTOS do backend (/stats) — sem recomputar.
    Os 6 stubs de aba (RankingTab/ViagensTab/QualidadeTab/BloqueiosTab/RotasTab/LogsTab): cada um exporta um componente nomeado homonimo que, por ora, renderiza um `PanelCard` com um texto neutro ("Carregando aba..." ou o titulo da aba). Manter MINIMO — serao substituidos integralmente nas waves 3 (cada arquivo pertence a um plano: RankingTab->08-04, ViagensTab->08-05, QualidadeTab->08-06, Bloqueios/Rotas->08-07, LogsTab->08-08). Definir uma assinatura de props vazia ou opcional para nao quebrar quando preenchidos (ex.: `export function RankingTab() {}`).
    READ-ONLY: nenhum botao de escrita aqui.
  </action>
  <acceptance_criteria>
    - `cd torre-de-controle && npx tsc -b --noEmit` exit 0
    - grep confirma consumo do hook: `grep -c "useRankingStats" torre-de-controle/src/app/pages/ranking/components/StatsCards.tsx` >= 1
    - grep confirma 4 KPICard: `grep -c "KPICard" torre-de-controle/src/app/pages/ranking/components/StatsCards.tsx` >= 4
    - grep confirma os 4 titulos: cada um de "Motoristas Ativos", "Media Top 3"|"Top 3", "Total Viagens"|"Viagens", "Bloqueios" aparece no arquivo
    - os 6 stubs existem e exportam: `grep -l "export function RankingTab" .../RankingTab.tsx` etc. (todos os 6 com export nomeado)
  </acceptance_criteria>
  <verify>
    <automated>cd torre-de-controle && npx tsc -b --noEmit</automated>
  </verify>
  <done>StatsCards mostra os 4 KPIs reais de /stats no padrao KPICard; os 6 stubs de aba existem com export nomeado, prontos para preenchimento na Wave 3.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: RankingPage shell (header + StatsCards + Tabs com as 6 abas)</name>
  <files>torre-de-controle/src/app/pages/ranking/RankingPage.tsx</files>
  <read_first>
    - torre-de-controle/src/app/pages/insights/InsightsPage.tsx (LAYOUT: `<div className="space-y-5">` + header com `<h1 className="text-2xl font-bold text-white">` e subtitulo `text-white/70`)
    - torre-de-controle/src/app/pages/viagens/ViagensPage.tsx (header + KPIs + conteudo, padrao mais simples)
    - ride-rank: C:\Users\antonio.magalhaes\Documents\Projetos\produção\ride-rank-buddy\src\pages\Index.tsx (ORDEM e nomes das 6 abas + icones: Ranking/Trophy, Viagens/FileText, Qualidade/BarChart3, Bloqueios/ShieldAlert, Rotas/Route, Logs/ScrollText)
    - torre-de-controle/src/components/ui/tabs.tsx (API: Tabs/TabsList/TabsTrigger/TabsContent)
  </read_first>
  <action>
    Criar `RankingPage` (PHASE8-RANKING-ROUTE-NAV) seguindo o layout das paginas do Torre:
    - `<div className="space-y-5">`
    - header: `<h1 className="text-2xl font-bold text-white">Ranking</h1>` + `<p className="text-sm text-white/70">Avaliacao e ranking de motoristas (somente leitura)</p>`.
    - `<StatsCards />` (do Task 2).
    - `<Tabs defaultValue="ranking" className="space-y-4">` com `<TabsList>` contendo os 6 `<TabsTrigger>` na ORDEM do ride-rank: ranking, viagens, qualidade, bloqueios, rotas, logs (com icone lucide em cada, opcional). Cada `<TabsContent value="...">` renderiza o componente de aba correspondente importado dos stubs: `<RankingTab/>`, `<ViagensTab/>`, `<QualidadeTab/>`, `<BloqueiosTab/>`, `<RotasTab/>`, `<LogsTab/>`.
    Estilizar o TabsList no tom Argon (fundo card/borda) — seguir o visual do Torre, nao copiar classes do ride-rank.
    Esta e a UNICA edicao de RankingPage no projeto — as abas das waves 3 NAO tocam este arquivo (so seus proprios stubs).
  </action>
  <acceptance_criteria>
    - `cd torre-de-controle && npx tsc -b --noEmit` exit 0
    - `cd torre-de-controle && npm run build` exit 0 (build de producao com o chunk lazy de ranking)
    - grep confirma 6 TabsTrigger: `grep -c "TabsTrigger" torre-de-controle/src/app/pages/ranking/RankingPage.tsx` === 6
    - grep confirma 6 TabsContent: `grep -c "TabsContent" torre-de-controle/src/app/pages/ranking/RankingPage.tsx` === 6
    - grep confirma os 6 componentes de aba referenciados: cada `<RankingTab`, `<ViagensTab`, `<QualidadeTab`, `<BloqueiosTab`, `<RotasTab`, `<LogsTab` aparece
    - grep confirma StatsCards montado: `grep "<StatsCards" torre-de-controle/src/app/pages/ranking/RankingPage.tsx` retorna 1 linha
  </acceptance_criteria>
  <verify>
    <automated>cd torre-de-controle && npx tsc -b --noEmit && npm run build</automated>
  </verify>
  <done>RankingPage renderiza header + 4 KPIs + 6 abas shadcn (stubs montados) no design Torre; build de producao passa.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rota protegida | /ranking fica dentro do AuthGuard + AppLayout (mesma protecao das demais rotas); sem auth, redireciona ao login. |
| browser -> /api/ranking/stats | Same-origin com cookie do Torre; numeros vem computados do backend, sem secret no front. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05 | Elevation of Privilege | rota /ranking | mitigate | Rota montada como filha de AuthGuard/AppLayout (router.tsx) — herda a protecao de sessao existente; nenhum bypass introduzido. |
| T-08-06 | Information Disclosure | StatsCards | accept | So exibe agregados (contagens/medias) de /stats; nenhum PII sensivel nem secret. |
</threat_model>

<verification>
- `cd torre-de-controle && npx tsc -b --noEmit` exit 0
- `cd torre-de-controle && npm run build` exit 0
- Item de menu + rota lazy + 6 abas + 4 KPIs reais presentes; 6 stubs prontos para Wave 3
</verification>

<success_criteria>
Casca navegavel e funcional: clicar em 'Ranking' no sidebar abre /ranking (lazy) com 4 KPIs reais e 6 abas no design Torre. Contratos de arquivo das abas fixados para a Wave 3 paralela sem conflito.
</success_criteria>

<output>
After completion, create `.planning/phases/08-ranking-ui/08-03-SUMMARY.md`
</output>
