---
phase: 11-mapa-frota-importada-geospatial
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - torre-de-controle/src/hooks/useFleetPositions.ts
  - torre-de-controle/src/components/domain/LiveMap.tsx
autonomous: true
requirements: [D-11-01, D-11-04, D-11-05, D-11-06]
must_haves:
  truths:
    - "O LiveMap mostra um toggle 'Frota importada' (perto do toggle mapa/satélite) que liga/desliga a camada"
    - "Com a camada ligada, os motoristas geocodados aparecem como ícones de CAMINHÃO (não pino)"
    - "A cor do caminhão segue o status do ranking: ATIVO verde, BLOQUEADO vermelho, sem-match cinza"
    - "Caminhões próximos clusterizam num círculo com contagem; clicar no cluster dá zoom/expande"
    - "Clicar num caminhão abre popup com nome, cidade/UF, data, veículo + (se ranked) rank/pontuação/status"
    - "A camada de veículos ao vivo (WebSocket/usePositionsStore) continua funcionando intacta"
  artifacts:
    - path: "torre-de-controle/src/hooks/useFleetPositions.ts"
      provides: "useFleetPositions — Eden Treaty GET /api/positions + TanStack Query (padrão useTrips/useRanking)"
      contains: "useFleetPositions"
    - path: "torre-de-controle/src/components/domain/LiveMap.tsx"
      provides: "camada GeoJSON cluster de caminhões por status + toggle + popup"
      contains: "fleet"
  key_links:
    - from: "torre-de-controle/src/hooks/useFleetPositions.ts"
      to: "/api/positions"
      via: "api.api.positions.get() (Eden Treaty)"
      pattern: "positions"
    - from: "torre-de-controle/src/components/domain/LiveMap.tsx"
      to: "useFleetPositions"
      via: "hook consumido quando a camada está ligada"
      pattern: "useFleetPositions"
    - from: "torre-de-controle/src/components/domain/LiveMap.tsx"
      to: "maplibre GeoJSON cluster"
      via: "addSource cluster:true + addLayer symbol(truck)/circle(cluster)"
      pattern: "cluster"
---

<objective>
Adicionar ao **LiveMap existente** a camada toggleável "Frota importada": consome `GET /api/positions` (Plan 01) via `useFleetPositions`, renderiza cada motorista geocodado como **ícone de caminhão** colorido por **status do ranking**, com **cluster** nativo do maplibre e **popup** de detalhes. A camada coexiste com a camada de veículos ao vivo (WebSocket) — toggle separado, default desligada.

Purpose: fechar o milestone v2.0 conectando visualmente posições (Phase 10) × ranking (Phase 7-9) no mapa.
Output: hook `useFleetPositions.ts` + extensão do `LiveMap.tsx` (source/layers GeoJSON + toggle + popup).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-mapa-frota-importada-geospatial/11-CONTEXT.md

<read_first>
ARQUIVO MODIFICADO (a camada nova + toggle ENTRAM aqui — preservar 100% do que existe):
@torre-de-controle/src/components/domain/LiveMap.tsx
  → 159 linhas. maplibre setup (default import `import maplibregl from 'maplibre-gl'` — NÃO adicionar optimizeDeps.exclude, quebrou o constructor antes, ver STATE). initRef guard StrictMode. mapRef. markersRef (camada AO VIVO via DOM markers — NÃO TOCAR). `mode` mapa/satélite + setStyle. `mapReady`. usePositionsStore (camada ao vivo). Toggle UI no canto sup. direito (linhas 122-138) — o toggle "Frota importada" entra ao lado. Legend (linhas 148-156). IMPORTANTE: setStyle (troca mapa/satélite) REMOVE sources/layers customizados — re-adicionar a camada de frota no evento 'styledata'/após setStyle (ver Task 2).

ANALOG — addSource/addLayer GeoJSON + setData + remove guard (ESPELHAR p/ a camada de frota):
@torre-de-controle/src/app/pages/geofences/GeofencesPage.tsx
  → renderGeofences() (linha 134): `if (map.getLayer(id)) map.removeLayer(id)` antes de re-add; `map.addSource(id, { type:'geojson', data })`; `map.addLayer({ id, type, source, paint })`; paint data-driven `['get','color']`; update via `(map.getSource(id) as maplibregl.GeoJSONSource).setData(geojson)`. Padrão FeatureCollection. ESTE É O PADRÃO NATIVO JÁ PROVADO NO CODEBASE.

ANALOG — hook Eden Treaty + TanStack Query (ESPELHAR p/ useFleetPositions):
@torre-de-controle/src/hooks/useTrips.ts → forma mínima: useQuery + api.api.X.get() + `if (error) throw` + retorno { data, isLoading, isError, error, refetch }.
@torre-de-controle/src/hooks/useRanking.ts (linhas 74-92) → useRankingDrivers: staleTime: 30_000, queryKey namespaced, retorno completo. Seguir staleTime ~60_000 (D-11-06: ~60s; a camada importada não tem refetch agressivo — re-import é manual).
@torre-de-controle/src/lib/api.ts → `api` treaty client (credentials:'include'). O GET novo está em `api.api.positions.get()` (Plan 01 registrou positionsReadPlugin → flui no App type).

ANALOG — camada ao vivo coexistente (NÃO QUEBRAR; usa hook de STORE, não query):
@torre-de-controle/src/hooks/useVehiclePositions.ts → usePositionsStore (Zustand, WebSocket). A frota importada é INDEPENDENTE: hook de QUERY (TanStack), source GeoJSON própria, toggle próprio. Nenhuma interação com markersRef/usePositionsStore.

CONTRATO consumido (Plan 01 — FleetPosition):
@api/src/modules/positions/positions.service.ts (criado no Plan 01)
  → FleetPosition: { motorista, cidade, uf, dataPosicao (ISO string), veiculo, lat (number), lng (number), ranked (bool), rank (number|null), pontuacao (number|null), status ('ATIVO'|'BLOQUEADO'|null), vinculo (string|null) }.

FORMATTERS p/ popup:
@torre-de-controle/src/lib/formatters.ts → formatDate(date, 'dd/MM/yyyy HH:mm') p/ dataPosicao.

TOKENS de cor por status (D-11-04 — usar os hex já allowlisted no LiveMap/STATE, constantes entre temas):
  ATIVO (success) = #2dce89 · BLOQUEADO (danger) = #f5365c · sem-match (muted) = #95959e.
  (São os mesmos hex de SLA_COLORS já no topo do LiveMap — reusar a paleta; status dot hex allowlisted por design, STATE.)
</read_first>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useFleetPositions hook (Eden Treaty GET /api/positions + TanStack Query)</name>
  <files>torre-de-controle/src/hooks/useFleetPositions.ts</files>
  <behavior>
    - useFleetPositions() retorna { data: FleetPosition[], isLoading, isError, error, refetch } (mesma forma de useTrips/useRankingDrivers).
    - queryKey ['fleet-positions'].
    - staleTime ~60_000 (D-11-06: re-import é manual, não precisa refetch agressivo).
    - Aceita um arg opcional `enabled?: boolean` repassado ao useQuery — assim o LiveMap só dispara o fetch quando a camada está ligada (toggle off = sem request).
    - queryFn chama api.api.positions.get(); em erro lança Error com a mensagem do error.value (padrão dos outros hooks).
    - data default [] quando undefined.
  </behavior>
  <action>
    Criar `useFleetPositions(opts?: { enabled?: boolean })`. Definir/exportar o tipo `FleetPosition` localmente no hook (mesma forma do contrato do Plan 01: motorista, cidade:string|null, uf:string|null, dataPosicao:string, veiculo:string|null, lat:number, lng:number, ranked:boolean, rank:number|null, pontuacao:number|null, status:'ATIVO'|'BLOQUEADO'|null, vinculo:string|null) — OU re-exportar via `import type` do contrato `../../../api/src/modules/positions/positions.service` se preferir o padrão de useRanking.ts (re-export do contrato). Escolher o re-export do contrato p/ evitar drift (mesmo padrão de RankedDriver em useRanking.ts).

    useQuery: queryKey ['fleet-positions'], staleTime 60_000, enabled: opts?.enabled ?? true, queryFn async → `const { data, error } = await api.api.positions.get(); if (error) throw new Error((error.value as any)?.error ?? 'Failed to fetch fleet positions'); return (data ?? []) as FleetPosition[]`. Retornar { data: q.data ?? [], isLoading, isError, error, refetch } como nos demais hooks.

    Importar api de '@/lib/api'. NÃO criar store Zustand (é query, não WS).
  </action>
  <verify>
    <automated>cd torre-de-controle && npx tsc -b --noEmit 2>&1 | Select-String "useFleetPositions"; if (-not $?) { Write-Output "no-errors-in-hook" }</automated>
  </verify>
  <acceptance_criteria>
    - `cd torre-de-controle && npx tsc -b --noEmit` não acusa erro em useFleetPositions.ts (o tipo FleetPosition resolve via contrato; `api.api.positions.get()` existe no App type — depende do Plan 01 ter registrado o plugin).
    - grep encontra `useFleetPositions`, `['fleet-positions']`, `staleTime`, `enabled`, `api.api.positions.get` no arquivo.
    - retorno tem a forma { data, isLoading, isError, error, refetch }.
  </acceptance_criteria>
  <done>useFleetPositions criado no padrão Eden Treaty + TanStack (staleTime 60s, enabled gate), tipo FleetPosition do contrato, tsc -b limpo no arquivo.</done>
</task>

<task type="auto">
  <name>Task 2: camada GeoJSON cluster de caminhões por status no LiveMap (tradeoff resolvido = opção a, nativo)</name>
  <files>torre-de-controle/src/components/domain/LiveMap.tsx</files>
  <action>
    DECISÃO DO TRADEOFF (D-11-05, firme — implementar a opção a; NÃO deliberar): **GeoJSON source nativa com `cluster:true` + symbol layer (ícone de caminhão via map.addImage) + circle layer p/ os clusters.** Justificativa registrar no SUMMARY: (1) o codebase já prova addSource/addLayer GeoJSON em GeofencesPage; (2) clustering é nativo do maplibre — NÃO adicionar supercluster nem qualquer dep de mapa (STATE: não mexer em deps de maplibre); (3) escala melhor que DOM markers e não conflita com a camada ao vivo (markersRef); (4) cor por status via `icon-color` num ícone SDF + um campo `color` no feature.

    Adicionar estado `const [showFleet, setShowFleet] = useState(false)` (camada default DESLIGADA — não perturba dashboard/torre que reusam o LiveMap). Consumir `const { data: fleet } = useFleetPositions({ enabled: showFleet })`.

    ÍCONE DE CAMINHÃO: registrar uma imagem SDF de caminhão via `map.addImage('truck-icon', img, { sdf: true })` (SDF habilita recolorir via icon-color). Gerar a imagem a partir de um SVG inline de caminhão (path do lucide `Truck` serve) → ImageBitmap/HTMLImageElement, ou usar `map.loadImage` com um data-URI do SVG. Registrar UMA vez após o 'load' (guardar flag p/ não re-adicionar; addImage lança se id duplicado — checar `map.hasImage('truck-icon')`).

    CONSTRUÇÃO DA SOURCE/LAYERS (função `renderFleet(map)` espelhando renderGeofences):
    - Montar FeatureCollection: cada FleetPosition → Feature Point [lng, lat] com properties { motorista, cidade, uf, dataPosicao, veiculo, ranked, rank, pontuacao, status, color }. `color` = ATIVO #2dce89 / BLOQUEADO #f5365c / (status null) #95959e.
    - `map.addSource('fleet', { type:'geojson', data, cluster:true, clusterRadius:50, clusterMaxZoom:14 })`.
    - Layer cluster (circle): id 'fleet-clusters', filter `['has','point_count']`, paint circle-color por contagem (step) + circle-radius por contagem; layer 'fleet-cluster-count' (symbol) com `text-field: ['get','point_count_abbreviated']`.
    - Layer caminhões (symbol): id 'fleet-trucks', filter `['!',['has','point_count']]`, layout `{ 'icon-image':'truck-icon', 'icon-size':0.7, 'icon-allow-overlap':true }`, paint `{ 'icon-color': ['get','color'] }` (SDF recolor).
    - Guard de re-render: remover layers/source antes de re-add (padrão GeofencesPage: getLayer→removeLayer, getSource→removeSource).

    EFEITOS:
    - useEffect([showFleet, fleet, mapReady]): se !mapReady → return. Se showFleet && fleet.length → renderFleet(map). Se !showFleet → remover os 3 layers + a source 'fleet' (toggle off limpa o mapa).
    - REGRA setStyle (mode mapa/satélite): trocar o estilo DROPA sources/layers customizados. No efeito de troca de estilo (o useEffect [mode, mapReady] existente que faz setStyle), após o setStyle reusar o evento `map.once('styledata', ...)` p/ re-registrar o truck-image e chamar renderFleet se showFleet. (Caso contrário, alternar mapa/satélite some os caminhões.)

    PRESERVAR: todo o bloco markersRef/usePositionsStore (camada ao vivo) intacto; initRef guard; cleanup do map.remove(). NÃO adicionar optimizeDeps.exclude. NÃO converter o import do maplibre.
  </action>
  <verify>
    <automated>cd torre-de-controle && npx tsc -b --noEmit; if ($?) { npx vite build 2>&1 | Select-String -Pattern "built in|error" | Select-Object -Last 3 }</automated>
  </verify>
  <acceptance_criteria>
    - `cd torre-de-controle && npx tsc -b --noEmit` exit 0 E `npx vite build` exit 0.
    - grep em LiveMap.tsx encontra: `useFleetPositions`, `addImage` (truck-icon SDF), `cluster: true` (ou `cluster:true`), `'fleet-trucks'`, `'fleet-clusters'`, `icon-color`, `truck-icon`.
    - grep confirma as 3 cores de status presentes: `#2dce89`, `#f5365c`, `#95959e`.
    - grep confirma `styledata` (re-registro da camada após setStyle mapa/satélite).
    - grep confirma que `markersRef` e `usePositionsStore` AINDA estão presentes (camada ao vivo intacta — não removida).
    - grep confirma ausência de `optimizeDeps` e de `import * as maplibregl` (default import preservado: `import maplibregl from 'maplibre-gl'`).
  </acceptance_criteria>
  <done>Camada 'fleet' GeoJSON nativa com cluster + caminhões SDF coloridos por status renderiza quando showFleet; sobrevive à troca mapa/satélite; camada ao vivo intacta; tsc -b + vite build exit 0.</done>
</task>

<task type="auto">
  <name>Task 3: toggle "Frota importada" + popup do caminhão + expand de cluster</name>
  <files>torre-de-controle/src/components/domain/LiveMap.tsx</files>
  <action>
    TOGGLE (D-11-06): adicionar um botão "Frota importada" no painel de controles do canto sup. direito (ao lado do toggle Mapa/Satélite, linhas ~122-138). Estado `showFleet` (Task 2). Estilo idêntico aos botões existentes (`cn('px-3 py-1.5 ...', showFleet ? 'bg-primary text-primary-foreground' : 'text-foreground hover:bg-accent')`), ícone lucide `Truck`. Ao clicar: `setShowFleet(v => !v)`.

    POPUP (D-11-05, T4 XSS — montagem SEGURA, sem innerHTML cru de dados): no evento `map.on('click', 'fleet-trucks', e => {...})`:
    - Pegar `const f = e.features[0]; const p = f.properties`. Coordenadas do `f.geometry.coordinates`.
    - Construir o conteúdo do popup SEM interpolar valores de usuário direto em innerHTML. Montar via `document.createElement` + `textContent` para cada campo (nome, cidade/UF, data, veículo, e se `ranked` truthy: rank/pontuação/status) — append num container; OU usar uma função de escape e só então setHTML. PROIBIDO: template string com `${p.motorista}` indo direto pra innerHTML/setHTML sem escape (T4 — espelha a regra de renderDiff do 08-08: dados como texto, nunca HTML cru). Aplicar `new maplibregl.Popup().setLngLat(coords).setDOMContent(containerEl).addTo(map)` (setDOMContent evita parse de HTML).
    - Campos: "Motorista: <nome>", "Local: <cidade>/<uf>", "Data: formatDate(dataPosicao,'dd/MM/yyyy HH:mm')", "Veículo: <veiculo ou '—'>". Se ranked: "Rank: #<rank> · <pontuacao> pts · <status>". As properties do GeoJSON chegam como string (maplibre serializa) — `ranked` pode vir 'true'/'false' string ou boolean; tratar ambos (`p.ranked === true || p.ranked === 'true'`).
    - Cursor: `map.on('mouseenter','fleet-trucks', () => map.getCanvas().style.cursor='pointer')` + mouseleave reset.

    EXPAND DE CLUSTER (D-11-05): `map.on('click','fleet-clusters', e => {...})`: pegar o cluster_id da feature, chamar `(map.getSource('fleet') as maplibregl.GeoJSONSource).getClusterExpansionZoom(clusterId)` e `map.easeTo({ center: feature.geometry.coordinates, zoom })`. (API maplibre nativa — sem dep extra.) Cursor pointer no hover do cluster também.

    REGISTRO dos handlers: registrar uma única vez (após os layers existirem). Como os layers são (re)criados em renderFleet, registrar os handlers por NOME de layer (maplibre permite `map.on('click', layerId, fn)` mesmo que o layer seja recriado, desde que registrado após a 1ª criação) — registrar dentro do efeito que cria a camada, com guarda p/ não duplicar listeners (flag ref `fleetHandlersRef`). Limpar no cleanup do map (já existe `map.remove()` que descarta tudo).

    OPCIONAL (discrição): item de legenda da frota (Caminhão verde/vermelho/cinza) quando showFleet — pode reusar o bloco Legend. Não obrigatório.

    PRESERVAR tudo o resto. NÃO usar dangerouslySetInnerHTML em lugar nenhum.
  </action>
  <verify>
    <automated>cd torre-de-controle && npx tsc -b --noEmit; if ($?) { npx vite build 2>&1 | Select-String -Pattern "built in|error" | Select-Object -Last 3 }</automated>
  </verify>
  <acceptance_criteria>
    - `cd torre-de-controle && npx tsc -b --noEmit` exit 0 E `npx vite build` exit 0.
    - grep em LiveMap.tsx encontra: `Truck` (ícone do toggle), `setShowFleet`, `Frota importada` (label do toggle), `Popup`, `setDOMContent` (montagem segura), `getClusterExpansionZoom` (expand de cluster), `'fleet-trucks'` e `'fleet-clusters'` nos handlers de click.
    - grep confirma `formatDate` usado no popup.
    - grep confirma AUSÊNCIA de `dangerouslySetInnerHTML` e de `.innerHTML =` com interpolação de `p.motorista`/dados (T4). Aceitável: `setDOMContent` ou textContent.
    - grep confirma que o toggle Mapa/Satélite original (`setMode`) e a legenda ainda existem (não removidos).
  </acceptance_criteria>
  <done>Toggle "Frota importada" liga/desliga a camada; clicar no caminhão abre popup seguro (setDOMContent) com nome/cidade/UF/data/veículo + rank/pontuação/status se ranked; clicar no cluster dá zoom; tsc -b + vite build exit 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LiveMap (browser) → GET /api/positions | Fetch autenticado (cookie via api treaty credentials:include) — dados já enriquecidos pelo Plan 01 |
| dados do popup → DOM | Strings (nome/cidade) vindas da API renderizadas no popup do maplibre |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-06 | Tampering (XSS) | popup do caminhão | mitigate | Conteúdo montado via `setDOMContent`/`textContent` (não innerHTML cru); `dangerouslySetInnerHTML` PROIBIDO. Espelha renderDiff seguro do 08-08. (T4 do CONTEXT) |
| T-11-07 | Information Disclosure | useFleetPositions | mitigate | O front só consome o resultado já enriquecido; nenhuma credencial de ranking/ride-rank trafega pro cliente (garantido no Plan 01, T2). Fetch com credentials:include (cookie de sessão). |
| T-11-08 | Information Disclosure | camada de frota | accept | Posições nível-cidade (centroide) exibidas a qualquer usuário logado — é visualização interna, sem GPS preciso em tempo real (T3 do CONTEXT). |
| T-11-09 | Denial of Service | render do mapa | accept | ~69 features + cluster nativo; volume baixo, sem risco de travar o render (cluster reduz marcadores em zoom baixo). |
</threat_model>

<verification>
- `cd torre-de-controle && npx tsc -b --noEmit` exit 0 + `npx vite build` exit 0.
- grep LiveMap: `useFleetPositions`, `addImage`+`truck-icon` SDF, `cluster: true`, `'fleet-trucks'`/`'fleet-clusters'`, `icon-color`, `#2dce89`/`#f5365c`/`#95959e`, `styledata`, `Truck`, `Frota importada`, `setShowFleet`, `Popup`+`setDOMContent`, `getClusterExpansionZoom`, `formatDate`.
- grep negativo: sem `dangerouslySetInnerHTML`, sem `optimizeDeps`, sem remoção de `markersRef`/`usePositionsStore` (camada ao vivo intacta).
</verification>

<success_criteria>
- LiveMap tem toggle "Frota importada" (default off) que liga/desliga a camada sem afetar a camada de veículos ao vivo.
- Motoristas geocodados aparecem como ícones de caminhão coloridos por status (verde/vermelho/cinza).
- Caminhões próximos clusterizam; clicar no cluster dá zoom; clicar no caminhão abre popup seguro com detalhes + ranking (se ranked).
- A camada sobrevive à troca mapa/satélite. tsc -b + vite build exit 0.
</success_criteria>

<output>
After completion, create `.planning/phases/11-mapa-frota-importada-geospatial/11-02-SUMMARY.md`
</output>
